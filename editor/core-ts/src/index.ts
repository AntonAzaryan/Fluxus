/**
 * Публичная поверхность пакета `@game-mvp/editor-core` — headless-каркаса
 * редактора: реестры вкладов (ED-25), операции авторинга с историей (ED-29,
 * ED-18), валидация (ED-8) и интерфейс хоста среды (ED-12) живут здесь.
 *
 * DOM в этом пакете нет и не будет: `lib` его tsconfig — только `ES2022`,
 * поэтому «операции исполнимы без интерфейса» (ED-29) проверяет компилятор, а
 * не дисциплина автора. Всё, что требует документа и элементов, — в
 * `@game-mvp/editor-ui`.
 *
 * Порядок реэкспортов — снизу вверх по зависимостям: документы и операции не
 * знают о реестрах, реестры собирают из операций каталог, ресурсы не знают ни
 * о ком. Барели подпакетов и есть их публичная поверхность; отдельного списка
 * имён здесь нет намеренно — второй перечень расходится с первым.
 */
export * from './document/index.js';
export * from './operations/index.js';
export * from './registry/index.js';
export * from './i18n/index.js';
export * from './validation/index.js';
export * from './host/index.js';
export * from './project/index.js';
