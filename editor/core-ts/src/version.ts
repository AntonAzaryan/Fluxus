/**
 * Версия редактора одним значением на оба пакета: ею помечается машинный
 * каталог возможностей (ED-30) — потребителю каталога нужно знать, какой
 * редактор его выдал, а `package.json` для него не публичен.
 *
 * Значение дублирует поле `version` пакета: тест `test/version.test.ts`
 * пиннит совпадение, чтобы копия не разъехалась с манифестом.
 */
export const EDITOR_VERSION = '0.1.0';
