/**
 * @contribution Рабочая область систем — вклад, а не часть каркаса.
 *
 * Пока в файле только вид редактируемого. Сама область стояла здесь на
 * материале-заглушке (`material.{ts,json}`) и правила карту флагов вымышленного
 * документа; ED-4 и ED-5 требуют другого — списка JSON-систем конфига сцены
 * (SYS-1) и блочной сборки триггеров и выражений, — и собирается это на
 * открытом проекте. Заглушка уехала фикстурой в набор тестов
 * (`test/support/stubArea.ts`): «вторая непохожая область», на которой
 * проверяется каркас, не должна быть production-модулем, который никто не
 * открывает.
 *
 * Вид документа переезда не пережил бы молча: по нему правила валидации
 * адресуют систему (`validation/engineRules.ts`, вид `system` в
 * `sceneProject.ts`), и проект вправе разложить системы отдельными
 * документами. Поэтому он остаётся объявленным здесь — там же, где его
 * объявит настоящая область.
 */

/** Вид редактируемого, который вносит эта область (ED-30). */
export const SYSTEM_DOCUMENT_KIND = 'system';
