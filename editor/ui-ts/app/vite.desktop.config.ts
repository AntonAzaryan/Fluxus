/**
 * Сборка редактора для десктоп-контейнера (`desktop-shell` DSK-4).
 *
 * Отличие от веб-сборки одно и то же, что у демо: копии дерева контента в
 * бандле нет. В контейнере дерево живое — его раздаёт контейнер и в него же
 * пишет мост (DSK-2); запечённый в бандл снимок заслонил бы запись собой, и
 * автор видел бы старые байты сразу после сохранения.
 *
 * Эндпойнта дерева (`contentEndpoint`) здесь нет по той же причине, по которой
 * его нет в обычной сборке: он плагин dev-сервера (`apply: 'serve'`), а
 * перечисление и запись на десктопе даёт хост среды поверх моста (ED-12).
 */
import base from './vite.config.js';

export default {
  ...base,
  publicDir: false,
  build: { ...base.build, outDir: 'dist-desktop' },
};
