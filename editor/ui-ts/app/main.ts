/**
 * Точка входа веб-приложения редактора (ED-12, веб-среда). Здесь только
 * монтирование корня: всё остальное — каркас областей и вклады — приходит
 * реестрами (ED-25), а не вызовами из этого файла.
 */
import { mountEditorRoot } from '../src/index.js';

mountEditorRoot(document);
