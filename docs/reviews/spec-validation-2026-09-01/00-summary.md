# Сводка: глобальная валидация спек против кода — 2026-09-01

Проверены все 39 капабилити (474 требования) двенадцатью независимыми проходами; подробности каждой находки — в файлах `01`–`12` рядом (там у каждой: что требует спека, что делает код, микропример и ссылка `файл:строка`).

**Исправлено:** все 11 находок 🔴 «код нарушает спеку» (DET-2, TICK-3, ABIL-3, NTR-4/5, NTR-14, REND-1, CAM-5, CAM-6, architecture.md §7, дельта PRES-2, terrain↔FOW-9) устранены 2026-09-01 — коммиты `e0e1ebc`, `fcf9065`, `4d7a022` в main — и удалены из групповых отчётов.

**Осталось: 140 находок.** 🟡 46 — реализовано частично или неточно · 🔵 44 — противоречие либо устаревший текст в спеках/доках · ⚪ 50 — мелочи.

| Файл | Область | 🟡 | 🔵 | ⚪ |
|------|---------|----|----|----|
| [01-core-math.md](./01-core-math.md) | Ядро: математика, RNG, детерминизм, тик | 2 | 3 | 5 |
| [02-ecs-dsl.md](./02-ecs-dsl.md) | ECS и data-driven слой | 1 | 5 | 3 |
| [03-sim-systems.md](./03-sim-systems.md) | Симуляционные системы: физика, террейн, передвижение, туман войны, арена | 3 | 3 | 6 |
| [04-gameplay.md](./04-gameplay.md) | Геймплейные системы: способности, баффы, время, NPC, ввод | 5 | 4 | 8 |
| [05-state-serialization.md](./05-state-serialization.md) | Состояние: сериализация, снапшоты/перемотка, игровой контент | 5 | 5 | 5 |
| [06-netcode.md](./06-netcode.md) | Сетевой код: netcode, транспорт, сессии | 3 | 4 | 2 |
| [07-rendering.md](./07-rendering.md) | Рендеринг, качество, отладочный рендер, камера | 1 | 2 | 3 |
| [08-client-apps.md](./08-client-apps.md) | Клиентская обвязка: client-shell, HUD, бот | 2 | 3 | 3 |
| [09-host-apps.md](./09-host-apps.md) | Хостовые приложения: server-agent, server-manager, desktop-shell | 6 | 3 | 4 |
| [10-editor-assets.md](./10-editor-assets.md) | Редактор, ассеты, презентационные документы | 5 | 4 | 2 |
| [11-tooling-diagnostics.md](./11-tooling-diagnostics.md) | Инструменты: Blender-пайплайн, диагностика, CLI-тестирование, бюджет производительности | 3 | 3 | 5 |
| [12-cross-consistency.md](./12-cross-consistency.md) | Перекрёстная согласованность спек и документов | 10 | 5 | 4 |

## 🟡 Реализовано частично или неточно

- 🟡 TICK-2 — маска кнопок шире 16 бит в загружаемом документе проходит молча `(01-core-math)`
- 🟡 OBS-1 / TICK-3 — «read-only отчёт» о тике на самом деле даёт запись в четыре части состояния `(01-core-math)`
- 🟡 EXPR-4 — интерфейс `ExpressionEvaluator` существует, но ничего через него не проходит `(02-ecs-dsl)`
- 🟡 FOW-5 — в коде остался «молчаливый запасной вариант», который спека запрещает `(03-sim-systems)`
- 🟡 ARENA-3 — проверка «цепкости» (`support`) сделана только для одной из двух дорог `(03-sim-systems)`
- 🟡 TERR-7 — ширина рампы проверяется не полностью `(03-sim-systems)`
- 🟡 ABIL-5 — «фигура шага» не участвует ни в одной проверке симуляции `(04-gameplay)`
- 🟡 ABIL-6 — прерывание «отпустил кнопку раньше времени» срабатывает только у тех способностей, которые сами его назвали `(04-gameplay)`
- 🟡 TIME-5 — половина систем не документирует своё отношение к замедлению, хотя требование обязывает `(04-gameplay)`
- 🟡 NPC-5 — наименьшая угроза вытесняется не всегда `(04-gameplay)`
- 🟡 BUFF-3 — умолчание политики стакинга не нормировано, а код его выбрал `(04-gameplay)`
- 🟡 SER-2 — сетевой сериализатор сам переставляет ключи, хотя требование это запрещает `(05-state-serialization)`
- 🟡 SER-5 — опубликованная схема строже загрузчика на поле `navigation.cost` `(05-state-serialization)`
- 🟡 SNAP-4 / SER-3 — снапшоты никогда не переводятся в MessagePack `(05-state-serialization)`
- 🟡 CONT-1 — механическая проверка границы контента знает не все виды контентных документов `(05-state-serialization)`
- 🟡 CONT-1 / CONT-4 — художественный ассет игры лежит внутри пакета движка, и это освобождение выдала себе сама проверка `(05-state-serialization)`
- 🟡 NET-13 / NTR-9 — правило «какие события видны игроку» нельзя поменять правкой контента, только правкой кода `(06-netcode)`
- 🟡 NTR-5 — вторая из двух обязательных сверок при входе не успевает завершиться до первого тика `(06-netcode)`
- 🟡 NET-12 / NET-15 — «точка зрения» игрока берётся из номера слота, и никто не проверяет, что это его команда `(06-netcode)`
- 🟡 REND-11 / REND-23 / REND-17 — вьюпорт редактора не рисует транзиентные эффекты `(07-rendering)`
- 🟡 HUD-4 — иконки достаются не тем же слоем ассетов, что у рендера `(08-client-apps)`
- 🟡 HUD-2 — у действия HUD нет формы «удержание», и одна кнопка сделана заглушкой `(08-client-apps)`
- 🟡 MGR-2 — в общем списке серверов нет счётчика рестартов `(09-host-apps)`
- 🟡 MGR-2 — форма запуска отдаёт только две настройки из шести `(09-host-apps)`
- 🟡 MGR-3 / SRV-6 — у упавшего сервера нельзя открыть детали и прочитать лог `(09-host-apps)`
- 🟡 MGR-5 — менеджер (и игровой клиент) сегодня не собираются в дистрибутив контейнера `(09-host-apps)`
- 🟡 SRV-7 — «собрать дистрибутив из разных версий» технически возможно `(09-host-apps)`
- 🟡 SRV-8 — агент раздаёт не живое дерево контента, а его снимок, запечённый в клиентский бандл `(09-host-apps)`
- 🟡 ED-19 — правило «у префаба нет записи манифеста» кричит на живой сцене 24 раза `(10-editor-assets)`
- 🟡 ED-13 — вьюпорт редактора не читает настройки камеры из манифеста, а игра читает `(10-editor-assets)`
- 🟡 ED-14 — «ссылка на отсутствующий ассет» до диска не подсвечивается `(10-editor-assets)`
- 🟡 ED-14 — манифест правится не весь: анимации, наклон и ссылка на карту кривизны недоступны `(10-editor-assets)`
- 🟡 ED-11 — у кисти кривизны всё-таки есть предел амплитуды `(10-editor-assets)`
- 🟡 PERF-2 — одна из пяти названных стадий конвейера не измеряется вообще ничем `(11-tooling-diagnostics)`
- 🟡 PERF-6 — оси стоимости на стороне симуляции меряются одним размером, а не двумя `(11-tooling-diagnostics)`
- 🟡 BLND-2 — предупреждение о расхождении привязано к экспорту `.glb`, а спека привязывает его к `.blend` `(11-tooling-diagnostics)`
- 🟡 `CLAUDE.md`, `docs/architecture.md` и вводная часть спеки `pathfinding` против требований NAV-7…NAV-10 — поиск пути описан как «ещё не сделан», хотя он сделан `(12-cross-consistency)`
- 🟡 DI-4 (`determinism-core`) против capability `npc-behavior` и содержимого `content/` — обоснование опирается на факт, которого больше нет `(12-cross-consistency)`
- 🟡 Изменение `dead-state-from-delivery`: код написан, спека — нет `(12-cross-consistency)`
- 🟡 `openspec list` перестал быть очередью: шесть из одиннадцати изменений уже влиты в спеки `(12-cross-consistency)`
- 🟡 Заготовки изменений держат номера требований, которые уже заняты `(12-cross-consistency)`
- 🟡 Счётчики стоимости (PERF-3/PERF-4) не имеют дома в спеке `diagnostics` `(12-cross-consistency)`
- 🟡 Четыре требования рендера ссылаются на ED-22 как на правило «кадр одинаков у всех потребителей», а ED-22 про палитру интерфейса `(12-cross-consistency)`
- 🟡 Две заготовки изменений не помечены как заготовки и роняют штатную проверку `(12-cross-consistency)`
- 🟡 `CLAUDE.md` против `openspec/config.yaml` — расхождение в одном пункте из девяти `(12-cross-consistency)`
- 🟡 Спеки ссылаются друг на друга устаревшими диапазонами номеров `(12-cross-consistency)`

## 🔵 Противоречия и устаревший текст в спеках/доках

- 🔵 TICK-3 против SHELL-8 — список исключений объявлен исчерпывающим, а в жизни есть четвёртый случай `(01-core-math)`
- 🔵 TICK-2 — внутри одного требования два правила о моменте исполнения тика `(01-core-math)`
- 🔵 DI-2 — в требовании упомянуты матрицы, которых в Math API нет и не планируется `(01-core-math)`
- 🔵 ECS-5 — «проверять JSON Schema на загрузке» противоречит SER-5 `(02-ecs-dsl)`
- 🔵 QUERY-1 vs SYS-3 — «незнакомый компонент в запросе не роняет прогон» на JSON-системах недостижимо `(02-ecs-dsl)`
- 🔵 SYS-9 — объявленный «закрытый» перечень жёстких ошибок неполон `(02-ecs-dsl)`
- 🔵 ECS-7 / CMD-4 — запись в поле сущности, у которой такого компонента нет, спекой не описана `(02-ecs-dsl)`
- 🔵 SYS-3 — перечень имён аргументов действий устарел `(02-ecs-dsl)`
- 🔵 Purpose спеки `pathfinding` — устаревший текст: «алгоритма нет», а алгоритм есть `(03-sim-systems)`
- 🔵 Открытый вопрос № 2 спеки `terrain` закрыт требованием NAV-7, но всё ещё числится открытым `(03-sim-systems)`
- 🔵 FOW-1 — обоснование предела радиуса обзора больше не соответствует коду `(03-sim-systems)`
- 🔵 ABIL-4 — сценарий «противник видит заряд» противоречит NET-12 и коду `(04-gameplay)`
- 🔵 TIME-5 — «скорость анимации вне TimeScale» противоречит уже действующему REND-38 `(04-gameplay)`
- 🔵 BUFF-7 — «аура накладывает бафф на сущности в фигуре шага» платформой невыразимо `(04-gameplay)`
- 🔵 INP-5 — «нейтральный ввод» против «последнего известного состояния» TICK-2 `(04-gameplay)`
- 🔵 SER-1 — обещание «переставил поля — изменился хеш» не выполняется и не может выполняться `(05-state-serialization)`
- 🔵 CONT-2 — «единственный такой документ» — устаревшая формулировка: рядом со сценой лежит второй, а третий вид контента в классификацию не входит вовсе `(05-state-serialization)`
- 🔵 SER-7 ↔ NET-17 — таблица поведений NPC объявлена полем сцены, но в хеш контент-пака не входит ни по спеке, ни по коду `(05-state-serialization)`
- 🔵 SER-7 — «все три поля» документа прогона устарели: их четыре `(05-state-serialization)`
- 🔵 SER-4 — «YAML не используется в проекте», а в репозитории 134 YAML-файла `(05-state-serialization)`
- 🔵 NET-2 / NET-4 / NET-5 против NTR-10 — два требования говорят прямо противоположное `(06-netcode)`
- 🔵 NTR-7 — потолок адаптивного запаса опирается на величину, которой ни в одном требовании нет `(06-netcode)`
- 🔵 netcode-transport, «Открытые вопросы» — вопрос про половину круга устарел вместе со слиянием NTR-7 `(06-netcode)`
- 🔵 NET-17 — правило нормализации «умолчание-ноль = отсутствующее умолчание» в коде действует не всегда `(06-netcode)`
- 🔵 REND-30 / REND-32 — «доля статики» приписана требованию, которое её не вводит `(07-rendering)`
- 🔵 REND-1 — вступление спеки перечисляет закрытый список входов короче, чем сам REND-1 `(07-rendering)`
- 🔵 SHELL-6 — спека говорит «пауза уезжает вводом», а код шлёт отдельное сообщение `(08-client-apps)`
- 🔵 SHELL-3/SHELL-4/SHELL-5 — канал возит третий вид сообщения, которого спека не знает `(08-client-apps)`
- 🔵 Документация отстала от спеки на целые требования (не нормативно, но вводит в заблуждение) `(08-client-apps)`
- 🔵 SRV-3 против MGR-5 — код пейринга обещан «по локальной команде», а на деле штампуется сам каждую минуту `(09-host-apps)`
- 🔵 SRV-4 / MGR-3 — «отклик по NTR-11» на деле не величина NTR-11, а её серверная половина `(09-host-apps)`
- 🔵 SRV-6 — «при ненулевом выходе» против «рядом с состоянием `crashed`» `(09-host-apps)`
- 🔵 ED-19 против REND-37 — одно и то же положение дел одна спека называет нормой, другая дефектом `(10-editor-assets)`
- 🔵 ED-19 — таксономия «unit / prop / decoration» не покрывает невидимые сим-сущности `(10-editor-assets)`
- 🔵 ED-14 против ASSET-14 — один пробел одна спека требует закрыть, другая объявляет признанным `(10-editor-assets)`
- 🔵 ED-9 — устаревшая ссылка на клиентскую оболочку `(10-editor-assets)`
- 🔵 BLND-1 против BLND-11 — спека сама себе запрещает то, что сама же требует `(11-tooling-diagnostics)`
- 🔵 CLI-2 — в спеке нет поля включения поиска пути, хотя код его принимает, а NTR-14 на него ссылается `(11-tooling-diagnostics)`
- 🔵 DIAG-3 — на уровне «границы систем» в трейсе есть запись, которой перечень уровня не знает `(11-tooling-diagnostics)`
- 🔵 `docs/architecture.md`, карта спецификаций — 16 строк из 40 несут устаревшие номера требований, и документ спорит сам с собой `(12-cross-consistency)`
- 🔵 `docs/architecture.md`, роадмап обрывается на этапе 41, хотя работы после него много `(12-cross-consistency)`
- 🔵 `docs/architecture.md`, раздел открытых вопросов — три пункта устарели или указывают в пустоту `(12-cross-consistency)`
- 🔵 `CLAUDE.md` — устаревшие числа и неполный перечень систем ядра `(12-cross-consistency)`
- 🔵 `README.md` — описание гейта и перечень пакетов отстали `(12-cross-consistency)`

## ⚪ Мелочи

- ⚪ DET-2 — у двух делений в коде не выписано обоснование, которого требование требует «на каждой площадке» `(01-core-math)`
- ⚪ DET-9 — тест «нативные системы стоят на якорях» закрывает 7 строк таблицы из 19 `(01-core-math)`
- ⚪ RNG-8 — граница `nextBelow` больше 2^32 проходит жёсткую проверку и молча ломается `(01-core-math)`
- ⚪ Ссылки на требования в комментариях кода местами указывают не туда `(01-core-math)`
- ⚪ TICK-2 — необязательность точки прицела нигде в требовании не сказана `(01-core-math)`
- ⚪ ACT-5 — расстояние до `nearestTo` считается не той же арифметикой, что `withinRadius` `(02-ecs-dsl)`
- ⚪ CMD-5 — точечное чтение буфера не учитывает отложенное снятие компонента `(02-ecs-dsl)`
- ⚪ SYS-5 — «мутирующей части у чтения быть не должно» держится только на типах `(02-ecs-dsl)`
- ⚪ LOC-5 — прыжок нормирует направление, а спека этого не говорит `(03-sim-systems)`
- ⚪ PHYS-6 — луч, начинающийся внутри чужого коллайдера, ведёт себя по-разному у прямоугольника и круга `(03-sim-systems)`
- ⚪ PHYS-11 — «положение сущности» в правиле стороны захода не участвует, а ход вдоль обрыва свободен без оговорки `(03-sim-systems)`
- ⚪ ARENA-3 — ядро переписывает флаг, записанный контентом `(03-sim-systems)`
- ⚪ FOW-4 — перечень «поверхности чтения» устарел относительно SYS-10 `(03-sim-systems)`
- ⚪ LOC-5/LOC-6 — прыжок затирает постоянный override уровня «летающей» сущности `(03-sim-systems)`
- ⚪ ABIL-2 — блок «эффекты» объявлен обязательным, а загрузчик его отсутствие принимает `(04-gameplay)`
- ⚪ ABIL-5 — сцена без полей точки прицела молча целится в самого владельца `(04-gameplay)`
- ⚪ TIME-8 — постановка модификатора на сущность без списка источников молча не делает ничего `(04-gameplay)`
- ⚪ TWEEN-1 — комментарий в коде объявляет расхождение со спекой, которого больше нет `(04-gameplay)`
- ⚪ NPC-6 — в ключе маршрута зашит предел в 4096 точек без единой проверки `(04-gameplay)`
- ⚪ NPC-5 — имена слотов threat-таблицы не следуют правилу нумерованных имён `(04-gameplay)`
- ⚪ NPC-4 — умолчание «бюджет решений не ограничен» не нормировано `(04-gameplay)`
- ⚪ ABIL-6 — прерывание по кнопке отмены без назначенного бита молчит `(04-gameplay)`
- ⚪ SNAP-1 — перечисление состава мира не называет счётчик живых сущностей, хотя он в снапшоте есть `(05-state-serialization)`
- ⚪ SNAP-2 / SNAP-4 — комментарии в коде повторяют формулировку, которую спека убрала специально `(05-state-serialization)`
- ⚪ REW-2 — публичная функция восстановления возвращает и режим мира, хотя требование это запрещает `(05-state-serialization)`
- ⚪ Комментарий `BranchHistory` объясняет её существование поведением, которого у кольца больше нет `(05-state-serialization)`
- ⚪ SER-6 — правило дополнения имён нулями реализовано в двух порождённых раскладках из четырёх `(05-state-serialization)`
- ⚪ SES-1 — режим сессии `local` объявлен, но собрать его нечем `(06-netcode)`
- ⚪ NTR-4 — на клиенте закрытые перечни причин отказа не проверяются, а сбой разбора выдаётся за конец матча `(06-netcode)`
- ⚪ QUAL-3 — подсистема HUD стоит в списке подсистем рендера и ничего о своей стоимости не объявляет `(07-rendering)`
- ⚪ Устаревший комментарий про пресет «баланс» `(07-rendering)`
- ⚪ Устаревшая таблица capability в `docs/architecture.md` `(07-rendering)`
- ⚪ SHELL-4 — счётчик потерянных событий до HUD не доезжает `(08-client-apps)`
- ⚪ SHELL-4 / HUD-5 — узкое окно, где «непроигрываемые» факты могут уехать как играбельные `(08-client-apps)`
- ⚪ SHELL-3 — буфер, приехавший до приветственного сообщения, не возвращается воркеру `(08-client-apps)`
- ⚪ DSK-8 — закрепление сертификата доступно только ОТВЯЗЫВАЕМОМУ сервису `(09-host-apps)`
- ⚪ MGR-4 — «остановить и сам агент» может не успеть за отведённые контейнеру 5 секунд `(09-host-apps)`
- ⚪ SRV-4 — сценарий «Метрики без влияния на матч» ничем не закреплён `(09-host-apps)`
- ⚪ Хозяйство спек — незаархивированный change с устаревшей дельтой MGR-5 `(09-host-apps)`
- ⚪ ASSET-16 — требование повторяет своими словами перечень, повторять который ASSET-1 запрещает `(10-editor-assets)`
- ⚪ ED-6 / ED-21 против SER-6 — сортировать ли имена полей компонента в конфиге сцены `(10-editor-assets)`
- ⚪ DIAG-4 — код `SINK_THREW` объявлен в словаре, но не выдаётся никогда `(11-tooling-diagnostics)`
- ⚪ BLND-10 — комментарий аддона обещает кламп амплитуды, которого спека запрещает `(11-tooling-diagnostics)`
- ⚪ CLI-7 — комментарий в CLI ссылается на файл, которого нет `(11-tooling-diagnostics)`
- ⚪ BLND-5 — «импорт операцией из работающего редактора» существует только наполовину `(11-tooling-diagnostics)`
- ⚪ Документация — роадмап не знает про BLND-13 `(11-tooling-diagnostics)`
- ⚪ FOW-7 перечисляет проходы пост-обработки без цветокоррекции `(12-cross-consistency)`
- ⚪ `docs/one-pager.md` спорит сам с собой о названии проекта `(12-cross-consistency)`
- ⚪ Пропуск ASSET-15 в спеке ассетов — не дефект, а бронь `(12-cross-consistency)`
- ⚪ Таблица слоёв в `fog-of-war` ссылается на SNAP-5 как на состав снапшота `(12-cross-consistency)`
