# Архитектурный обзор движка

> Требования переехали в `openspec/specs/` — это документ «как всё устроено целиком» и «в каком порядке строим». Нормативные формулировки живут только в спеках; здесь их дублировать нельзя.

## 1. Слои

```text
┌──────────────────────────────────────────────────┐
│  Blender + аддон — редактор level-дизайна        │
│  Расстановка, террейн, кривизна: кисти и превью  │
│  .blend → .glb → импортёр tools/blender-ts       │
└──────────────┬───────────────────────────────────┘
               │ импорт — операция слоя editor-core (BLND-5, ED-29):
               │ initial, decorations, ассет террейна, карта кривизны
┌──────────────▼───────────────────────────────────┐
│  Редактор (TypeScript, веб и десктоп)            │
│  Data-Driven логика: системы, схемы, prefabs,    │
│  манифест, триггеры; превью и правка сцены       │
│  Вьюпорт — та же реализация rendering + camera   │
└──────────────┬───────────────────────────────────┘
               │ sim-документы: компоненты, системы, prefabs, конфиг сцены
               │ presentation-документы: манифест визуалов, карта кривизны,
               │ парный presentation-слой сцены (ED-3, ED-19, ED-21)
┌──────────────▼───────────────────────────────────┐
│  Deterministic Core (TS, позже Rust)             │
│  ┌────────────────────────────────────────────┐  │
│  │ System Evaluator (JSON → pipeline)         │  │
│  │ Action Executor / Expression (JsonLogic)   │  │
│  │ ECS foundation + Query API                 │  │
│  │ Time / Tween / Snapshot / RNG              │  │
│  │ Visibility (система FoW на TS)             │  │
│  └────────────────────────────────────────────┘  │
│        ▲ DI        ▲ DI            ▲ DI          │
│    Math API   Physics API      Navigation API    │
│               (обяз. при FoW)  (нет реализации)  │
└──────────────▲───────────────────────────────────┘
               │ worldInit: террейн (immutable) + стартовая расстановка
               │ tick(state, inputs) → TickResult
               │ DiagnosticsSink ← push во время тика (DIAG-1)
        ┌───────┴────────────────┐
        │  External Observers     │  ← аналитика / логи / звук
        │  Netcode snapshot filter│  ← per-client фильтрация (viewpoint)
        │  CLI (golden-file)      │  ← cross-language sync
        └───────┬─────────────────┘
               │ зависимость строго односторонняя (DI-3, NTR-1, ASSET-1)
┌──────────────▼───────────────────────────────────┐
│  Netcode transport (engine/net-ts)               │
│  MatchServer (чистый цикл) │ MatchClient         │
│  Protocol ─ Transport (loopback / WebSocket)     │
│  Session: авторитет (local/listen/dedicated)     │
│    × рандеву (in-process/адрес/инвайт) × лобби   │
├──────────────────────────────────────────────────┤
│  Presentation (assets-ts + render-ts)            │
│  assets: handle'ы, загрузчики, кэш, манифест     │
│    визуалов, карта кривизны, эффекты камеры      │
│  rendering: extractor → viewBuffer → подсистемы  │
│    (террейн и кривизна, модели, скины, пол)      │
│  camera: режимы → поза → стек эффектов           │
├──────────────────────────────────────────────────┤
│  Client shell (engine/client-ts)                 │
│  ядро в воркере, рендер в главном потоке,        │
│  transferable ping-pong + обратный канал ввода   │
├──────────────────────────────────────────────────┤
│  Игра (game/demo-ts) — потребитель оболочки:     │
│  сборка демо-арены, читает дерево контента       │
└──────────────────────────────────────────────────┘
```

Выходов из ядра два, и они разные. `TickResult` — отчёт о тике, читаемый observer'ами ПОСЛЕ него. `DiagnosticsSink` — push-канал, работающий ВО ВРЕМЯ тика: по нему идут нарушенные инварианты и трейс потока команд. Второй нужен именно потому, что первого не существует в момент, когда тик оборвался исключением. Подробности — capability `diagnostics`.

Из нижних четырёх секций независимых потребителей одного и того же выхода — три: транспорт, presentation и оболочка. Четвёртая, игра, стоит не рядом с ними, а поверх оболочки: `TickResult` она читает через неё, а собственного канала к ядру у неё нет. Presentation — такой же наблюдатель, как netcode и CLI: ассеты не влияют на симуляцию (ASSET-1), рендер читает `TickResult` и внутри себя волен считать во float (REND-1), камера — presentation-конвейер позы и в мир не пишет (CAM-1). Оболочка клиента разносит эту пару по потокам: ядро тикает в воркере, рендер и камера живут в главном, а границей между ними служит presentation-состояние (`client-shell` SHELL-1, SHELL-2). Вьюпорт редактора берёт ту же реализацию `rendering` и тот же конвейер `camera` — второго рендера не заводится (ED-1, ED-13), — а наполняться presentation-состояние умеет и не от тика, а от редактируемых документов (REND-11).

Внутри рендера у инстанса модели **два яруса представления**, и выбирает ярус запись манифеста визуалов, а не код (`rendering` REND-20, `assets` ASSET-13). **Батчевый** — умолчание: инстансы одной записи живут записями разделяемого `InstancedMesh`, скиннинг идёт на GPU по запечённым в текстуру матрицам костей (bone-VAT, ASSET-12), анимационное состояние инстанса — это индекс клипа и фаза, скин — индекс слоя в массиве текстур. Число draw calls растёт с числом ЗАПИСЕЙ манифеста, а не с числом инстансов, и пер-инстансных скелетов на CPU не остаётся. **Детальный** — прежнее пер-инстансное поддерево со своим скелетом и микшером; его получают записи с процедурным контролем костей (REND-5), записи, которым художник назначил его явно, и модели, у которых запечённых производных нет (деградация стоимости, а не отказ отрисовки). Наблюдаемое поведение у ярусов одно: машина состояний REND-4, поза, границы и picking считаются одним кодом — различается только носитель. Поверх ярусов работают отсечение по консервативным границам модели по всем её клипам (REND-21) и выбор уровня детализации по экранному размеру с порогами из записи (REND-22).

Верхний ярус с 2026-08-09 разделён по инструментам, а не по слоям. Поверхность level-дизайна — Blender: расстановка, террейн и кривизна авторятся там и приезжают одно-направленным импортом glTF (capability `blender-pipeline`); собственный редактор остаётся превью и инструментом data-driven-авторинга — системы, схемы, prefabs, манифест, — а level-дизайн-функциональность в нём не развивается. Второго пути правки при этом не появляется: импортёр — headless-потребитель слоя операций редактора и его канонического сохранения (BLND-5, ED-29, ED-21), поэтому валидация, сериализация и правила порядка у обоих инструментов одни.

## 2. Карта спецификаций

| Capability | Что покрывает | ID требований |
|---|---|---|
| `determinism-core` | Гарантии детерминизма, языки реализации, DI, единая шкала `order` | DET-1..9, CORE-1..4, DI-1..5 |
| `fixed-point-math` | Q16.16, умножение, округление, overflow, насыщение при делении и sqrt, `sin`/`cos` таблицей | FP-1..8 |
| `tick-loop` | `tick()`, `InputFrame`, слоты игроков, `TickResult`, observers | TICK-1..5, OBS-1..6 |
| `ecs-foundation` | ECS, типы полей, Query API, Entity IDs, Command Buffer | ECS-1..6, QUERY-1..3, ID-1..6, CMD-1..7 |
| `data-driven-systems` | System/Action/Expression DSL, Event Bus | SYS-1..8, ACT-1..3, EXPR-1..7, EVT-1..4 |
| `time-system` | TimeScale, стакинг, твины | TIME-1..9, TWEEN-1..7 |
| `rng` | PRNG, именованные стримы | RNG-1..8 |
| `snapshot-rewind` | Снапшоты, перемотка, машина состояний мира | SNAP-1..6, REW-1..11, WSM-1..6 |
| `serialization` | JSON / MessagePack, `Serializer`, порядок ключей, конфиг сцены, документ начальной расстановки | SER-1..8 |
| `terrain` | Height field, карты уровней и пола, `levelAt`, cliff-геометрия, интерфейс пола для геймплея | TERR-1..8 |
| `arena` | Граница арены, принадлежность, сужение, выход за край и провал сквозь пол | ARENA-1..6 |
| `physics` | Коллизии, статика обрывов, разрешение движения, детерминированный raycast, направленный гейт обрыва, sensor-пересечения | PHYS-1..12 |
| `locomotion` | Передвижение управляемой сущности: разгон и торможение, машина уклона, переката и прыжка, тайминги по глобальным тикам | LOC-1..6 |
| `pathfinding` | Navigation API: контракт запроса пути, бюджет поиска, путь как данные (реализации нет) | NAV-1..6 |
| `cli-testing` | CLI, golden-file, cross-language сверка, трейс, кросс-слойная сюита, записанные матчи | CLI-1..10 |
| `netcode` | Server-auth, предсказание, per-client фильтрация, единая версия | NET-1..17 |
| `netcode-transport` | Модуль сетевого слоя, транспорт, сервер матча, протокол, хендшейк, темп, доставка событий, эпоха мира | NTR-1..16 |
| `net-session` | Основание матча: где исполняется авторитетный цикл, рандеву и инвайты, лобби и заморозка ростера, граница доверия режимов | SES-1..9 |
| `fog-of-war` | `Vision`/`Visibility`/`Stealth`, `VisibilitySystem` | FOW-1..9 |
| `editor` | Редактор сцен и геймплея на стеке движка, веб и десктоп: вьюпорт, расстановка, кисти, триггеры, превью; оболочка — визуальный язык, каркас областей, реестры вкладов, локали, слой операций, машинное само-описание, знак и подсказка инструмента, клавиатура активной области | ED-1..32 |
| `diagnostics` | Приёмник диагностики, форма записи, уровни трейса, трейс потока Command Buffer | DIAG-1..7 |
| `assets` | Presentation-ассеты: handle'ы, загрузчики, кэш, манифест визуалов, карта кривизны, эффекты камеры, раздел decoration-видов, секция конфига камеры, выборка поверхности модели, запечённые производные модели и параметры яруса записи | ASSET-1..13 |
| `rendering` | Рендер-наблюдатель: интерполяция, модели, анимации, кости, скины, террейн, кривизна, наклон по нормали, вертикальное смещение инстанса, перёд модели, подсистемы, документные источники инстансов и террейна, picking и служебные наложения вьюпорта, переподача манифеста, набор decoration-инстансов, ярусы представления инстанса, отсечение и уровни детализации | REND-1..22 |
| `camera` | Конвейер позы: режимы follow/free-RTS/fly, зум, слой эффектов по манифесту, кадрирование по границам, машинное описание типов эффектов; общий клиенту и вьюпорту редактора | CAM-1..9 |
| `client-shell` | Клиентская оболочка веба: ядро в воркере, рендер в main, канал transferable ping-pong, conflation, обратный канал | SHELL-1..7 |
| `input-devices` | Слой между устройствами и `InputFrame`: единый контракт источника, семантический сэмпл, латчинг фронтов до границы тика, биндинги-данные, сосуществование устройств | INP-1..5 |
| `match-hud` | Экранный интерфейс идущего матча: DOM-слой поверх вьюпорта в главном потоке, наблюдатель доставленного состояния, действия с двумя адресатами, композиция виджетов данными, иконки как asset ID, миникарта с таблицей маркеров, живой портрет как изолированный стенд модели | HUD-1..7 |
| `game-content` | Дерево `content/`: контент вне пакетов движка, два поддерева, один документ — один источник правды, границы фикстур, доставка в рантайм, авторские источники level-дизайна | CONT-1..6 |
| `presentation-scene` | Парный presentation-документ сцены: парность именем, состав и запись decoration, квантование трансформа, изоляция от симуляции, переход decoration ↔ prop | PRES-1..5 |
| `blender-pipeline` | Blender как редактор level-дизайна: одно-направленный импорт glTF, парность источника именем и владение пространственным слоем, конвенции экспорта, детерминизм и атомарность, импортёр как потребитель слоя операций редактора, изоляция от движка и тестов, аддон, террейн и кривизна, паритет визуала, hot-reload | BLND-1..12 |
| `desktop-shell` | Десктоп-контейнер веб-приложений репозитория: один контейнер на редактор и игру, словарь границы (пути, байты, события, диалоги, окно), раздача бандла и дерева контента, профили приложений как whitelist возможностей, независимость от движка и сменяемость реализации | DSK-1..6 |
| `bot-player` | Бот-игрок: обычный `MatchClient` со слотом ростера, контракт мозга как DI-шов, граница честности (только фильтрованные снапшоты), исполнение вне потока сервера, детерминизм-нейтральность, профили поведения как контент | BOT-1..6 |

Требование ищется по ID: `openspec spec show <capability>` либо grep по `openspec/specs/`.

## 3. Механизм против политики

Сквозное разделение, которое легко потерять при реализации:

| Механизм (core) | Политика (evaluator / геймплей) |
|---|---|
| Переходы WSM + `HistoryProvider.seekTo(tick)` | Ульта отката: глубина, cooldown, стоимость, автостоп |
| `TimeScaleModifiers.sources` + произведение с клампом | Как стакаются слоу: сильнейший / аддитив / мультипликатив |
| `VisionModifier.sources` | Правила баффов обзора |
| Фильтр снапшота с параметром `viewpoint` | Кто авторизован получать `viewpoint = ALL` |
| Хендшейк предъявляет пару версии и сверяет её | Что показать игроку на отказе, откуда качать обновление |
| Predicted-кадр при отсутствии ввода к дедлайну | Сколько тиков терпеть молчащего игрока до конца матча |
| Слоты матча как упорядоченный список | Кто в какой слот попадает — матчмейкинг |
| `levelAt` + фильтр видимости по уровню | Что даёт превышение по высоте в балансе |
| Действие `carveFloor` и запрос `hasFloorAt` (TERR-8) | Что выбивает пол, каким радиусом, какой ценой |
| Клетка без пола как состояние | Судьба попавшего в неё: смерть, падение, урон |
| Граница арены + событие `LeftArena` | Что бывает за выход: килл, урон, возврат, отсрочка |
| Изменение радиуса командой | Расписание сужения: когда, на сколько, до какого предела |
| `findPath` + бюджет поиска в узлах | Куда идёт агент, когда перепланирует, как обходит живых |
| Проигрывание/кроссфейд клипов | Маппинг «состояние → клип» в манифесте визуалов |
| Пост-анимационный override костей | Лимиты углов и скорость доворота в манифесте |
| Подмена текстур инстанса | Состав скинов в манифесте |
| Конвейер позы камеры и стек её эффектов (CAM-2, CAM-6) | Секция эффектов в манифесте визуалов: что трясёт камеру, с какой амплитудой (ASSET-8) |
| Посадка инстанса по нормали визуальной поверхности (REND-10) | Сама карта кривизны и коэффициент наклона записи (ASSET-7, ASSET-6) |
| Дуга прыжка и снижение при провале как вертикальное смещение инстанса (REND-12) | Высота дуги, скорость и глубина снижения в записи манифеста (ASSET-6) |
| Батчевая отрисовка, VAT-скиннинг, отсечение и переключение уровней (REND-20..22) | Ярус записи и пороги LOD в манифесте визуалов: что рисуется батчем, что штучным скелетом, на каком экранном размере грубеет (ASSET-13) |

Правило: core никогда не знает баланса. Если в ядро просится число, которое может поменять геймдизайнер, — оно живёт в JSON.

С 2026-08-05 у правой колонки есть адрес: игровой контент живёт в дереве `content/` вне пакетов движка, и это проверяется механически (capability `game-content`, CONT-1). Тестовые фикстуры движка — golden-эталоны и сцены кросс-слойной сюиты — контентом не являются и остаются в `engine/` (CONT-4): основание границы — что именно пинает тест, и оно записано в самом требовании; здесь достаточно помнить, что golden-эталон фиксирует поведение движка, а не баланс игры.

## 4. LLM-friendly dev workflow

Не требования к системе, а следствие JSON-first архитектуры — почему она выбрана:

- JSON-first упрощает генерацию контента агентом.
- Строгие JSON-схемы → агент генерит валидные компоненты и системы.
- Документированные Action DSL и JsonLogic-операции — контекст для few-shot.
- Cross-language парность поддерживается агентом (генерация Rust-версии по TS-спецификации), включая системы на TS вроде `VisibilitySystem`; сверка — golden-файлами.

## 5. Roadmap

Фокус: полностью рабочий data-driven тик + детерминизм с самого начала. Единый интерфейс `System` появляется рано, чтобы подменяемость JSON↔код проверялась не в конце, а в момент появления evaluator'а.

Порядок подчинён приоритету «сначала гибкий редактор с системами, потом геймплей сверху». Редактор идёт сразу после минимума, который ему нужен (evaluator, формат данных, террейн), а не после всего ядра: он и есть проверка гипотезы, ради которой затевалась data-driven архитектура — что геймдизайнер собирает системы и арену без правки кода. Проверять её после того, как поверх формата написаны физика, netcode и FoW, поздно.

| Этап | Задача | Результат |
|---|---|---|
| 0 | Формальная спецификация core | ✅ `openspec/specs/` + JSON-схемы |
| 1 | Fixed-point Math API | ✅ `engine/core-ts/src/math/fixed.ts`, `math/vector.ts`, `math/mathApi.ts` |
| 2 | ECS foundation + Query API + Command Buffer | ✅ Собственное SoA-хранилище, битовые маски, flush per-system |
| 3 | Entity IDs + RNG streams | ✅ Generational IDs 24+24; xorshift128 с именованными стримами |
| 4 | `System` / `SystemContext` + `SystemRegistry` | ✅ Контракт зафиксирован, DI Math/Physics |
| 5 | Event Bus + Scheduler + Tick loop | ✅ `tick()` на системах TS, мутабельный мир |
| 6 | Expression Evaluator | ✅ `engine/core-ts/src/dsl/expr.ts`: свой обход JsonLogic-AST, Q16.16, sandbox через тип |
| 7 | Action Executor | ✅ `engine/core-ts/src/dsl/actions.ts`: девять действий, все ECS-мутации командами, `spawn` с переопределением полей prefab'а |
| 8 | System Evaluator = `EvaluatedSystem` | ✅ `engine/core-ts/src/dsl/evaluatedSystem.ts`: JSON-система в том же реестре, `override` по имени, валидация дерева на регистрации |
| 9 | Serialization + JSON-схемы | ✅ `src/sim/serialization.ts`, `src/sim/scene.ts`, `src/dsl/schemas.ts`, `engine/schemas/*.json`: plain-форма мира, конфиг сцены, схемы порождаются из ядра |
| 10 | CLI + golden-file test suite | ✅ `src/sim/scenario.ts`, `bin/sim.mjs`, `engine/tests/golden/`: прогон сценария из одного JSON, побитовая сверка (`viewpoint = ALL`). Ввод и RNG вошли в эталоны change'ами `input-as-components` и `random-in-dsl` |
| 11 | Terrain: карты уровней и пола, `levelAt`, генерация cliff-геометрии | ✅ `engine/core-ts/src/systems/terrain.ts`: ассет в конфиге сцены, `levelAt`/`levelOf`/`hasFloorAt` в `SystemContext`, карта пола — компонент на singleton'е, обрывы — производные отрезки для физики |
| 12 | Editor MVP | ✅ (десктопная оболочка закрыта этапом 38) `editor/core-ts` (`@game-mvp/editor-core`) — headless-каркас: сессия документов и одна на всех история операций (ED-18, ED-29), реестры вкладов и машинный каталог (ED-25, ED-30), валидация со структурным результатом поверх валидаторов движка (ED-8), строковые ресурсы с локалями `ru`/`en` и подсказки по вычисленному ключу (ED-27, ED-28), интерфейс хоста среды (ED-12), модель блоков DSL, производная от каталогов ядра; DOM в `lib` этого пакета нет — «операции исполнимы без интерфейса» проверяет компилятор. `editor/ui-ts` (`@game-mvp/editor-ui`) — DOM-сторона и веб-приложение: визуальный язык на токенах (ED-22), каркас четырёх рабочих областей со скелетом «навигатор — поверхность — инспектор», палитра команд и поиск (ED-23, ED-24), вьюпорт сцены поверх `render-ts` и конвейера `camera` со стартовым обзорным кадром (ED-1, ED-13, ED-15, CAM-8), знаковый бар и клавиатура активной области (ED-31, ED-32), кисти террейна и кривизны (ED-10, ED-11), расстановка, picking и выделение сим-объектов и декораций (ED-16, ED-17, ED-19), область объектов — схемы компонентов и пара «prefab — запись манифеста» (ED-6, ED-7), область систем на блоках DSL (ED-4, ED-5), область ассетов с просмотрщиком и секцией эффектов камеры (ED-20, ED-14), превью в воркер-оболочке `client-ts` (ED-9, ED-26), канонический round-trip сохранения (ED-21). Движковые блокеры сняты по ходу: REND-11 документный источник инстансов, REND-14 документный источник террейна, REND-15/REND-16 picking и наложения, REND-17 переподача манифеста, REND-18 набор decoration-инстансов, кадрирование камеры (CAM-8) и машинное описание типов её эффектов (CAM-9), переподача источников камере (CAM-7), поле `initial` конфига сцены (SER-7, SER-8), раздел decoration-видов манифеста (ASSET-9). Decorations живут в парном presentation-документе сцены — capability `presentation-scene` (PRES-1..PRES-5), заведённая этим этапом. Не закрыто: теги prefab'а (`ecs-foundation` ECS-4). Десктопная среда (ED-12) закрыта этапом 38: контейнер `desktop-shell` и адаптер хоста `editor/ui-ts/src/host/desktop.ts` — шов не менялся, появилась вторая его реализация. С 2026-08-09 **направление level-дизайна заморожено, превью и data-driven-области живут** (этап 35): расстановка, террейн и кривизна авторятся в Blender, ED-требования описывают сохраняемое поведение и нормативно не менялись, а текстурирование террейна ушло из редактора в конвейер (стаб `terrain-texturing`). Требование-к-коду карта — `docs/reviews/2026-08-08-editor-coverage.md` (исторический отчёт плюс дополнение 2026-08-09), порядок работ — `docs/editor-implementation-plan.md` |
| 13 | Physics + raycast/LoS | ✅ `engine/core-ts/src/systems/physics.ts`: коллайдер-компонент, статика обрывов, сетка broad-phase, разрешение движения по осям с событием `Collision`, raycast с маской |
| 14 | Snapshot ring buffer + dirty-tracking | ✅ `src/sim/history.ts`: `HistoryProvider` за интерфейсом, ring buffer со своим интервалом и глубиной; per-component dirty в `src/ecs/world.ts` наполняет `TickResult.changes` |
| 15 | Time system + Tween system | ✅ `src/systems/time.ts`, `src/systems/tween.ts`, `src/systems/modifiers.ts`: `getEffectiveDelta` в `SystemContext`, стакинг через слоты `sources`, нативный `TweenSystem` и действие `addTween` |
| 16 | World state machine + Rewind | ✅ `src/sim/rewind.ts`: `pause`/`beginRewind`/`seekTo`/`resume`, восстановление на месте + реплей по каноническим вводам, exempt-поля, `mode`/`isReplay` в `TickResult` |
| 17 | Netcode: per-client фильтрация в ядре | ✅ `src/sim/filter.ts`: фильтр снапшота и событий с `viewpoint` (NET-12..15). Транспорт вынесен в этап 21, предсказание (NET-2/4/5) — за ним |
| 18 | FoW: компоненты + `VisibilitySystem` + fog-mask | ✅ (кроме рендера) `src/systems/visibility.ts`: `Vision`/`Visibility`/`Stealth`/`Team`/`VisionModifier`, пересчёт по `withinRadius` + `raycast` + фильтр уровня, эмит только при смене битов. Fog-mask (FOW-7..9) — рендер, вне ядра |
| 19 | Arena: граница, принадлежность, сужение, события | ✅ `src/systems/arena.ts`: окружность в ассете, мутабельный радиус компонентом, `LeftArena` и `FellThroughFloor` по переходу (ARENA-1..6). Этапом не значилась — capability появилась после составления roadmap |
| 20 | (Будущее) Rust-порт ядра | Cross-language парность, включая LoS. Трейс даёт субтиковое разрешение там, где golden локализует расхождение только до тика (DIAG-6) |
| 21 | Netcode transport MVP: сервер матча, транспорт, два headless-клиента | ✅ `engine/net-ts` (NTR-1..13): `MatchServer` как чистый цикл без I/O, `Transport` за интерфейсом (loopback + WebSocket), закрытый набор сообщений, хендшейк со сверкой версии (NET-16) и хеша `worldInit` (DET-1), темп с буфером задержки ввода, персональные снапшоты через фильтр ядра, клиент без предсказания со швом под него. Парность записанного матча с `runScenario` — автотестом |
| 22 | Диагностика и трейс ядра | ✅ `src/debug.ts` (приёмник на `Simulation`, DI-5), трейс потока Command Buffer в `src/ecs/commands.ts`, JSONL-вывод CLI (`--trace=`) — DIAG-1..7, CLI-7 |
| 23 | (Будущее) Pathfinding для крипов/NPC | 🟡 Шов готов: `NavigationApi` и `navigation?` в `SystemContext` (`pathfinding` NAV-1..6, DI-4). Алгоритм, навигационные данные и связность сетки — отдельным этапом |
| 24 | (Будущее) Предсказание своей сущности и reconciliation | NET-2/4/5 слоем над клиентом MVP; включается по замеру «нажал→увидел», а не заранее |
| 25 | Render + assets MVP | ✅ `engine/assets-ts` (`@game-mvp/assets`: handle'ы, реестр загрузчиков, MDX/PNG/манифест визуалов) и `engine/render-ts` (`@game-mvp/render`: `RenderHost`-наблюдатель с интерполяцией, подсистемы террейна и моделей, data-driven анимации, bone-контроль, скины) + демо (change `add-render-assets-mvp`, ASSET-1..6, REND-1..8). Fog-mask (FOW-7..9) и частицы — последующие этапы |
| 26 | Клиентская оболочка веба: ядро в воркере | ✅ `engine/client-ts` (`@game-mvp/client`): воркер-хост с тикером, канал transferable ping-pong с ack-conflation (состояние conflatable, события reliable), handshake, обратный канал ввода и WSM, `MessagePort`-транспорт под локальный сервер во втором воркере. `RenderHost` разнесён на `Extractor`/`ViewBuffer` по границе `TickView` — подсистемы не тронуты; демо переехало на воркер-сборку (change `add-worker-client-shell`, SHELL-1..7) и живёт отдельным приложением `game/demo-ts` — оболочка остаётся чистой библиотекой, а контент читает игра (CONT-4) |
| 27 | Игровой контент в дерево `content/` | ✅ Capability `game-content` (CONT-1..5): контент вне пакетов движка, одно-корневое дерево, механическая проверка границы, фикстуры движка контентом не являются (change `extract-game-content-tree`) |
| 28 | Одна сцена дуэли: паритет оболочек | ✅ Одна `content/scenes/duel.scene.json` на матч и демо — выполнение CONT-3, невыполненного этапом 27; движение унифицировано на `Velocity` + физике, физика — зависимость сборки в конфиге матча (NTR-14), событие каста — одно имя `CastFireball`. Эталоны `match-*` переписаны (change `unify-duel-scene`) |
| 29 | Fixed-тригонометрия таблицей | ✅ `sin`/`cos` в Math API и в таблице операторов DSL (FP-7, FP-8; EXPR-2). Единица угла — binary angle measure (оборот = 2^16, заворачивание маской); нормативная таблица первой четверти из 257 узлов с линейной интерполяцией — литерал в исходнике, побитовая парность с Rust-портом — свойство таблицы и правила, а не libm (change `add-fixed-trigonometry`) |
| 30 | Каст фаербола в JSON | ✅ Первый из двух блокеров data-driven геймплея снят. Система `Cast` в `content/scenes/duel.scene.json` разворачивает `aimDir` (единственный скаляр `InputFrame`, TICK-2) в вектор через `cos`/`sin` этапа 29 и спавнит `Fireball` с переопределением `Position`/`Velocity`; нативные `FireballLaunch` и упаковка направления в i32 из демо удалены, `InputFrame` и протокол не тронуты. Событие `CastFireball` несёт `dirX`/`dirY` (change `cast-fireball-in-json`; в change'е не отмечен только ручной прогон демо в браузере) |
| 31 | Разрушение пола действием ядра | ✅ Второй блокер снят: интерфейс пола нормирован (TERR-8) и реализован — действие `carveFloor { at, radius }` в `dsl/actions.ts`, оператор `hasFloorAt` в `dsl/expr.ts`, обход области построчный и круг тот же, что у `withinRadius`. Раскладка карты по словам осталась внутри механизма; демо потеряло нативную `FireballImpact` и локальную адресацию слова, эталон `carve-floor` заведён. Политика — что выбивает пол, каким радиусом, какой ценой — остаётся в JSON: сцена дуэли этой политики больше не несёт (снаряд пола не выбивает), и механизм закреплён эталоном `engine/tests/golden/carve-floor.scenario.json` (change `carve-floor-action`; не отмечен только ручной прогон демо) |
| 32 | Визуальная кривизна террейна и посадка по нормали | ✅ `engine/assets-ts` — вид ассета «карта кривизны» (ASSET-7) и параметры `surfaceAlign` в манифесте (ASSET-6); `engine/render-ts` — `visualSurface.ts` (высота и нормаль визуальной поверхности), сглаженный рельеф поверх ступеней (REND-9) и наклон инстансов slerp'ом с лимитом угла и snap'ом на разрыве (REND-10); карта кривизны демо-арены в `content/visuals/`. Симуляция, `worldInit` и golden не затронуты по построению. Редактору отсюда достаётся кисть кривизны (ED-11) — спека без кода (change `terrain-visual-curvature`) |
| 33 | Камера изометрической арены | ✅ `engine/render-ts/src/camera/` (`camera` CAM-1..7, `assets` ASSET-8): конвейер «режимы → логическая поза → стек эффектов → финальная поза», follow/free-RTS/fly по Dota-семантике, зум, маршрутизация ввода (камере — своё, в `InputFrame` не течёт), эффекты по манифесту визуалов. Конвейер общий игровому клиенту и вьюпорту редактора: follow-цель nullable, поверхность и границы инжектируются, эффекты опциональны (change `camera-system`, стык с редактором — `editor-camera-seam`) |
| 34 | Режимы сессии: игра по инвайту без выделенного сервера | ✅ Capability `net-session` (SES-1..9) и подслой `engine/net-ts/src/session` + `src/lobby`: три оси сетевой сборки — размещение авторитета (`local`/`listen`/`dedicated`), рандеву (внутрипроцессное, адрес, инвайт) и уже бывший транспорт (NTR-2). Реализованы `listen` (авторитетный `MatchServer` в процессе игрока-хоста, свой клиент внутрипроцессным транспортом, SES-6) и `dedicated`; лобби собирает ростер и морозит его в `MatchConfig`, не трогая набор сообщений матча (SES-4). `protocol/`, `matchServer.ts` и `matchClient.ts` не изменены ни строкой — это и есть проверяемая форма SES-2, а канонический лог совпадает у обоих режимов побитово (SES-9). Транспорт шага — WebSocket на одном порту с каналами по пути URL; WebRTC DataChannel для хоста во вкладке приходит следующим change'ем новой реализацией `Transport` и `Rendezvous` (change `net-session`) |
| 35 | Blender как редактор level-дизайна: конвейер glTF → документы | ✅ (кроме задач, помеченных в change'е **[вручную]**: сборка `duel.blend`, прогон аддона по чек-листу, замер цикла, миграция моделей контента) Capability `blender-pipeline` (BLND-1..12): строго одно-направленный конвейер «Blender → движок». Источник `.blend` парен сцене именем (по образцу PRES-1), экспорт — glTF с custom properties, импортёр `tools/blender-ts` (`@game-mvp/blender-ts`, CLI `npm run import`) переписывает только производный пространственный слой — `initial` конфига сцены (SER-7, SER-8), `decorations` парного presentation-документа (PRES-2), ассет террейна (TERR-3) и карту кривизны (ASSET-7), — прочие поля документов не трогая байт-в-байт (BLND-2). Импорт детерминирован и атомарен (BLND-4, BLND-6) и исполняется слоем операций `editor/core-ts` с каноническим сохранением (BLND-5, ED-29, ED-21) — вторых реализаций валидации, сериализации и порядка не заведено. Аддон `tools/blender-addon/` (Python, вне workspace и вне гейта) — основная поверхность авторинга: панели типизированных свойств, операторы, кисти уровня/рампы/пола и кривизны с Geometry Nodes-превью клифов, живая проверка через `import --dry-run` (BLND-8). Паритет визуала (BLND-11): загрузчик glTF заведён в `assets-ts` по ASSET-3 с каноническим представлением ASSET-5, MDX остаётся историческим форматом реестра — сами модели `content/visuals/` на MDX до ручной миграции; единый конфиг камеры получил машинный адрес — секцию манифеста визуалов (CAM-1, ASSET-10). Цикл hot-reload — `import --watch` и переподача документов существующими каналами (REND-11/14/17/18, CAM-7; BLND-12). Рантайм и тесты движка о Blender не знают, гейт зелёный без него (BLND-7); авторские `.blend`/`.glb` живут в дереве контента и не читаются ни одним каналом загрузки (CONT-6). Ядро и netcode не затронуты ни строкой; редактор нормативно не менялся, но его level-дизайн-направление заморожено — этап 12 (change `blender-level-design-integration`) |
| 36 | Батчевое представление инстансов: VAT, отсечение, LOD | ✅ (кроме интерактивного смоука в демо/редакторе) `engine/assets-ts` — запечённые производные модели (ASSET-12): bone-VAT с таблицей клипов, консервативные границы по всем клипам, маска видимости частей по кадрам, набор вариантов скина, кэш по идентичности ассета; параметры яруса и порогов LOD в записи манифеста (ASSET-13). `engine/render-ts` — два яруса представления инстанса (REND-20): батчевый `InstancedMesh` со скиннингом по VAT и скином индексом слоя массива текстур (REND-6), детальный — прежнее поддерево со скелетом; одна машина состояний REND-4 за интерфейсом `AnimationBackend` на оба яруса; отсечение записей по консервативным границам с компактацией инстанс-буферов (REND-21); выбор уровня детализации по экранному размеру с гистерезисом (REND-22). Публичный контракт рендера перестал отдавать `THREE.Object3D`: picking и `instanceFor` отдают преобразование и границы (REND-3). Декорации идут в батчевый ярус тем же сведением (REND-18). Ядро, netcode и контент не затронуты (change `batched-render`) |
| 37 | Бот-игрок: программный участник матча | ✅ (кроме ручного прогона dev-сборки в браузере) Capability `bot-player` (BOT-1..6) и пакет `engine/bot-ts` (`@game-mvp/bot`): бот входит в матч обычным клиентом — свой `MatchClient`, свой слот ростера, ввод из программного источника (INP-1); `MatchServer`, `MatchClient` и протокол не изменены ни строкой (SES-2). Мозг спрятан за контрактом `BotBrain` (`observe`/`sample`) — точкой инверсии зависимостей по образцу DI-швов ядра (BOT-2): замена classic → ML меняет фабрику в сборке, а не хостинг. Граница честности держится конструированием (BOT-3): мозгу передаются профиль, свой слот и `ClientStep` — отфильтрованный персональный поток своего клиента (NET-12..15), и врага в тумане у него нет, как нет и у человека. Исполняется хост вне потока сервера (BOT-4): `BotHost` принимает `Transport`, тонкие entry для браузерного Worker и node `worker_threads` идут через `PortTransport` оболочки. Первый мозг — классический, слоями: восприятие с памятью «последнее виденное» и буфером задержки реакции, utility-скоринг МАРШРУТОВ, слой СПОСОБНОСТЕЙ и микро-слой на Yuka (пин версии). Маршрут и кнопка решаются параллельно, а не соревнуются: бот кастует на бегу. Способности слой знает списком из профиля — бит `buttons`, цель (`enemy`/`threat`/`cliff`), дальность, удержание, кулдаун, — и о механике каждой не знает ничего: шесть боевых кнопок сцены демо (заряжаемый каст, купол, захват, щит, уклон, прыжок) играются одним кодом, а ульта отката времени в список не входит — её бит ведёт хост мира (NET-11). Рельеф сцены (`terrain` TERR-1/TERR-3) приезжает мозгу СБОРКОЙ, тем же путём и по тем же основаниям, что центр арены: иммутабельная часть террейна не снапшотится (TERR-6), контент-пак клиент резолвит локально (NET-16) — граница BOT-3 нормирует источник состояния, а не содержимое ассета. По нему бот берёт уступ прыжком и им же уходит с плато вниз: наземному `cliffRise = 0` обрыв закрыт в обе стороны (PHYS-11). Ручки человечности и сложности — JSON-профили `content/bots/` (BOT-6, форма 2), а не константы кода. Детерминизм ядра не задет: единственный канал влияния мозга — обычные `InputFrame`, и матч с ботом реплеится от записанных вводов побитово без запуска мозга (BOT-5) — change `bot-player` |
| 38 | Десктоп-контейнер редактора и игры | ✅ (кроме дистрибутивов под win/mac — собран и проверен linux) Capability `desktop-shell` (DSK-1..6) и пакет `desktop/shell-ts` (`@game-mvp/desktop-shell`): один контейнер на оба веб-приложения репозитория, различают их профили-данные (`apps/*.app.json`), а не вторая кодовая база. Три слоя: контракт границы без единого импорта (мост — пути, байты, события, диалоги, окно; схема профиля), Node-слой хоста (корни профиля с атомарной записью и наблюдением, раздача бандла и ЖИВОГО дерева контента одним адресным пространством — `fetch('/<id>')` работает как в браузере, ASSET-2) и тонкий клей Electron (`contextIsolation`, `nodeIntegration: false`, custom protocol вместо локального порта). Возможность, не объявленная профилем, недостижима отсутствием канала, а не проверкой: у игры моста нет вовсе. Независимость контейнера от движка и редактора закреплена гейт-тестом (DSK-3), контрактный сьют границы параметризован реализацией и работает на чистом Node — `npm run check` зелёный без установленного Electron (DSK-6); тот же сьют целиком проходит и реализация на Electron, но вне гейта (`npm run contract:electron`: вызовы исполняются в странице настоящего окна). Десктопная реализация `EnvironmentHost` — адаптер на стороне редактора (`editor/ui-ts/src/host/desktop.ts`), выбор среды — по наличию моста в странице; этим снят последний незакрытый пункт этапа 12 (ED-12). Движок, контент и golden-эталоны не затронуты (change `desktop-shell`) |

Этапы 28–31 были последовательностью, а не альтернативами, и пройдены целиком: 28 убил дублирование документов, 30 и 31 перевели драфтовые нативные системы демо в JSON, и нативными в сцене дуэли остались только `InputSystem` и `PhysicsSystem` — механизм движка, которому в JSON не место (TICK-4, DI-3). Этап 29 самостоятелен и стоял перед 30 потому, что делал его дешёвым: с тригонометрией `aimDir` остался тем, чем назван, и нормативный `InputFrame` не тронут вовсе.

Этапы с 21-го появились после составления roadmap и потому идут за «будущим» 20-м: ни транспорт, ни диагностика, ни навигация, ни рендер не были условием ни одного этапа ядра.

Бывший этап сериализации разделён надвое: формат данных нужен редактору, ring buffer снапшотов — нет, он нужен перемотке и netcode. Разделение стало возможным после `mutable-world-state`: снапшоты перестали быть условием того, чтобы история влезла в память.

Заглушек в `TickResult` больше нет: `changes` наполняется per-component dirty (шаг 14), `mode`/`isReplay` — машиной состояний и реплеем (шаг 16).

Что изменилось по ходу шагов 1–5 (детали — в архивных change'ах `2026-08-02-implement-deterministic-core` и `2026-08-02-mutable-world-state`): сторонняя ECS-библиотека не используется (ECS-1), мир мутабелен и история живёт снапшотами по интервалу (TICK-1, SNAP-4). Шаг 9 из-за этого упрощается: dirty-tracking остаётся нужен сетевой дельте и `TickResult.changes`, но перестаёт быть условием того, чтобы история влезла в память.

Что изменилось на шаге 6 (change `2026-08-02-expression-evaluator`): библиотека `json-logic-js` не используется — форма AST сохранена, обход свой, потому что библиотечная арифметика идёт в double, а расширение операторов — глобальная мутация синглтона. Зависимостей у ядра по-прежнему ноль.

Каждый этап заводится как change: `/opsx:propose "<этап>"`.

## 6. Открытые вопросы

Не противоречия ядра, а нерешённые дизайн/фичасибилити-вопросы. Каждый помечен слоем, на котором решается.

1. **Пейс глобальной перемотки** (геймплей). Механизм решён, но в FFA до 10 игроков перемотка одного игрока замораживает мир всем на до 7 с. Открыт баланс: как часто доступна ульта, не убивает ли динамику 20-минутного матча.
2. **Модель отзывчивости** (замер). Схема выбрана: server-auth без предсказания, клиент симуляцию не тикает (`netcode-transport` NTR-10). Lockstep-relay отклонён — размазывает авторитет вопреки NET-1 и потребовал бы писать сервер заново. Прототип есть (`engine/net-ts`), и он даёт число: «нажал→увидел» локально порядка 40–50 мс при 60/30/2. Открытым остаётся, достаточно ли этого на реальном канале и когда включать предсказание своей сущности (NET-2/4/5) — решается замером, а не заранее.
3. **Гранулярность dirty-tracking** (перф). Per-component или per-entity-per-component — по замерам на реальной сцене, этап 14.
4. **Суб-стримы RNG** (контент/редактор). Дефолт — один стрим на систему. Нужны ли именованные суб-стримы и как их объявлять в редакторе — по мере появления систем с несколькими источниками случайности.
5. **Стоимость per-client фильтрации** (netcode/перф). 10 игроков × 30 Гц = до 10 фильтраций снапшота на тик, при delta — ещё и 10 baseline-diff'ов. Замерить; при упоре — снизить частоту рассылки или отложить per-client delta.
6. **Гранулярность видимости** (дизайн/перф). MVP — per-team битмаска. Per-entity потребует другой структуры и ломает дешёвый dirty. Решать только если потребует дизайн.
7. **Кто получает `viewpoint = ALL`** (netcode). Для локального прогона решено: полный поток идёт только соединению, объявленному наблюдателем в конфиге матча, и игровому слоту не выдаётся ни при каких условиях — номер команды слота проверяется на диапазон `[0, 31]`, чтобы `VIEWPOINT_ALL` не въехал опечаткой в конфиге (`netcode-transport` NTR-9). Граница доверия теперь названа по режимам сессии (`net-session` SES-7): в `dedicated` нефильтрованного состояния нет ни у кого из играющих, в `listen` оно по построению лежит в памяти хоста, и режим объявлен пригодным только для доверенных сторон. Открытой остаётся продакшн-авторизация выделенного режима: сетевой аутентификации в MVP нет (инвайт — допуск, а не удостоверение, SES-5), и матч с недоверенными участниками непригоден в любом режиме, пока её нет.
8. **Политика видимости событий и снарядов** (геймплей). Точная политика при разной видимости источника и цели. Отдельно: видны ли снаряды/VFX от невидимой сущности — у снаряда может быть своя видимость, независимая от кастера.
9. **LoS-обрезка тумана в рендере** (рендер). Нужен ли 2D shadow-casting в MVP или достаточно круга-radius. С появлением обрывов вопрос обострился: круг, нарисованный поверх обрыва, засвечивает область, которую симуляция считает невидимой по `FOW-5`.
10. **FoW × глобальный rewind** (геймдизайн/netcode). Во время перемотки, инициированной одним игроком: все видят мир глазами инициатора или каждый свою историческую видимость?
11. **Политика арены** (геймплей). Механизм решён (`arena`: граница, события `LeftArena` и `FellThroughFloor`, сужение радиуса командой), политика — нет: что бывает за выход за край, по какому расписанию сужается арена, какова судьба провалившегося сквозь пол. Политика — свойство конкретной арены, а не игры: продуктовая рамка — песочница арен-потасовок (one-pager), и каждая сцена задаёт свой свод правил своими JSON-системами. Описывается после этапа 8. Узкие вопросы модели террейна (связность сетки, символы флагового слоя, обрезка reveal-круга) живут в `## Open Questions` спеки `terrain`.

12. **Направленный обзор с высоты** (геймдизайн/рендер). Нормативная часть закрыта: FOW-5 приведён к реальному рельефу — обрыв несёт оба тега блокировки (TERR-5), поэтому LoS отсекает пару раньше правила высоты, и сценарий «наблюдатель сверху» переписан под это. Открытым остаётся дизайн-вопрос: нужен ли отдельный направленный алгоритм обзора вниз (в пределах видимой сверху зоны, а не «всё, что не за обрывом»). Живёт в `## Open Questions` спеки `fog-of-war`, здесь — только указатель.
13. **Апдейт-гейт на клиенте** (клиент/продукт). Хендшейк появился: пара версии предъявляется в `Hello`, обе половины сверяются до того, как о матче сообщено что-либо, отказ называет разошедшуюся половину, а хеш `worldInit` сверяется отдельным исходом «разошлись данные матча» (`netcode-transport` NTR-5). Механизм закрыт. Открытым остаётся продуктовая часть: что клиент показывает игроку на отказе и откуда тянет обновление. Канал обновления сервером матча не является — эту границу проводит NET-16 (раздел «Обновление»), и здесь она только упоминается. — *клиент.*

14. **Хранение байтов художественных ассетов** (инфраструктура). Выбор свободен: спека уже фиксирует всё, что делает его свободным — отсутствующий presentation-ассет не меняет ни байта `TickResult` и не входит в `worldInit` (`assets` ASSET-1), ID ассета есть путь от корня дерева, а не адрес хранилища (ASSET-2, `game-content` CONT-2), тесты движка проходят в срезе репозитория без дерева контента (CONT-4), а доставка есть свойство оболочки (CONT-5). Поэтому вариант хранилища намеренно не нормативен: требование вида «бинарники хранятся в LFS» сузило бы выбор, ничего не защитив. Открыт сам выбор — обычный git (сейчас; дерево контента весит порядка сотни килобайт), Git LFS или контент-адресуемое хранилище с манифестом и скриптом выкачки. У вопроса есть срок: переезд с обычного git переписывает историю и ломает все существующие клоны, поэтому решать его нужно до того, как бинарники наберут вес, а не после. Триггер — суммарный вес художественных ассетов около гигабайта либо переход ре-экспортов в регулярные; до него делать нечего. Текущий наклон — LFS, как прозрачный и не требующий своего тулинга; аргумент против него (трафик, который жёг бы CI) снимается тем же CONT-4 — сборке ассеты не нужны, и клонировать она может, не выкачивая их.

## 7. Технические риски

- **Time-манипуляции + сетевой реалтайм-синк на таймингах** — наиболее рискованная связка (риск из one-pager). Механизм заложен через глобальный `Rewinding` и авторитетный сервер, но пейс и ощущение проверяются только прототипом.
