# Tasks: terrain-holes

## 1. Терейн: предикат опоры и восстановление пола

- [ ] 1.1 Добавить в `TerrainApi` запрос `hasFloorWithin(position, radius)`: обход клеток bounding box круга, ближайшая точка прямоугольника клетки через clamp, `distSqLe`, включительная граница; пустое пересечение с сеткой → ответ по ближайшей клетке (TERR-4); ранний выход при первой клетке с полом
- [ ] 1.2 Тесты предиката: точка на полу/над дырой, круг краем над дырой, круг целиком над дырой, касание границы клетки (включительность), круг за краем сетки, вырождение radius=0 в `hasFloorAt`
- [ ] 1.3 Тест восстановления пола (TERR-6): установка бита слова через Command Buffer, dirty-дельта, откат снапшотом возвращает выбитое состояние; уровни и cliff-геометрия не затронуты

## 2. Арена: опорная проверка

- [ ] 2.1 Добавить поле `support: fixed` (дефолт 0) в `ARENA_STATE_COMPONENT`; валидация диапазона `[0, 1]` на загрузке prefab/сцены с тестом
- [ ] 2.2 В `ArenaSystem` заменить точечный `hasFloorAt` на опорную проверку: inradius коллайдера через Physics API (круг — радиус, AABB — меньшая полуось), радиус опоры `support × inradius`, вырождение в точку при `support = 0`, без коллайдера или без Physics API
- [ ] 2.3 Тесты ARENA-5: нависание краем (событие не эмитится), полная потеря опоры (эмитится), нулевой коэффициент = текущее поведение, ненулевой без коллайдера = центр, перенос через дыру за один тик без провала, снаряд с override уровня не падает
- [ ] 2.4 Убедиться, что сборка без Physics API тикает штатно с сущностями `support > 0` (DI-3)

## 3. Схемы, golden, спеки

- [ ] 3.1 `npm run schemas` — регенерация схем с новым полем `ArenaState`
- [ ] 3.2 Golden-сценарий на провал по опорной области (сущность с `support > 0` над выбитым полом) + `npm run golden`; существующие эталоны с дефолтом `support = 0` не должны менять поведение — при сдвиге plain-формы снапшота перезапись по процедуре golden-update с диффом в коммите
- [ ] 3.3 `npm test` и `npm run typecheck` из корня
- [ ] 3.4 `openspec validate --specs --strict` и синхронизация дельт (`/opsx:sync` или архивация change'а по готовности)
