## 1. Фаза 1 — публичный контракт без Object3D, culling, шаринг материалов

- [x] 1.1 Перевести `PickProxy` с `node: Object3D` на трансформ (позиция/кватернион/масштаб) + `ModelBounds`; обновить `picking.ts` (тот же AABB-алгоритм, другой источник трансформа) и `eachProxy`/`proxyOf`/`fillProxy` в `models.ts`
- [x] 1.2 Перевести потребителей прокси и `instanceFor` (overlays REND-16, editor `sceneStage`, тесты render-ts/editor, HUD-портрет) на трансформ+границы; `instanceFor` больше не отдаёт `holder`
- [x] 1.3 Frustum culling детального яруса: консервативные границы инстанса, тест против пирамиды камеры в `updateFrame`, скрытие невидимых holder'ов; без аллокаций на кадр
- [x] 1.4 Шаринг материалов детального инстанса до первой подмены скина (copy-on-write в `createModelInstance`/`applySkin`); общая геометрия и материал заглушек
- [x] 1.5 Прогнать `npm run check`; убедиться, что кадр редактора и демо визуально прежний (запуск `npm run dev -w @game-mvp/editor-ui`)

## 2. Фаза 2 — запечённые производные в assets-ts (ASSET-11, ASSET-12)

- [ ] 2.1 `bakeDerivatives(model: NormalizedModel, params): BakedDerivatives` — bone-VAT (RGBA32F-раскладка, таблица клипов), сэмплирование треков с фиксированным fps; чистая функция, Node-headless
- [ ] 2.2 Консервативные границы по всем клипам (вход REND-21) и маска видимости частей по кадрам (риск BooleanKeyframeTrack из design)
- [ ] 2.3 Набор вариантов скинов записи: нормализация размеров, раскладка под массив текстур, сквозные индексы вариантов
- [ ] 2.4 Кэш производных по идентичности ассета (ASSET-2): запекание один раз на модель; явное «производных нет» для потребителя
- [ ] 2.5 Тесты: побитовый детерминизм двойного запекания; Node без GPU/DOM; сверка позы VAT против позы скелета на опорной модели (допуск)
- [ ] 2.6 Поля записи манифеста (ASSET-12): явный ярус, пороги LOD; валидация манифеста, умолчания (батчевый; детальный при `boneControls`); обновить схему манифеста и тесты

## 3. Фаза 3 — батчевый ярус в render-ts (REND-20)

- [ ] 3.1 VAT-материал: `onBeforeCompile` поверх `MeshStandardMaterial` — скиннинг по VAT-текстуре, смешивание двух клипов (кроссфейд), выбор слоя массива текстур скина
- [ ] 3.2 Пул батчей на (модель × материал): `InstancedMesh`, преаллоцированные инстанс-атрибуты (матрица, clip×2, phase×2, вес перехода, skin), рост ёмкости без пересоздания живых записей
- [ ] 3.3 Скалярный бэкенд `AnimationController` (clip, phase, one-shot, кроссфейд — REND-4) за общим интерфейсом с mixer-бэкендом; события тика и `setClipOverride` работают в обоих
- [ ] 3.4 Выбор яруса по записи манифеста в `models.ts`; деградация модели без производных в детальный ярус с предупреждением один раз; `poseAll` пишет позу батчевой записи в инстанс-матрицу (посадка, наклон, yaw, смещение — REND-9/10/12/13 без изменений поведения)
- [ ] 3.5 Culling батчевых записей: тест границ по клипам, компактация видимых в начало буферов, `instanceCount`; picking и сведение — по полному набору (REND-21)
- [ ] 3.6 Скины батчевого инстанса: индекс варианта на запись, `setSkin`/переподача манифеста (REND-6, REND-17) без пересоздания записи
- [ ] 3.7 Декорации в батчевый ярус тем же `syncPool` (REND-18): статические записи, клип покоя; проверить превью/выход и отсутствие мигания при правке набора
- [ ] 3.8 Переподача манифеста (REND-17) для батчевого яруса: смена модели/`hiddenParts`/яруса записи пересобирает записи в новый батч, остальное — на месте
- [ ] 3.9 Тесты render-ts: жизненный цикл в батче, culling (армия за краем — `instanceCount`), смена скина одной записи, one-shot в батче, деградация без производных, picking по батчевой записи
- [ ] 3.10 Нагрузочный смоук в демо/редакторе: сотни инстансов одной модели — draw calls не растут с N (замер `renderer.info`)

## 4. Фаза 4 — LOD (REND-22)

- [ ] 4.1 LOD-цепочка в производных (при наличии данных у модели): разделяемые упрощённые геометрии, батч на уровень
- [ ] 4.2 Выбор уровня по экранному размеру с гистерезисом; пороги из записи манифеста (ASSET-12); сохранение состояния записи при переключении
- [ ] 4.3 Тесты: переключение без пересоздания, устойчивость у порога, модель без цепочки — прежнее поведение; picking независим от уровня

## 5. Завершение

- [ ] 5.1 Обновить `docs/architecture.md` (ярусы рендера) и координацию спек: `npm run spec-graph -- check` после `/opsx:archive`
- [ ] 5.2 Полный `npm run check` + `npm run coverage` (аггрегатный) — батчевые ветки исполняются тестами
