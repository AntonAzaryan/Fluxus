# Tasks: projectile-cliff-gate

## 1. Core

- [x] 1.1 `collisionGeometry.ts`: условие активации гейта `cliffRise === 0` вместо `<= 0`; комментарий и doc-строка функции обновлены под отрицательный допуск
- [x] 1.2 Тесты PHYS-11: `cliffRise = −1` — подъём 1 блокирован, спуск 1 разрешён, скольжение вдоль ребра не цепляется; `0` и положительные — поведение прежнее (регресс)
- [x] 1.3 Проверить зеркала: подсказки полей коллайдера в бандлах редактора (ED-28), сгенерированные схемы — обновить, если описание `cliffRise` называет только неотрицательные значения

## 2. Спека

- [x] 2.1 Синк дельты PHYS-11 в основную спеку (`opsx:sync`); `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные

## 3. Контент демо

- [ ] 3.1 Снаряды (`Fireball`, `HeavyFireball`): `cliffRise: −1`; тесты — вверх о плато взрывается, вниз с плато пролетает, по рамповому коридору пролетает, над ямой (отсутствие пола) летит, не падает
- [ ] 3.2 Карта `duel.scene.json`: четыре возвышенности уровня 1 (вместо 2/3/4/5) с сохранением рамповых коридоров (`^`-флаги и градиентные клетки под ними); спавны и прочие слои не трогать
- [ ] 3.3 README демо: снять устаревшие оговорки про «обрывы блокируют в обе стороны» и «выстрел с плато детонирует о свой край»; описать новую семантику
- [ ] 3.4 `npm run check` зелёный; golden-эталоны без изменений
