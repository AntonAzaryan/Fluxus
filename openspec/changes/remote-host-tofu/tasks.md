# tasks: remote-host-tofu

## 1. Хост-слой контейнера (desktop/shell-ts, чистый Node — в гейте)

- [ ] 1.1 Книга доверия `src/host/trust.ts`: чтение/атомарная запись `trust.json` («origin → отпечаток», валидация 64 hex, «не понял = записи нет») (DSK-8, D1); тесты: запись переживает перечтение, мусорный файл читается как пустая книга, чужие ключи не роняют чтение
- [ ] 1.2 Порядок решения `decideCertificate` с инъецированным `ask` (D2): пины сервисов → книга → вопрос; согласие пишет книгу, отказ не пишется (D5); смена отпечатка — вопрос с обоими отпечатками (сценарий «Отпечаток известного origin изменился»); тесты на все четыре сценария DSK-8
- [ ] 1.3 Схлопывание по origin (D3): параллельные решения одного origin ждут один `ask`; тест: два одновременных вызова — один вопрос, оба исхода одинаковы

## 2. Клей Electron (вне гейта, DSK-6)

- [ ] 2.1 `certificate-error` в `openSession` переводится на `decideCertificate`; `ask` — `dialog.showMessageBox` с origin, отпечатком (при смене — обоими) и кнопками «Доверять»/«Отклонить»; закрытие окна при открытом вопросе — «отклонить» (D2); `typecheck:electron` зелёный
- [ ] 2.2 Случаи контрактного сьюта (`contract:electron`, автоответ вместо диалога): согласие открывает wss к незакреплённому сервису и повтор проходит без вопроса; отказ отвергает; смена отпечатка предъявляет вопрос со сменой (сценарии DSK-8)

## 3. Сопутствующее

- [ ] 3.1 Текст `PAGE_LIMIT` в `game/server-agent-ts/src/client/browserSocket.ts`: причина отказа называет оба поддержанных пути — локальный агент профиля и диалог доверия контейнера для удалённого (D6); тест текста, если он закреплён тестом
- [ ] 3.2 Синхронизация дельт в основные спеки (`opsx:sync`) — иначе `spec-graph check` красный на новых цитатах

## 4. Сквозная проверка

- [ ] 4.1 `npm run check` зелёный
- [ ] 4.2 Ручная: менеджер в контейнере → «добавить хост» с адресом удалённого агента (можно второй агент на этой же машине на другом порту) → диалог доверия → согласие → хост подключён, пейринг прошёл; перезапуск менеджера — без вопроса; подмена сертификата агента (удалить `tls/` в его каталоге состояния) → предупреждение о смене
