# Tasks: bloom-tone-mapping

## 1. Формат и валидация (engine/assets-ts)

- [ ] 1.1 Тип секции `postprocess` (`toneMapping.operator`/`exposure`, `bloom.enabled`/`strength`/`threshold`/`radius`) рядом с `PresentationLighting` (design D7); словарь операторов закрыт, умолчания документированы и равны сегодняшнему кадру
- [ ] 1.2 Валидация: закрытый состав адресно, `exposure > 0`, `strength/threshold ≥ 0`, `radius` в [0, 1], неизвестный оператор адресно; тексты находок на русском
- [ ] 1.3 Тесты формата: валидные/вырожденные секции, документ без секции разбирается как прежде

## 2. Подсистема пост-обработки (engine/render-ts)

- [ ] 2.1 Подсистема за контрактом REND-8: HDR-цель half-float с проверкой расширения и LDR-деградацией (design D6), проход порога + mip-пирамида bloom + tone mapping через `ShaderChunk` (design D1, D3, D4); при умолчаниях — ни целей, ни проходов
- [ ] 2.2 `applyConfig` в рантайме (ED-15): смена оператора — пересборка одного материала прохода, смена чисел — униформы
- [ ] 2.3 Ручки `postprocess.bloom` и `postprocess.bloomResolution` (QUAL-1/QUAL-3, design D5); счётчики проходов и текселей в сток стоимости (PERF-2/PERF-3)
- [ ] 2.4 Точка освобождения: цели, материалы, пирамида (REND-31)
- [ ] 2.5 Тесты подсистемы: умолчания не добавляют проходов, порог применяется до тонемапа, ручки режут только вниз, снос отдаёт ресурсы

## 3. Пересечение с туманом (FOW-7)

- [ ] 3.1 Порт входа маскирующего прохода (цвет + глубина, design D2): при активной цепочке туман читает выход тонемапа, при неактивной рисует сцену сам, как сегодня; регистрация пост-обработки раньше тумана
- [ ] 3.2 Тесты четырёх сочетаний маска×цепочка и переключения в рантайме: порядок проходов сцена → bloom → tone mapping → маска, свечение под туманом затемнено, кадр без обоих — прямая отрисовка

## 4. Сквозная проверка слоёв

- [ ] 4.1 Интеграционный тест: сцена с секцией и без — `worldInit` и снапшоты не отличаются ни байтом (PRES-4)
- [ ] 4.2 Вьюпорт редактора получает пост-обработку и валидацию секции без правок своего кода (ED-22); правки — дефект шва, разобраться до пуша

## 5. Контент демо

- [ ] 5.1 `content/scenes/duel.presentation.json`: секция `postprocess` (оператор и лёгкий bloom, подгонка глазами — design Risks/Open Questions); `performance`-пресет выключает bloom
- [ ] 5.2 Прогнать `npm run demo`: emissive и факелы светятся, ночная фаза цикла держит глубину, туман гасит свечение; `npm run bench:demo` без деградации порядка

## 6. Спека и гейт

- [ ] 6.1 `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные после синка дельт (REND-34 ↔ PRES-2 ↔ FOW-7)
- [ ] 6.2 `npm run check` целиком; дифф `*.cost.json` демо принять отдельно через `npm run golden:cost` с объяснением в коммите (design D5)
