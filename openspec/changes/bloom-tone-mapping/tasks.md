# Tasks: bloom-tone-mapping

## 1. Формат и валидация (engine/assets-ts)

- [x] 1.1 Тип секции `postprocess` (`toneMapping.operator`/`exposure`, `bloom.enabled`/`strength`/`threshold`/`radius`) рядом с `PresentationLighting` (design D7); словарь операторов закрыт, умолчания документированы и равны сегодняшнему кадру
- [x] 1.2 Валидация: закрытый состав адресно, `exposure > 0`, `strength/threshold ≥ 0`, `radius` в [0, 1], неизвестный оператор адресно; тексты находок на русском
- [x] 1.3 Тесты формата: валидные/вырожденные секции, документ без секции разбирается как прежде
<!-- 1.1–1.3: секция живёт в `presentationPostprocess.ts` — по образцу `presentationLighting.ts`.
     Примитивы валидации секций (подсекция, закрытый состав, числовое поле, цвет) вынесены в общий
     `presentationFields.ts` и переиспользованы обеими секциями: копия этих сорока строк была бы
     вторым определением одних и тех же правил и текстов находок. -->

## 2. Подсистема пост-обработки (engine/render-ts)

- [x] 2.1 Подсистема за контрактом REND-8: HDR-цель half-float с проверкой расширения и LDR-деградацией (design D6), проход порога + mip-пирамида bloom + tone mapping через `ShaderChunk` (design D1, D3, D4); при умолчаниях — ни целей, ни проходов
- [x] 2.2 `applyConfig` в рантайме (ED-15): смена оператора — пересборка одного материала прохода, смена чисел — униформы
- [x] 2.3 Ручки `postprocess.bloom` и `postprocess.bloomResolution` (QUAL-1/QUAL-3, design D5); счётчики проходов и текселей в сток стоимости (PERF-2/PERF-3)
- [x] 2.4 Точка освобождения: цели, материалы, пирамида (REND-31)
- [x] 2.5 Тесты подсистемы: умолчания не добавляют проходов, порог применяется до тонемапа, ручки режут только вниз, снос отдаёт ресурсы
<!-- 2.1: свечение и сведение яркости сводит ОДИН проход, а не два (`RESOLVE_FRAGMENT`):
     пирамида собирается из дотонемапных значений (порог применён выше по цепочке), а оператор
     применяется к сумме — порядок REND-34 соблюдён, а лишней полноэкранной цели и лишнего прохода
     ради него не заводится. Апсемпл-цепочки из схемы UnrealBloomPass нет по той же причине:
     аддитивное накопление в чужую цель потребовало бы гасить `renderer.autoClear`, то есть трогать
     состояние рендерера, которого пакет не держит; ярусы поэтому сводятся весами прямо в проходе
     сведения, а `radius` — тот же зеркальный вес, что у композита UnrealBloomPass.
     Экспозиция названа именем чанка (`toneMappingExposure`): рендерер кладёт в программу своё
     `renderer.toneMappingExposure` ДО загрузки униформ материала, поэтому действует авторское
     число секции. -->

## 3. Пересечение с туманом (FOW-7)

- [x] 3.1 Порт входа маскирующего прохода (цвет + глубина, design D2): при активной цепочке туман читает выход тонемапа, при неактивной рисует сцену сам, как сегодня; регистрация пост-обработки раньше тумана
- [x] 3.2 Тесты четырёх сочетаний маска×цепочка и переключения в рантайме: порядок проходов сцена → bloom → tone mapping → маска, свечение под туманом затемнено, кадр без обоих — прямая отрисовка
<!-- 3.1: порт — `ScenePostSource` в `types.ts`, рядом с `ShadowCasterSink`/`LightCarrierSink`;
     подсистема тумана берёт его опцией `post`, как берёт приёмник теневых кастеров. «Свечение под
     туманом затемнено» пиннится структурно: `tScene` маскирующего прохода — выход сведения, а сам
     он рисуется последним, — живого WebGL в пакете нет и пиксели сравнивать нечем. -->

## 4. Сквозная проверка слоёв

- [x] 4.1 Интеграционный тест: сцена с секцией и без — `worldInit` и снапшоты не отличаются ни байтом (PRES-4)
- [x] 4.2 Вьюпорт редактора получает пост-обработку и валидацию секции без правок своего кода (ED-22); правки — дефект шва, разобраться до пуша
<!-- 4.2: валидация секции досталась редактору по построению — правило `presentationRule`
     (`editor/core-ts`) зовёт `validatePresentationScene`, и второй реализации у него нет.
     САМА пост-обработка потребовала правок вьюпорта, и это не дефект шва, а его форма: подсистема
     ВЛАДЕЕТ финальным проходом кадра (как туман по FOW-7), а вьюпорт до сих пор рисовал кадр
     собственным `renderer.render(scene, camera3)`. Порт здесь невозможен: отдать владение проходом
     можно только вызовом. Правок три, и все — проводка уже имеющегося, а не новая функция
     вьюпорта: регистрация подсистемы первой (`sceneStage.ts`), `postprocess.render(...)` вместо
     прямой отрисовки там же и секция документа в кадр (`sectionsOf` в `sceneDocuments.ts` —
     приватный разбор, отдающий кадру ОБЕ секции документа, `lighting` и `postprocess`, за один
     проход валидации). Своей пост-обработки поверх подсистемы вьюпорт не ведёт
     — этого REND-34 и требует; тождество пиннится тестами `sceneQuality.test.ts` (реестр ручек
     вьюпорта) и `sceneDocuments.test.ts` (секция настоящей сцены доезжает до кадра как есть). -->

## 5. Контент демо

- [x] 5.1 `content/scenes/duel.presentation.json`: секция `postprocess` (оператор и лёгкий bloom, подгонка глазами — design Risks/Open Questions); `performance`-пресет выключает bloom
<!-- 5.1: стартовые значения — оператор `aces`, экспозиция 1, bloom включён (strength 0.35,
     threshold 1, radius 0.5). Числа подобраны по единицам three.js, а не по картинке: порог 1 —
     белая точка линейного кадра, то есть светится только заяркостное, — и ЖДУТ ПОДГОНКИ ГЛАЗАМИ
     (задача 5.2). `performance` гасит bloom потолком ручки; сведение яркости остаётся на всех
     пресетах — это один проход постоянной стоимости, и выключать его значило бы менять облик
     кадра, а не его цену (QUAL-2). -->
- [ ] 5.2 Прогнать `npm run demo`: emissive и факелы светятся, ночная фаза цикла держит глубину, туман гасит свечение; `npm run bench:demo` без деградации порядка
<!-- 5.2 ждёт глаз человека: браузерный прогон демо и `npm run bench:demo` headless-агентом не
     выполняются. Вместе с прогоном стоит перечитать числа секции 5.1 — они стартовые. -->

## 6. Спека и гейт

- [x] 6.1 `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные после синка дельт (REND-34 ↔ PRES-2 ↔ FOW-7)
- [x] 6.2 `npm run check` целиком; дифф `*.cost.json` демо принять отдельно через `npm run golden:cost` с объяснением в коммите (design D5)
<!-- 6.2: `npm run golden:cost` перезаписал четыре эталона (`match-walk`, `match-fuzz`,
     `match-props`, `scaling`). Дифф НЕ аддитивный, и так и задумано: бенч-стенд регистрирует
     подсистему пост-обработки с активной секцией (`BENCH_POSTPROCESS`) — иначе оба новых счётчика
     лежали бы нулями во всех эталонах, и самая дорогая работа REND-34 (до семи полноэкранных
     проходов на кадр) гейтом не стереглась бы вовсе. Основание то же, каким в стенде обоснован
     свет: «без неё счётчики `lighting*` лежали бы нулями... и теневой проход гейт бы не стерёг»
     (`benchLoad.ts`). Что сдвинулось и почему:

     - `postprocessPasses`/`postprocessTexels` — вся новая работа кадра. Две секции эталона несут
       РАЗНЫЕ числа (`performance` 48 проходов против `ultra` 168 на `match-walk`): производительный
       пресет гасит bloom потолком `postprocess.bloom`, и пирамиды в его кадре нет (QUAL-4);
     - `fogRenderPasses` вдвое МЕНЬШЕ (48 → 24): при активной цепочке сцену рисует она, а туман
       заказывает один маскирующий проход. Сумма двух счётчиков равна вызовам спая рендерера —
       это утверждает `cost.test.ts`, и она выросла ровно на проходы цепочки;
     - `frameSubsystems`/`syncTickSubsystems` и `frameInstances`/`syncTickInstances` — на одну
       подсистему больше в реестре стенда (6 → 7). Работы кадра это не добавляет (`syncTick` и
       `updateFrame` у неё пустые), но обход подсистем стал длиннее, и эталон это видит. -->
