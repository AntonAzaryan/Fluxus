## 1. Бот-заполнитель слота (BOT-7)

- [x] 1.1 Политика отложенного подключения ботов: обвязка (`bot-ts`/`net-ts`-сборка), которая по дедлайну подключает ботов к слотам, не занятым людьми; человек до дедлайна занимает слот штатно — `engine/bot-ts/src/fill.ts` (`BotSlotFiller`)
- [x] 1.2 Тесты (node, in-process транспорты): друг успел до дедлайна → матч с двумя людьми, бот не входил; никто не пришёл → бот в слоте, матч стартует; `Hello` после старта → штатный отказ (NTR-6); сервер нигде не ветвится по типу участника — `engine/bot-ts/test/fill.test.ts`

## 2. Выделенный сервер демо-арены

- [x] 2.1 Обвязка `engine/net-ts/bin/` (расширение `serve.mjs` или `demo-serve.mjs`): демо-сцена и матч-конфиг из `content/`, WebSocket, `--port`, `--bot-fill-ms`, бот-заполнитель из группы 1 — `bin/demo-serve.mjs` + поток ботов `bin/demoBot.worker.mjs` (BOT-4)
- [x] 2.2 Авто-рестарт: по `End` (включая silence) поднимается следующий матч с тем же конфигом; цикл в обвязке, `MatchServer` не тронут — проверено вживую (silence 2 с → матч #2 на том же порту)
- [x] 2.3 npm-скрипт запуска из корня + короткий README/подсказка в выводе CLI — `npm run demo:serve`, раздел «Стенд демо-арены» в `engine/net-ts/README.md`, подсказки в шапке вывода

## 3. Демо-сборка браузера

- [ ] 3.1 Сим-воркер демо: сессия `local` — `MatchServer` + p1 через `MessagePort`-транспорт + бот в слоте p2; главный поток — `NetworkShell` вместо `WorkerShell`
- [ ] 3.2 Режимы старта страницы: дефолт — локальный матч с ботом; `?solo` — прежний `WorkerShell` (rewind/pause живут здесь); `?server=ws://…` — подключение к выделенному серверу; выбор слота p1→p2 с откатом при отказе, внятная ошибка «матч занят»
- [ ] 3.3 Кнопка подключения в углу демо (DOM, вне HUD-композиции): переход на `?server=` с захардкоженным адресом сборки; `match-status` прячет pause/rewind, когда оболочка их не даёт
- [ ] 3.4 Тесты: интеграционный прогон демо-сборки на in-process транспортах (человек+бот, старт, ввод доезжает, снапшоты доставляются в `TickView`); тест деградации `match-status`

## 4. Ворота

- [ ] 4.1 `openspec validate demo-net-play --strict` и `npm run spec-graph -- check` зелёные
- [ ] 4.2 `npm run check` из корня зелёный; golden/match-эталоны без диффа
- [ ] 4.3 Ручная проверка юзкейса: `npm run <demo-server>` + две вкладки браузера — p1 и p2 играют, бот появляется при одиночном заходе после дедлайна
