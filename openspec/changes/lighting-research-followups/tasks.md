# Tasks: lighting-research-followups

## 1. HDR-emissive (ASSET-5, D4)

- [ ] 1.1 Поле `emissiveStrength` (default 1) в нормализованном материале `assets-ts/src/model.ts`; MDX-загрузчик пишет 1
- [ ] 1.2 Парсинг `KHR_materials_emissive_strength` в `assets-ts/src/loaders/gltf.ts` + тест на glTF-фикстуре со значением >1 и без расширения
- [ ] 1.3 `render-ts/src/model/build.ts`: `material.emissiveIntensity` из поля; тест «сила 4 доезжает без клампа»

## 2. Hemisphere и rim (REND-29, D1/D2)

- [ ] 2.1 Типы и валидация подсекций `hemisphere` и `rim` секции `lighting` в assets-ts (закрытый состав, адресные отказы) + тесты валидации
- [ ] 2.2 `lighting/config.ts`: разбор подсекций — «нет подсекции — нет источника»; умолчания воспроизводят прежний кадр байт-в-байт
- [ ] 2.3 `subsystems/lighting.ts`: создание/снятие `HemisphereLight` и rim-`DirectionalLight` (без теней, вне реестра кастеров), обновление в рантайме без пересборки (ED-15) + тесты
- [ ] 2.4 Тест: сцена без подсекций — сцена-граф и униформы байт-в-байт как до правки

## 3. Blob-тени (REND-30, D3)

- [ ] 3.1 Расширение `PRESENTATION_SHADOW_MODES` значением `blob` (`none` < `blob` < `hybrid` < `full`) в assets-ts + валидация
- [ ] 3.2 Инстансированные декали в подсистеме освещения: позиции из существующего обхода динамического яруса, размер из габарита вида манифеста; появление/исчезновение вместе с инстансом + тесты
- [ ] 3.3 Режимные переходы: `blob` не строит карт теней, статические кастеры без теней; потолок пресета режет `hybrid`→`blob` по рангу + тесты
- [ ] 3.4 Счётчик числа пятен за кадр в сток стоимости (PERF-2); тест детерминизма счётчика
- [ ] 3.5 Тест «пятно не выдаёт скрытого туманом»: пятно рисуется до маски FOW-7

## 4. LUT (REND-34, D5)

- [ ] 4.1 Загрузчик `.cube` в assets-ts (вид «lut», реестр ASSET-3): парсер → `Float32Array` + размер, лимит стороны, адресные отказы; headless-тесты
- [ ] 4.2 Типы и валидация подсекции `lut` (`asset`, `amount`) секции `postprocess` + тесты
- [ ] 4.3 `postprocess/*`: `Data3DTexture` из ассета, выборка в resolve после tone mapping, lerp по `amount`; недоступный ассет → кадр без LUT + предупреждение + тесты
- [ ] 4.4 Ручка `postprocess.lut` (потолок-выключатель) + тест пресета; реестр ручек в `demoQuality.test.ts`
- [ ] 4.5 Освобождение GPU-ресурсов LUT точкой REND-31 + тест

## 5. Цикл суток (REND-32, D6)

- [ ] 5.1 Поля hemisphere/rim в фазах: типы, валидация «фаза не включает то, чего нет в статике» + тесты
- [ ] 5.2 `lighting/config.ts` (`resolvePhase`) и `lighting/cycle.ts`: правила дыр и интерполяция новых полей + тесты перехода

## 6. Финализация

- [ ] 6.1 `npm run check` зелёный; cost-бейзлайны без диффа (сцены бейзлайнов новых полей не несут)
- [ ] 6.2 Сверка дельт со спеками: `openspec validate --strict`, `spec-graph check`
