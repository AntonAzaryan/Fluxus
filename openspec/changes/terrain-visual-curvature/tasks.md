# Tasks: terrain-visual-curvature

## 1. Ассеты (`engine/assets-ts`)

- [ ] 1.1 Вид ассета `terrain-curvature` и его загрузчик в реестре (ASSET-3): парсинг JSON с текстовыми рядами, алфавит `.`/`1`–`7`/`a`–`g`, валидация размеров и рваных рядов, `failed` с указанием клетки/ряда (ASSET-7)
- [ ] 1.2 Расширение схемы и парсера манифеста визуалов: `surfaceAlign { factor, maxAngle? }` на запись, верхнеуровневый дефолт, ссылка `terrain.curvatureMap`; дефолты `factor = 1`, без лимита (ASSET-6)
- [ ] 1.3 Headless-тесты загрузчика и манифеста: валидные/невалидные карты, несовпадение сетки, дефолты наклона

## 2. Рендер: поверхность (`engine/render-ts`)

- [ ] 2.1 Хелпер `visualSurface(x, z) → { height, normal }`: билинейная интерполяция по угловым значениям одного уровня, рампы в базовой высоте, аналитическая нормаль (design §2)
- [ ] 2.2 Геометрия террейна с кривизной в подсистеме террейна: сглаженный рельеф поверх ступеней, skirt у cliff-стенок; без карты кривизны — прежние ступени (REND-9)
- [ ] 2.3 Обработка несовпадения сетки: предупреждение и рендер без кривизны (ASSET-7)
- [ ] 2.4 Тесты хелпера: непрерывность высоты/нормали внутри уровня, отсутствие «перетекания» через cliff-границу, ограничение амплитуды < полушага

## 3. Рендер: наклон инстансов (`engine/render-ts`)

- [ ] 3.1 Наклон в подсистеме моделей: up = slerp(вертикаль, нормаль, k), лимит угла, per-instance сглаживание, порядок «позиция → наклон → кости» (REND-10, design §3)
- [ ] 3.2 Snap наклона при разрыве непрерывности (спавн, телепорт, rewind/replay — REND-2); пропуск наклона для сущностей с override уровня (TERR-4)
- [ ] 3.3 Тесты: k=0/k=1/промежуточный, лимит угла, snap при перемотке, сущность с override уровня

## 4. Контент и проверка

- [ ] 4.1 Карта кривизны демо-арены в `content/visuals/` + параметры наклона в манифесте (здание k=0, юнит k=1)
- [ ] 4.2 Прогон `npm test` и `npm run typecheck` из корня; убедиться, что golden-файлы не изменились (кривизна не касается симуляции)
- [ ] 4.3 Визуальный смоук: холм, вогнутость, кривизна у обрыва и рампы, юнит бежит по рельефу

## 5. Спека редактора

- [ ] 5.1 Убедиться, что ED-11 не требует кода (редактор — roadmap stage 12); дельта уходит в главную спеку при архивации без задач реализации
