## 1. Списки источников и таблица каналов (ядро)

- [ ] 1.1 Параметризовать `modifierList()` видом значения (тип `i32`, нейтраль `0`) и добавить свёртку `union(ctx, entity)` в контракт `ModifierList` (design D2); проверка — юнит-тесты `modifiers` на занятость слотов, нейтраль 0 и OR-свёртку, `npm run typecheck`.
- [ ] 1.2 Завести компоненты `StealthSources`, `DetectionSources`, `StealthState { mask }`, `DetectionState { mask }` в `systems/visibility.ts`, удалить `STEALTH_SCHEMA`, нормированный порядок группы `fog` — по FOW-3 (design D9); проверка — тест состава/порядка `fowComponents` и `npm run typecheck`.
- [ ] 1.3 Поле `softStealthChannels` конфига сцены: валидация (`[0,31]`, без повторов, требует `fog` — SER-7), `hardChannelsMask` в `Scene`, передача в `VisibilitySystem` из сборки (design D4); проверка — тесты загрузчика на отказ без `fog`, отказ по диапазону/повтору и на умолчание «все жёсткие».

## 2. Пересчёт видимости

- [ ] 2.1 Переписать фильтр скрытости в `VisibilitySystem`: свёртка эффективных масок раз на сущность, пер-наблюдательное `(stealth & hardMask & ~detection) != 0`, мягкие каналы не влияют на `Visibility` (FOW-5, design D5); проверка — `test/visibility.test.ts`: лестница слабый/сильный, детекция у одного наблюдателя команды взводит бит команды, мягкий канал не режет, два источника в разных каналах.
- [ ] 2.2 Публикация `StealthState`/`DetectionState` командами только при изменении (FOW-3, design D3); проверка — тест «источники не менялись → компоненты не dirty» и тест свежести: система раньше якоря 900 читает состояние прошлого тика.
- [ ] 2.3 Прогнать `npm run schemas` (схемы сцены/сценария получают `softStealthChannels` и новые компоненты); проверка — дифф `engine/schemas/*.json` в коммите, `spec-graph check` зелёный.

## 3. DSL

- [ ] 3.1 Оператор `maskCovered [mask, cover]` в `dsl/expr.ts` + генерация схемы (EXPR-2, design D6); проверка — тесты `expr.test.ts`: покрытие, пустая маска, сырые i32 без приведения масштаба, ошибка на не-числах.
- [ ] 3.2 Тест-сценарий политики таргетинга: JSON-условие `maskCovered` над `StealthState` цели и `DetectionState` атакующего запрещает выбор непокрытой цели (FOW-13); проверка — интеграционный тест системы-способности на фикстуре.

## 4. NPC

- [ ] 4.1 Отсечение скрытых целей в выборе цели/скоринге платформы NPC через опциональные handle `StealthState`/`DetectionState` (NPC-10, design D7); проверка — тесты `npc`: агент без детекции не выбирает скрытую цель (жёсткий и мягкий каналы), агент с детекцией выбирает, угроза от скрытого источника копится, но целью он не становится.

## 5. Миграция потребителей и эталоны

- [ ] 5.1 Перевести фикстуры и тесты с `Stealth { active }` на источник в `StealthSources` (`core-ts/test`, `net-ts/test/fixtures.ts`, `integration-ts/test/eventDelivery.test.ts`), обновить `editor/ui-ts/src/areas/objectsSchemas.ts` и `game/demo-ts/app/sim.ts`; проверка — `npm test` пакетов ядра, net, integration, editor-ui зелёный.
- [ ] 5.2 Регенерация эталонов: `npm run golden` и `npm run golden:cost` (рост стоимости `VisibilitySystem` принимается явно); проверка — дифф базлайнов в коммите, `npm test` зелёный.

## 6. Презентация (минимум FOW-13)

- [ ] 6.1 Необязательная секция стелса парного presentation-документа с документированными умолчаниями и адресным отказом на неизвестный ключ (PRES-2, design D8); проверка — тесты валидации документа.
- [ ] 6.2 Применение в демо-рендере: свой юнит с ненулевым `StealthState` — полупрозрачность, чужой невскрытый мягкий — силуэт-плейсхолдер; вскрытость — OR `DetectionState` доставленных союзников; проверка — тест подачи на доставленном снапшоте (без Blender/браузера).

## 7. Ворота

- [ ] 7.1 `npm run check` из корня зелёный (typecheck, eslint, knip, jscpd, depcruise, spec-graph, тесты); дифф эталонов и схем — в том же коммите, что и код.
