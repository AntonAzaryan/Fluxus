# Tasks: editor-render-source

## 1. Спеки

- [ ] 1.1 Дельта `specs/rendering/spec.md`, `ADDED` REND-11: документный источник как второй продюсер presentation-состояния; декларативный набор с устойчивыми ключами и сведением; отсутствие императивного API инстансов; взаимоисключающесть продюсеров; инкрементальность обновления
- [ ] 1.2 Там же, REND-11: явные списки — чего в документном режиме нет (интерполяция REND-2, события и производные от них REND-4/REND-5, фильтр видимости FOW-7..9) и что применяется без изменений (манифест ASSET-6, скины REND-6, кривизна REND-9, посадка REND-10)
- [ ] 1.3 Там же, REND-11: превью ассета (`editor` ED-20) как вырожденный случай — набор из одного инстанса без сцены и террейна; клип задаётся набором с умолчанием на клип покоя
- [ ] 1.4 Дельта `specs/rendering/spec.md`, `MODIFIED` REND-1 (полный текст с правкой): «единственный вход **данных симуляции**»; закрытый перечень легальных несимуляционных входов; конверсия Q16.16 → float на входной границе с инвариантом «глубже fixed-point не проникает»; запрет float → fixed адресован потоку рендера, квантование авторских величин — за редактором вызовом ядра
- [ ] 1.5 Сверить, что дельта не занимает и не правит чужих номеров: заняты только REND-11 (новое) и REND-1 (изменение); ED-1, ED-15, ED-16, ED-20, REND-3, REND-8 — только ссылками
- [ ] 1.6 `openspec validate --change "editor-render-source" --strict` — зелено

## 2. Сверка нормы с кодом (без изменения поведения)

- [ ] 2.1 `engine/render-ts/src/types.ts`, шапка модуля: комментарий дословно повторяет старую REND-1 («единственный вход данных — `TickResult`», «конверсия … происходит ровно один раз», «обратной конверсии float → fixed в пакете не существует») — переписать под новую формулировку, перечислив фактические входы пакета
- [ ] 2.2 `engine/render-ts/src/extractor.ts:234`: комментарий «Единственная точка конверсии Q16.16 → float во всём рендере» неверен уже сегодня (`subsystems/terrain.ts` делит на `FIXED_ONE` при приёме сетки) — переформулировать как «точка конверсии потока тиков», сославшись на инвариант REND-1
- [ ] 2.3 `engine/render-ts/src/subsystems/terrain.ts`: пометить приём `tileSize` и координат cliff-отрезков как вторую точку границы (REND-1, SHELL-5, TERR-2) — сейчас конверсия там не прокомментирована и читается как случайная
- [ ] 2.4 Пройти по `engine/render-ts/src` и убедиться, что за входной границей fixed-point значений и их арифметики нет; найденное — либо перенести на границу, либо зафиксировать отдельной задачей, а не расширять норму
- [ ] 2.5 `npm run typecheck` и `npm test` из корня — зелено; правки только в комментариях, поведение не меняется

## 3. Проверка

- [ ] 3.1 `openspec validate --specs --strict` — все capability проходят
- [ ] 3.2 `git diff --stat`: тронуты `openspec/changes/editor-render-source/` и комментарии в `engine/render-ts/src`; ядро, netcode, контент, `engine/tests/golden/` и `openspec/specs/` не тронуты
