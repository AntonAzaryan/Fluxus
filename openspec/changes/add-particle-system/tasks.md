# Tasks — add-particle-system

## 1. Зависимость и ассетный слой (ASSET-14)

- [x] 1.1 Добавить `three.quarks` в `engine/render-ts/package.json` с точной версией (без `^`); убедиться, что peer-требование к three совместимо с ^0.185
- [x] 1.2 Загрузчик эмиттерного ассета в `assets-ts`: формат `*.effect.json` в реестре ASSET-3, структурная валидация формы корня (headless, без three.quarks), адресные ошибки; тесты в Node с фикстурами валидного и невалидного документа
- [x] 1.3 Расширить тип и валидацию манифеста визуалов: секция `particles` (`byKind`/`byState`/`byEvent`, запись `{ effect, socket?, scale? }`, закрытый состав) и эмиттерный decoration-вид (`effect` вместо модели); тесты валидации, включая неизвестный ключ записи и ссылку на несуществующий ассет

## 2. Подсистема частиц (REND-24)

- [x] 2.1 `render-ts/src/subsystems/particles.ts` по скелету effects.ts: один `BatchedRenderer`, `syncTick` (оболочки byKind/byState по доставленному состоянию, one-shot'ы по свежим событиям), `updateFrame` (`update(dt)` по часам кадра), `snapAll` гасит one-shot'ы и частицы
- [x] 2.2 Ленивое разворачивание документа эффекта в объекты three.quarks с кэшем по идентичности документа; пул one-shot-инстансов на эффект (без clone-на-выстрел); предупреждение один раз и пропуск на невалидной/недоступной ссылке
- [x] 2.3 Привязка к сокету: резолв узла инстанса по имени с кэшем на оболочке, мировая поза каждый кадр, fallback на позицию сущности с предупреждением; интерполяция позиции эмиттера из presentation-состояния
- [x] 2.4 Decoration-эмиттеры: сведение с декларативным набором decoration (устойчивость ключа, создание/удаление/обновление записи), отрисовка этой подсистемой; picking не участвует
- [x] 2.5 `applyManifest`: пересведение живых эмиттеров по новому документу единственным правилом сведения (REND-17)
- [x] 2.6 Тесты подсистемы (по образцу тестов effects): жизненный цикл byState-оболочки, one-shot ровно один раз и сброс перемоткой, сокет и fallback, decoration-эмиттер, пересведение манифеста, счётчики пула

## 3. Подключение и контент

- [x] 3.1 Зарегистрировать подсистему в сборке рендера (host/client и вьюпорт редактора) с прокинутыми `stateComponents`; проверить, что сцена без секции `particles` рендерится без изменений поведения
- [x] 3.2 Демо-контент: 2–3 эффекта `*.effect.json` в `content/visuals/` (дебаф byState, one-shot по событию, факел decoration-видом) и записи манифеста; ручная проверка в редакторе
- [x] 3.3 Спека: снять открытый вопрос «Частицы» в `openspec/specs/rendering/spec.md` при sync изменения; `npm run spec-graph -- check` зелёный
- [x] 3.4 `npm run check` зелёный из корня; в коммит — обновления lock-файла
