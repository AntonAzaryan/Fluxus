# Tasks: editor-terrain-source

Код приехал раньше нормы (задача W0-4 плана редактора), поэтому задач «написать» здесь нет: раздел 2 — **сверка нормы с кодом**, и расхождение решается в пользу нормы (CORE-3), а не подгонкой нормы под факт.

## 1. Спеки

- [ ] 1.1 Дельта `specs/rendering/spec.md`, `ADDED` REND-14: доставка сетки после инициализации как второй продюсер данных террейна; декларативность (сетка целиком, императивной правки клетки нет); сетка приходит производной от ядра, рендер cliff-геометрию, углы рампы и уровни не выводит
- [ ] 1.2 Там же, REND-14: инкрементальность по отношению к кадру с инвариантом «результат совпадает с полной пересборкой на той же сетке»; повторная доставка той же сетки возвращает карту пола документа (возврат из превью ED-9); взаимоисключающесть продюсеров данных террейна по образцу REND-11
- [ ] 1.3 Там же, REND-14: карта кривизны из памяти как несохранённый документ (ED-11), приоритет над догруженным ассетом, несовпадение сетки — предупреждение и игнор (ASSET-7), отсутствие карты — плоские ступени REND-7; неизменность игрового пути (TERR-6, REND-8, SHELL-5, REND-7)
- [ ] 1.4 Дельта `specs/rendering/spec.md`, `MODIFIED` REND-1 (полный текст с правкой): пункт про ассеты расширен на карту кривизны из памяти, пункт про сетку — на доставку после инициализации; в перечень точек приёма fixed-point добавлена доставка сетки; закрытость списка, критерий «не канал к миру» и запрет float → fixed — дословно прежние
- [ ] 1.5 Проверить, что дельта не занимает и не правит чужих номеров: заняты только REND-14 (новое) и REND-1 (изменение); REND-7, REND-8, REND-9, REND-11, TERR-2, TERR-5, TERR-6, ASSET-7, ED-9..ED-11, ED-15, ED-18, SHELL-5 — только ссылками
- [ ] 1.6 Убедиться, что дельт у `assets` и `terrain` нет и почему — обоснование в `design.md` (Decisions 3 и 4); если сверка раздела 2 покажет обратное, вернуться сюда, а не расширять REND-14
- [ ] 1.7 `openspec validate editor-terrain-source --type change --strict` — зелено (флага `--change` у `validate` в этой версии CLI нет)

## 2. Сверка нормы с кодом (без изменения поведения)

- [ ] 2.1 `engine/render-ts/src/subsystems/terrain.ts`, `applyGrid`: сверить с REND-14 — сетка принимается целиком, императивного входа клетки рядом нет, cliff-отрезки берутся из `grid.cliffs` и не выводятся заново; шапка модуля ссылается на REND-14, а не только на ED-10/ED-15
- [ ] 2.2 Там же: сверить возврат пола — сведение идёт с собственной копией карты пола, а не с прежней сеткой, поэтому повторная доставка той же сетки снимает мутацию потока тиков превью (REND-14, ED-9); проверить, что это закреплено тестом, а не только комментарием
- [ ] 2.3 Там же: сверить инкрементальность — правка клетки метит грязными только чанки своей окрестности, пересборка идёт в `updateFrame` («не позже следующего кадра»), владение стенкой на границе чанков однозначно; убедиться, что инвариант «совпадает с полной пересборкой» проверяется тестом, включая правку на границе чанков
- [ ] 2.4 `engine/render-ts/src/surfaceSource.ts`: сверить `setCurvature` с REND-14 — карта из памяти принимается без модуля ассетов, главнее догруженного ассета, несовпадение сетки даёт предупреждение и игнор (ASSET-7), `null` возвращает плоские ступени REND-7
- [ ] 2.5 `engine/render-ts/src/surfaceSource.ts` и `visualSurface.ts`: сверить `setGrid`/`update` с REND-1 — приём `tileSize` остаётся точкой входной границы, глубже fixed-point не проникает; комментарии называют REND-14 как второй случай той же границы
- [ ] 2.6 Пройти по `engine/render-ts/src` и убедиться, что документная доставка не завела второго пути отрисовки террейна: геометрию строят те же генераторы, что игровой путь, и ни один сценовый объект не принадлежит источнику
- [ ] 2.7 Зафиксировать найденные расхождения нормы и кода списком: каждое — либо правка кода отдельной задачей, либо новый change, но не расширение REND-14 задним числом
- [ ] 2.8 `npm run check` из корня — зелено; правки этого раздела не меняют поведения

## 3. Проверка

- [ ] 3.1 `openspec validate --specs --strict` — все capability проходят
- [ ] 3.2 Сверить порядок с `editor-render-source`: его дельта тоже `MODIFIED` REND-1 и уже синхронизирована в основную спеку. Если он будет архивирован позже этого change'а, его текст REND-1 затрёт расширение — тогда правится его дельта (`/opsx:update editor-render-source`), а не основная спека руками
- [ ] 3.3 Убедиться, что тронуты только `openspec/changes/editor-terrain-source/` и, если задачи раздела 2 потребовали, комментарии в `engine/render-ts/src`; ядро, netcode, `content/`, `engine/tests/golden/` и `openspec/specs/` не тронуты
