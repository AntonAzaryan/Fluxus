# Tasks: component-handle-api

## 1. Механизм в ядре (D1, D2, SYS-5, SYS-10)

- [ ] 1.1 Типы `FieldHandle`/`ComponentHandle` (opaque), резолверы и чтение по handle в `ecs/world.ts` одной общей внутренней функцией со строковым путём (владение ECS-7, диагностика); проверка — юнит-тесты резолва (включая ошибку на неизвестном имени с именем в тексте)
- [ ] 1.2 Прокладка в `SystemContext` (`types.ts`, контекст тика); проверка — `npm run typecheck` и регенерация api-surface эталона
- [ ] 1.3 Паритетный тест (D4): `getByHandle` против `get` побитово по всем полям/сущностям, случай без владения, чтение после перемотки и после роста хранилища; проверка — тест красный при любом расхождении семантики

## 2. Миграция NPC и замер (D3)

- [ ] 2.1 Перевести горячие циклы `systems/npc/` (behavior, movement, threat, perception, director, runtime) на handle-чтение; проверка — все тесты NPC зелёные, golden `npc-stress` cost-эталон без изменений
- [ ] 2.2 Замер `npc-stress` до/после (wall-time из bench-вывода) — числа в отчёт коммита; проверка — деградации нет, ожидаемо заметное снижение

## 3. Миграция остальных нативных систем (D3)

- [ ] 3.1 physics, locomotion, abilities/runtime + buffs + phase, visibility, tween — handle-чтение в циклах на сущность; проверка — попакетно `npx vitest run` соответствующих тестов после каждой системы
- [ ] 3.2 Убедиться, что DSL-evaluator и его тесты не тронуты (строковый путь жив); проверка — тесты `dsl/` без правок

## 4. Гейт и эквивалентность

- [ ] 4.1 Полный `npm run check`; golden-эталоны (`*.golden.json`) и cost-эталоны (`*.cost.json`) обязаны быть без диффа — любое их изменение означает ошибку миграции, а не повод регенерировать
- [ ] 4.2 Обновить цитаты требований в коде (SYS-10) и `npm run spec-graph -- check`
