# Tasks: sec02-action-arg-convention

Требование — `specs/data-driven-systems/spec.md` (**SYS-3**), разбор решений — design.md (D1–D4). Гейт один и он локальный: `npm run check` из корня репозитория (CI нет).

Изменение заводилось по факту сделанной работы: валидация спек против кода (`docs/reviews/spec-validation-2026-09-01/02-ecs-dsl.md`) показала, что норма описывает примерно половину действующей конвенции и потому не защищает от пробела в ней.

## 1. Требование (D1–D4)

- [x] 1.1 `specs/data-driven-systems/spec.md`: MODIFIED SYS-3 — конвенция имён аргументов выписана по ролям целиком; ключ вне конвенции и ключ вне состава фильтров запроса отвергаются на регистрации; правило сформулировано как обязанность пополнять конвенцию вместе с новым действием. В пункт о существовании компонентов добавлена ссылка на QUERY-1. Новые сценарии: опечатка в имени необязательного аргумента, опечатка в имени фильтра запроса, новое действие с новым именем аргумента.
- [x] 1.2 Перенести дельту в `openspec/specs/data-driven-systems/spec.md`. Изменение при этом НЕ архивируется: оно остаётся в очереди как запись о принятом решении. Проверка — `npx openspec validate --specs --strict` и `npm run spec-graph -- check`.

## 2. Код (D2, D3)

- [x] 2.1 `engine/core-ts/src/dsl/evaluatedSystem.ts`: ветка `default` обхода аргументов действия отвергает ключ вне конвенции, называя ключ и путь до узла.
- [x] 2.2 Там же: `checkQuery` отвергает ключ вне закрытого состава фильтров (`all`, `any`, `not`, `withTag`, `withinRadius`).

## 3. Тесты

- [x] 3.1 `engine/core-ts/test/evaluatedSystem.test.ts`: аргумент вне конвенции у действия, опечатка в имени необязательного аргумента (`limitt`) и опечатка в имени фильтра запроса (`withinRadiuss`).

## 4. Гейт

- [x] 4.1 Тесты пакетов `core`, `integration` и `demo` (последний читает `content/` целиком — способности, баффы, системы сцены), `npm run typecheck`, `npm run lint`, `npm run lint:dead`, `npm run spec-graph -- check`, `npx openspec validate --specs --strict` и `--changes --strict`.
