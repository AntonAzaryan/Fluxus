# Tasks: rewind-ult

## 1. Core: exempt-компонент и кэш восстановления

- [x] 1.1 `engine/core-ts/src/sim/rewind.ts`: форма exempt `{ component }` — снятие значений у всех живых владельцев до restore, запись обратно живым владельцам после реплея; тесты REW-9 (компонентная форма, сущность создана после конфигурирования, freeze в замороженном мире)
- [x] 1.2 Стоимость `seekTo` при скрабе: замер профиля `interval: 30` на демо-сцене, решение по кэшу восстановленного состояния (реализован и снят — обратный шаг им воспользоваться не может, design Decision 3); тест «путь к точке на состояние не влияет» бит-в-бит
- [x] 1.3 Конвенция события-запроса (имя, payload `initiator`/`depthTicks`): констатировать в типах хоста, ядро событие не интерпретирует; юнит-тест на читаемость запроса из `TickResult`

## 2. Net: дренаж запроса и драйвер скраба

- [x] 2.1 `MatchServer`: дренаж `RewindRequested` после тика → `pause(); beginRewind()`; игнор запроса вне `Running` (REW-12); тест
- [x] 2.2 Драйвер скраба: чтение бита инициатора из входящих кадров в `Rewinding`, `seekTo(point − step)` за цикл рассылки, `step` — конфиг матча (REW-13); тест
- [x] 2.3 Автостоп на `depthTicks` и на глубине истории; резюм на отпускание; таймаут свежести кадров инициатора = отпускание (дисконнект не вешает мир); тесты
- [x] 2.4 End-to-end поверх `integration-ts/test/rewind.test.ts`: ульта из JSON-системы → скраб → резюм; сегментация канонического лога и реплей бит-в-бит; замер стоимости `seekTo` при `interval: 30` (лог в отчёт задачи)

## 3. Client: локальный режим

- [x] 3.1 `workerShell`: тот же дренаж запроса после тика в локальном режиме (SHELL-6), скраб по удержанию из локального семплера; тест

## 4. Render: обратный ход презентации

- [x] 4.1 `viewBuffer.frame()`: знак dt от `TickView.mode` (`Running` +, `Paused` 0, `Rewinding` −), кламп по модулю; тест REND-25 (пауза замораживает, скраб реверсирует)
- [x] 4.2 `VatAnimationBackend`: заворот отрицательной фазы, снятие клампа нулевого кадра, дренаж фейда по |dt|; тест
- [x] 4.3 `MixerAnimationBackend`: `timeScale = dir`, one-shot при реверсе отступает к началу и снимается; тест
- [x] 4.4 `ModelsSubsystem`: гейт «вне `Running` время вперёд не идёт»; тест сценария «мир замер — клипы замерли»
- [x] 4.5 `extractor`: снап только по смене `mode`/эпохи, интерполяция внутри скраба (дельта REND-2); тест «скраб без телепортов»

## 5. Спека и врата

- [x] 5.1 `openspec validate rewind-ult --strict` и `npm run spec-graph -- check` зелёные; перекрёстные ссылки NTR-8/NTR-7/REW-5 на месте
- [x] 5.2 `npm run check` из корня зелёный; golden-эталоны без изменений (механизм не трогает контент)

## 6. Демо-обвязка (минимум, без полного контента ульты)

- [x] 6.1 Бит действия ульты + биндинг свободной клавиши в демо (R занята захватом), JSON-система: rising edge → гейт cooldown 20 с → `emitEvent` запроса с `depthTicks: 420`; exempt-компонент `RewindCooldown` в сцене и в конфиге матча
- [ ] 6.2 Ручная проверка в демо: каст → заморозка → скраб при удержании → резюм на отпускании → автостоп на 7 с; анимации замирают/реверсируются/идут вперёд
- [x] 6.3 Бот при перемотке: сброс памяти по discontinuity/кромке `Rewinding` (пауза ветвь не трогает), hold-интент в замороженном мире с повтором последнего отданного прицела; тесты `engine/bot-ts/test/rewind.test.ts` (BOT-2 алгоритм не нормирует — дельты спеки не требуется)
