# Tasks: rewind-ult

## 1. Core: exempt-компонент и кэш восстановления

- [ ] 1.1 `engine/core-ts/src/sim/rewind.ts`: форма exempt `{ component }` — снятие значений у всех живых владельцев до restore, запись обратно живым владельцам после реплея; тесты REW-9 (компонентная форма, сущность создана после конфигурирования, freeze в замороженном мире, реплей внутри `seekTo` тронул exempt-поле — write-back накрыл результат, значение не зависит от `interval`)
- [ ] 1.2 Кэш последнего восстановленного состояния в контроллере: реплей от кэша внутри интервала, инвалидация по эпохе/`dropAfter`; тест бит-в-бит эквивалентности реплея от кэша и от снапшота
- [ ] 1.3 Конвенция события-запроса (имя, payload `initiator`/`depthTicks`): констатировать в типах хоста, ядро событие не интерпретирует; юнит-тест на читаемость запроса из `TickResult`

## 2. Net: дренаж запроса и драйвер скраба

- [ ] 2.1 `MatchServer`: дренаж `RewindRequested` после тика → `pause(); beginRewind()`; игнор запроса вне `Running` (REW-12); тест
- [ ] 2.2 Драйвер скраба: чтение бита инициатора из входящих кадров в `Rewinding`, `seekTo(point − step)` за цикл рассылки, `step` — конфиг матча (REW-13); тест
- [ ] 2.3 Автостоп на `depthTicks` и на глубине истории; резюм на отпускание; таймаут свежести кадров инициатора = отпускание (дисконнект не вешает мир); тесты
- [ ] 2.4 `matchClient`: признак разрыва — смена режима доставленного состояния (или `isReplay`), а не рост эпохи; сброс буфера интерполяции на той же границе; сравнение пар `(эпоха, тик)` и пересинхронизация оценки серверного тика остаются на эпохе (NTR-10); тесты «шаг скраба не даёт snap», «возобновление даёт snap, хотя эпоха не выросла»
- [ ] 2.5 End-to-end поверх `integration-ts/test/rewind.test.ts`: ульта из JSON-системы → скраб → резюм; сегментация канонического лога и реплей бит-в-бит; замер стоимости `seekTo` при `interval: 30` (лог в отчёт задачи)

## 3. Client: локальный режим

- [ ] 3.1 `workerShell`: тот же дренаж запроса после тика в локальном режиме (SHELL-6), скраб по удержанию из локального семплера; тест

## 4. Render: обратный ход презентации

- [ ] 4.1 `viewBuffer.frame()`: знак dt от `TickView.mode` (`Running` +, `Paused` 0, `Rewinding` −), кламп по модулю; тест REND-25 (пауза замораживает, скраб реверсирует)
- [ ] 4.2 `VatAnimationBackend`: заворот отрицательной фазы, снятие клампа нулевого кадра, дренаж фейда по |dt|; тест
- [ ] 4.3 `MixerAnimationBackend`: `timeScale = dir`, one-shot при реверсе отступает к началу и снимается; тест
- [ ] 4.4 `ModelsSubsystem`: гейт «вне `Running` время вперёд не идёт»; тест сценария «мир замер — клипы замерли»
- [ ] 4.5 `extractor`: снап только по смене `mode` (и `isReplay`), интерполяция внутри скраба при растущей эпохе (дельта REND-2, NTR-10); тест «скраб без телепортов»

## 5. Спека и врата

- [ ] 5.1 `openspec validate rewind-ult --strict` и `npm run spec-graph -- check` зелёные; перекрёстные ссылки NTR-4/NTR-7/NTR-10/NTR-16/REW-4/REW-5 на месте
- [ ] 5.2 `npm run check` из корня зелёный; golden-эталоны без изменений (механизм не трогает контент)

## 6. Демо-обвязка (минимум, без полного контента ульты)

- [ ] 6.1 Бит действия ульты + биндинг свободной клавиши в демо (R занята захватом), JSON-система: rising edge → гейт cooldown 20 с → `emitEvent` запроса с `depthTicks: 420`; exempt-компонент `RewindCooldown` в сцене и в конфиге матча
- [ ] 6.2 Ручная проверка в демо: каст → заморозка → скраб при удержании → резюм на отпускании → автостоп на 7 с; анимации замирают/реверсируются/идут вперёд
