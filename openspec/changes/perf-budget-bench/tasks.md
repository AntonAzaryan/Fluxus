# Tasks: perf-budget-bench

## 1. Счётчики стоимости (PERF-3)

- [x] 1.1 Сток счётчиков рендера в `engine/render-ts` по образцу `debug.ts` ядра: плоская структура именованных счётчиков, инкременты в заранее созданные поля, отсутствие стока — ноль работы и ноль аллокаций; юнит-тест инертности (без стока горячий путь не считает и не аллоцирует)
- [x] 1.2 Инструментировать маску FoW (`fog/mask.ts`, `subsystems/fog.ts`): обработанные тексели, тесты субсэмплов против сегментов, байты загрузки текстуры маски, тексели блита миникарты — счётчики двигаются от разрешения/наблюдателей/сегментов (тест на пропорциональность)
- [x] 1.3 Инструментировать презентационный тракт: инстансы, тронутые в `syncTick`/`frame` подсистем (`PresentationStage` — общий шов), вызовы рендерера через структурный спай; теги стадий tick/channel/syncTick/frame на счётчиках (PERF-2)
- [x] 1.4 Счётчики ядра через сток диагностики (DIAG-1..7, уровень `systems`): исполненные команды Command Buffer, вычисления выражений DSL, пары broad-phase, рейкасты; тик не приобретает ни экспортов, ни аллокаций (прогнать чек-лист determinism-review); тест: два прогона одного сценария — побитово одинаковые счётчики

## 2. Голден-гейт стоимости (PERF-4, PERF-6)

- [x] 2.1 Бенч-прогон в `engine/integration-ts`: записанные матчи (`match-*`, CLI-10) через ядро и презентационный тракт (ViewBuffer → PresentationStage со спаем рендерера, инжектированные часы) — сбор счётчиков всех стадий
- [x] 2.2 Эталоны `engine/tests/golden/*.cost.json`: точная сверка счётчиков; регенерация `UPDATE_COST=1`, корневой скрипт `npm run golden:cost` (в `npm run golden` не входит — удорожание принимается отдельным жестом); тест краснеет с диффом при расхождении
- [x] 2.3 Оси масштабирования S/L (PERF-6): синтетическая нагрузка на `syntheticTick` и фикстурах render-ts — число сущностей, наблюдателей, сегментов укрытий, разрешение маски 4/8; эталон хранит счётчики обоих размеров
- [x] 2.4 Тест машинной независимости: повторный прогон бенча в одном процессе и headless-запуск без GPU/браузера дают побитово тот же эталон (гейт зелёный на голой машине)

## 3. Wall-time сторожа (PERF-5)

- [x] 3.1 `engine/render-ts/test/bench.test.ts` по конвенциям `client-ts`: warmup, печать `[bench] …` при каждом прогоне, ассерт только на порядок; оси — разрешение маски 4/8, наблюдатели, сегменты
- [x] 3.2 Сторож ядра: тиков/сек на записанном матче (там же, в бенч-прогоне integration-ts) — печать всегда, ассерт на порядок

## 4. Референс-профили и браузерный бенч (PERF-1, PERF-2, PERF-7)

- [x] 4.1 Формат референс-профиля устройства и первый профиль `game/demo-ts/bench/profiles/desktop-dev.json`: бюджеты p50/p99 времени кадра (продуктовый минимум 60 fps ⇒ ~16.6 мс p99); числа — данные, в спеку и код не пишутся
- [x] 4.2 Probe стадий в демо за флагом (тик воркера, `remote.frame`, `fog.render` — швы из D4): выключенный probe не меняет обычный запуск
- [x] 4.3 `npm run bench:demo`: Playwright/Chromium поверх демо-стенда (бот-соперник, фиксированный сид, N секунд), отчёт p50/p99 и разбивка стадий против бюджета профиля с вердиктом «сходится / не сходится»; в гейт не входит
- [ ] 4.4 Обновить CLAUDE.md (раздел «Commands»): `npm run golden:cost`, `npm run bench:demo` и их роли (гейт против диагностики)

## 5. Гейт

- [ ] 5.1 Smoke-проверка гейта: локально (без коммита) поднять субсэмплы/разрешение маски — cost-голден краснеет с читаемым диффом счётчиков; wall-time сторож на порядок не реагирует
- [ ] 5.2 `npm run check` зелёный: typecheck, eslint, knip, jscpd, depcruise (бенч не читает `content/` — CONT-4/engine-no-content), spec-graph check без висячих ID; поведенческие golden-наборы не затронуты
