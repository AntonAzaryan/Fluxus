## 1. Синхронизация спеки

- [ ] 1.1 Применить дельты в `openspec/specs/`: `snapshot-rewind` (SNAP-1, SNAP-5), `tick-loop` (OBS-1, OBS-3 с переименованием, OBS-6), `ecs-foundation` (QUERY-3 с переименованием), `data-driven-systems` (SYS-6), `fog-of-war` (FOW-5)
- [ ] 1.2 `openspec validate --specs --strict` из корня — зелено; ID требований в заголовках сохранены, новых номеров не заведено
- [ ] 1.3 Перечитать DET-1 и SYS-5 после применения: оба не менялись и обязаны читаться согласованно с новой парой `WorldState`/`SimulationState` (DET-1 п. 3 и абзац исключений) и с формулировкой SYS-6

## 2. Проверка реализации на соответствие доописанному

Правок кода этот change не планирует: словарь спеки приводится к границе, которую ядро уже проводит. Задачи ниже — сверка, а не работа; расхождение любой из них означает дефект реализации (CORE-3) и отдельный change.

- [ ] 2.1 Сверить состав снапшота с SNAP-1: `takeSnapshot`/`restoreSnapshot` (`engine/core-ts/src/sim/tick.ts`) переносят все части `SimulationState` и ничего сверх
- [ ] 2.2 Сверить время жизни dirty-набора с OBS-6: очистка в начале тика (в том числе на тике вне `Running`), отсутствие набора в копии мира и пустой набор после восстановления (`engine/core-ts/src/ecs/world.ts`, `sim/tick.ts`)
- [ ] 2.3 Сверить результат запроса с QUERY-3: собственный результат на каждый вызов, общий переиспользуемый буфер отсутствует, вложенный запрос внешний набор не портит (`engine/core-ts/src/ecs/query.ts`)
- [ ] 2.4 Сверить, что тест на вложенный запрос внутри `forEach` существует; если нет — завести его в `engine/core-ts/test/` как регресс на пункт QUERY-3 о буфере
- [ ] 2.5 Убедиться, что доступа к миру мимо `SystemContext` у нативных систем нет (SYS-6): мутаторы мира по-прежнему не экспортируются из `engine/core-ts/src/index.ts`

## 3. Хвосты для отдельной работы

Подметание требований вне набора этого change'а. Здесь только фиксация списка — правки идут отдельным change'ом, чтобы не расширять набор задним числом.

- [ ] 3.1 Записать в очередь change на `fog-of-war` FOW-4 (`WorldView` → read-only часть `SystemContext`) и `rendering` REND-1 (`dispatch()` → `onTick`, либо введение шага требованием в `tick-loop`)
- [ ] 3.2 Записать в ту же очередь `netcode` NET-12 («единый полный `WorldState`» → `SimulationState`), `client-shell` SHELL-1/SHELL-2 (проверить прочтение `WorldState`), `snapshot-rewind` REW-2 (перечисление вместо имени объёма), `determinism-core` DET-1 п. 3 (перечисление плоской формы вместо имени `WorldState`)
- [ ] 3.3 Отметить в очереди открытый вопрос про второе значение слова «снапшот» — персональный отфильтрованный срез `netcode` NET-12 против `Snapshot` из SNAP-1
