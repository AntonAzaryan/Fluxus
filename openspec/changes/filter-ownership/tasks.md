## 1. Фильтр как механизм ядра

- [ ] 1.1 `engine/core-ts/src/sim/filter.ts`: переписать шапку модуля и комментарии — фильтр как механизм ядра по NET-13, а не как «netcode вправе поставить свой»; предикат `relevantEntityVisible` и набор `EVENT_ENTITY_FIELDS` объявить реализацией нормы NET-13, а не решением модуля
- [ ] 1.2 Там же: зафиксировать публичность сущностей без `Visibility` явной проверкой в тестах, а не комментарием (NET-12)
- [ ] 1.3 Проверить, что ни один экспорт ядра, нужный фильтру, не появился в `src/index.ts` ради сетевого слоя (NTR-1, TICK-3): фильтр вызывается через уже опубликованную поверхность

## 2. Тесты ядра на фильтрацию

- [ ] 2.1 Тест: сущность без `Visibility` (носитель карты пола, носитель арены) остаётся во всех персональных снапшотах при любом `viewpoint`
- [ ] 2.2 Тест: сцена без `fog` — персональный снапшот совпадает по составу сущностей с каноническим
- [ ] 2.3 Тест предиката событий по NET-13: видима хотя бы одна названная сущность — уходит; ни одной ссылки — уходит всем; ссылка под именем вне закрытого набора видимости не добавляет
- [ ] 2.4 Тест: шина событий внутри персонального снапшота отобрана тем же предикатом, что и поток событий (NET-18) — событие невидимого врага в ней отсутствует

## 3. Проекция на провод

- [ ] 3.1 `engine/net-ts`: строить кадр `Snapshot` из перечисленных NET-18 частей, а не из объекта, возвращённого ядром, целиком; состояния стримов RNG в кадр не класть
- [ ] 3.2 Там же: убедиться, что исключение действует и для соединения наблюдателя (`viewpoint = ALL`) — отдельной ветки «спектейтору можно всё» не заводить
- [ ] 3.3 Проверить, что эпоха (NTR-16) едет полем сообщения рядом с проекцией, а не внутри неё
- [ ] 3.4 Тест сетевого слоя: в декодированном клиентом кадре состояний стримов нет ни при каком `viewpoint`; номер тика, машина состояний и отобранная шина — есть
- [ ] 3.5 Шов с `net-event-delivery` (П2): проверить, что шина в кадре и поток `Events` отобраны одним предикатом (NET-13), одним вызовом фильтра и на одном моменте — тике публикации (NTR-15); второго отбора и второго момента не заводить. Если `net-event-delivery` ещё не применён, требование к составу держит NET-18 в одиночку и потока событий рядом просто нет
- [ ] 3.6 Проверить, что для наблюдателя (`viewpoint = ALL`) шина в кадре неотобрана так же, как неотобран его поток событий: фильтр снимается целиком, а не по каналам

## 4. Конфиг матча: предикат и пересчёт видимости

- [ ] 4.1 `engine/net-ts`: поле конфига матча, называющее действующий предикат видимости события; отсутствие поля даёт нормированный NET-13 предикат
- [ ] 4.2 Там же: поле включения и параметров пересчёта видимости — по форме поля физики (NTR-14) и поля `visibility` документа сценария (CLI-2)
- [ ] 4.3 Регистрация `VisibilitySystem` из конфига матча общим путём сборки — одинаково на сервере и на клиенте (NTR-14)
- [ ] 4.4 Отказ сборки матча до первого тика, если сцена включает `fog`, а пересчёт видимости не объявлен; текст отказа называет пересчёт видимости
- [ ] 4.5 Тест: матч со сценой без `fog` собирается без объявления пересчёта и не требует его

## 5. Интеграция и записанные матчи

- [ ] 5.1 `engine/integration-ts`: конфиги записанных матчей (CLI-10) объявляют пересчёт видимости; проверить, что сервер и клиент поднимают один состав систем
- [ ] 5.2 Тест вертикали (CLI-9): на тике, где противник ушёл в туман, персональный снапшот второго клиента его теряет, а сущность террейна и арены сохраняет
- [ ] 5.3 Проверить парность записанного матча со сценарием (NTR-8): она идёт через канонический лог, поэтому изменением состава кадра двигаться не должна

## 6. Эталоны и проверка

- [ ] 6.1 `npm run record` и `npm run golden` из корня; дифф эталонов матчей разобрать — двигаться должно ровно появление пересчёта видимости, а не состав снапшота CLI
- [ ] 6.2 Убедиться, что вывод CLI (CLI-3) по-прежнему содержит состояния стримов: исключение NET-18 касается провода, а не прогона сценария
- [ ] 6.3 `npm run typecheck` и `npm test` из корня — зелено
- [ ] 6.4 `openspec validate --change "filter-ownership" --strict` — зелено
- [ ] 6.5 Сведение швов перед архивацией: NET-18 и NTR-15 согласны о том, что шина в кадре — отобранная проекция, а не шина тика; NET-18 и NTR-16 согласны о том, что эпоха едет полем сообщения рядом с проекцией, а не внутри неё
