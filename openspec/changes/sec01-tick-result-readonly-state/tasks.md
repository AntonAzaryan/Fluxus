# Tasks: sec01-tick-result-readonly-state

Требование — `specs/tick-loop/spec.md` (**OBS-1**), разбор решений — design.md (D1–D4). Гейт один и он локальный: `npm run check` из корня репозитория (CI нет).

Изменение заводилось по факту сделанной работы: валидация спек против кода (`docs/reviews/spec-validation-2026-09-01/01-core-math.md`) назвала расхождение, правка кода и дельта требования делались одним заходом.

## 1. Дельта требования (OBS-1)

- [x] 1.1 `specs/tick-loop/spec.md`: MODIFIED OBS-1 — поле `state` несёт объём SNAP-1 read-only проекцией; отдельным абзацем сказано, почему неизменяемости самих полей отчёта для этого мало и что норма держится типом. Блок MODIFIED несёт требование ЦЕЛИКОМ вместе со всеми действующими сценариями: MODIFIED заменяет блок, и опущенный сценарий архивация молча выкинула бы.
- [x] 1.2 Там же: абзац о том, что проекция объём не сужает — снятие снапшота и чтение остаются доступны, — и два новых сценария: «Наблюдатель правит состояние через отчёт» и «Наблюдатель ведёт историю мира».
- [x] 1.3 Перенести дельту в `openspec/specs/tick-loop/spec.md`. Изменение при этом НЕ архивируется: оно остаётся в очереди как запись о принятом решении. Проверка — `npx openspec validate --specs --strict` и `npm run spec-graph -- check`.

## 2. Код ядра (D1–D3)

- [x] 2.1 `engine/core-ts/src/types.ts`: `ReadonlyRngRegistry` (только `snapshot()`) и `ReadonlySimulationState`; `RngRegistry` и `SimulationState` расширяют их.
- [x] 2.2 Там же: `TickResult.state` типизирован проекцией, с комментарием о том, что отчёт наблюдаем и каналом записи не является.
- [x] 2.3 Там же: `HistoryProvider.record` принимает проекцию (D3).
- [x] 2.4 `engine/core-ts/src/sim/tick.ts`: `takeSnapshot` принимает проекцию; `restoreSnapshot` полный тип сохраняет — это исключение 2 TICK-3, а не чтение.
- [x] 2.5 `engine/core-ts/src/sim/history.ts` и `src/sim/filter.ts`: `RingHistory.record` и `filterSnapshot` — по проекции.

## 3. Тест (D4)

- [x] 3.1 `engine/core-ts/test/tick.test.ts`, describe «наблюдаемость (OBS-1..3)»: типовая проверка «отчёт о тике не даёт записи ни в одну часть состояния» — пять запрещённых записей под `@ts-expect-error` в неисполняемом теле плюс рантайм-ассерты того, что отчёт остался живым view на то же состояние.

## 4. Гейт

- [x] 4.1 `npm run typecheck` из корня: правка типовая, и именно typecheck доказывает, что потребители отчёта только читали.
- [x] 4.2 `npm run check` из корня.
