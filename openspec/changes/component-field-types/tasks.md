## 1. Набор типов поля

- [ ] 1.1 `engine/core-ts/src/types.ts`: добавить `entity` в `FIELD_TYPES`; в комментарии к `ComponentSchema` — ссылка на ECS-3 как на источник закрытого набора, а не перечисление правил в коде
- [ ] 1.2 `engine/core-ts/src/ecs/world.ts`: выбор контейнера поля по его типу — `Int32Array` для `i32`/`fixed`, 48-битный (`Float64Array`, ID-1) для `entity`; наружу поле остаётся числом
- [ ] 1.3 Там же: значение по умолчанию поля `entity` — «ссылки нет» (`-1`) при создании стора, при добавлении компонента и при спавне из prefab'а без явного значения (ECS-3, ECS-6)
- [ ] 1.4 Валидация схем (ECS-5): неизвестное имя типа поля — ошибка загрузки с внятным текстом; сообщение называет компонент, поле и полученное имя

## 2. Запись непредставимого значения

- [ ] 2.1 Точка применения команды записи поля (`ecs/commands.ts` → мутатор мира): значение вне диапазона типа поля — жёсткая ошибка, а не усечение (ECS-3); текст называет компонент, поле, тип и значение
- [ ] 2.2 Убедиться, что проверка не срабатывает на завёрнутых значениях fixed-арифметики (FP-4) — тест на запись `INT32_MIN`/`INT32_MAX` в поле `fixed`
- [ ] 2.3 Проверить, что проверка не аллоцирует и не зависит от числа полей — она на горячем пути записи

## 3. Ссылка на сущность

- [ ] 3.1 Тесты хранения: полный `EntityId` с `generation ≥ 256` кладётся в поле `entity` и читается без потерь; тот же идентификатор в поле `i32` — ошибка (ECS-3)
- [ ] 3.2 Тест «ссылка на убитую цель»: цель удалена, слот переиспользован — поле не изменилось, проверка живости по полному идентификатору отвечает «невалидна», ядро полей не чистило
- [ ] 3.3 Тест снапшота и отката: поле `entity` входит в снапшот, после отката на тик до смерти цели ссылка снова валидна (ID-4, SNAP-1)
- [ ] 3.4 Тест prefab/overrides: `entity`-поле задаётся prefab'ом и переопределением при спавне (ECS-4, CMD-6); поле без значения получает «ссылки нет»

## 4. Модель типов DSL

- [ ] 4.1 `engine/core-ts/src/dsl/expr.ts`: привести тексты ошибок коэрсий к EXPR-7 — оператор и полученный тип названы; проверить, что `number` в позиции условия (`if`, `and`, `or`, `!`) уже ошибка, а не приведение
- [ ] 4.2 `engine/core-ts/src/dsl/actions.ts`: `cond` действия `if` требует `bool` строго; запись вектора в поле компонента (`modifyComponent`) и в данные события (`emitEvent`) — ошибка вычисления, а не раскладка по именам
- [ ] 4.3 Проверить, что `number` в выражениях нигде не проходит через 32-битный контейнер и не усекается: идентификатор из `forEach … as` и из поля `entity` доживает до предиката живости целым
- [ ] 4.4 Тесты модели типов: число в `cond`; булево в арифметике; `==` над векторами; вектор в поле; строка в позиции значения; чтение флагового поля сравнением с нулём (FOW-3)

## 5. Схемы и эталоны

- [ ] 5.1 `npm run schemas` из `engine/core-ts`: набор типов поля входит в порождаемые `engine/schemas/*.json` (SER-5); дифф — в коммит, руками файлы не править
- [ ] 5.2 Golden-пара на поле `entity` в `engine/tests/golden/`: сцена со ссылкой, смерть цели, переиспользование слота, откат; фикстура движка (CONT-4), а не контент
- [ ] 5.3 `npm run golden` из корня: убедиться, что прочие эталоны не сдвинулись — поведение `i32`/`fixed` не менялось

## 6. Проверка

- [ ] 6.1 `npm run typecheck` и `npm test` из корня — зелено
- [ ] 6.2 Сверить текст ECS-3 с четырьмя его потребителями (`time-system` TIME-7, TWEEN-1/3/4, `fog-of-war` FOW-3, `data-driven-systems` EXPR-2): каждая цитата теперь подтверждается требованием, правки самих потребителей не нужны
- [ ] 6.3 Зафиксировать в очереди работ отдельный change на ARENA-3 (`inside`/`onFloor` объявлены `bool` при отсутствующем bool-типе)
