# Tasks: desktop-shell

## 1. Каркас пакета и контракт моста

- [ ] 1.1 Создать workspace-member `desktop/shell-ts` (`@game-mvp/desktop-shell`): package.json (Electron и упаковщик — devDependencies только здесь), tsconfig, vitest; подключить к корневым `typecheck`/`lint`/`test`
- [ ] 1.2 Описать типы поверхности моста в `src/bridge/`: fs-примитивы (list/read/write/watch по путям в объявленных корнях), диалоги, оконные события, схема профиля (бандл, корень контента, whitelist возможностей) — без единого импорта извне (DSK-2, DSK-5)
- [ ] 1.3 Тест независимости от движка по образцу `engine/integration-ts/test/contentBoundary.test.ts`: `desktop/shell-ts` не импортирует `@game-mvp/*`; добавить lint-ограничение импортов (DSK-3)

## 2. Node-слой хоста и контрактный сьют

- [ ] 2.1 Реализовать `src/host/`: файловые операции в пределах корней профиля, атомарная запись (по образцу `tools/blender-ts`), watcher, отдача байтов для раздачи — чистый Node, без Electron (DSK-2, DSK-4)
- [ ] 2.2 Написать контрактный сьют границы: поведение fs-примитивов, невыход за корни, read-only-профиль отвергает запись, события watcher'а; сьют параметризован реализацией — это ворота для любой будущей реализации контейнера (DSK-5, DSK-6)
- [ ] 2.3 Убедиться, что `npm run check` зелёный без установленного Electron (DSK-6)

## 3. Electron-клей

- [ ] 3.1 Main-процесс: окно, профили из манифеста приложения, protocol handler раздачи бандла и дерева контента поверх `src/host/`; базовый адрес контента сообщается странице на старте (DSK-1, DSK-4)
- [ ] 3.2 Preload: `contextBridge` собирает поверхность моста по whitelist профиля один раз на старте окна; `contextIsolation: true`, `nodeIntegration: false` (DSK-5)
- [ ] 3.3 Smoke-прогон вне гейта: скрипт, поднимающий контейнер с каждым профилем и проверяющий загрузку страницы и доступность заявленных возможностей моста

## 4. Профиль игрового клиента

- [ ] 4.1 Профиль игры: бандл `engine/client-ts/demo` (`npm run demo:build`), read-only дерево контента через protocol handler, мост пустой (DSK-5)
- [ ] 4.2 Проверить загрузку ассетов по ID-путям и работу воркеров/канала в контейнере без правок кода клиента (DSK-1, DSK-4); найденные зашитые адреса свести к базовому адресу от хоста

## 5. Профиль редактора

- [ ] 5.1 Адаптер десктопного `EnvironmentHost` в `editor/ui-ts/src/host/` поверх моста: `ContentTreeHost`, `ContentWatcher`, `ContentPicker`, `WindowHost`; выбор реализации хоста по наличию моста в странице (ED-12)
- [ ] 5.2 Профиль редактора: бандл `editor/ui-ts/app`, read/write + watch + диалоги; прогнать существующие проверки шва (memory/overlay) против адаптера, где применимо
- [ ] 5.3 Сквозная проверка: открыть дерево `content/` в контейнере, выполнить правку и сохранение — байты на диске неотличимы от сохранения веб-средой (ED-12, ED-21)

## 6. Документация и упаковка

- [ ] 6.1 `docs/architecture.md`: строка capability `desktop-shell` в таблице и упоминание в roadmap; краткий README пакета
- [ ] 6.2 Упаковка дистрибутивов (electron-builder или Forge) под win/mac/linux для обоих профилей; вне `npm run check`
- [ ] 6.3 `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные; финальный `npm run check`
