# Tasks: desktop-shell

## 1. Каркас пакета и контракт моста

- [x] 1.1 Создать workspace-member `desktop/shell-ts` (`@game-mvp/desktop-shell`): package.json (Electron и упаковщик — devDependencies только здесь), tsconfig, vitest; подключить к корневым `typecheck`/`lint`/`test`
- [x] 1.2 Описать типы поверхности моста в `src/bridge/`: fs-примитивы (list/read/write/watch по путям в объявленных корнях), диалоги, оконные события, схема профиля (бандл, корень контента, whitelist возможностей) — без единого импорта извне (DSK-2, DSK-5)
- [x] 1.3 Тест независимости от движка по образцу `engine/integration-ts/test/contentBoundary.test.ts`: `desktop/shell-ts` не импортирует `@game-mvp/*`; добавить lint-ограничение импортов (DSK-3)

## 2. Node-слой хоста и контрактный сьют

- [x] 2.1 Реализовать `src/host/`: файловые операции в пределах корней профиля, атомарная запись (по образцу `tools/blender-ts`), watcher, отдача байтов для раздачи — чистый Node, без Electron (DSK-2, DSK-4)
- [x] 2.2 Написать контрактный сьют границы: поведение fs-примитивов, невыход за корни, read-only-профиль отвергает запись, события watcher'а; сьют параметризован реализацией — это ворота для любой будущей реализации контейнера (DSK-5, DSK-6)
      `test/contractSuite.ts`. Реализаций под ним две, и обе настоящие: чистый Node в гейте (`test/hostBridge.contract.test.ts`) и Electron вне гейта (`test/electron/contract.ts`, `npm run contract:electron`) — «любая реализация SHALL проходить один общий сьют» держится прогоном, а не обещанием. Невыход за корни включает ссылку из дерева наружу (`linkOutside`): лексической проверке пути она не видна вовсе.
- [x] 2.3 Убедиться, что `npm run check` зелёный без установленного Electron (DSK-6)
      Проверено буквально: `node_modules/electron` убран из дерева — `typecheck`, `lint`, `spec-graph check` и тесты пакета зелёные. Клей Electron в гейтовый tsconfig не входит (`tsconfig.electron.json` отдельно), а типизированный линт на нём выключен секцией `eslint.config.js` — иначе отсутствующие типы красили бы каждую строку.

## 3. Electron-клей

- [x] 3.1 Main-процесс: окно, профили из манифеста приложения, protocol handler раздачи бандла и дерева контента поверх `src/host/`; базовый адрес контента сообщается странице на старте (DSK-1, DSK-4)
- [x] 3.2 Preload: `contextBridge` собирает поверхность моста по whitelist профиля один раз на старте окна; `contextIsolation: true`, `nodeIntegration: false` (DSK-5)
- [x] 3.3 Smoke-прогон вне гейта: скрипт, поднимающий контейнер с каждым профилем и проверяющий загрузку страницы и доступность заявленных возможностей моста
      Двумя прогонами. Контракт: `npm run contract:electron -w @game-mvp/desktop-shell` — общий сьют границы (24 случая) в настоящем окне через `--contract`; проверено, что прогон не пуст: снятая проверка границы корня краснит его. Загрузка приложений: `bin/smoke.mjs` (`npm run smoke -w @game-mvp/desktop-shell [-- editor|game]`). Сверяет: мост в странице есть, его поверхность совпадает с whitelist профиля (лишний метод — тоже отказ), базовый адрес раздачи сообщён, ассет дерева получен страницей обычным `fetch` по ID-пути, страница не жалуется. Предусловия (бандл, бинарь Electron) называются вслух, а не изображают отказ контейнера.

## 4. Профиль игрового клиента

- [x] 4.1 Профиль игры: бандл `engine/client-ts/demo` (`npm run demo:build`), read-only дерево контента через protocol handler, мост пустой (DSK-5)
      `apps/game.app.json`; бандл — `demo:build:desktop` (та же сборка без копии дерева в `publicDir`: снимок контента внутри бандла заслонил бы живое дерево, которое раздаёт контейнер).
- [x] 4.2 Проверить загрузку ассетов по ID-путям и работу воркеров/канала в контейнере без правок кода клиента (DSK-1, DSK-4); найденные зашитые адреса свести к базовому адресу от хоста
      Прогон: `xvfb-run -a node bin/smoke.mjs game` → «мост [пуст], база fluxus://app/, ассет 3726 Б, Worker function», жалоб страницы нет (воркеры и канал поднялись). Кода клиента не тронуто ни строкой. Аудит адресов: единственный зашитый — `fetch('/${id}')` в `demo/main.ts`; это адрес ОТ КОРНЯ ПРОИСХОЖДЕНИЯ, а не от контейнера, и раздача кладёт дерево в то же пространство имён, что и `publicDir` dev-сервера, — свести его к «базовому адресу от хоста» значило бы завести в клиенте знание о контейнере, ровно то, что DSK-1 запрещает.

## 5. Профиль редактора

- [x] 5.1 Адаптер десктопного `EnvironmentHost` в `editor/ui-ts/src/host/` поверх моста: `ContentTreeHost`, `ContentWatcher`, `ContentPicker`, `WindowHost`; выбор реализации хоста по наличию моста в странице (ED-12)
      `editor/ui-ts/src/host/desktop.ts`; выбор среды — одна строка в `app/main.ts` по `desktopBridgeOf(globalThis)`, не флаг сборки и не user-agent. `chooseRoot` отдаёт корень профиля и системного диалога не открывает: выдать доступ к произвольному каталогу значило бы расширить профиль на живой сессии (DSK-5).
- [x] 5.2 Профиль редактора: бандл `editor/ui-ts/app`, read/write + watch + диалоги; прогнать существующие проверки шва (memory/overlay) против адаптера, где применимо
      `apps/editor.app.json`. `editor/ui-ts/test/desktopHost.test.ts` гоняет утверждения шва (запись↔чтение, одноуровневое перечисление в нормированном порядке, невыход за корень, честный отказ там, где возможности нет) против адаптера поверх НАСТОЯЩЕГО Node-слоя контейнера на временном дереве — подставной «мост» проверял бы согласие теста с самим собой.
- [x] 5.3 Сквозная проверка: открыть дерево `content/` в контейнере, выполнить правку и сохранение — байты на диске неотличимы от сохранения веб-средой (ED-12, ED-21)
      Двумя уровнями. В гейте: один и тот же документ уходит в веб-хост (перехваченное тело PUT) и в десктопный (файл на диске) — байты совпадают побайтно. В контейнере: `xvfb-run -a node bin/smoke.mjs editor` → «мост [dialog,read,watch,window,write], база fluxus://app/, ассет 16 Б, Worker function, сквозная правка сошлась побайтно» — страница пишет документ мостом, читает обратно, прогон сверяет байты на диске. Правит smoke временное дерево, а не `content/` репозитория: прогон не оставляет следов.

## 6. Документация и упаковка

- [x] 6.1 `docs/architecture.md`: строка capability `desktop-shell` в таблице и упоминание в roadmap; краткий README пакета
      Строка capability, этап 38 в roadmap и снятая оговорка «кроме десктопной оболочки» у этапа 12 (ED-12 закрыт этим change'ем); `desktop/shell-ts/README.md`; отметка в `docs/editor-implementation-plan.md`.
- [x] 6.2 Упаковка дистрибутивов (electron-builder или Forge) под win/mac/linux для обоих профилей; вне `npm run check`
      `bin/pack.mjs` (staging: бандл, дерево контента и профиль с путями ВНУТРЬ дистрибутива) + `electron-builder.cjs` (цели win/mac/linux, публикации нет). Проверено на linux: `node bin/pack.mjs game --linux dir` собрал AppImage и tar.gz, а разложенный каталог запускается и без упаковщика — профиль берётся из `fluxus.app` манифеста приложения, ассет дерева отдаётся страницей (`SMOKE ... probe 200/3726`). **Дистрибутивы win/mac в этом окружении не собирались**: NSIS требует wine, а DMG — macOS; конфиг для них написан, но не исполнялся.
- [x] 6.3 `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные; финальный `npm run check`
      `openspec validate --specs --strict` — 31 passed, 0 failed; `openspec validate desktop-shell --strict` — valid; `npm run spec-graph -- check` — граф зелёный.
