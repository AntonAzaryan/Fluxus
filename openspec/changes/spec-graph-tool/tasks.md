# spec-graph-tool — tasks

## 1. Парсер и модель графа

- [ ] 1.1 `scripts/spec-graph.mjs`: обход `openspec/specs/*/spec.md`, парсинг `### Requirement: <ID> <заголовок>` в модель {capability, id, title, файл, строка, тело секции}; шапка/Purpose capability сохраняются отдельно
- [ ] 1.2 Извлечение ссылок из тел секций: паттерн ID, сверка со словарём определений, направление «цитирующий → цитируемый», исключение самоопределения; заявленная атрибуция (`` `имя` ID ``) — отдельным полем ребра
- [ ] 1.3 Агрегация до capability-уровня (рёбра с весом = число ссылок)
- [ ] 1.4 Тест-на-формат (D8): парсер по текущему дереву находит 317 требований, ID уникальны, число ссылок в ожидаемом диапазоне; тест в корневой vitest-набор

## 2. Команды навигации

- [ ] 2.1 `find <текст>` — поиск по заголовкам и телам, вывод только ID + заголовки
- [ ] 2.2 `where <ID>` и `show <ID…>` — секция + Purpose по умолчанию, `--capability` для всей спеки
- [ ] 2.3 `refs <ID>` (in/out) и `impact <ID>` (транзитивное замыкание зависимых)
- [ ] 2.4 `deps <capability>` и `graph --mermaid`
- [ ] 2.5 `--json` на всех командах; `--help` с честным ограничением «main specs, дельты changes/ не видны»
- [ ] 2.6 Скрипт `spec-graph` в корневом `package.json` (`npm run spec-graph -- <cmd>`)

## 3. Линт

- [ ] 3.1 `check`, инварианты: висячие ссылки, дубли ID, заявленная атрибуция ≠ факт, секция без SHALL/MUST NOT, цикл на capability-уровне; exit 1 при находках, режим отчёта — не в `npm run check` (D7)
- [ ] 3.2 Конфиг слоёв `scripts/spec-graph.layers.json`: порядок foundation → sim → net → client → editor/content по `docs/architecture.md`, правило «вверх нельзя», mechanism не цитирует game-content; исключения — рёбра editor↔(rendering, camera, serialization) с обоснованиями
- [ ] 3.3 Проверка слоёв в `check`; `terrain → ED-10` в исключения не вносится — остаётся находкой
- [ ] 3.4 `check --metrics` (всегда exit 0): fan-in, instability Ce/(Ca+Ce), сироты
- [ ] 3.5 Тесты линта на синтетических фикстурах: по одному красному кейсу на каждый инвариант
- [ ] 3.6 Прогон `check` по текущему дереву, находки — списком в комментарий/вывод задачи (не правятся здесь; известный красный: до-`spec-modality-and-refs` дефекты)

## 4. Интеграция для агентов

- [ ] 4.1 CLAUDE.md: норма пользования — цитируешь ID через `spec-graph show`, перед правкой требования `impact`, свою capability читай целиком
- [ ] 4.2 Скилл `.claude/skills/spec-graph/SKILL.md` по образцу `spec-coverage`: триггеры («найди требование», «кто зависит от», «покажи граф спек»), шпаргалка команд, ограничение про дельты
- [ ] 4.3 Финальная проверка: `npm run check` зелёный (без spec-graph-линта), сценарий из design («контур задачи ~70 строк») воспроизводится руками
