## 1. Ассеты (engine/assets-ts, ASSET-11)

- [ ] 1.1 Реализовать пересечение луча с треугольниками канонической модели в канонических осях: точка, `t`, нормаль треугольника; модель без геометрии — «нет пересечения»
- [ ] 1.2 Завести ленивую ускоряющую структуру (BVH) на разделяемых данных ассета: строится по первому запросу, кэшируется на ассете, геометрия не копируется (design §2)
- [ ] 1.3 Node-тесты ASSET-11: вертикальный луч по glTF-фикстуре настила, разделение структуры двумя потребителями, модель без треугольников; без браузера и рендер-библиотеки

## 2. Формат и редактор (editor/core-ts)

- [ ] 2.1 Добавить `walkable` в схему и валидацию записи decoration (PRES-2): необязательный булев, небулево — адресный отказ; состав записи остаётся закрытым
- [ ] 2.2 Канонический round-trip (ED-21): `false`/отсутствие неразличимы и не сериализуются; тесты «открыл — сохранил» на документах с и без `walkable`
- [ ] 2.3 Локали находок валидации (ru/en) для небулева `walkable`

## 3. Поле высот (engine/render-ts)

- [ ] 3.1 Прокинуть `walkable` в `DecorationInstance` и через `DecorationSet` до владельца поля; правка флага/позиции walkable-записи — инвалидация клеток под старым ∪ новым bbox (design §5)
- [ ] 3.2 Реестр walkable-инстансов в `VisualSurfaceSource`/`visualSurface.ts`: bbox → клетки, матрица инстанса = матрица картинки (посадка/наклон по террейн-форме, design §3), `heightAt`/`heightInCell`/`normalAt` с правилом max; клетки без walkable — прежний путь байт-в-байт
- [ ] 3.3 Асинхронная догрузка модели walkable-декорации: до `ready` — террейн-форма, по готовности — notify списком клеток (по образцу карты кривизны)
- [ ] 3.4 Нормаль на walkable-поверхности — нормаль треугольника; посадка и наклон инстансов (REND-10) на мешe со snap'ом для декораций и временным сглаживанием для юнитов (design §6)
- [ ] 3.5 Тесты поля: юнит на настиле над лощиной, max при пересечении двух мешей, fallback до готовности ассета, сцена без walkable — прежнее поле, посадка walkable-декорации без рекурсии

## 4. Picking (engine/render-ts)

- [ ] 4.1 Объединить марш по террейн-форме с рейкастом по walkable-инстансам: min-t, walkable-попадание — surface-hit с клеткой под мировой точкой (design §4); аллокаций в пути наведения не добавлять
- [ ] 4.2 Тесты REND-15: кисть по настилу (попадание в мост, не в лощину), клик по мосту в общем порядке — decoration-инстанс раньше поверхности, walkable незагруженной модели не пикается

## 5. Конвейер Blender (tools/blender-ts, tools/blender-addon)

- [ ] 5.1 Импорт custom property `walkable` в запись decoration (BLND-3): `true`/`1` → поле, `false`/`0`/отсутствие → поля нет; иное значение и walkable не на visual-объекте — адресная ошибка (BLND-6)
- [ ] 5.2 Фикстуры `.gltf`/`.glb` и тесты импортёра: мост (terrain + walkable-объект), повторный импорт — пустой дифф (BLND-4), ошибочные значения
- [ ] 5.3 Чекбокс `walkable` в панели свойств размещения аддона (BLND-8); превью посадки не заводится (design, Open Questions §4)

## 6. Финал

- [ ] 6.1 Прогнать `npm run check` из корня; убедиться, что голдены, `worldInit` и реплеи не тронуты (изменение — presentation-only, PRES-4)
