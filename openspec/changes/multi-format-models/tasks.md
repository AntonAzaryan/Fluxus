## 1. Контракт модели: слот текстуры (ASSET-5)

- [x] 1.1 Превратить `TextureSlotRef` в размеченное объединение «ссылка на файл | встроенное декодированное изображение | источника нет» в `engine/assets-ts/src/model.ts`; описать в комментарии, почему не «путь плюс необязательные пиксели» (design, решение 3)
- [x] 1.2 Перевести `mdxLoader` на новый вид слота: путь из `Image`, replaceable-слот WC3 — вариант «источника нет»; убедиться, что сквозная нумерация слотов сохранена
- [x] 1.3 Обновить тесты `engine/assets-ts/test/mdx.test.ts` под новый вид слота — эталонная модель обязана дать те же слоты, что и до правки

## 2. Загрузчик glTF: встроенные изображения и `.glb`

- [x] 2.1 Отдавать встроенное изображение (`images[i].bufferView`) как слот со встроенным декодированным изображением: декодировать через существующий `decodePng` один раз на ассет
- [x] 2.2 Встроенное изображение формата без декодера (JPEG) — слот без источника плюс предупреждение, а не отказ загрузки модели (design, решение 3)
- [x] 2.3 Разбирать двоичный контейнер `.glb` в том же `gltfLoader`: определять вид по сигнатуре содержимого, а не по имени файла; зарегистрировать загрузчик под `.gltf` и `.glb` (design, решение 4)
- [x] 2.4 Тест на фикстуре: `.glb` со встроенной текстурой даёт ту же нормализованную модель, что и распакованная пара `.gltf` + `.bin` с внешним PNG
- [x] 2.5 Тест: слот со встроенным изображением несёт пиксели, слот с внешним `uri` — путь, разрешённый от ID ассета

## 3. Перёд модели (ASSET-6, REND-13)

- [x] 3.1 Добавить в `EntityVisual` (`engine/assets-ts/src/manifest.ts`) опциональный перёд модели в градусах; провалидировать как конечное число, дополнить `checkUnknownKeys`
- [x] 3.2 Тест валидации манифеста: перёд опционален, нечисловое значение даёт ошибку с путём до поля
- [x] 3.3 Брать перёд из записи манифеста в `ModelsSubsystem` (пер-инстансно, при создании записи) и применять к развороту; конверсия градусы → радианы на границе рендера (REND-1)
- [x] 3.4 Удалить опцию `facingOffset` из `ModelsOptions` целиком — не депрекейтить (design, риски)
- [x] 3.5 Тест рендера: два инстанса разных записей манифеста с разным передом развёрнуты каждый по-своему; запись без переда ведёт себя как до появления параметра

## 4. Разрешение клипа (REND-4)

- [x] 4.1 Переписать `resolveClip` в `engine/render-ts/src/model/animation.ts` на две полные фазы: точное совпадение имени, затем единственное совпадение по подстроке; убрать фолбэк на `clips[0]`, возвращать «не разрешено»
- [x] 4.2 Проводить неразрешённую и неоднозначную запись до предупреждения, называющего запись; дедуплицировать предупреждение на запись, оставляя текущий клип
- [x] 4.3 Тест: при клипах `Idle` и `2H_Melee_Idle` запись `Idle` разрешается в `Idle` независимо от порядка клипов в модели
- [x] 4.4 Тест: запись, не совпавшая ни с одним клипом, даёт одно предупреждение и оставляет текущий клип, а не играет произвольный
- [x] 4.5 Тест: запись, совпавшая по подстроке более чем с одним клипом, диагностируется как неоднозначная

## 5. Контент и демо

- [x] 5.1 Вернуть в `content/visuals/manifest.json` состояние `idle` к точному имени клипа `Idle` (обходной путь `Unarmed_Idle` больше не нужен)
- [x] 5.2 Задать перёд модели в записи `Hero` и снять временную компенсацию `facingOffset` из `engine/client-ts/demo/main.ts` вместе с её комментарием
- [x] 5.3 Модель героя ОСТАВЛЕНА распакованной (`.gltf` + `.bin` + внешний PNG) сознательно. Свернуть её в `.glb` значило бы встроить базовую текстуру внутрь модели, оставив второй скин (`barbarian-ember.png`) лежать рядом файлом: два скина одной модели стали бы асимметричны — один внутри, другой снаружи, — а подмена по слоту (REND-6) работает с путями. Симметрия скинов для геймдизайнера дороже, чем минус два файла в дереве. Поддержка `.glb` от этого не осталась непроверенной: она закрыта фикстурой, сравнивающей обе упаковки (задача 2.4), а тест движка на дерево контента смотреть и не вправе (CONT-4)

## 6. Приёмка

- [x] 6.1 `npm run check` из корня зелёный
- [x] 6.2 Демо запускается, герой развёрнут по ходу движения, `idle`/`move`/`jump` и выстрел играют, смена скина по `T` работает
- [x] 6.3 Проверить, что ни один тест движка не читает дерево контента (CONT-4)
- [x] 6.4 `openspec validate multi-format-models --strict` зелёный
