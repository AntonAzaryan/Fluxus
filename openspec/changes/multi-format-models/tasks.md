## 1. Контракт модели: слот текстуры (ASSET-5)

- [ ] 1.1 Превратить `TextureSlotRef` в размеченное объединение «ссылка на файл | встроенное декодированное изображение | источника нет» в `engine/assets-ts/src/model.ts`; описать в комментарии, почему не «путь плюс необязательные пиксели» (design, решение 3)
- [ ] 1.2 Перевести `mdxLoader` на новый вид слота: путь из `Image`, replaceable-слот WC3 — вариант «источника нет»; убедиться, что сквозная нумерация слотов сохранена
- [ ] 1.3 Обновить тесты `engine/assets-ts/test/mdx.test.ts` под новый вид слота — эталонная модель обязана дать те же слоты, что и до правки

## 2. Загрузчик glTF: встроенные изображения и `.glb`

- [ ] 2.1 Отдавать встроенное изображение (`images[i].bufferView`) как слот со встроенным декодированным изображением: декодировать через существующий `decodePng` один раз на ассет
- [ ] 2.2 Встроенное изображение формата без декодера (JPEG) — слот без источника плюс предупреждение, а не отказ загрузки модели (design, решение 3)
- [ ] 2.3 Разбирать двоичный контейнер `.glb` в том же `gltfLoader`: определять вид по сигнатуре содержимого, а не по имени файла; зарегистрировать загрузчик под `.gltf` и `.glb` (design, решение 4)
- [ ] 2.4 Тест на фикстуре: `.glb` со встроенной текстурой даёт ту же нормализованную модель, что и распакованная пара `.gltf` + `.bin` с внешним PNG
- [ ] 2.5 Тест: слот со встроенным изображением несёт пиксели, слот с внешним `uri` — путь, разрешённый от ID ассета

## 3. Перёд модели (ASSET-6, REND-13)

- [x] 3.1 Добавить в `EntityVisual` (`engine/assets-ts/src/manifest.ts`) опциональный перёд модели в градусах; провалидировать как конечное число, дополнить `checkUnknownKeys`
- [x] 3.2 Тест валидации манифеста: перёд опционален, нечисловое значение даёт ошибку с путём до поля
- [x] 3.3 Брать перёд из записи манифеста в `ModelsSubsystem` (пер-инстансно, при создании записи) и применять к развороту; конверсия градусы → радианы на границе рендера (REND-1)
- [x] 3.4 Удалить опцию `facingOffset` из `ModelsOptions` целиком — не депрекейтить (design, риски)
- [x] 3.5 Тест рендера: два инстанса разных записей манифеста с разным передом развёрнуты каждый по-своему; запись без переда ведёт себя как до появления параметра

## 4. Разрешение клипа (REND-4)

- [ ] 4.1 Переписать `resolveClip` в `engine/render-ts/src/model/animation.ts` на две полные фазы: точное совпадение имени, затем единственное совпадение по подстроке; убрать фолбэк на `clips[0]`, возвращать «не разрешено»
- [ ] 4.2 Проводить неразрешённую и неоднозначную запись до предупреждения, называющего запись; дедуплицировать предупреждение на запись, оставляя текущий клип
- [ ] 4.3 Тест: при клипах `Idle` и `2H_Melee_Idle` запись `Idle` разрешается в `Idle` независимо от порядка клипов в модели
- [ ] 4.4 Тест: запись, не совпавшая ни с одним клипом, даёт одно предупреждение и оставляет текущий клип, а не играет произвольный
- [ ] 4.5 Тест: запись, совпавшая по подстроке более чем с одним клипом, диагностируется как неоднозначная

## 5. Контент и демо

- [ ] 5.1 Вернуть в `content/visuals/manifest.json` состояние `idle` к точному имени клипа `Idle` (обходной путь `Unarmed_Idle` больше не нужен)
- [x] 5.2 Задать перёд модели в записи `Hero` и снять временную компенсацию `facingOffset` из `engine/client-ts/demo/main.ts` вместе с её комментарием
- [ ] 5.3 Свернуть модель героя обратно в один `.glb` со встроенной текстурой, если это упрощает дерево контента; иначе оставить как есть и записать причину в комментарий изменения

## 6. Приёмка

- [ ] 6.1 `npm run check` из корня зелёный
- [ ] 6.2 Демо запускается, герой развёрнут по ходу движения, `idle`/`move`/`jump` и выстрел играют, смена скина по `T` работает
- [ ] 6.3 Проверить, что ни один тест движка не читает дерево контента (CONT-4)
- [ ] 6.4 `openspec validate multi-format-models --strict` зелёный
