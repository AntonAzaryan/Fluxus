## 1. Механизм: контракт, сэмплер, биндинги

- [x] 1.1 Создать `engine/client-ts/src/input/`: контракт источника (опрос непрерывного состояния + пуш фронтов, D2), тип семантического сэмпла с именами действий (D3)
- [x] 1.2 Сэмплер: латчинг фронтов до границы тика (INP-2), свёртка источников — действия OR, непрерывные от последнего активного (INP-5/D4), кламп единичного круга и квантование Q16.16 в одной точке — `fixed.fromFloat` + перенос `aimAngle` из демо (INP-3/D5)
- [x] 1.3 Формат биндингов (JSON): «физический орган → имя действия», отдельно «имя действия → бит» как данные уровня контента (INP-4/D3); валидация при загрузке, тест «дефолтные раскладки валидны»
- [x] 1.4 Юниты на сэмплер: тап между тиками не теряется и не удваивается, фронт с опросного источника (prev/current-маски), диагональ ≤ 1, свёртка двух источников, нейтраль при пропаже активного источника — контракт закреплён до миграции демо (D8)

## 2. Клавиатура+мышь (целевое) и миграция демо

- [x] 2.1 `KeyboardMouseSource`: поглотить `heroMoveFromKeys` (убрать из `render-ts/src/camera/rig.ts` симуляционную часть, камерное оставить), aim через существующий raycast указателя, действия по биндингам
- [x] 2.2 Перевести `demo/main.ts` на сэмплер: удалить инлайновые латчи `pendingCast`/`pendingDodge`/`pendingJump` и прямую сборку `buttons`; константы битов из `demo/sim.ts` — в данные «действие → бит»
- [x] 2.3 Проверить руками в демо: WASD, каст, уклон/перекат (даблтап LOC-4), прыжок, смена скина и камерные клавиши не попадают в канонический ввод (CAM-1)

## 3. Тач (телефон)

- [x] 3.1 `TouchSource`: pointer-события с capture по pointerId, плавающий стик движения, стик/тап прицела, кнопки действий; геометрия зон — конфигурация (D6)
- [x] 3.2 Минимальная отрисовка накладки в демо (зоны и стик) — уровнем приложения, не движка
- [x] 3.3 Юниты на синтетических pointer-событиях: мультитач (движение + прицел одновременно), короткий тап латчится (INP-2), отпускание → нейтраль
- [x] 3.4 Ручная проверка на телефоне; подобрать дефолтные зоны (Open Question из design)

## 4. Геймпад

- [x] 4.1 `GamepadSource`: активация по `gamepadconnected`, опрос в такте сэмплера, фронты по prev/current-маскам, deadzone стиков как параметр биндингов, отключение → неактивность без залипания (INP-5/D7)
- [x] 4.2 Юниты на синтетическом состоянии геймпада: частичный наклон стика доезжает дробным move (INP-3), фронты не удваиваются
- [x] 4.3 Ручная проверка с реальным геймпадом: подключение посреди матча, сосуществование с клавиатурой (действия с клавиатуры + движение со стика)

## 5. Финализация

- [x] 5.1 `npm run check` из корня зелёный; golden-эталоны без диффа (слой не трогает симуляцию)
- [x] 5.2 Прогнать `openspec validate --specs --strict`; сверить формулировки требований INP-1..5 с реализацией
