# Tasks: render-quality-presets

## 1. Реестр ручек и контроллер качества (QUAL-1)

- [x] 1.1 Декларация ручек в контракте подсистемы (расширение REND-8): имя с неймспейсом подсистемы (`fog.maskResolution`), стоимостная ось, семантика (потолок над авторским значением | прямое значение), диапазон, дефолт; реестр собирается из деклараций при регистрации — центрального списка нет (D1)
- [x] 1.2 `QualityController` поверх `PresentationStage`: применение документа пресета при регистрации подсистемы (поздняя регистрация получает текущие значения) и при смене пресета в рантайме без пересборки рендера (D2); тесты на оба пути
- [x] 1.3 Валидация документа пресета против реестра: неизвестная ручка — адресный отказ с именем, отсутствующая — документированный дефолт, значение вне диапазона — отказ; тесты

## 2. Ручки существующих подсистем (QUAL-3, FOW-10)

- [x] 2.1 `fog.maskResolution` как потолок: действующее разрешение = min(сценного из секции `fog`, потолка пресета), документ сцены не меняется ни байтом, консервативность FOW-9 сохраняется на грубой маске; тесты: сцена 8 + потолок 4 → 4, ультра без потолка → 8
- [x] 2.2 Ручки остальных стоимостных осей: множитель порогов LOD (REND-22), ярус представления по умолчанию (REND-20), плотность частиц (REND-24), вкл/выкл пост-проходов; каждая — декларацией своей подсистемы
- [x] 2.3 Подсистемы без растущей стоимости объявляют константность явно (QUAL-3) — пройтись по зарегистрированным подсистемам демо и вьюпорта, зафиксировать объявления

## 3. Документы пресетов и подключение (QUAL-1)

- [x] 3.1 Три документа `game/demo-ts/app/presets/{performance,balanced,ultra}.json`; `balanced` — сегодняшнее поведение (без потолков), `performance` — первые реальные потолки (разрешение маски, ярус по умолчанию, частицы)
- [x] 3.2 Переключатель пресета в демо: выбор в настройках, хранение в localStorage, дефолт `balanced`, применение через контроллер в рантайме
- [x] 3.3 Вьюпорт редактора подключает контроллер с зашитым `ultra` (авторская картинка, не бюджет)

## 4. Инварианты и пресетные бюджеты (QUAL-2, QUAL-4 — после посадки perf-budget-bench)

- [x] 4.1 Тест инвариантности симуляции: один записанный матч на пресетах «производительность» и «ультра» — канонический лог симуляции совпадает побитово (D6)
- [x] 4.2 Тест информации: на производительном пресете видимая область консервативна (FOW-9), состав сущностей в доставленном состоянии не зависит от пресета (NET-12)
- [x] 4.3 Cost-эталоны на пресет: бенч-прогон гейта параметризуется пресетом, эталон — секции `performance`/`ultra` в одном `*.cost.json` (D5); регенерация той же `npm run golden:cost`

## 5. Аллокационная дисциплина кадра (REND-26)

- [x] 5.1 Аудит покадрового пути подсистем (`frame`/`updateFrame`, интерполяция, атрибуты): устранить аллокации, пропорциональные инстансам/контенту, в установившемся кадре; осознанно оставленные пометить `ponytail`
- [x] 5.2 Тест на стабильной сцене: N кадров без доставок и смены состава — счётчики/спаи не фиксируют роста буферов, путь кадра не создаёт объектов на инстанс (проверка на швах PresentationStage)

## 6. Гейт

- [ ] 6.1 Обновить CLAUDE.md: пресеты качества (дом документов, семантика потолка, дефолты демо/редактора)
- [ ] 6.2 `npm run check` зелёный; голдены ядра не изменились (QUAL-2 — пресет не касается симуляции); cost-эталоны обоих пресетов в коммите
