## 1. Ввод: hold/release (INP-2)

- [ ] 1.1 Расширить контракт источника ввода held-маской действий (`engine/client-ts/src/input/types.ts`); реализовать в `keyboardMouse.ts` (keydown/keyup + сброс на blur), `gamepad.ts` (опрос), `touch.ts` (состояние жеста)
- [ ] 1.2 `sampler.ts`: `sample()` возвращает `held | latched` (OR по источникам), обнуляя только латч; свёртка нескольких источников по INP-5
- [ ] 1.3 Тесты семплера: тап между тиками — ровно один тик; удержание N тиков — бит стоит N тиков; отпускание — falling edge; blur сбрасывает held; клавиатура и геймпад дают одинаковую семантику
- [ ] 1.4 Проверить существующие rising-edge потребители (локомоушен, каст в `duel.scene.json`) — без повторных срабатываний при удержании; тест на «каст не спамится при зажатой кнопке»

## 2. Локомоушен: селективный лок манёвров (LOC-7)

- [ ] 2.1 `engine/core-ts/src/systems/locomotion.ts`: опциональные параметры `lockComponent`/`maneuverLockBit`; гейт стартов Dodge (Normal), Roll (Window), Airborne (Normal); без параметров — поведение байт-в-байт
- [ ] 2.2 Тесты: лок блокирует все три старта манёвров, ходьба (разгон/торможение) не затронута; лок посреди Dodge не прерывает манёвр; проигнорированный фронт не буферизуется; rewind с локом детерминирован
- [ ] 2.3 Прогнать golden-эталоны — без диффа (механизм опт-ин)

## 3. Физика: нормаль поверхности (PHYS-9)

- [ ] 3.1 `engine/core-ts/src/systems/physics.ts`: нормаль события `Collision` — по линии центров для пары с кругом-препятствием; от ближайшей точки AABB к центру круга для круга-движущегося о AABB/статик; фолбэк на осевую при вырождении; AABB-AABB без изменений
- [ ] 3.2 Тесты: рикошетная геометрия — снаряд о круглый щит не по оси даёт диагональную нормаль единичной длины (Q16.16); контакт с гранью AABB даёт осевую; контакт с углом — диагональную; вырожденный случай детерминирован
- [ ] 3.3 Прогнать golden-эталоны — без диффа (нормаль текущими сценариями не потребляется); при диффе — разобраться, а не перезаписывать

## 4. Спеки и ворота

- [ ] 4.1 `openspec validate demo-core-primitives --strict` и `npm run spec-graph -- check` зелёные
- [ ] 4.2 `npm run check` из корня зелёный
