schema: spec-driven
created: 2026-08-05

# Спеки не меняются: поведение каста остаётся прежним, меняется форма записи
# системы — нативная TS уступает место JSON. Смысл `aimDir` нигде не нормирован
# (TICK-2 задаёт только тип поля), единица угла уже нормирована FP-7.
# Обоснование — proposal.md, раздел «Modified Capabilities».
skip_specs: true
