# Tasks

## 1. Очередь отложенного монтирования (REND-44)

- [x] 1.1 `models/spawnQueue.ts`: отложение, отмена пометкой, порция под бюджетом, синхронный слив, порядок по расстоянию до камеры
- [x] 1.2 Поле `pendingMount` записи инстанса — точка отмены без вырезания из середины очереди
- [x] 1.3 `ModelsSubsystem.updateBudgeted` и сток покадрового обновления для сборки без фазы

## 2. Точки очереди в подсистеме

- [x] 2.1 Создание записи откладывает монтирование; decoration-набор — нет (документ авторинга, ED-15)
- [x] 2.2 `remove` отменяет отложенное — одна точка на все случаи ухода записи
- [x] 2.3 Синхронный слив на переподаче манифеста (REND-17) и смене поколения ассетов (REND-1); снос очередь отменяет

## 3. Тесты

- [x] 3.1 Волна под малым потолком доезжает за несколько кадров с прогрессом каждым кадром и без потерь
- [x] 3.2 Первая порция при нулевом потолке; отмена снятой до монтирования сущности
- [x] 3.3 `snapAll` доделывает синхронно; снос отменяет
- [x] 3.4 Неограниченный бюджет и сборка без сцены не откладывают ничего; `modelsInstancesCreated` не двигается
- [x] 3.5 Ближние к камере монтируются раньше дальних

## 4. Спека

- [x] 4.1 Дельта `rendering`: правка REND-44 (волна спавна) и два сценария
- [x] 4.2 Оговорка в строке 54 таблицы этапов `docs/architecture.md`
- [x] 4.3 `openspec validate --strict`, `npm run spec-graph -- check`, архивация
