# Tasks: arena-support-validation

Требование — `specs/arena/spec.md` (**ARENA-3**), разбор решений — design.md (D1–D3). Гейт один и он
локальный: `npm run check` из корня репозитория (CI нет).

Стаб развёрнут по находке 🟡 ARENA-3 валидации спек против кода
(`docs/reviews/spec-validation-2026-09-01/03-sim-systems.md`).

## 1. Дельта требования (D1, D2, D3)

- [x] 1.1 `.openspec.yaml`: снять маркер `skip_specs` — дельта у изменения появилась.
- [x] 1.2 `specs/arena/spec.md`: MODIFIED ARENA-3 — правило диапазона одно на обе дороги; документ (prefab
      и переопределение записи расстановки, SER-8) отвергается валидацией загрузки с указанием места
      находки; мутация отвергается системой арены при чтении жёсткой ошибкой FP-4 с указанием сущности и
      значения; кламп и молчаливое игнорирование запрещены явно; класс ошибки принадлежит ARENA-3, а
      перечень SYS-9 её не повторяет. Прежний сценарий «Коэффициент опоры вне диапазона» сохранён как общий, и под
      ним разложены частные: «…в заготовке сцены», «…в записи расстановки», «…записан по ходу матча»,
      «Мутация в пределах диапазона». Блок MODIFIED несёт требование ЦЕЛИКОМ со всеми действующими сценариями.
- [x] 1.3 Перенести дельту в `openspec/specs/arena/spec.md`. Изменение при этом НЕ архивируется: оно
      остаётся в очереди как запись о принятом решении. Проверка — `npx openspec validate --specs
      --strict` и `npm run spec-graph -- check`.

## 2. Код (D1, D2)

- [x] 2.1 `engine/core-ts/src/systems/arena.ts`: приватный предикат диапазона — единственная копия правила;
      `checkArenaSupport` (префабы) переведён на него, добавлен `checkArenaSupportOverride`
      (переопределения записи расстановки).
- [x] 2.2 `engine/core-ts/src/sim/placement.ts`: вызов проверки переопределений на каждую запись — одна
      воронка на все три документа расстановки (SER-8); шапка модуля перечисляет проверку в своём составе.
- [x] 2.3 `engine/core-ts/src/systems/arena.ts`: `updateArenaState` читает и проверяет `support` до
      ветвления «в полёте / на земле» и передаёт значение в `standsOnFloor` аргументом — значение вне
      диапазона обрывает тик сообщением, называющим сущность и значение.

## 3. Тесты

- [x] 3.1 `engine/core-ts/test/arena.test.ts`: переопределение расстановки вне диапазона отвергается, и
      сообщение называет номер записи; значение внутри диапазона проходит.
- [x] 3.2 Там же: мутация вне диапазона обрывает тик; мутация внутри диапазона — штатная, и следующая
      проверка пола идёт по новому значению.

## 4. Гейт

- [x] 4.1 Тесты ядра и интеграции, `npm run typecheck`, `npm run lint`, `npm run lint:dead`,
      `npm run spec-graph -- check`, `npx openspec validate --specs --strict` и `--changes --strict`.
      Golden- и cost-эталоны не перегенерируются: ни один существующий документ значения вне диапазона не
      держит, поведение честных сцен не меняется.
