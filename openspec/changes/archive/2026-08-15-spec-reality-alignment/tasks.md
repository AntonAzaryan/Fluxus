## 1. Запрет тумана без террейна в ядре (A-1)

- [x] 1.1 Добавить в `loadScene` (`engine/core-ts/src/sim/scene.ts`) проверку «`fog` есть, `terrain` нет» до создания мира: бросок с префиксом `SER-7:` и русским сообщением, называющим недостающий `terrain` и причину (запрос уровня для FOW-5)
- [x] 1.2 Добавить тест отказа в `engine/core-ts/test/` рядом с прочими тестами загрузчика: сцена с `fog` без `terrain` отвергается, сообщение называет `terrain`; сцена с обоими полями и сцена с `terrain` без `fog` поднимаются

## 2. Фикстуры и сцены с туманом без террейна

- [x] 2.1 `engine/core-ts/test/filter.test.ts`: добавить ровный террейн в `SCENE`, сверить ожидания тестов, зависящие от состава сущностей и ID
- [x] 2.2 `engine/net-ts/test/fixtures.ts` (`fogScene`) и зависящие от неё тесты (`session`, `wireSnapshot`, `interpolation`): добавить ровный террейн, поправить ожидания, сдвинувшиеся из-за носителя карты пола
- [x] 2.3 `editor/core-ts/test/dslBlocks.test.ts` и `editor/ui-ts/test/*`: добавить террейн в сцены-фикстуры с флагом `fog`
- [x] 2.4 Прогнать полный поиск по дереву (`fog` в `.ts`/`.json`/`.mjs` вне `openspec/`), убедиться, что сцен с туманом без террейна не осталось
- [x] 2.5 `engine/tests/golden/visibility.scenario.json`: добавить ровный террейн без обрывов, сохранив предмет сценария (видимость через стену), — и только после этого перегенерировать эталон `npm run golden` из корня, приложив дифф

## 3. Проверка

- [x] 3.1 `npm test -w @game-mvp/core` и `npm test -w @game-mvp/integration` зелёные
- [x] 3.2 `npm run check` из корня зелёный (при падениях вне области чейнджа — зафиксировать их отдельно как чужие)

## 4. Спека

- [x] 4.1 `openspec validate --strict` для чейнджа зелёный
- [x] 4.2 Синк дельт в главные спеки; `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные
- [x] 4.3 Архив чейнджа
