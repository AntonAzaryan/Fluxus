# Tasks: fog-observer-inputs

## 1. Ядро: эффективный радиус обзора состоянием (F-2, FOW-3, FOW-5)

- [x] 1.1 `VisionState { radius: fixed }`: схема, константа компонента, место в `fowComponents` после `DetectionState`; проверка — тест схемы группы и порядка компонентов
- [x] 1.2 Публикация свёртки пересчётом: один расчёт на носителя `Vision`, команда только при изменении (FOW-6), дописывание компонента отсутствующему носителю; отбор кандидатов берёт ту же величину; проверка — тесты «радиус с модификатором опубликован», «без модификаторов равен `Vision.radius`», «носитель без `Team`/`Position` тоже публикуется», «радиус не менялся — команды нет»
- [x] 1.3 Регенерация схем (`npm run schemas`), поверхности экспорта (`UPDATE_API=1`) и поведенческих эталонов (`npm run golden`) — дифф обязан состоять ровно из нового компонента

## 2. Ядро: помощники горячего пути (P-2, P-3)

- [x] 2.1 `cellAtXY(grid, x, y)` рядом с `cellAt` (тот делегирует), экспорт из `index.ts`; проверка — тест равенства обоих входов на границах клеток
- [x] 2.2 Необязательный буфер индексов у `queryInto` (SYS-10): вызывающему, которому нужны только id, не приходится заводить второй массив размером в мир; проверка — тест «отбор без буфера индексов даёт те же id и то же число»

## 3. Плоская форма: таблица колонок и уровень симуляции (P-8, F-1)

- [x] 3.1 Таблица колонок «имя → конструктор массива» отдельным модулем `channelLayout.ts` (зависимостей у неё нет вовсе, а читают её двое — экстрактор и кодек), из неё — `ensureCapacity`, `CHANNEL_COLUMNS` и биты колонки `flags`; проверка — существующий страж числа колонок и roundtrip кодека зелёные
- [x] 3.2 Раскладка кодека выводится из той же таблицы (группы f64/f32/i32/u8), версия кодека растёт; проверка — roundtrip всех колонок и тест «сущность стоит каналу ровно объявленных колонок»
- [x] 3.3 Колонка `simLevel`: уровень сущности правилом `terrain.levelOf` (override, иначе клетка); `EntityView.simLevel`; проверка — тест «сущность с `LevelOverride` над клеткой другого уровня даёт разные `level` и `simLevel`»
- [x] 3.4 Туман читает `simLevel` (срез по уровню и `segmentCasts`), модели остаются на `currLevel`; поправлен комментарий шапки `fog.ts`; проверка — тест маски с наблюдателем `{level: 0}` над клеткой уровня 1

## 4. Путь извлечения: аллокации (P-2, P-3, REND-26)

- [x] 4.1 Обход живых сущностей — переиспользуемый буфер и `queryInto` вместо `listAlive`; проверка — тест «второй extract того же мира не аллоцирует колонок»
- [x] 4.2 Слоты способностей: спецификация выборки — константа модуля, буфер переиспользуется, внутренние словари слотов возвращаются в запас вместо пересоздания; проверка — тест «слот исчез — стат владельца исчезает вместе с ним» (риск переиспользования — недочищенный словарь) плюс структурный страж «`query(` в пути извлечения нет»
- [x] 4.3 `cellAtXY` вместо `Vec2` на сущность; таблица масштабов статов по индексу источника вместо строкового ключа; проверка — тест «масштаб `fixed`-поля применён, словаря строковых ключей нет»

## 5. Буфер presentation-состояния (P-1, P-4, P-5, P-6, REND-2)

- [x] 5.1 Каденс доставок по часам: EMA интервала по режимам, темп = отношение тиков в секунду, знаменатель альфы — сглаженный интервал с полом `tickSeconds`; проверка — тест хоста со скрабом `every ≠ 1` и тест альфы на многотиковом интервале
- [x] 5.2 `branchChanged` отдельно от `snapAll`: бит заголовка кодека, накопление в отправителе, гашение памяти курсов только по нему; проверка — тест «пауза матча не стирает память курсов, реплей стирает»
- [x] 5.3 Кэши `kinds`/`aim` экстрактора чистятся по СМЕНЕ ВЕТВИ (точный признак задачи 5.2: id переиспользуются только за стёртой ветвью, а пауза их не трогает); живой id со сменившимся видом пересоздаёт запись буфера; проверка — тесты «переиспользованный id после разрыва получает свой вид, а не прежний» и «внутри одной ветви кэш держит»
- [x] 5.4 Часы кадра неактивного продюсера сбрасываются; тела `frame` обоих хостов — одна функция; проверка — тест «кадр после долгой неактивности не приносит накопленный интервал»
- [x] 5.5 Пара курсов в `EntityView` и интерполяция по кратчайшей дуге (REND-2): `prevFacingYaw`, snap на разрыве, `interpolateYaw` в экспорте пакета; проверка — тесты пары через ±π, при остановке и на разрыве. Кадровый ВЫЗОВ функции — одна строка в `subsystems/models.ts` (цель доворота), и он остаётся владельцу того файла: данные и функция приезжают этим change'ем

## 6. Туман: смена разрешения и мелкие находки (F-3, F-4, F-6, F-7, F-8)

- [x] 6.1 Смена разрешения не снимает туман: `built` держится, показанная маска — нули, последний известный вход перепоставлен отложенным (`MaskRebuild.requeue`); проверка — тест «кадр сразу после смены разрешения рисует маскирующий проход и весь мир под туманом; ближайшие кадры её достраивают без доставки»
- [x] 6.2 Публикуется ПОКРЫТЫЙ прямоугольник маски (`ширина текселей / разрешение`) в униформу пост-прохода и в слой миникарты; проверка — тест отображения шейдера у дальнего края при нецелом `rect × resolution`
- [x] 6.3 Проба `fog.mask`: доля вскрытого — по опубликованному целевому растру, `rebuildCount`/`dissolving` — в секции `clock`; проверка — тест «два дампа одного доставленного состояния совпадают телом секции»
- [x] 6.4 Команда героя латчится первым успешным чтением; отсутствие героя не останавливает перестройку по союзникам; проверка — тест «герой ушёл из доставки — маска продолжает следовать за союзником»
- [x] 6.5 Своя цель кадра тумана с `samples: 4`; проверка — тест параметров цели

## 7. Демо и сборка

- [x] 7.1 Стат `visionRadius` демо указывает на `VisionState.radius`; проверка — тест демо-сцены «доставленный радиус равен эффективному»
- [x] 7.2 Эталоны стоимости и памяти (`npm run golden:cost`) — дифф объясняется четырнадцатой колонкой канала и публикацией радиуса
- [x] 7.3 Полный гейт `npm run check`
