## 1. Порядок в очереди

- [x] 1.1 Убедиться, что `dedup-normative-duplicates` синхронизирован в главную спеку раньше этого change; если он ещё в очереди — синхронизировать его первым (`proposal.md`, «Порядок относительно `dedup-normative-duplicates`»)
  - Проверка выполнена дважды, и на архивации ответ оказался «нет»: `dedup-normative-duplicates` по-прежнему в очереди (0/22), в главную спеку не синхронизирован. Синхронизировать его первым значило бы протащить в главные спеки все семь его правок ради одного абзаца `REND-9` — работа его собственного change'а, а не этого. Поэтому порядок признан перевёрнутым и сработала задача 1.2.
- [x] 1.2 Если порядок перевернулся: перед синхронизацией вернуть второй абзац `REND-9` в `openspec/changes/curvature-render-smoothing/specs/rendering/spec.md` к дореформенной редакции (текст приведён в `proposal.md` дословно) и после слияния перестроить дельту `dedup-normative-duplicates` по слитому тексту
  - Обе половины рецепта исполнены при архивации. Абзац дельты возвращён к дореформенной редакции дословно по `proposal.md` («Кривизна — presentation-данные (ASSET-1): она MUST NOT влиять на `worldInit`, снапшоты, FoW и golden-файлы, и MUST NOT менять cliff-геометрию и проходимость.») — иначе `REND-9` сослался бы на полный перечень последствий `ASSET-1`, которого в `ASSET-1` ещё нет, и упоминание FoW потерялось бы. После слияния дельта `REND-9` в `dedup-normative-duplicates` перестроена по слитому тексту тем же приёмом, что и `ASSET-8`, — отдельным коммитом.
- [x] 1.3 `openspec validate curvature-render-smoothing --type change --strict` и `openspec validate --specs --strict` — зелено

## 2. Поле высот

- [x] 2.1 `engine/render-ts/src/visualSurface.ts`: разделить хранение на базу и слагаемое кривизны — рядом с `corners` (мировые высоты углов) держать узловые смещения углов, чтобы генераторы могли спросить «есть ли у клетки кривизна» без обратного вычитания
- [x] 2.2 Там же, `sample(cell, u, v)`: применить `S(t) = t² · (3 − 2t)` к параметрам ТОЛЬКО в слагаемом кривизны; база остаётся билинейной по `cornerLevels × heightStep` (design, Решение 2). Формула по-прежнему одна на всех — второй копии не заводить
- [x] 2.3 Там же: производные в `normalAt` пересчитать под новую форму — `d/du` базы прежняя, у слагаемого кривизны добавляется множитель `S'(u) = 6u(1 − u)`; численного дифференцирования не вводить
- [x] 2.4 Там же: добавить в `VisualSurface` метод `normalInCell(cellX, cellY, wx, wy, out)` — близнец `heightInCell`, считает нормаль по углам ЗАДАННОЙ клетки с клампом точки к её границам
- [x] 2.5 Там же: добавить `hasCellCurvature(x, y)` (или эквивалент) — «все четыре узловых смещения клетки нулевые»; это порог разбиения, а не проверка карты
- [x] 2.6 Проверить, что `offsetOfCorner` и `computeCells` не тронуты: стенсиль усреднения прежний, `SHAPE_RADIUS = 2` в `subsystems/terrain.ts` остаётся верным

## 3. Геометрия террейна

- [x] 3.1 `engine/render-ts/src/types.ts`: добавить в `RenderConfig` поле `curvatureTessellation` рядом с `heightStep`, с комментарием в том же духе («параметр рендера, не ядра»); стартовое значение — 4
- [x] 3.2 `engine/render-ts/src/subsystems/terrain.ts`, `buildFloorGeometry`: клетка без кривизны — прежний одиночный квад по `cornerHeights`; клетка с кривизной — сетка `N × N` подклеток, вершины по `surface.heightInCell(x, y, wx, wy)`. Параметр `N` приходит аргументом, как `heightStep`
- [x] 3.3 Там же: заполнять атрибут нормалей по `surface.normalInCell(x, y, wx, wy)`; без `surface` (плоские ступени) нормаль вертикальна для площадки и постоянна для рампы
- [x] 3.4 `toBufferGeometry`: принимать готовые нормали и ставить их атрибутом; `computeVertexNormals()` оставить только там, где нормали не пришли — стенки обрыва считают её по-прежнему плоской
- [x] 3.5 `buildWallGeometry`: cliff-отрезок даёт полосу из `N` квадов; в каждой точке разбиения верх и низ — `max`/`min` высот поля в обеих клетках отрезка через тот же `heightInCell` (design, Решение 5). Владение отрезком чанком (`bounds`, `cellA`) не меняется
- [x] 3.6 `TerrainSubsystem.rebuildChunk`: прокинуть `N` из `ctx.config` в оба генератора; логика `dirtyChunks`, `markShapeCell` и `applyGrid` не меняется

## 4. Наложения

- [x] 4.1 `engine/render-ts/src/subsystems/overlays.ts`: ячейку превью кисти и контур клетки строить тем же разбиением по `heightInCell`, а не плоским квадом по `cornerHeights`; подъём `lift` над поверхностью сохранить
- [x] 4.2 Проверить, что подписка на `onChange` источника поверхности и флаг `surfaceDirty` продолжают перестраивать наложения после мазка кисти (ED-11, REND-16)

## 5. Тесты

- [x] 5.1 `engine/render-ts/test/visualSurface.test.ts`: переписать клаузу теста «нормаль билинейной формы на границе может ломаться» — теперь на границе клеток одного уровня SHALL совпадать и высота, и нормаль; сравнивать `normalAt` в `border ± 1e-6`
- [x] 5.2 Там же: тест «кривизна не перетекает через cliff-границу» дополнить нормалями — на кромке обрыва нормали верхней и нижней клетки различны, скругления нет
- [x] 5.3 Там же: тест «без кривизны совпадает со ступенями» усилить до совпадения ГЕОМЕТРИИ — `buildFloorGeometry` без карты и с картой из одних `.` дают одинаковые позиции и одинаковое число вершин
- [x] 5.4 `engine/render-ts/test/terrain.test.ts`: тест разбиения — клетка с кривизной даёт `N × N` квадов, соседняя плоская остаётся одним квадом; вершины разбитой клетки на общем ребре лежат ровно на прямой соседа (T-стыка со щелью нет)
- [x] 5.5 Там же: тест кромки стенки — верхняя кромка полосы совпадает с кромкой пола в тех же точках разбиения, зазора нет ни в одной из них
- [x] 5.6 Там же: тест «плато с кривизной без граней» — по обе стороны каждой внутренней границы клеток одного уровня нормали вершин пола совпадают в пределах допуска
- [x] 5.7 `engine/render-ts/test/picking.test.ts`: попадание по холму считается по сглаженному полю — марш через `heightInCell` даёт точку на поверхности, а не на билинейной хорде (REND-15)
- [x] 5.8 Проверить, что тесты `applyGrid`/`setCurvature` (`terrain.test.ts`, `visualSurface.test.ts`) остались зелёными с поправкой только на числа вершин: границы инвалидации не менялись

## 6. Проверка

- [x] 6.1 `npm run check` из корня — typecheck, lint, test зелёные (проверено ревью)
- [x] 6.2 Убедиться, что `engine/tests/golden/` и `engine/schemas/` не изменились: кривизна — presentation-данные, `npm run golden` в этой работе не запускается (design, «Почему детерминизм здесь ни при чём»)
- [ ] 6.3 `npm run dev -w @game-mvp/editor-ui`, кисть кривизны ED-11 по плато у обрыва: холм без граней, силуэт обрыва резкий, превью кисти лежит на холме, дыр и щелей на стыке пола со стенкой нет
  - Не сделано и при архивации не закрывается: живой прогон браузером этого прохода проверял эргономику вьюпорта (`editor-viewport-ergonomics`, задача 6.3), кисть кривизны по кромке обрыва в нём не гонялась. Автоматикой закрыты все четыре наблюдения по отдельности — плато без граней (5.6), резкая кромка (5.2), превью по сглаженному полю (4.1, 5.7), кромка стенки без зазора (5.5), — но «в кадре глазами» не подтверждено. Остаётся работой следующего прохода.
- [x] 6.4 Замерить `TerrainSubsystem.floorVertexCount` на арене с нарисованным рельефом и зафиксировать цифру при `N = 4`; если бюджет кадра не сходится — менять параметр, а не схему (design, Open Questions)
