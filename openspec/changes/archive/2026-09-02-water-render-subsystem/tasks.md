# Tasks: water-render-subsystem

## 1. Секция water: формат, парс, валидация (D4, REND-35, PRES-2)

- [x] 1.1 Разбор секции `water` парного документа: `cells` + `bodies`, дефолты, конвертация цветов в линейное пространство на приёме; проверка — юнит-тесты: валидная секция, дефолты каждого необязательного поля
- [x] 1.2 Адресная валидация: символ вне алфавита, ряд другой длины, индекс без тела, нечисловые/отрицательные поля, секция при сцене без террейна, неизвестный ключ секции и тела; проверка — тест на каждую находку с адресом
- [x] 1.3 Валидация парного документа в целом: неизвестный ключ `water` до этого change отвергался составом PRES-2 — убедиться, что существующие тесты состава документа дополнены секцией; проверка — тесты presentation-загрузчика зелёные

## 2. Геометрия тела и глубинная текстура (D1, REND-35)

- [x] 2.1 Greedy-меш тела воды из клеточного региона на высоте уреза (урез × шаг высоты); проверка — юнит-тесты: число квадов и покрытие на фигурных регионах, два тела в одной карте
- [x] 2.2 Глубинная текстура тела: заполнение CPU-выборкой порта источника поверхности с плотностью «текселей на клетку», HALF_FLOAT; фолбэк без порта — база уровней; проверка — контрольные точки «урез − поле» с кривизной и walkable-вкладом, вариант без порта
- [x] 2.3 Инвалидация по затронутым клеткам: кривизна, walkable, догрузка ассета, переподача сетки террейна (REND-14) перезаполняют текстуру не позже следующего кадра; проверка — тест подписки на канал затронутых клеток

## 3. Материал-ядро и два источника детали (D2, D3, REND-35)

- [x] 3.1 Фрагментное ядро: глубинный цвет с `banding`, полоса пены у берега, Fresnel-tint, specular от направленного света сцены; линейный цвет на выходе, `transparent`, `depthWrite:false`; проверка — компиляция материала, юнит-тесты вычисления параметров uniform'ов
- [x] 3.2 Источник детали `procedural`: шум в шейдере, ноль текстурных ассетов; проверка — компиляция варианта, тест «тело без блока detail не обращается к модулю ассетов»
- [x] 3.3 Источник детали `textured`: tileable-нормали/шум пены/flow map по ID (ASSET-2); недоступный ассет → предупреждение + procedural-фолбэк; проверка — тест фолбэка и предупреждения с причиной
- [x] 3.4 Обе ветви за общим интерфейсом семпла: глубина/пена/берег не зависят от источника; проверка — тест общих uniform'ов обоих вариантов

## 4. Подсистема и кадр (D6, REND-35)

- [x] 4.1 `subsystems/water.ts` за контрактом REND-8: init со сценой/ассетами/портом поверхности, `syncTick`, `updateFrame`, регистрация в списке; сцена без секции — ноль вклада; проверка — тест «кадр без секции не создаёт ни мешей, ни текстур»
- [x] 4.2 Порядок прозрачных: вода до частиц и превью каста (`renderOrder`); маска тумана поверх — вручную через `npm run demo`; проверка — существующие тесты порядка проходов пост-цепочки зелёные
- [x] 4.3 Рантайм-обновление секции без пересборки (ED-15) и освобождение ресурсов (REND-31); проверка — тест переподачи документа: меши/текстуры обновлены, старые освобождены

## 5. Рябь (D5, REND-36)

- [x] 5.1 Отбор источников из presentation-состояния: движущиеся сущности в клетках воды, амплитуда от скорости, детерминированный выбор N ближайших с устойчивым tie-break; проверка — юнит-тесты: два прогона над одним состоянием дают один набор, переполнение не мигает
- [x] 5.2 Кольца в шейдере от uniform-источников, часы презентации (REND-25), сброс на разрыве непрерывности (REND-2); проверка — тесты: пауза замораживает возраст, телепорт сбрасывает источники
- [x] 5.3 Тест «невидимой ряби нет»: сущность вне отфильтрованного снапшота источником не становится (QUAL-2 по построению)

## 6. Качество и стоимость (D7, REND-35, QUAL-1/3, PERF-3)

- [x] 6.1 Ручки в реестре: `water.rippleSources`, `water.detailLayers`, `water.depthTexelsPerCell` с семантикой `min(авторское, потолок)`; проверка — тесты клампов, 0 источников выключает рябь, документ сцены не мутируется
- [x] 6.2 Счётчики в сток стоимости: тел нарисовано, квадов, активных источников ряби, текселей глубины заполнено; проверка — счётчики детерминированы на повторном прогоне записанного матча

## 7. Вода в демо (D8)

- [x] 7.1 Секция `water` в `content/scenes/duel.presentation.json`: водоём в лощине существующей карты кривизны, урез ниже пола нулевого уровня; `duel.scene.json` не трогается; проверка — golden симуляции и записи матчей без диффа
- [x] 7.2 Значения новых ручек в `game/demo-ts/app/presets/{performance,balanced,ultra}.json` (performance: рябь 0 либо минимум, деталь 2 слоя); проверка — валидация пресетов против реестра
- [x] 7.3 Глазами: `npm run demo` — берег, пена, рябь от юнитов читаются при MOBA-камере; `npm run bench:demo` — вода в бюджете профиля устройства

## 8. Гейт

- [x] 8.1 Регенерация эталонов стоимости `npm run golden:cost` — дифф обоих пресетов в коммите отдельным осознанным решением (PERF-4, QUAL-4)
- [x] 8.2 Цитаты требований в коде (REND-35, REND-36) и `npm run spec-graph -- check`
- [x] 8.3 `npm run check` целиком зелёный; golden симуляции не менялись (вода — чистая презентация, PRES-4)
