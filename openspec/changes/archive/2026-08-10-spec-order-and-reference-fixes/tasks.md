## 1. Порядок ключей: хеш-потоки, документы, плоская форма

- [x] 1.1 `openspec/specs/serialization/spec.md`: `SER-6` — правило о хеш-потоке обобщить с `DET-1` на класс «поток, подаваемый в хеш-функцию», назвав оба потока (`determinism-core` DET-1, `netcode` NET-17) и оговорив, что производность потока от документа фиксированной формы или от плоской формы сортировку не отменяет; добавить сценарий на хеш контент-пака
- [x] 1.2 Там же: ветку документов фиксированной формы уточнить — порядок нормативен ровно в той мере, в какой его называет владелец; перечень состава порядком MUST NOT считаться (`SER-7` перечисляет состав, не порядок); документ без названного порядка сохраняет порядок производителя (`editor` ED-21); `SER-7` из списка владельцев порядка убрать; добавить сценарий на конфиг сцены, записанный редактором
- [x] 1.3 Там же: плоскую форму мира вывести отдельным случаем со ссылкой на владельца `SER-1`
- [x] 1.4 Там же: `SER-1` — назвать порядок полей плоской формы, объявить его частью формата и связать с `cli-testing` CLI-6 и `determinism-core` DET-1 пункт 3; добавить сценарий на реализацию, переставившую поля
- [x] 1.5 Сверить названный порядок с фактическими эталонами `engine/tests/golden/*.golden.json`: он обязан совпасть во всех файлах, и ни один байт эталонов правкой не затрагивается
- [x] 1.6 Убедиться, что `SER-7`, `SER-8`, `CLI-3`, `NET-17`, `DET-1`, `ED-21` не тронуты

## 2. Адреса и основания в перечне ASSET-1

- [x] 2.1 `openspec/specs/assets/spec.md`: `ASSET-1` — пункт о снапшотах переадресовать на `snapshot-rewind` SNAP-1 (закрытый состав снапшота), сохранив совместимость реплеев
- [x] 2.2 Там же: пункт о сетевом трафике — основанием назвать `SNAP-1`, оставив `netcode` NET-8 и NET-12 адресами потока и персонального снапшота
- [x] 2.3 Убедиться, что `SNAP-1`, `NET-8`, `NET-12`, `NET-17` не тронуты

## 3. Протухшие ссылки

- [x] 3.1 `openspec/specs/determinism-core/spec.md`: `DET-9` — обоснование места `VisibilitySystem` сослать на `netcode` NET-12 (фильтрация персонального снапшота) вместо `fog-of-war` FOW-6; остальное требование не трогать
- [x] 3.2 `openspec/specs/rendering/spec.md`: `REND-9` — потребителем проходимости назвать `physics` PHYS-10 (статические коллайдеры обрывов) вместо PHYS-6 (raycast)
- [x] 3.3 `openspec/specs/netcode/spec.md`: `NET-10` — снять пересказ формулы `SNAP-6` в скобках
- [x] 3.4 `openspec/specs/presentation-scene/spec.md`: Purpose capability — снять заявление о владении инвариантом «правка presentation-слоя не меняет ни байта симуляции», заменив его применением инварианта `assets` ASSET-1 к парному документу. Purpose требованием не является и правится в главной спеке напрямую, дельтой — MUST NOT

## 4. Проверка

- [x] 4.1 `openspec validate spec-order-and-reference-fixes --strict` — зелено
- [x] 4.2 `openspec validate --specs --strict` после применения дельт — зелено
- [x] 4.3 `npm run spec-graph -- check` — зелено
- [x] 4.4 Пройти по каждой введённой ссылке и убедиться, что норма читается у адресата: `SNAP-1` (состав снапшота закрыт), `NET-12` (фильтрация персонального снапшота), `PHYS-10` (статические коллайдеры обрывов), `NET-17` (сортировка ключей хеш-потока), `ED-21` (байтовый round-trip сохранения), `CLI-6` (парность реализаций)
- [x] 4.5 Убедиться, что дифф содержит только `openspec/`
