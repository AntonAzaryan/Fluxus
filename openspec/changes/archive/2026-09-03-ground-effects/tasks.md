# Tasks: ground-effects

## 1. Наземные примитивы (REND-43, D1–D4)

- [x] 1.1 `effectShapes.ts`: буфер параметрической сетки (строки × столбцы), его создание, освобождение и правило дробления по `surfaceCells`/`curvatureTessellation`; проверка — юнит-тесты числа вершин и индексов
- [x] 1.2 Покадровая запись позиций: радиальная фигура (диск, кольцо, сектор) и полоса, высота каждой вершины — выборка поля (REND-9); проверка — тест на ступенчатой и на изогнутой фикстуре: вершины совпадают с `heightAt` в своих точках
- [x] 1.3 Мягкость кромки альфой вершин; материал наземного узла с `vertexColors`, пул раздельный по примитиву; проверка — тест «кромка гаснет, центр непрозрачен»
- [x] 1.4 Место среди прозрачных: `renderOrder` после воды, `depthTest` включён, `depthWrite` выключен, подъём над полем; проверка — тест значений на узле

## 2. Луч и лента (REND-23, D5, D6)

- [x] 2.1 `beam`: концы вспышки из полей события (начало — сущность, конец — `target`/`other`/координаты), концы оболочки — сущность и `targetFromStat`; цель не доехала — кадр без луча и без предупреждения; проверка — тесты обоих родов и отсутствующей цели
- [x] 2.2 `ribbon`: кольцевой буфер поз оболочки фиксированной длины, полоса по нему, гашение по возрасту, сброс истории разрывом непрерывности (REND-2); проверка — тесты длины, гашения и сброса
- [x] 2.3 Пульс альфы по часам презентации у луча и ленты — тем же полем `blink`, что мигание передержки; проверка — тест «в стоящем мире пульс замирает»

## 3. Запись манифеста (REND-23)

- [x] 3.1 Поля формы в `VisualEffect` и их валидация закрытым составом (`visualSections.ts`); `radius` обязателен, пока запись не назвала `width`; проверка — тесты каждой находки с адресом
- [x] 3.2 Прогрев новых примитивов (`effectsPrewarm.ts`): по узлу на примитив манифеста; проверка — тест «после прогрева пул несёт узел каждого примитива»

## 4. Стоимость, память, стражи

- [x] 4.1 Счётчик `effectsGroundVertices` в `cost.ts` со стадией `frame`; проверка — счётчик детерминирован на повторном прогоне записанного матча
- [x] 4.2 Ресурсы наземных узлов через `own` и освобождение в `dispose`; проверка — расширение `lifetime.test.ts` и инвариант PERF-9 зелёный
- [x] 4.3 `guard.test.ts` (все `new THREE.<resource>` внутри `own`) и `domBoundary.test.ts` зелёные

## 5. Контент дуэли

- [x] 5.1 `SlowDome` — наземное кольцо, `BossField` — наземный диск; сферы сняты (design D7); проверка — golden поведения и записи матчей без диффа, тест зеркала радиусов зелёный
- [x] 5.2 Лента как примитив реализована и покрыта тестами; в контенте дуэли она НЕ размещена — открытый вопрос: у вида ровно одна запись эффекта, и у снаряда она занята его шаром (моделей у `Fireball` нет). «Несколько изображений одного источника» — задача следующего change'а; `BossWave` вместо ленты стал наземным диском, потому что его запись — телеграф зоны урона, а не украшение (тест `demoBoss` этого и требует)

## 6. Шов сокетов частиц (§4.2.1 смежное)

- [x] 6.1 `SocketSource` принимает `nodePose(entity, node, out)`; резолв предпочитает его, обход дерева остаётся фолбэком детального яруса; проверка — тесты обеих веток и источника без обеих

## 7. Шаг тика подсистемам

- [x] 7.1 Демо называет подсистемам шаг СИМУЛЯЦИОННОГО тика (открытый вопрос фазы 1); проверка — типы и тесты демо зелёные

## 8. Гейт

- [x] 8.1 Целевые прогоны, `npm run check:impact`, затем полный `npm run check`
- [x] 8.2 `openspec validate --specs --strict`, `npm run spec-graph -- check`, roadmap этап 59, архивация change'а
