# Tasks: vfx-authoring-panel

## 1. Панель секций VFX (ED-14, D1–D5)

- [x] 1.1 Модуль строк таблиц (`assetVfxRows.ts`): источники выбранной таблицы, изображения источника, поля выбранного изображения, привязки эмиттеров; находки валидации у своей строки (ED-8)
- [x] 1.2 Модуль черновика (`assetVfxDraft.ts`): таблица, имя источника, примитив, цвет, радиус и ширина, ссылка на эмиттерный ассет; заведение изображения и привязки — операциями (ED-29)
- [x] 1.3 Область просмотрщика включает черновик в свою запись состояния и три группы — в зону инспектора (ED-23, ED-24)
- [x] 1.4 Строки обеих локалей (ED-27, ED-28); проверка — существующие тесты разрешения ключей зелёные
- [x] 1.5 Номер 0 адресует и одиночную запись источника (D4); проверка — правка поля одиночной записи из панели

## 2. Мёртвый путь сокетов (REND-24, D6)

- [x] 2.1 `particleSockets.ts`: обход дерева инстанса, кэш узла и предупреждение «сокет требует детального яруса» удалены; источник отвечает только позой узла
- [x] 2.2 Потребители кэша (`particleShell.ts`, `particles.ts`) укорочены вслед; проверка — тесты сокета переписаны на позу, батчевый ярус проверяется работающим

## 3. Тесты и гейт

- [x] 3.1 DOM-тесты панели (`assetVfxPanel.test.ts`): секции в инспекторе, правка поля, заведение источника, список из двух изображений, снятие последнего, находка на месте, эмиттер целиком
- [x] 3.2 Целевые прогоны, затем полный `npm run check`
- [x] 3.3 `openspec validate --specs --strict`, `npm run spec-graph -- check`, оговорка к строке 64 roadmap, архивация change'а
