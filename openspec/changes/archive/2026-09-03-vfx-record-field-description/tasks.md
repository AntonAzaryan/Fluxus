# Tasks: vfx-record-field-description

## 1. Описание состава записи у владельца формата (ASSET-6, REND-23, D1–D2)

- [x] 1.1 Форма машинного описания поля документа (`ManifestFieldSpec`: имя, род, обязательность, целость, границы, состав составного) — рядом с общими полями манифеста
- [x] 1.2 `EFFECT_FIELDS` — описание состава записи эффекта в смысловом порядке; `verticalOffset`, окно стата, порог цвета и мигание названы составными со своим составом
- [x] 1.3 Валидация читает из описания ключи (закрытый состав), обязательные строки и границы общего прохода чисел; радиус и выборки следа остаются со своими находками, тексты находок не менялись
- [x] 1.4 Описание и типы экспортированы из `@fluxus/assets`

## 2. Панель строит строки из описания (ED-14, D4)

- [x] 2.1 Список «дописать поле» — поля описания в его порядке; своих списков полей и родов у панели не осталось
- [x] 2.2 Контрол строки по роду поля: строка — текстом, число — числом; поле, которого описание не знает, показывается по тому, что в записи лежит
- [x] 2.3 Строки составного поля вкладом `assetVfxComposite.ts`: подполя, запись целиком, черновик заводимого (ED-23), снятие поля
- [x] 2.4 Находки владельца у своих строк, включая строки подполей (ED-8); адрес изображения — у модуля операций (D5)
- [x] 2.5 Строки обеих локалей для двух новых действий (ED-27, ED-28)

## 3. Тесты и гейт

- [x] 3.1 `assets-ts`: описание и то, чем валидация закрывает состав, — одно и то же (перечень «допустимы:» находки, состав составных полей, границы чисел, обязательные поля)
- [x] 3.2 `editor/ui-ts`: список подсказки равен описанию; окно стата, порог цвета и мигание правятся и заводятся из панели; находка у строки подполя; отказ владельца полуправки не оставляет
- [x] 3.3 Целевые прогоны, `npm run check:impact`, затем полный `npm run check`
- [x] 3.4 `openspec validate --specs --strict`, `npm run spec-graph -- check`, архивация change'а
