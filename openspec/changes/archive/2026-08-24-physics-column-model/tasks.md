# Tasks — physics-column-model

## 1. Спека

- [x] 1.1 Синхронизировать delta в основную спеку `openspec/specs/physics/spec.md` (PHYS-1 MODIFIED, PHYS-14 ADDED) и прогнать `openspec validate --specs --strict` + `npm run spec-graph -- check`

## 2. Механизм в ядре

- [x] 2.1 Помощник «эффективный уровень сущности» в `systems/physics.ts` — единственная точка чтения TERR-4/ARENA-6 для гейтов; переиспользуемый скретч-кэш уровней на тик (без аллокаций на тик), сцена без террейна → уровень 0
- [x] 2.2 Поле высоты в компоненте коллайдера: чтение в физике, семантика «нет поля / 0 → полоса не ограничена», `h ≥ 1 → [L, L+h−1]`
- [x] 2.3 Гейт полос в разрешении движения и событиях: блокировка (PHYS-8), `Collision` (PHYS-9), `Overlap` (PHYS-12) — вторым условием рядом с масками. Полоса движущегося: по состоянию до хода для блокировки, по разрешённому состоянию — для заметаемого объёма `Overlap` (PHYS-14); уровни препятствий — одна оценка за тик до разрешения. `collisionGeometry.ts` не трогается
- [x] 2.4 Гейт полос в `raycast` для динамических коллайдеров по существующей опции уровня (PHYS-13); статика обрывов без изменений
- [x] 2.5 Поле высоты — содержимое сцены, генерируемые схемы не меняются: компонент коллайдера объявляет сцена (PHYS-2), а `engine/schemas/*.json` описывают форму объявления, а не список полей. `npm run schemas` прогнан и подтвердил отсутствие диффа
- [x] 2.6 Вынести `PhysicsWorld` в `systems/broadPhase.ts` — граница PHYS-5, дословный перенос, реэкспорт из `physics.ts`; правило полос — в `systems/columnModel.ts`. Повод: гейт довёл `physics.ts` до предела `max-lines`

## 3. Тесты

- [x] 3.1 Тесты сценариев PHYS-14 в `engine/core-ts/test/physics.test.ts`: подножие обрыва, рампа (высота 2), летающий над зоной, поле без значения, луч с уровнем/без, полоса снаряда один раз за тик, сцена без террейна
- [x] 3.2 Тест согласованности: гейт полос и гейт обрыва (PHYS-11) читают уровень через один помощник — уровни одной сущности в обоих путях совпадают
- [x] 3.3 Обновить тесты, читающие PHYS-1 (2D-проекция «независимо от уровня»), под новые формулировки сценариев
- [x] 3.4 Оба направления умолчания «поле без значения»: полоса не ограничена и у препятствия, и у движущегося — пара засчитывается при любых уровнях

## 4. Гейт качества

- [x] 4.1 `npm run check` из корня зелёный; golden-эталоны и `*.cost.json` без диффа (гейт аддитивен) — если дифф есть, это дефект реализации, не повод перезаписи
