## 1. Хвост в probe и отчёте бенча (PERF-7)

- [x] 1.1 `benchProbe.ts`: добавить в `BenchSummary` худшие кадры окна (топ-5 по периоду rAF: период, работа кадра, стадии; отбор в `report()` из существующего кольца, без аллокаций на кадр), поднять `BENCH_PROBE_VERSION`; проверить юнит-тестами probe: фриз-кадр попадает в топ, порядок по периоду, потолок соблюдён
- [x] 1.2 `bench-demo.mjs`: печатать колонку max в таблице серий и блок худших кадров с раскладкой по стадиям и остатком «вне стадий»; проверить прогоном `npm run bench:demo` (обе формы — со стоящим героем и с `--drive cliff-jump`): разовый фриз виден в отчёте при зелёных p50/p99, вердикт и код возврата не изменились

## 2. Источник числа программ (реестр RDBG-1)

- [x] 2.1 источник `render.programs`: код в render-ts (`debug/sources.ts`), регистрация у владельца рендерера (сборка демо) — секция дампа с числом живых программ из `renderer.info.programs` (единицы названы, RDBG-7; чтение готового состояния, RDBG-8); проверить тестом источника: секция в дампе при включённом источнике, отсутствие при выключенном, число растёт после компиляции новой программы
- [x] 2.2 `bench-demo.mjs`: читать `render.programs` в обе пробы рядом с `fog.mask` и печатать в drive-блоке число живых программ на границах окна («было N → стало M» с дельтой и оговоркой метода — уравновешенная пересборка даёт ноль; без влияния на вердикт); проверить прогоном `--drive cliff-jump`: строка печатается, при отсутствии источника замер не отказывает и называет причину — «не зарегистрирован» и «noData» различены

## 3. Ворота

- [x] 3.1 `npm run check` из корня зелёный; `*.cost.json` не изменились (RDBG-8: отладка невидима счётчикам) — иначе это дефект источника, а не повод для `golden:cost`
