# Tasks

## 1. Формат: блок тинта, растворение и высота якоря (ASSET-18, ASSET-6)

- [x] 1.1 Типы `VisualTint`, `TintFlash`, `VisualDissolve` и поле `anchorHeight` записи в `engine/assets-ts/src/manifest.ts`
- [x] 1.2 Валидация блоков и поля в `manifestFields.ts`/`manifestValidate.ts`: закрытые ключи, цвет `#rrggbb`, доли в `[0..1]`, положительные длительности
- [x] 1.3 Тесты валидации: опциональность блоков, находки с путями до полей

## 2. Канал тинта (REND-40)

- [x] 2.1 `models/instanceTint.ts` — состояние канала на записи, база порта, вспышка события, сведение кадра без аллокаций
- [x] 2.2 Батчевый ярус: инстанс-атрибут `instanceTint` в `model/batch.ts`, фрагментный патч и ключ программы в `model/vatMaterial.ts`
- [x] 2.3 Маска команд-цвета в ключе батча и в решении о пересборке инстанса (`tintMaskToken`)
- [x] 2.4 Детальный ярус: множитель в `color` собственных материалов (copy-on-write REND-6), база — в `userData`
- [x] 2.5 Вспышка по событию доставки — тем же разбором событий, что и one-shot клипы
- [x] 2.6 Публичный порт `ModelsSubsystem.setTint`
- [x] 2.7 Тесты: атрибут батча, цвет материалов детального яруса, маска на обоих ярусах, спад вспышки к базе, прогретая программа с каналом

## 3. Растворение трупа (REND-4)

- [x] 3.1 Часы растворения в `models/instanceFade.ts`; нарисованная доля — произведение проявленности и целостности
- [x] 3.2 Уход из КАДРА по концу растворения и возврат по признаку «сущность больше не мертва»
- [x] 3.3 Тесты: таймлайн, возрождение, снятый маркер, `snapAll`, уход в туман посреди растворения, снятый переподачей блок

## 4. Авторская высота якоря (REND-41)

- [x] 4.1 `anchorHeight` в публичном виде инстанса и в `screenAnchors.ts`: потребитель → запись → верх габаритов
- [x] 4.2 Тесты: авторская высота с масштабом набора, приоритет числа потребителя

## 5. Игра: палитра команд и подача (политика)

- [x] 5.1 Документ палитры `game/demo-ts/app/presets/teams.json`
- [x] 5.2 Подача `app/teamTint.ts`: стат `team` → цвет палитры → порт рендера
- [x] 5.3 Регистрация в `main.ts` рядом с подачей стелса
- [x] 5.4 Записи `content/visuals/manifest.json`: вспышка на `HeroHit`, растворение, высота якоря
- [x] 5.5 Тесты: цвет из стата, повторная подача, совместимость со стелсом

## 6. Спека

- [x] 6.1 Дельты `rendering` (REND-40 новое, REND-4 и REND-41 правка) и `assets` (ASSET-18 новое, ASSET-6 правка)
- [x] 6.2 Строка 57 в таблице этапов `docs/architecture.md`
- [x] 6.3 `openspec validate --strict`, `npm run spec-graph -- check`, архивация изменения
