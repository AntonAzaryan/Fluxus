## 1. Спека netcode

- [x] 1.1 Проверить, что номер NET-17 свободен по всему `openspec/`, включая архив
- [x] 1.2 `specs/netcode/spec.md` — MODIFIED NET-16: версия как пара «идентификатор сборки ядра + хеш контент-пака», сверяются обе половины
- [x] 1.3 NET-16: убрать претензию версии на ассеты террейна и арены, отдав их зоне DET-1, и записать следствие (разные библиотеки ассетов при одной поднятой арене — одна версия)
- [x] 1.4 NET-16: авторитет и точка проверки — версия матча = версия серверной сборки, сервер матча единственный рубеж, матчмейкинг рубежом не считается
- [x] 1.5 NET-16: обновление внешним каналом, сервер контент-пак не раздаёт
- [x] 1.6 NET-16: версия матча фиксируется на старте, выкатка идущий матч не прерывает, обновившийся клиент реконнектом не допускается
- [x] 1.7 NET-16: честная граница гарантии — защита от расхождения по недосмотру, не от читера; почему солгавшему это ничего не даёт (NET-1, NET-7, NET-4)
- [x] 1.8 NET-16: сценарии — старая сборка, только контент, только ядро, забытый бамп, разные библиотеки ассетов, выкатка во время матча, реконнект обновившегося, подменённый хеш, версии совпали и данные разошлись
- [x] 1.9 `specs/netcode/spec.md` — ADDED NET-17: состав канонического представления (шесть пунктов), исключения `terrain`/`arena`/`capacity` с основаниями
- [x] 1.10 NET-17: правила нормализации — умолчания разрешаются, `prefabs`/`systems` отсутствующие эквивалентны пустым, таблица твинов — нет
- [x] 1.11 NET-17: байтовый канон и FNV-1a 32 те же, что у хеша `worldInit`; чистая функция от конфига, без зависимости от сетевого слоя (DI-3)
- [x] 1.12 NET-17: сценарии — переформатирование, перестановка компонентов, умолчание `modifierSlots`, пустые твины против отсутствующих, замена ассета арены

## 2. Спека determinism-core

- [x] 2.1 `specs/determinism-core/spec.md` — MODIFIED DET-1: ссылка на NET-17 в абзаце про конфиг сцены, граница с обеих сторон (ассеты и `capacity` остаются здесь)
- [x] 2.2 DET-1, пункт 3: назвать ёмкость мира явно — на неё опирается исключение `capacity` в NET-17
- [x] 2.3 DET-1, сценарий «Разный порядок правил при тех же данных» — дополнить ссылкой на NET-17

## 3. Реализация в core-ts

- [x] 3.1 Вынести `canonicalJson` из `src/sim/worldInit.ts` в `src/sim/canonicalJson.ts`, `worldInit.ts` переключить на импорт (двух копий канона быть не должно)
- [x] 3.2 `src/sim/contentPack.ts` — `contentPackForm(def: SceneDef)`: шесть пунктов состава, разрешение умолчаний, различение «нет таблицы твинов» и «пустая таблица», исключение `terrain`/`arena`/`capacity`
- [x] 3.3 `src/sim/contentPack.ts` — `contentPackHash(def: SceneDef)`: канонический JSON → FNV-1a 32 → восемь строчных hex-цифр
- [x] 3.4 Экспортировать `contentPackHash`, `contentPackForm` из `src/index.ts` рядом с `worldInitHash`/`worldInitForm`

## 4. Тесты

- [x] 4.1 `test/contentPack.test.ts` — форма хеша: восемь строчных hex-цифр, детерминированность повторного вызова
- [x] 4.2 Переформатирование конфига (другой порядок ключей в объектах) хеш не меняет
- [x] 4.3 Перестановка элементов `components` хеш меняет; перестановка `systems` — тоже
- [x] 4.4 Конфиг без `modifierSlots` и конфиг с `modifierSlots: 4` дают один хеш; со значением, отличным от умолчания, — разный
- [x] 4.5 Отсутствующие `prefabs`/`systems` эквивалентны пустым; `tweens: []` и отсутствие `tweens` — не эквивалентны
- [x] 4.6 Замена ассета `arena`/`terrain` и смена `capacity` хеш контент-пака не меняют, а хеш `worldInit` — меняют (обе зоны в одном тесте, чтобы граница проверялась, а не подразумевалась)
- [x] 4.7 Изменение JSON-системы (тело `do`) хеш меняет — базовый сценарий, ради которого требование заведено

## 5. Проверка

- [x] 5.1 `npm test` в `engine/core-ts` зелёный
- [x] 5.2 Убедиться, что golden-эталоны не тронуты: CLI-3 в этом change не меняется
- [x] 5.3 Убедиться, что `worldInitHash` не изменился ни на одном эталоне после выноса `canonicalJson`
