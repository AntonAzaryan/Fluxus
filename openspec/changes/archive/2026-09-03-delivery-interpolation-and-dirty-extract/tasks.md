# Tasks: delivery-interpolation-and-dirty-extract

## 1. Каденс: джиттер и отставание показа (D1, D2, REND-2, SHELL-7)

- [x] 1.1 Наблюдение джиттера по режимам: экспоненциальное среднее модуля отклонения пробы интервала от его сглаженной оценки, той же постоянной сглаживания; сброс вместе с оценкой интервала на входе в перемотку; проверка — тест «ровный каденс даёт нулевой джиттер, дрожащий — ненулевой»
- [x] 1.2 Отставание `clamp(интервал + 2·джиттер, интервал, min(конфиг, 2·интервала))` и время показа `now − delay`; альфа — доля от штампа `prev` по сглаженному интервалу; проверка — тест «ровный каденс: отставание ровно интервал, альфа как прежде»
- [x] 1.3 Отложенный слот записи (шесть интерполируемых величин) и проход продвижения: `prev ← curr`, `curr ← отложенное` либо схлопывание; продвижение в `apply` при отставании в интервал, в `frame` — когда время показа дошло; проверка — тесты «поздний пакет доигрывается», «пачка не телепортирует»
- [x] 1.4 Разрыв непрерывности сбрасывает отложенное и штампы; темп обратного хода (REND-25) не меняется; проверка — тесты снапа и скраба зелёные без правок

## 2. Наблюдаемость каденса (RDBG-7)

- [x] 2.1 Запись каденса в доставленном состоянии (интервал, джиттер, отставание) — стабильный объект, мутируемый буфером; документный продюсер её не заполняет
- [x] 2.2 Источник `net.delivery` печатает три величины в ЧАСОВОЙ секции дампа; проверка — тест «величины в `clock`, тело секции их не содержит»

## 3. Частичный кадр: отправитель (D4, D5, SHELL-3, SHELL-4)

- [x] 3.1 Зеркало последнего доставленного кадра в `Extractor`: те же колонки таблицы плюс разреженные статы, слот на сущность, рост вместе со сценой
- [x] 3.2 Отбор строк сравнением с зеркалом; появившиеся — всегда; исчезнувшие — списком идентификаторов; проверка — тесты «стоящая сущность не едет», «появившаяся едет», «исчезнувшая едет списком»
- [x] 3.3 `markDelivered()` двигает зеркало; вызывают его `RenderHost` после `apply` и `ShellSender` после записи кадра; проверка — тест «изменение тика N, чей кадр не уехал, едет следующим кадром» (конфляция SHELL-4)
- [x] 3.4 Полный кадр на `snapAll`, смене ветви и первой доставке; зеркало при этом очищается; проверка — тест «после снапа кадр полный»

## 4. Частичный кадр: раскладка и приёмник (D6, D7)

- [x] 4.1 Кодек: бит «кадр полный», слово «число удалённых», секция идентификаторов удалённых; версия растёт; проверка — roundtrip частичного кадра и кадра с удалёнными
- [x] 4.2 `ViewBuffer`: удаление по списку на частичном кадре, по отсутствию — на полном; схлопывание пары и гашение кадровых признаков недоставленных записей в проходе продвижения; проверка — тесты «недоставленная запись стоит, а не дёргается», «удаление списком»
- [x] 4.3 Счётчики стоимости: `extractChannelValues` считает реально отданные строки, рядом — просмотренные на изменение; проверка — тест «стоящая сцена не платит каналу»

## 5. Остаток фазы 1

- [x] 5.1 `floorMirror`: поиск сущности террейна через `queryInto` в переиспользуемый буфер вместо `listAlive`; проверка — структурный страж пути извлечения расширен на файл

## 6. Эталоны и гейт

- [x] 6.1 `npm run golden:cost` — объём канала на записанных матчах падает; дифф объясняется отбором строк
- [x] 6.2 Полный гейт `npm run check`
