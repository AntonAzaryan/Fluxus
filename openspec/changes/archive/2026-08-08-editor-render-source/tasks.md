# Tasks: editor-render-source

## 1. Спеки

- [x] 1.1 Дельта `specs/rendering/spec.md`, `ADDED` REND-11: документный источник как второй продюсер presentation-состояния; декларативный набор с устойчивыми ключами и сведением; отсутствие императивного API инстансов; взаимоисключающесть продюсеров; инкрементальность обновления
- [x] 1.2 Там же, REND-11: явные списки — чего в документном режиме нет (интерполяция REND-2, события и производные от них REND-4/REND-5, фильтр видимости FOW-7..9) и что применяется без изменений (манифест ASSET-6, скины REND-6, кривизна REND-9, посадка REND-10)
- [x] 1.3 Там же, REND-11: превью ассета (`editor` ED-20) как вырожденный случай — набор из одного инстанса без сцены и террейна; клип задаётся набором с умолчанием на клип покоя
- [x] 1.4 Дельта `specs/rendering/spec.md`, `MODIFIED` REND-1 (полный текст с правкой): «единственный вход **данных симуляции**»; закрытый перечень легальных несимуляционных входов; конверсия Q16.16 → float на входной границе с инвариантом «глубже fixed-point не проникает»; запрет float → fixed адресован потоку рендера, квантование авторских величин — за редактором вызовом ядра
- [x] 1.5 Сверить, что дельта не занимает и не правит чужих номеров: заняты только REND-11 (новое) и REND-1 (изменение); ED-1, ED-15, ED-16, ED-20, REND-3, REND-8 — только ссылками
  - Сверено при архивации: в дельте ровно два блока — `ADDED` REND-11 и `MODIFIED` REND-1. Остальные упомянутые ID (ASSET-1, ASSET-6, ASSET-7, CAM-1, CORE-3, ED-1, ED-9, ED-15..ED-18, ED-20, FOW-7, OBS-1, OBS-3, REND-2..REND-10, SHELL-5) встречаются только ссылками.
- [x] 1.6 `openspec validate --change "editor-render-source" --strict` — зелено
  - Флаг у CLI сегодня другой: `openspec validate editor-render-source --type change --strict` → «Change 'editor-render-source' is valid».

## 2. Сверка нормы с кодом (без изменения поведения)

- [x] 2.1 `engine/render-ts/src/types.ts`, шапка модуля: комментарий дословно повторяет старую REND-1 («единственный вход данных — `TickResult`», «конверсия … происходит ровно один раз», «обратной конверсии float → fixed в пакете не существует») — переписать под новую формулировку, перечислив фактические входы пакета
- [x] 2.2 `engine/render-ts/src/extractor.ts:234`: комментарий «Единственная точка конверсии Q16.16 → float во всём рендере» неверен уже сегодня (`subsystems/terrain.ts` делит на `FIXED_ONE` при приёме сетки) — переформулировать как «точка конверсии потока тиков», сославшись на инвариант REND-1
- [x] 2.3 `engine/render-ts/src/subsystems/terrain.ts`: пометить приём `tileSize` и координат cliff-отрезков как вторую точку границы (REND-1, SHELL-5, TERR-2) — сейчас конверсия там не прокомментирована и читается как случайная
- [x] 2.4 Пройти по `engine/render-ts/src` и убедиться, что за входной границей fixed-point значений и их арифметики нет; найденное — либо перенести на границу, либо зафиксировать отдельной задачей, а не расширять норму
  - Точки границы помечены: `extractor.ts` (поток тиков), `subsystems/terrain.ts` и `visualSurface.ts` и `camera/rig.ts` (приём сетки), `documentSource.ts` (набор приезжает уже во float).
  - Найдено одно расхождение с инвариантом REND-1: полезная нагрузка события (`RenderEvent.data`) копируется полем в поле, координатные поля остаются в Q16.16 и делятся на `FIXED_ONE` у потребителя (`camera/director.ts:falloff`). Перенести на границу нечем — какие поля события координатные, знает контент, а не рендер. Помечено комментариями в `types.ts` и `camera/director.ts`; норму под факт не расширяли, нужен отдельный change (нормировать «координатные поля события» либо приводить их в Extractor'е по объявленной схеме).
- [x] 2.5 `npm run typecheck` и `npm test` из корня — зелено; правки только в комментариях, поведение не меняется
  - Прогон попакетный (`render`, `client`, `integration`), а не корневой: соседние пакеты в этот момент правились параллельно. Правки этого пункта — только комментарии; реализация REND-11 приехала отдельной задачей плана редактора (W0-2).

## 3. Проверка

- [x] 3.1 `openspec validate --specs --strict` — все capability проходят
  - Прогнано при архивации: 27 из 27.
- [x] 3.2 `git diff --stat`: тронуты `openspec/changes/editor-render-source/` и комментарии в `engine/render-ts/src`; ядро, netcode, контент, `engine/tests/golden/` и `openspec/specs/` не тронуты
  - Сверено по коммитам change'а `d971299` и `3b256ad`: тронуты только `engine/render-ts/{src,test}` и `openspec/changes/editor-render-source/tasks.md`. `engine/core-ts`, `engine/net-ts`, `content/`, `engine/tests/golden/` и `openspec/specs/` не тронуты ни одним из них.
  - Уточнение к формулировке задачи: в `engine/render-ts/src` этими коммитами приехали не только комментарии — реализация REND-11 (`documentSource.ts`, `stage.ts`) шла тем же коммитом (W0-2 плана редактора). Перечень нетронутого от этого не меняется.
  - REND-11 и правленый REND-1 в `openspec/specs/rendering/spec.md` стояли уже до работ ветки — спека была синхронизирована раньше, чем протикали задачи; при архивации это сверено построчно, дельта и главная спека совпадают.
