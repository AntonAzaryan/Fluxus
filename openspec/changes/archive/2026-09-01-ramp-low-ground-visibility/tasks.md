## 1. Ядро: валидация стороны флага рампы (TERR-5)

- [x] 1.1 В `engine/core-ts/src/systems/terrain.ts` добавить в `createTerrainGrid` проверку рядом с `validateRampWidth`: клетка-рампа с соседом по стороне на уровень ниже без флага рампы отвергается адресной ошибкой (клетка, сосед, оба уровня); проверить прогоном `npx vitest run test/terrain.test.ts`
- [x] 1.2 Тесты `engine/core-ts/test/terrain.test.ts`: невалидная карта «рампа на верхней стороне», валидная «рампа на нижней», валидная цепочка склона 0→1→2 из двух рамп, невалидная терраса `0-плоская|1-рампа|2-плоская`; все зелёные
- [x] 1.3 Тест видимости (`engine/core-ts/test/visibility.test.ts` или сценарный): наблюдатель на клетке рампы (нижний уровень) не видит сущность верхнего уровня, а наблюдатель нижнего уровня видит сущность на рампе; зелёный без правок `VisibilitySystem`

## 2. Контент и фикстуры

- [x] 2.1 `content/scenes/duel.scene.json`: опустить уровень 16 клеток рампы (x=19 и x=28, ряды 13–16 и 31–34) с 1 до 0, флаги не трогать; `npm run demo:debug` отрабатывает, сцена грузится
- [x] 2.2 `engine/tests/golden/terrain.scenario.json`: опустить уровень 2 клеток рампы на нижний; починить прочие inline-карты тестов, которые новая валидация отвергла (по красным тестам `npm test`)
- [x] 2.3 Тест-страж равенства cliff-геометрии: `createTerrainGrid` до и после правки уровня клетки рампы (пары уровней/флагов из 2.1) даёт тот же набор отрезков; закрепляет D2

## 3. Рендер: срез reveal по уровню тайла (FOW-9)

- [x] 3.1 В `engine/render-ts/src/fog/mask.ts` пропускать тексель, чья клетка сетки строго выше `observer.level` (сетка уже в подсистеме, D4); тексель относится к клетке по центру, сомнение — в туман
- [x] 3.2 Тесты маски (`engine/render-ts/test`): наблюдатель нижнего уровня напротив проёма рампы — пол верхнего уровня под туманом; наблюдатель на плато — нижний пол и плато за низиной открыты (существующее поведение не сломано)

## 4. Голдены и гейт

- [x] 4.1 После зелёных юнитов — `npm run golden` из корня; в диффе базлайнов только уровни `worldInit` и маски `Visibility`, изменение cliff-отрезков = ошибка правки карт
- [x] 4.2 При красном диффе стоимости — `npm run golden:cost`, дифф в коммит как явное принятие; иначе пропустить
- [x] 4.3 `npm run check` целиком зелёный (typecheck, eslint, knip, jscpd, depcruise, spec-graph, тесты)

## 5. Заготовка «подглядывание» (D5)

- [x] 5.1 Создать stub-change `ramp-peek-vision`: `proposal.md` c `## Notes` (идея: политика ограниченного обзора верхнего уровня с рампы поверх фильтра высоты FOW-5), `skip_specs` по правилам стабов; `openspec validate` зелёный
