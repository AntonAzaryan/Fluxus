## 1. Имя capability в перекрёстных ссылках

- [x] 1.1 `openspec/specs/rng/spec.md`, RNG-6: «сверяется между реализациями (CLI-6)» → «(`cli-testing` CLI-6)»
- [x] 1.2 Там же, RNG-8: «(`determinism-core` CLI-6)» → «(`cli-testing` CLI-6)»
- [x] 1.3 `openspec/specs/terrain/spec.md`, TERR-4: «наравне с Physics API (`ecs-foundation` DI-3)» → «(`determinism-core` DI-3)»
- [x] 1.4 Перепрогнать машинную сверку «имя capability в бэктиках против владельца префикса ID» по всем спекам: после правки список расхождений должен быть пуст

## 2. Модальность в редакторе

- [x] 2.1 `openspec/specs/editor/spec.md`, ED-10: «— ручная правка текстовых карт не должна быть обязательной» → «; ручная правка текстовых карт MUST NOT быть обязательной»
- [x] 2.2 Там же, ED-11: то же для «ручная правка текстовых рядов»
- [x] 2.3 Там же, ED-14: «Ручная правка манифеста не должна быть обязательной» → «Ручная правка манифеста MUST NOT быть обязательной»
- [x] 2.4 Проверить, что ED-20 не тронут: он образец формы, а не предмет правки

## 3. Типы полей по ECS-3

- [x] 3.1 `openspec/specs/arena/spec.md`, ARENA-3: `ArenaState { inside: i32, onFloor: i32 }`; кодирование флага — ссылкой на `ecs-foundation` ECS-3, без пересказа правила
- [x] 3.2 Там же: дефолт обоих полей объявить числом `1` со словесной расшифровкой «внутри арены, на полу»; следующий абзац про спавн за краем сохранить
- [x] 3.3 Там же: добавить сценарий «В поле inside лежит не единица» — один из двух новых сценариев этого изменения (второй — в FOW-2), прочие сценарии ARENA-3 не трогать
- [x] 3.4 `openspec/specs/fog-of-war/spec.md`, FOW-2: `Visibility { visibleTo: i32 }`; «(`u32`/`u64`, до 32/64 команд — с запасом для „до 10 игроков“)» → одна ширина 32 бита с диапазоном команд `[0, 31]`, тип — ссылкой на ECS-3; запрет второго поля ради 64 команд; ссылка на `bitTest` (`data-driven-systems` EXPR-2, `tick-loop` TICK-4)
- [x] 3.5 Там же: сохранить абзац-обоснование битмаски (fixed-size, dirty как сравнение двух чисел, per-team гранулярность) и сценарий «Сущность видна двум командам»; добавить сценарий «Номер команды вне ширины маски»
- [x] 3.6 Там же, FOW-3: `Stealth { active: i32 }`; абзац-обоснование ниже не трогать
- [x] 3.7 `openspec/specs/time-system/spec.md`, TWEEN-1: «поля компонента ECS SHALL быть скалярами `i32`/`fixed`» → «значение поля компонента ECS SHALL быть скаляром типа из закрытого набора, который задан там же»; сценарий «Путь к полю в компоненте» не трогать

## 4. Проверка

- [x] 4.1 `openspec validate --specs --strict` — зелено (был зелёным и до правки: см. `design.md`, Context)
- [x] 4.2 Дифф главных спек: ровно десять требований в шести capability, ни одного нового ID, ни одной новой capability
- [x] 4.3 Убедиться, что `engine/`, `content/` и `docs/` не затронуты; `npm test` и `npm run golden` запускать незачем — правка текстовая
- [x] 4.4 Сверить `engine/core-ts` на соответствие исправленному тексту: `ArenaState.inside`/`onFloor` и `Stealth.active` объявлены `i32`, дефолт `inside`/`onFloor` — `1`; `Visibility.visibleTo` — одно поле `i32`, `MAX_TEAMS = 32`, номер команды вне `[0, 31]` отвергается. Сверено при написании дельт: расхождений нет. Расхождение означало бы дефект реализации (CORE-3) и отдельный change, а не откат этой правки
