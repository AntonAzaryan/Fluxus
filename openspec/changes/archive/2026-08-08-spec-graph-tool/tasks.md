# spec-graph-tool — tasks

## 1. Парсер и модель графа

- [x] 1.1 `scripts/spec-graph.mjs`: обход `openspec/specs/*/spec.md`, парсинг `### Requirement: <ID> <заголовок>` в модель {capability, id, title, файл, строка, тело секции}; шапка/Purpose capability сохраняются отдельно
- [x] 1.2 Извлечение ссылок из тел секций: паттерн ID, сверка со словарём определений, направление «цитирующий → цитируемый», исключение самоопределения; заявленная атрибуция (`` `имя` ID ``) — отдельным полем ребра
- [x] 1.3 Агрегация до capability-уровня (рёбра с весом = число ссылок)
- [x] 1.4 Тест-на-формат (D8): парсер по текущему дереву находит 317 требований, ID уникальны, число ссылок в ожидаемом диапазоне; тест в корневой vitest-набор

## 2. Команды навигации

- [x] 2.1 `find <текст>` — поиск по заголовкам и телам, вывод только ID + заголовки
- [x] 2.2 `where <ID>` и `show <ID…>` — секция + Purpose по умолчанию, `--capability` для всей спеки
- [x] 2.3 `refs <ID>` (in/out) и `impact <ID>` (транзитивное замыкание зависимых)
- [x] 2.4 `deps <capability>` и `graph --mermaid`
- [x] 2.5 `--json` на всех командах; `--help` с честным ограничением «main specs, дельты changes/ не видны»
- [x] 2.6 Скрипт `spec-graph` в корневом `package.json` (`npm run spec-graph -- <cmd>`)

## 3. Линт

- [x] 3.1 `check`, инварианты: висячие ссылки, дубли ID, заявленная атрибуция ≠ факт, секция без SHALL/MUST NOT, цикл на capability-уровне; exit 1 при находках, режим отчёта — не в `npm run check` (D7)
- [x] 3.2 Конфиг слоёв `scripts/spec-graph.layers.json`: порядок foundation → sim → net → client → editor/content по `docs/architecture.md`, правило «вверх нельзя», mechanism не цитирует game-content; исключения — рёбра editor↔(rendering, camera, serialization) с обоснованиями
- [x] 3.3 Проверка слоёв в `check`; `terrain → ED-10` в исключения не вносится — остаётся находкой
- [x] 3.4 `check --metrics` (всегда exit 0): fan-in, instability Ce/(Ca+Ce), сироты
- [x] 3.5 Тесты линта на синтетических фикстурах: по одному красному кейсу на каждый инвариант
- [x] 3.6 Прогон `check` по текущему дереву, находки — списком в комментарий/вывод задачи (не правятся здесь; известный красный: до-`spec-modality-and-refs` дефекты)

## 4. Интеграция для агентов

- [x] 4.1 CLAUDE.md: норма пользования — цитируешь ID через `spec-graph show`, перед правкой требования `impact`, свою capability читай целиком
- [x] 4.2 Скилл `.claude/skills/spec-graph/SKILL.md` по образцу `spec-coverage`: триггеры («найди требование», «кто зависит от», «покажи граф спек»), шпаргалка команд, ограничение про дельты
- [x] 4.3 Финальная проверка: `npm run check` зелёный (без spec-graph-линта), сценарий из design («контур задачи ~70 строк») воспроизводится руками
