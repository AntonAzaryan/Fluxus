# Tasks: hybrid-shadow-composite

## 1. Спека (REND-30, REND-33, REND-34)

- [x] 1.1 Дельта `rendering`: MODIFIED REND-30 — одна карта из двух целей глубины (минимум глубин), `staticShare` снят, затенение `hybrid` равно `full`, подгонка пирамиды по коробке арены в пространстве света, пирамида портом владельцу нарисованного; сценарии «Тень в hybrid не светлее, чем в full», «Тень у края арены при косом солнце», «Кастер за краем кадра»
- [x] 1.2 Дельта `rendering`: MODIFIED REND-33 — гашение отобранного источника запасом до границы отбора, без памяти о прошлых кадрах
- [x] 1.3 Дельта `rendering`: MODIFIED REND-34 — цепочка апсемплов bloom, сведение читает одну текстуру; сценарий «Свечение вокруг яркой точки»
- [x] 1.4 `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные

## 2. Сведение ярусов теней (L-2)

- [x] 2.1 `lighting/shadowComposite.ts`: порт `ShadowRendererLike`, три цели (две ярусов без сравнения, карта — со сравнением), полноэкранный проход `gl_FragDepth = min(static, dynamic)`, счётчики проходов; освобождение по REND-31
- [x] 2.2 Подсистема: один источник во всех режимах, авторская интенсивность целиком, кадр яруса + сведение, прайминг обоих ярусов на первом кадре целей, подтверждение перерисовки фактом прохода; проверка — тесты «карта сведения несёт ОБА яруса», «затенение `hybrid` равно `full`», «НЕСОСТОЯВШИЙСЯ теневой проход перерисовку не подтверждает»
- [x] 2.3 Без порта рендерера `hybrid` исполняется как `full`; проверка — тест «без порта рендерера `hybrid` исполняется как `full`»
- [x] 2.4 `staticShare` снят из `lighting/config.ts`, `assets-ts` (валидация секции), отладочного дампа и фикстур тестов; проверка — тесты валидации секции и дампа

## 3. Пирамида теневой камеры (M-1, L-9)

- [x] 3.1 `ShadowCasterSink.shadowFrustum?()` в контракте и его реализация подсистемой (обновление матриц источника, переиспользуемая пирамида); проверка — тесты «пирамида теневой камеры отдаётся владельцу инстансов портом», «в режимах без карт теней пирамиды нет вовсе», «пирамида следует направлению света и переиспользуется»
- [x] 3.2 `lighting/arena.ts`: коробка арены (план + высота уровней с запасом на кастеров)
- [x] 3.3 `lighting/shadowMaps.ts`: подгонка фрустума по восьми углам коробки в пространстве света, асимметричные границы, смещения по большему текселю; пересчёт на кадре перехода цикла при поехавшем направлении
- [x] 3.4 Проверка — тесты «все восемь углов арены — внутри фрустума при косом и низком солнце», «верхний ярус кастеров тоже внутри», «плотность текселей на дуэльной арене выросла не меньше чем в 1.4 раза», «переход переобтягивает фрустум», «установившаяся фаза фрустум не трогает»

## 4. Цепочка апсемплов bloom (L-7)

- [x] 4.1 `postprocess/passes.ts`: ядро тента данными, программа апсемпла, вклад мелкого яруса из авторской ширины свечения, сведение с одной текстурой
- [x] 4.2 `postprocess/chain.ts`: ступени вверх 4→3→2→1→0 аддитивным блендингом, освобождение материалов ступеней
- [x] 4.3 Проверка — тесты порядка проходов и целей, «ядро тента — одно на даунсемпл и апсемпл», модель цепочки на CPU: «профиль от одной яркой точки убывает МОНОТОННО», «прямое сложение ярусов даёт ступени, цепочка апсемплов — гладкий спад»

## 5. Сборки и эталоны

- [x] 5.1 Демо передаёт рендерер подсистеме освещения; стенд эталонов (`benchLoad`) и стенд интеграции — двойник порта
- [x] 5.2 `npm run golden:cost`: `postprocessPasses` 7 → 11 и `postprocessTexels` +25 % (цепочка апсемплов), материалы пост-обработки 6 → 10, ресурсы освещения — три цели, их текстуры, материал и квад сведения. Счётчики теней не двигаются: чередование ярусов прежнее
- [x] 5.3 `npm run check`
