## 1. Правило хода при пересечении

- [x] 1.1 `blocks()`: снаружи — свёрнутый объём, внутри — сравнение расстояний между центрами
- [x] 1.2 Расстояние считается удвоенным, без деления и без `fixed.add` (сумма границ выходит за i32)
- [x] 1.3 Правило применяется и к статике, и к динамике

## 2. Проверка

- [x] 2.1 Тест: сущность внутри препятствия выходит наружу, глубже — нет
- [x] 2.2 Тест: сущность, стоящая на границе обрыва, из него выбирается
- [x] 2.3 `npm test` и `npm run typecheck` зелёные; golden-эталоны не изменились
- [x] 2.4 `openspec validate physics-move-out-of-overlap --strict`
