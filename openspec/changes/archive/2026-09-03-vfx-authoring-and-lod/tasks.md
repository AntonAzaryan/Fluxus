# Tasks: vfx-authoring-and-lod

## 1. Список изображений одного источника (REND-23, D1)

- [x] 1.1 Тип записи таблицы — запись либо СПИСОК (`manifest.ts`), резолверы `resolveEffectsBy*` рядом с односоставными; проверка — тесты обеих форм и первой записи у односоставного резолвера
- [x] 1.2 Валидация списка (`visualSections.ts`): каждая запись по общим правилам, адрес находки несёт номер, пустой список — ошибка; проверка — тесты обеих находок
- [x] 1.3 Общий набор оболочек (`shellSupport.ts`) заводит оболочку на каждую запись списка, ключ несёт номер; событие запускает вспышку на каждую; прогрев обходит список; проверка — тесты оболочек типа и состояния, вспышек события и укоротившегося списка (REND-17)
- [x] 1.4 Контент дуэли: `Fireball` и `HeavyFireball` — шар плюс `ribbon`-след; проверка — тесты демо читают записи резолверами, golden'ы поведения без диффа

## 2. Отсечение VFX по камере (REND-24, REND-43, D2, D3)

- [x] 2.1 `cameraCull.ts`: пирамида и позиция камеры кадра, вопрос «виден ли якорь радиуса R и не дальше ли D» без аллокаций; камера — опция подсистемы (REND-8)
- [x] 2.2 Эмиттер вне пирамиды не шагается (`particleShell.ts`), ручка `particles.cullDistance` с умолчанием «предела нет»; проверка — тесты «за кромкой не шагает», «в кадре шагает», «вернулся — продолжил», «без камеры отсечения нет», «ручка гасит видимый далёкий»
- [x] 2.3 Наземная фигура вне пирамиды не переписывает вершин (оболочка и вспышка); проверка — тесты обоих родов и сборки без камеры
- [x] 2.4 Счётчики `particlesShellsCulled` и `effectsShapesCulled` в сток PERF-3 стадии `frame`; проверка — экономия видна числом в тестах
- [x] 2.5 Камера подсистемам эффектов и частиц в демо и во вьюпорте редактора

## 3. Потолок дробления наземных фигур (REND-43, QUAL-3, D4)

- [x] 3.1 Ручка `effects.shapeDetail` семантики потолка вместо `constantCost` подсистемы эффектов; потолок действует НАРЯДУ с потолком кода в правиле дробления
- [x] 3.2 Смена потолка переснимает живые оболочки (топология фиксируется взятием, REND-26); проверка — тесты «грубее — меньше вершин, место и размер те же» и «живая оболочка пересобрана»
- [x] 3.3 Реестры ручек сцены и вьюпорта редактора закреплены поимённо с границами; проверка — `quality.test.ts` и `sceneQuality.test.ts`

## 4. Атлас кадров (ASSET-14, REND-24, D5)

- [x] 4.1 Числа атласа проверяются формой в `particleEffect.ts` (целое ≥ 1 по каждой оси), адрес находки — путь до узла и поля; проверка — тесты нуля, дробного, чужого типа и законного отсутствия
- [x] 4.2 Фикстура документа с атласом 4×2 и поведением `FrameOverLife`, записанная самой библиотекой; проверка — батч рисует настройками атласа, прогрев заводит его конвейер до первого кадра
- [x] 4.3 Предупреждение рендера один раз, когда кадр ведётся по атласу 1×1; проверка — тест «два экземпляра, одно предупреждение, кадр рисуется»

## 5. Авторинг секций VFX в редакторе (ED-29, D6)

- [x] 5.1 `assetVfx.ts`: дописать изображение источнику (одиночная запись становится списком, возвращается номер), записать одно поле изображения (`null` снимает), привязать источник к эмиттерному ассету; правила формата — у `validateManifest`
- [x] 5.2 Регистрация набором визуальных операций, строки описаний обеих локалей; проверка — тесты обратимости (ED-18), адресности отказов и каталога описаний
- [x] 5.3 Читатели секций для интерфейса: имена источников таблицы, изображения источника списком, запись эмиттера

## 6. Горячая перезагрузка эмиттерных ассетов (ED-15, D7)

- [x] 6.1 `ParticlesSubsystem.refreshAssets()`: забыть документы, снести пул и завести оболочки заново обычным сведением; проверка — тесты «правленый документ доезжает», «ассет запрошен заново», «без доставленного состояния ничего не заводится»
- [x] 6.2 Вьюпорт редактора подписан на пересоздание модуля ассетов и отписывается сносом

## 7. Гейт

- [x] 7.1 Целевые прогоны, `npm run check:impact`, затем полный `npm run check`
- [x] 7.2 Эталоны стоимости и памяти перегенерированы (`npm run golden:cost`) — два новых счётчика PERF-3
- [x] 7.3 `openspec validate --specs --strict`, `npm run spec-graph -- check`, roadmap этап 64, архивация change'а
