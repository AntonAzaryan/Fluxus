## 0. Порядок в очереди

- [x] 0.1 Перед синхронизацией перечитать `openspec/changes/*/specs/{rendering,assets}/spec.md`: единственная известная вторая дельта на требование этого change'а — `MODIFIED REND-1` в `terminology-sweep` (`proposal.md`, «Порядок в очереди»)
- [x] 0.2 Если `terminology-sweep` синхронизирован раньше — перестроить `MODIFIED REND-1` этого change'а по слитому тексту: перенести правку первого абзаца (окно валидности view через `tick-loop` OBS-3) и сценарий «Рендер удержал TickResult до кадра», и только поверх них класть пункт про decoration-инстансы и фразу про величины парного документа
  - Условие не сработало: `terminology-sweep` на архивации по-прежнему в очереди (1/17), его правка REND-1 в главную спеку не ушла. Дельта этого change'а построена на действующем тексте — первый абзац с `dispatch()` совпадает с главной спекой дословно, — и перестраивать её не потребовалось. Сработала зеркальная задача 0.3.
- [ ] 0.3 Если раньше синхронизирован этот change — перестроить `MODIFIED REND-1` в `terminology-sweep` по слитому тексту тем же приёмом, каким туда уже перенесены REND-14 и карта кривизны из памяти (`docs/editor-implementation-plan.md`)
- [ ] 0.4 После слияния проверить итоговый `REND-1` глазами: он несёт обе правки сразу — окно валидности через OBS-3 и decoration-инстансы в закрытом списке несимуляционных входов
- [x] 0.5 Проверить, что `REND-18` и `ASSET-9` остались следующими свободными номерами своих префиксов, а `PRES-` не занят

## 1. Манифест визуалов: раздел decoration-видов (ASSET-9)

- [x] 1.1 `engine/assets-ts/src/manifest.ts`: раздел decoration-видов рядом с `entities` — запись того же состава (`EntityVisual`), собственный ключ
- [x] 1.2 Там же: валидация единого пространства ключей — имя, занятое в обоих разделах, отвергается с этим именем в причине; `checkUnknownKeys` пополняется новым разделом
- [x] 1.3 Разрешение визуального ключа в запись — одно место на оба раздела, чтобы потребители не выбирали раздел сами
- [x] 1.4 Тесты `engine/assets-ts/test/manifest.test.ts`: пересечение имён отвергается; вид без prefab'а валиден; запись decoration-вида с неприменимыми частями (таблицы клипов, кости, дуга прыжка) валидна и смысла не получает

## 2. Парный документ: формат и загрузка (PRES-1, PRES-2, PRES-3)

- [x] 2.1 `engine/assets-ts/src/loaders/`: загрузчик парного presentation-документа, регистрация в реестре (ASSET-3), типизированный handle (ASSET-2); отсутствие файла — не ошибка загрузчика, а отсутствие слоя у потребителя
- [x] 2.2 Валидатор документа: закрытый состав документа и записи, обязательные `visual`/`x`/`y`, необязательные `yaw`/`scale`/`skin`, конечность чисел, `scale > 0`; сообщение адресует запись индексом, а не документ целиком
- [x] 2.3 Правило имени пары (базовое имя сцены + `.presentation.json`) — одна функция на все пакеты; путь считается от корня дерева контента (ASSET-2, CONT-2)
- [x] 2.4 Тесты: разбор валидного документа; каждая форма нарушения — своё сообщение и свой адрес; отсутствующий и пустой `decorations` неразличимы

## 3. Рендер: набор decoration-инстансов (REND-18, REND-1)

- [x] 3.1 `engine/render-ts/src/`: приём декларативного набора decoration-инстансов — сведение по ключам (создать/обновить/убрать), собственная нумерация, набор живёт рядом с `PresentationStage`, а не внутри продюсера
- [x] 3.2 `subsystems/models.ts`: построение и обновление decoration-инстансов тем же путём, что документные, — запись манифеста, скин (REND-6), перёд (REND-13), наклон по нормали (REND-10), посадка на визуальную поверхность (REND-9)
- [x] 3.3 Явные отсутствия: интерполяции нет (snap), событийных клипов и доворота костей нет, вертикального смещения (REND-12) нет, фильтра видимости нет; клип — состояние покоя записи
- [x] 3.4 Смена продюсера (`publish`/`detach`, REND-11) набор decoration MUST NOT гасить — проверить на входе в превью и выходе из него; шапка модуля документного источника перестаёт называть парный presentation-документ своим входом (правка REND-11 в дельте)
- [x] 3.5 `picking.ts`: decoration-инстансы участвуют в разрешении курсора наравне с инстансами presentation-состояния; при совпадающей дистанции — после них
- [x] 3.6 `subsystems/overlays.ts`: подсветка выделения работает по decoration-инстансу тем же наложением, что по сим-объекту
- [x] 3.7 Тесты `engine/render-ts/test/`: сведение набора по ключам без пересоздания при правке полей; сосуществование с потоком тиков; попадание picking'а в декорацию на кривизне; пустой набор даёт прежний кадр

## 4. Редактор: документ, операции, валидация

- [x] 4.1 `editor/core-ts/src/project/pairing.ts`: снять раздел «Шов третьего документа» — формат теперь есть; `presentation` перестаёт быть документом без правил
- [x] 4.2 Открытие проекта (`sceneProject.ts`, `sceneDiscovery.ts`): поиск парного документа по правилу имени, отслеживаемый список `decorations` (дескрипторы ED-29), отсутствие файла — сцена без декораций
- [x] 4.3 Операции авторинга (`editor/ui-ts/src/areas/scenePlacement.ts` либо соседний вклад): поставить, подвинуть, повернуть, отмасштабировать, удалить decoration; дописывание в конец, перестановки записей нет; квантование по PRES-3 в самой операции
- [x] 4.4 Удаление выделенного одной операцией по мультивыделению из сим-объекта и decoration (сценарий ED-16)
- [x] 4.5 `sceneDocuments.ts`: набор decoration-инстансов как производная документа — рядом с `placementsOf`, тем же способом (ключи из дескрипторов сессии)
- [x] 4.6 `sceneInteraction.ts`: выделение и picking по decoration наравне с сим-объектами (ED-17); снять из шапки раздел «Чего здесь нет: decorations»
- [x] 4.7 `editor/core-ts/src/validation/crossDocument.ts`: правило «`visual` не разрешается в запись манифеста» — междокументное, раскладку получает параметром (`DocumentSite`), молчит при незагруженном манифесте
- [x] 4.8 Операция перевода decoration ↔ prop (PRES-5): перенос между документами с приведением позиции и курса; вид не переписывается. Тест на асимметрию погрешности — `decoration → prop` объект не двигает вовсе (значение уже на шаге PRES-3), `prop → decoration` смещает не больше чем на половину шага PRES-3; кругового побайтового возврата `prop → decoration → prop` тест не требует и требовать не должен
- [x] 4.9 Сохранение: парный документ — член группы записи (уже так), пустой слой файла не создаёт; проверить round-trip ED-21 на документе, написанном руками

## 5. Тесты инварианта и контента

- [x] 5.1 `engine/integration-ts`: прогон сцены с парным документом и той же сцены без него дают побитово совпадающие эталоны (PRES-4, CLI-6, CLI-8)
- [x] 5.2 Там же: два клиента с разными парными документами входят в один матч — хеши входа совпадают (NTR-5), рассинхронизации нет
- [x] 5.3 `contentBoundary.test.ts`: новый вид контентного документа учтён в данных проверки границы (CONT-1)
- [x] 5.4 `content/scenes/duel.presentation.json`: демонстрационный слой для сцены дуэли — контент, а не фикстура; golden-эталоны движка при этом не меняются (CONT-4)
- [x] 5.5 `editor/ui-ts` тесты: постановка, перемещение, удаление и undo/redo декорации; единообразие выделения; квантование в операции

## 6. Гейт

- [x] 6.1 `npm run check` из корня (typecheck + lint + test) — зелёный, проверено ревью
- [x] 6.2 `openspec validate presentation-scene-layer --type change --strict` и `openspec validate --specs --strict` — проверено ревью
- [x] 6.3 Схемы `engine/schemas/` не перегенерируются: парный документ и манифест — не документы ядра (SER-5 их не порождает); убедиться, что `npm run schemas` даёт пустой дифф — прогнан ревью, дифф пуст, `engine/core-ts` и `engine/tests/golden/` побайтово нетронуты
