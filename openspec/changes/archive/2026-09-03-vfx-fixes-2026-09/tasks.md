# Tasks: vfx-fixes-2026-09

## 1. Общая опора оболочек (V-9, V-10, D4)

- [x] 1.1 `ShellSet<R, I, S>` в `shellSupport.ts`: карта оболочек, ключ, переиспользуемый набор живых ключей, `ensure`/`sweep`, колбэки `create`/`acquire`/`release`/`pose`; проверка — юнит-тесты набора через обе подсистемы
- [x] 1.2 Обход источников (`syncShellSources`) и кэш имён таблицы состояний с коротким замыканием на пустом `stateComponents` — ОДИН на обе подсистемы; проверка — тест «пустой список состояний: таблица byState пропускается молча» в эффектах и в частицах
- [x] 1.3 `createStateReader` помнит незеркалированные имена: колбэк зовётся один раз на имя, а не на каждый промах; проверка — тест числа вызовов колбэка на N доставках
- [x] 1.4 `eventPointOf` перебирает `entity, other, source, target` (`core-ts/src/sim/filter.ts`) и говорит один раз, когда точка не разрешилась; проверка — тесты события только с `target` и события без единого поля
- [x] 1.5 Перевод `effects.ts` и `particles.ts`/`particleShell.ts` на общий набор; эффекты применяют `EntityView.scale`; проверка — существующие тесты обеих подсистем зелёные плюс тест масштаба размещения у оболочки эффекта

## 2. Возраст события при спавне (V-1, D1)

- [x] 2.1 Опция `tickSeconds` обеих подсистем с документированным умолчанием; вспышка стартует с возрастом `(view.tick − event.tick) × tickSeconds`, отжившая не спавнится; проверка — тест доставки с событиями трёх тиков
- [x] 2.2 Выстрел частиц догоняет возраст шагом экземпляра порциями ≤ 0.1 с с потолком числа порций; проверка — тест «выстрел старого тика уже отыграл часть своей длительности»

## 3. Оболочка частиц доживает (V-2, D2)

- [x] 3.1 Список догорающих: `looping = false; endEmit()` на сведении вместо возврата в пул, шаг на общих часах, возврат в пул по концу эмиссии и нулю частиц; проверка — тест «частицы переживают уход сущности»
- [x] 3.2 `snapAll` гасит догорающих мгновенно (REND-2); снос подсистемы и переподача манифеста их не теряют; проверка — тесты перемотки и сноса

## 4. Ресурсы и оборот библиотеки (V-3, V-5, V-8, V-11)

- [x] 4.1 Материалы батча (`batch.material`, `batch.settings.material`) — через `own('material', 'particles.effects', …)` в точке появления батча и в снос пула; проверка — расширение `lifetime.test.ts`
- [x] 4.2 `acquire()` пула сбрасывает кватернион и масштаб; сброс из `spawnShot` убирается; проверка — тест «экземпляр после сокетной оболочки достаётся декорации без поворота кости»
- [x] 4.3 Снятие систем экземпляра с регистрации в батче на `release` и регистрация на `acquire`; остаточная работа библиотеки помечена `ponytail`; расхождение клампов 0.25 с / 0.1 с документировано в точке шага; проверка — тест «в батче зарегистрированы только живые экземпляры»
- [x] 4.4 Шаг пропускается при `parent === null`, `init` требует `ctx.scene.isScene`; проверка — тесты обоих стражей

## 5. Плотность частиц (V-4, D3)

- [x] 5.1 Минимум ручки `particles.density`; пресеты уже в диапазоне (performance 0.5, прочие 1) — правки не потребовалось; проверка — тест «byKind-эмиттер эмитит на минимуме ручки» и поимённый тест реестра ручек

## 6. Прогрев (V-6)

- [x] 6.1 `EffectsSubsystem.prewarm()` отдаёт пуловый тёплый корень; `game/demo-ts/app/prewarm.ts` компилирует его вместе с прочими; проверка — тест «после прогрева пул эффектов не пуст, а сцена кадра не изменилась»
- [x] 6.2 `expand()` частиц передаёт `onLoad` загрузчику и отдаёт шаблонные текстуры наружу; демо заливает их `renderer.initTexture`; проверка — тест перечисления текстур образца

## 7. Превью каста (V-7, D6)

- [x] 7.1 `PreviewShape` читает `CompiledStep.range`: радиус круга — `min(range, shapeA)`, конус без радиуса — `range`; проверка — тесты `range < radius` и конуса без радиуса
- [x] 7.2 Открытый вопрос AABB шага-вектора зафиксирован в отчёте и в design (ядро его не читает; правило — валидация редактора)

## 8. Шар заряда записью манифеста (V-10, D5, REND-23)

- [x] 8.1 Поля записи эффекта: `radiusFromStat`, `offset`, `colorAt`, `blink` — типы и валидация закрытого списка (`engine/assets-ts`); проверка — тесты валидации каждого поля и неизвестного ключа
- [x] 8.2 Подсистема эффектов ведёт оболочку статом: радиус, цвет порога, мигание, вынос по курсу прицела; проверка — тесты окна стата, порога и отсутствия стата
- [x] 8.3 Запись `effects.byState.Charging` в `content/visuals/manifest.json`; `chargeBalls.ts` удалён, `main.ts` его не собирает; проверка — тесты демо зелёные, ручной прогон `npm run demo`

## 9. Гейт

- [x] 9.1 `npm run check:impact` (правка `content/visuals/manifest.json` уводит выборку в полный гейт), затем полный `npm run check`
- [x] 9.2 Эталоны памяти перезаписаны `npm run golden:cost`: `particles.effects.material` 1 → 3 — два материала батча библиотеки, которых учёт раньше не видел (V-3). Эталоны стоимости не изменились ни байтом
- [x] 9.3 `openspec validate --specs --strict`, `npm run spec-graph -- check`, архивация change'а
