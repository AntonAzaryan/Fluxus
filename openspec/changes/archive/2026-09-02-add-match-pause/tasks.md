# tasks: add-match-pause

## 1. Протокол и сервер (engine/net-ts)

- [x] 1.1 Типы `PauseRequest` и `Pause` в `messages.ts` + кодеки обеих форм (SER-3); тест: раунд-трип сериализации, неизвестный тип по-прежнему рвёт соединение (NTR-4)
- [x] 1.2 Состояние паузы хоста в `MatchServer`: причина заморозки (`match-pause` vs перемотка), `pause()`/`resume()` ядра, отсчёт возобновления по wall-clock хоста (D1, D2); тесты: заморозка не исполняет тиков, возобновление продолжает с того же тика
- [x] 1.3 Политика из документа матча: секция `pause` в `matchConfigOf`, учёт бюджетов, право противника на снятие, максимум длительности паузы (D3); тесты: запрос в бюджете проходит, сверх бюджета — адресный отказ без разрыва (D4), просроченная пауза возобновляется сама
- [x] 1.4 Рассылка `Pause` всем соединениям (игроки, заместители, наблюдатель) на каждую смену состояния; тест: наблюдатель получает те же сообщения
- [x] 1.5 Ввод в заморозке: `Input` отбрасывается без эффекта и без искажения счётчиков NTR-11, порог молчания NTR-6 в заморозке не тикает (D5, риск зависшего матча); тесты на оба пункта
- [x] 1.6 Перекрёсток с перемоткой: `PauseRequest`/API-пауза в `Rewinding` — именованный отказ; запрос перемотки в заморозке невозможен по построению (нет тиков) — зафиксировать тестом (NTR-20)
- [x] 1.7 Реконнект в паузу: после `Start` — замороженный снапшот, затем `Pause` с текущим состоянием, порядок фиксирован (D8); тест: вернувшийся владелец и севший в паузу заместитель получают состояние паузы
- [x] 1.8 Серверное API паузы для обвязки (стенд/агент) — тот же путь, что у запроса игрока, минус политика бюджета игрока; тест: админ-пауза работает при исчерпанных бюджетах игроков

## 2. Клиент и канал оболочки

- [x] 2.1 `matchClient`: приём `Pause`, состояние паузы в плоской форме канала оболочки (D7); тест: состояние доезжает до потребителя канала
- [x] 2.2 Отправка `PauseRequest` обратным каналом оболочки из HUD-действия (HUD-2); тест: действие уходит и отказ возвращается адресно

## 3. HUD (engine/hud-ts)

- [x] 3.1 Виджет оверлея паузы: состояние, инициатор, визуальный отсчёт от доставленной длительности, схождение к обновлениям (HUD-9); тесты: молчание канала без доставленной паузы оверлея не рисует, отказ политики показывается причиной

## 4. Демо и боты

- [x] 4.1 Политика паузы в `content/matches/duel.match.json` (бюджет, задержка снятия, отсчёт, максимум — ориентировочные числа D3) и кнопки паузы в HUD демо; проверка: два игрока на стенде ставят и снимают паузу
- [x] 4.2 Политика разрыва `--on-disconnect pause` в стенде: отвязка владельца → API-пауза; комбинация с заместителем — по полю политики (D6); тест стенда: разрыв замораживает матч, возврат владельца возобновляет
- [x] 4.3 Боты на паузе: мозг не зацикливается, очередь не копится, `Pause` разобран и проигнорирован (D9); тест в `engine/bot-ts`

## 5. Интеграция и гейт

- [x] 5.1 Парность записи: лупбэк-матч с паузами и возобновлениями реплеится побитово, в логе нет следов паузы (NTR-8, сценарий NTR-20); тест в `engine/integration-ts`
- [x] 5.2 `npm run check` зелёный; golden-эталоны не тронуты (пауза не меняет ни одного тика)
