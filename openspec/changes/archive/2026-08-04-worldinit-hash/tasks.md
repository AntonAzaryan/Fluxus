## 1. Спеки

- [x] 1.1 `specs/determinism-core/spec.md` — MODIFIED DET-1: состав `worldInit` без состояния RNG (вынос ко второму члену тройки со ссылкой на RNG-7)
- [x] 1.2 `specs/determinism-core/spec.md` — MODIFIED DET-1: состав канонического представления (террейн, центр арены, плоская форма мира, режим), исключения (радиус, обрывы, RNG, шина, тик, конфиг сцены)
- [x] 1.3 `specs/determinism-core/spec.md` — MODIFIED DET-1: байтовый поток (компактный JSON, ключи по SER-6, UTF-8 без BOM), функция FNV-1a 32 (RNG-3), форма результата (8 строчных hex-цифр)
- [x] 1.4 `specs/determinism-core/spec.md` — MODIFIED DET-1: сценарии «Тот же мир при другом seed» и «Разный порядок правил при тех же данных»
- [x] 1.5 `specs/cli-testing/spec.md` — MODIFIED CLI-3: поле `worldInitHash` между `seed` и `ticks` плюс сценарий «Разошлись начальные данные, а не ход симуляции»
- [x] 1.6 `openspec validate worldinit-hash --strict` проходит

## 2. Реализация

- [x] 2.1 Новый `src/sim/worldInit.ts`: сборка канонического представления и `worldInitHash()` на существующем `fnv1a32` (`src/math/rng.ts`) и `utf8Bytes`
- [x] 2.2 Канонический JSON: рекурсивная сортировка ключей, компактная запись — сортировка строится на месте, а не полагается на порядок вставки (SER-6)
- [x] 2.3 `src/sim/scene.ts`: проверить, достаточно ли того, что загрузчик уже отдаёт, для сборки представления — правка не потребовалась: `Scene.terrain.grid` даёт распакованную сетку, `Scene.arena.center` — центр, оба публичны с волны 2
- [x] 2.4 `src/sim/scenario.ts`: расчёт хеша после начальной расстановки и до первого `tick()`, поле `worldInitHash` в `RunOutput` между `seed` и `ticks`
- [x] 2.5 `src/index.ts`: экспорт `worldInitHash`

## 3. Тесты

- [x] 3.1 `test/worldInit.test.ts` — стабильность: переформатированный ассет террейна (другие отступы/порядок ключей в исходном JSON, те же карты) даёт тот же хеш
- [x] 3.2 `test/worldInit.test.ts` — различие: другая карта уровней даёт другой хеш
- [x] 3.3 `test/worldInit.test.ts` — различие: другой центр арены даёт другой хеш (проверяет, что центр не потерялся — в мир он не попадает)
- [x] 3.4 `test/worldInit.test.ts` — различие: другая стартовая расстановка даёт другой хеш
- [x] 3.5 `test/worldInit.test.ts` — независимость от seed: тот же мир при другом `seed` даёт тот же хеш
- [x] 3.6 `test/worldInit.test.ts` — форма результата: ровно 8 символов `[0-9a-f]`, включая случай со старшим нулевым полубайтом
- [x] 3.7 `test/worldInit.test.ts` — сцена без террейна и без арены хеш всё равно даёт (ключи просто отсутствуют)

## 4. Эталоны

- [x] 4.1 `npm run golden` — перегенерировать все 10 эталонов
- [x] 4.2 Проверить дифф каждого из 10 файлов: **ровно одна** добавленная строка `worldInitHash`, других изменений нет
- [x] 4.3 Повторный `npm test` — эталоны стабильны (хеш не плавает между прогонами)

## 5. Документация

- [x] 5.1 `docs/architecture.md` §2: диапазоны ID приведены к фактическим — `fixed-point-math` FP-1..6, `rng` RNG-1..8, `ecs-foundation` CMD-1..7, `physics` PHYS-1..10
- [x] 5.2 `docs/architecture.md` §6 п.12 переформулировать: FOW-5 приведён к реальности волной 1, открытым остаётся направленный обзор с высоты (живёт в Open Questions спеки `fog-of-war`)
- [x] 5.3 `docs/architecture.md` §6 п.13 снять — хеш `worldInit` реализован
- [x] 5.4 `docs/architecture.md` §6: новый пункт о механизме сверки версии игры (правило зафиксировано в NET-16, механизм апдейт-гейта на клиенте — вне ядра)

## 6. Проверка

- [x] 6.1 `npx vitest run` — все тесты зелёные
- [x] 6.2 `npx tsc --noEmit` — чисто
- [x] 6.3 `openspec validate --specs --strict` — 16/16 после sync
- [x] 6.4 `git status` — изменены только файлы из списка Impact
