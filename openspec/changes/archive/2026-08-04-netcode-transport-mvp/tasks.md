## 1. Спеки

- [x] 1.1 Новая capability `netcode-transport` — NTR-1..13 в `specs/netcode-transport/spec.md` change'а
- [x] 1.2 `specs/tick-loop/spec.md` — MODIFIED TICK-2: таймаут как дедлайн тика по расписанию, запрет досрочного тика при полном комплекте фреймов
- [x] 1.3 `openspec validate netcode-transport-mvp --strict` проходит
- [x] 1.4 Sync: `openspec/specs/netcode-transport/spec.md` получает `## Purpose` — «транспортный слой поверх модели `netcode`: чем является сервер матча, каким набором сообщений стороны разговаривают, в каком темпе идёт матч и как это проверяется локально; модель — что передаём, что фильтруем, какая версия — остаётся в `netcode` и здесь не дублируется»
- [x] 1.5 Sync: `openspec/specs/netcode/spec.md` `## Purpose` — абзац «Прототип отзывчивости» переписать под принятое решение (стартовый режим — server-auth без предсказания, не lockstep-relay); требования `netcode` не трогать
- [x] 1.6 Sync: `openspec/specs/netcode/spec.md` `## Open Questions` — «Модель отзывчивости» закрыть решением со ссылкой на NTR-10; «Кто получает `viewpoint = ALL`» сузить до продакшн-авторизации со ссылкой на NTR-9 (для локального прогона решено)
- [x] 1.7 `openspec validate --specs --strict` — 17/17 после sync

## 2. Модуль

- [x] 2.1 `engine/net-ts/package.json`: `@game-mvp/net`, ESM, зависимость `@game-mvp/core` через `file:../core-ts`, dev-зависимости vitest/typescript/@types/node тех же версий, что в `core-ts`
- [x] 2.2 `engine/net-ts/tsconfig.json` по образцу `core-ts`; `npm run typecheck` и `npm test` — те же имена скриптов
- [x] 2.3 `@msgpack/msgpack` в зависимости; lock-файл сгенерировать против публичного реестра
- [x] 2.4 Проверить обратную границу (NTR-1): `engine/core-ts/package.json` не получает ни одной новой зависимости, `src/index.ts` не получает ни одного нового экспорта. Если понадобилось — остановиться и завести change в ядре

## 3. Протокол

- [x] 3.1 `src/protocol/messages.ts`: закрытый набор из NTR-4, типы обоих направлений, исходы `Reject` и `End` как закрытые перечисления
- [x] 3.2 `src/protocol/codec.ts`: кодирование через `Serializer` ядра, MessagePack по умолчанию, JSON — дебаг-режим (NTR-13); плоская форма строится с порядком ключей по SER-6
- [x] 3.3 Разбор входящего: неизвестный тип, чужое направление и недопустимое для состояния соединения сообщение дают названный исход разрыва, а не игнор (NTR-4)
- [x] 3.4 Санитизация `InputFrame` на границе: слот отправителя, `buttons` в `0..65535`, тик в допустимом окне (NTR-7)

## 4. Транспорт

- [x] 4.1 `src/transport/transport.ts`: интерфейс — отправка, приём, закрытие, уведомление о закрытии (NTR-2)
- [x] 4.2 `src/transport/loopback.ts`: внутрипроцессная пара, доставка через микротаск (синхронная доставка запрещена, NTR-2), опциональная искусственная задержка
- [x] 4.3 `src/transport/webSocketServer.ts` и `webSocketClient.ts`: стороны разведены по файлам — клиентская стоит на глобальном `WebSocket` и переезжает в браузер без правок, серверная тянет Node-only `ws`. Границы сообщений даёт сам WebSocket
- [x] 4.4 Проверить, что ни сервер, ни клиент не импортируют конкретную реализацию транспорта

## 5. Сервер матча

- [x] 5.1 `MatchConfig` в `src/server/matchServer.ts` (отдельный файл не понадобился): слоты (упорядоченный список игроков), `seed`, описание матча (идентификатор сцены, стартовая расстановка), `tickRate`/`snapshotRate`/`inputDelay`, порог молчания слота, предикат видимости событий, разрешение наблюдателя
- [x] 5.2 `src/server/matchServer.ts`: чистый цикл без сокетов, таймеров и часов (NTR-3); вход — сообщение от соединения и продвижение тика, выход — адресованные сообщения
- [x] 5.3 Хендшейк: сверка пары версии через `contentPackHash` до выдачи чего-либо о матче, `Reject` называет разошедшуюся половину, `Welcome` несёт слот, описание матча и `worldInitHash` (NTR-5)
- [x] 5.4 Ожидание полного состава, `Start`, запрет изменения состава после старта, `Reject` на повторный вход (NTR-6)
- [x] 5.5 Расписание тиков и буфер задержки ввода: predicted-фрейм при отсутствии, отброс опоздавшего, оба со счётчиками (NTR-7)
- [x] 5.6 Персональные снапшоты вызовом `filterSnapshot` ядра с `viewpoint` команды; `VIEWPOINT_ALL` только соединению-наблюдателю (NTR-9)
- [x] 5.7 Канонический `inputs[]` и выгрузка матча в форме `ScenarioDef` (NTR-8)
- [x] 5.8 Разрыв соединения: predicted-фреймы, порог молчания, `End` с названным исходом (NTR-6)
- [x] 5.9 `src/server/host.ts` (и зеркальный `src/client/host.ts`): планировщик тиков и связка с транспортом — единственное место с сокетами, таймерами и часами

## 6. Клиент

- [x] 6.1 `src/client/matchClient.ts`: чистый цикл без I/O — приём сообщений, отправка ввода, применение снапшотов; `tick()` не зовётся (NTR-10)
- [x] 6.2 Хендшейк на стороне клиента: подъём `worldInit` по своему контент-паку, сверка хеша, разрыв с исходом «разошлись данные матча», отличным от «версия устарела» (NTR-5)
- [x] 6.3 `src/client/inputRing.ts`: кольцо своих `InputFrame` глубиной ~120 тиков (NET-9, NTR-10)
- [x] 6.4 Применение снапшотов: отброс снапшота с тиком не больше применённого (NTR-10)
- [x] 6.5 `src/client/interpolation.ts`: буфер интерполяции, отставание стартом ~100 мс (NET-3); сущность, пропавшая из снапшота, гасится, а не телепортируется (NET-3, NET-14)
- [x] 6.6 `src/metrics.ts`: раздельные счётчики задержки «нажал → увидел», применённых/предсказанных/опоздавших/вышедших за окно кадров, размера снапшота, отставания буфера (NTR-11). Мера отклика заменена с RTT по ходу реализации — см. развилку 7 в design.md

## 7. Запуск

- [x] 7.1 `bin/serve.mjs`: поднять сервер матча из конфига-файла, флаг разрешения наблюдателя, флаг JSON-режима сериализатора
- [x] 7.2 `bin/play.mjs`: headless-клиент, ввод из записанного сценария ввода либо с клавиатуры, вывод — принятые снапшоты и счётчики (NTR-12)
- [x] 7.3 `README` модуля: как поднять матч на два терминала, какие числа смотреть при замере отзывчивости

## 8. Тесты

- [x] 8.1 `test/protocol.test.ts` — круг кодирования обоими сериализаторами; неизвестный тип, чужое направление, сообщение вне состояния дают разрыв с названным исходом
- [x] 8.2 `test/handshake.test.ts` — расхождение идентификатора сборки; расхождение хеша контент-пака; отказ называет половину; о составе матча отклонённый не узнаёт
- [x] 8.3 `test/handshake.test.ts` — версии совпали, ассет арены различается: расхождение `worldInitHash`, исход «разошлись данные матча», ни одного исполненного тика
- [x] 8.4 `test/handshake.test.ts` — неизвестный клиенту идентификатор сцены даёт тот же исход, и сцена ему не досылается
- [x] 8.5 `test/pacing.test.ts` — фрейм не пришёл: predicted-фрейм в каноническом логе, счётчик вырос
- [x] 8.6 `test/pacing.test.ts` — фрейм опоздал: отброшен, мир не переигран, счётчик вырос
- [x] 8.7 `test/pacing.test.ts` — комплект фреймов раньше срока: тик не исполняется досрочно (TICK-2)
- [x] 8.8 `test/pacing.test.ts` — битая маска `buttons` отвергается на границе, матч продолжается
- [x] 8.9 `test/filtering.test.ts` — два игрока разных команд: тик один, снапшота два, невидимого противника в снапшоте нет; игровому слоту `VIEWPOINT_ALL` не выдаётся ни при каких аргументах
- [x] 8.10 `test/match.test.ts` — матч двух игроков на loopback: ввод игрока 1 доезжает до состояния, которое видит игрок 2
- [x] 8.11 `test/match.test.ts` — **парность (NTR-8)**: записанный матч прогоняется через `runScenario` ядра и даёт побитово те же состояния и тот же `worldInitHash`
- [x] 8.12 `test/match.test.ts` — снапшот с устаревшим тиком отбрасывается клиентом
- [x] 8.13 `test/match.test.ts` — разрыв соединения: матч продолжается на predicted-фреймах, по порогу молчания приходит `End`; повторный вход получает `Reject`
- [x] 8.14 `test/boundary.test.ts` — `engine/core-ts` не зависит от `net-ts` и не получил новых экспортов (NTR-1)

## 9. Документация

- [x] 9.1 `docs/architecture.md` §1: слой netcode в схеме — сервер матча и транспорт вне ядра
- [x] 9.2 `docs/architecture.md` §2: строка `netcode-transport` в карте спецификаций, диапазон NTR-1..13
- [x] 9.3 `docs/architecture.md` §5: этап 17 закрывается, добавляется этап «Netcode transport MVP: сервер матча, транспорт, два headless-клиента»
- [x] 9.4 `docs/architecture.md` §6 п.13 снять — механизм сверки версии реализован хендшейком (NTR-5)
- [x] 9.5 `docs/architecture.md` §6 п.2 переформулировать: модель отзывчивости выбрана (server-auth без предсказания), открытым остаётся замер и решение о включении предсказания
- [x] 9.6 `docs/architecture.md` §6 п.7 переформулировать: для локального прогона решено (NTR-9), открытой остаётся продакшн-авторизация полного потока
- [x] 9.7 `docs/architecture.md` §3: строки «механизм против политики» из design.md change'а
- [x] 9.8 `README.md` корня: раздел про `engine/net-ts` и команды локального матча

## 10. Проверка

- [x] 10.1 `cd engine/net-ts && npx vitest run` — зелено
- [x] 10.2 `cd engine/net-ts && npx tsc --noEmit` — чисто
- [x] 10.3 `cd engine/core-ts && npx vitest run` — зелено, эталоны не тронуты
- [x] 10.4 `git status` — `engine/tests/golden/` без изменений, `engine/core-ts/src/` без изменений
- [x] 10.5 Ручной прогон: три процесса, два игрока, матч идёт; счётчики RTT и предсказанных фреймов осмысленны

## 11. Найдено реализацией

- [x] 11.1 `NTR-11` переписан: мера отклика — задержка «нажал → увидел», а не RTT; чистый круг в этом протоколе неоткуда взять без лишнего сообщения и он отвечает не на тот вопрос (design.md, развилка 7)
- [x] 11.2 `NTR-11` дополнен нормой «каждый `seq` учитывается однократно»: predicted-кадр повторяет `seq`, и наивная метрика уезжала в живом прогоне до 4.8 с при исправном канале. Регрессия — `test/match.test.ts`
- [x] 11.3 Оценка серверного тика ограничена сверху: односторонний `max` копил расхождение таймеров без предела, пока кадры не выпали бы из окна приёма. Регрессия — `test/match.test.ts`
- [x] 11.4 `MatchConfig.teams` валидируется на диапазон `[0, 31]`: `VIEWPOINT_ALL` — это `-1`, и без проверки он въезжал бы игроку опечаткой в конфиге (NTR-9). Регрессия — `test/filtering.test.ts`
- [x] 11.5 **Дефект ядра, не этого change'а:** `core-ts/bin/sim.mjs` не запускался на Node 22.22 — `visibility.ts` и `time.ts` использовали parameter property, а strip-only режим их не поддерживает (`ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`). Ломало CLI-1. Починено вынесением полей в явные объявления, а не флагом `--experimental-transform-types`: флаг печатает ExperimentalWarning, а stdout CLI сверяется побайтно (CLI-4). Регрессия — `core-ts/test/cli.test.ts` (подпроцесс с очищенным `NODE_OPTIONS`). Тем же снят флаг с bin-скриптов `net-ts`
