## 1. Спека

- [x] 1.1 Синхронизировать дельты в главные спеки (`physics` PHYS-13; `fog-of-war` FOW-5, FOW-9) и снять Open Question «Направленный обзор с высоты»; `openspec validate --specs --strict` и `spec-graph check` зелёные

## 2. Ядро: направленное перекрытие луча (PHYS-13, FOW-5)

- [x] 2.1 В переборе статики `raycast` (`createPhysicsApi`): скип пересечения, когда луч входит в ребро с верхней стороны (сторона входа — по знаку компоненты направления на оси нормали ребра; равные уровни — блок); движение (`nearestBlocker`/гейт PHYS-11) не трогать
- [x] 2.2 Тесты ядра: сверху вниз через ребро — видно (бит взводится), снизу вверх — нет, спуск + дальний подъём — перекрыт вторым ребром, равные уровни — блок; существующие тесты visibility, чьи ожидания перевернулись, обновить осознанно со ссылкой на FOW-5/PHYS-13

## 3. Голдены

- [x] 3.1 Прогнать golden-тесты; при диффе убедиться, что меняются только биты `Visibility` пар «через ребро сверху вниз», затем `npm run golden` и дифф баз в коммит (процедура golden-update)

## 4. Маска рендера (FOW-9)

- [x] 4.1 `FogSegment` несёт `levelNeg`/`levelPos` (перенос из `CliffEdge` в `fogSegmentsOf`), `FogObserver` — уровень; тень отбрасывают только рёбра, у которых наблюдатель на нижней стороне; наблюдатель на линии ребра — нижняя сторона (в сторону тумана)
- [x] 4.2 Полярный depth-буфер на наблюдателя (design D3): переиспользуемый буфер, минимум по бину, консервативный min соседних бинов, полоса полутона ~1 тексель; 2×2 субсэмплы удалить; тесты: направленность (сверху низ открыт, снизу верх в тени), прежние тесты кромки/тени/градиента зелёные
- [x] 4.3 Перестройка маски только при изменении сигнатуры входов (design D4); тесты: неизменные входы — маска и текстура не перестраиваются, смена конфига/уровня/позиции — перестраиваются
- [x] 4.4 Подсистема fog: уровень наблюдателя из `EntityView.currLevel` в `FogObserver`

## 5. Демо и гейт

- [x] 5.1 `npm run check` зелёный; прогон демо: с плато виден нижний уровень (и в симуляции — врага внизу доставляет, проверить в сетевом режиме), снизу плато в тумане; fps на плато не проседает
- [x] 5.2 Скриншот-проверка (headless-Chromium, CDP-скрипт из scratchpad): наблюдатель на плато — низ открыт, тени соседних обрывов направленные
