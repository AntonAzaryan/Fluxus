# Tasks: sec01-rng-bound-range

Требование — `specs/rng/spec.md` (**RNG-8**). Гейт один и он локальный: `npm run check` из корня репозитория (CI нет).

design.md у изменения нет намеренно: технического решения здесь не принимается — граница, которую формула требования уже подразумевает, записывается словами и проверяется assert'ом (proposal.md, «Why»).

## 1. Дельта требования

- [x] 1.1 `specs/rng/spec.md`: MODIFIED RNG-8 — `bound` целое в `1…2^32−1`, верхняя граница выведена из формулы, обе проверяются жёстко до продвижения генератора. Блок MODIFIED несёт требование целиком вместе с действующими сценариями.
- [x] 1.2 Там же: новый сценарий «bound не помещается в u32».

## 2. Код и тесты

- [x] 2.1 `engine/core-ts/src/math/rng.ts`: assert `nextBelow` проверяет `bound <= 2^32−1`; сообщение называет диапазон.
- [x] 2.2 `engine/core-ts/test/rng.test.ts`: `nextBelow(2^32)` бросает, не продвинув генератор; `nextBelow(2^32−1)` законен и даёт значение в диапазоне.

## 3. Гейт

- [x] 3.1 `npm run check` из корня; golden-эталоны не двигаются.
