# Delta: cli-testing — ужесточение тестового харнесса

## ADDED Requirements

### Requirement: CLI-8 Механические guard-проверки инвариантов

Запреты, которые спека налагает на код симуляции, SHALL проверяться автоматической проверкой исходного кода, входящей в обычный прогон тестов: обращения к недетерминированным и запрещённым API (wall-clock и таймеры — DET-5, источники случайности вне ГПСЧ ядра — DET-4, трансцендентные функции платформенной математики и float-литералы в геймплейной математике — DET-2), а не только договорённостью в документации. Проверка SHALL покрывать код ядра и чистые циклы сетевого слоя, которые NTR-3 и NTR-10 объявляют свободными от часов и ввода-вывода; хосты, транспорт и рендер под неё не подпадают — они легально живут во времени и float.

Проверка MUST NOT запрещать точные на целых числах операции платформенной математики (минимум, максимум, модуль, округления вниз/вверх, целочисленное умножение): они детерминированы на всех платформах, и запрет целиком дал бы поток ложных срабатываний, после которого проверку отключили бы. Списки запрещённого и разрешённого — часть проверки; их изменение SHALL быть явной правкой, попадающей в дифф на ревью.

Границы ядра SHALL проверяться так же механически: код ядра MUST NOT импортировать ничего за пределами собственного модуля, а манифест ядра MUST NOT объявлять runtime-зависимостей (CORE-1). Проверка границ существует потому, что общее хранилище зависимостей делает чужие пакеты физически досягаемыми для импорта независимо от манифеста.

Публичная поверхность ядра SHALL фиксироваться эталоном: перечень экспортируемых имён сверяется с эталонным списком, и его обновление выполняется отдельной явной командой по модели CLI-5. Случайный экспорт мутатора мира — это открытие side-channel мимо Command Buffer (DET-7), и он обязан краснеть до ревью, а не обнаруживаться на нём.

#### Scenario: Побег из детерминизма ловится строкой, а не расследованием

- **WHEN** в систему ядра добавляется вызов wall-clock или трансцендентной функции платформенной математики
- **THEN** guard-проверка краснеет на обычном прогоне тестов, указывая файл и строку, — до того, как расхождение пришлось бы искать сверкой эталонов

#### Scenario: Точная целочисленная операция не даёт ложного срабатывания

- **WHEN** система ядра использует минимум, модуль или округление вниз на целых значениях
- **THEN** guard-проверка остаётся зелёной

#### Scenario: Импорт чужого пакета из ядра

- **WHEN** в код ядра добавляется импорт модуля другого пакета, досягаемого через общее хранилище зависимостей
- **THEN** проверка границ краснеет, хотя манифест ядра не менялся

#### Scenario: Случайный экспорт мутатора

- **WHEN** правка добавляет в публичную поверхность ядра имя, которого нет в эталоне
- **THEN** сверка поверхности краснеет, и расширение либо принимается осознанно явной командой обновления эталона, либо откатывается

### Requirement: CLI-9 Интеграционная проверка вертикали слоёв

SHALL существовать интеграционная суита, проверяющая цепочку целиком — авторитетный сервер, персональный снапшот, клиент, рендер-хост — на настоящих реализациях всех слоёв, без моков, в одном процессе без сокетов и таймеров (NTR-12). Попарные проверки швов остаются в пакетах-потребителях; вертикаль существует потому, что два зелёных попарных теста не доказывают живость цепочки: слой может потреблять то, что предыдущий уже не поставляет.

Суита SHALL обращаться к слоям только через их публичные поверхности — тем же контрактом, которым слои зовёт внешний потребитель. Проверка, залезающая во внутренности, фиксировала бы реализацию, а не поведение, и краснела бы на рефакторингах, не меняющих вертикаль.

Суита SHALL включать прогон со случайными потоками ввода от фиксированного seed: пространство сценариев не ограничивается придуманными автором, а оракулом служит реплей-парность NTR-8 — записанный фазз-матч обязан побитово воспроизводиться прогоном сценария.

#### Scenario: Ввод доезжает до меша чужого клиента

- **WHEN** в интеграционном матче один игрок двигается, а второй молчит
- **THEN** у молчащего клиента позиция чужого меша в рендер-хосте меняется в соответствии с интерполированным снапшотом, пришедшим по цепочке

#### Scenario: Рассинхрон шва net→render виден вертикали

- **WHEN** фильтрация снапшота перестаёт поставлять данные, которые потребляет рендер-хост, при зелёных попарных тестах обоих швов
- **THEN** интеграционная суита краснеет

#### Scenario: Фазз воспроизводим

- **WHEN** фазз-прогон от одного и того же seed запускается дважды
- **THEN** оба прогона дают побитово одинаковый канонический лог, и его реплей через прогон сценария совпадает с состоянием сервера

### Requirement: CLI-10 Записанные матчи в golden-наборе

Универсальный тест-сьют (CLI-4) SHALL включать записанные матчи: канонический лог матча (NTR-8) в форме сценария CLI-2 хранится вместе с остальными сценариями набора и сверяется с эталоном побитово по правилам CLI-5. Отдельный формат фикстуры MUST NOT вводиться — запись матча уже является сценарием, и реплей идёт тем же прогоном, что и остальные golden-тесты.

Требование существует потому, что парность NTR-8 как одноразовый ассерт проверяет только матчи, разыгранные в тестах текущей реализации; фикстура превращает её в регрессионную сетку: изменение подстановки кадров, пейсинга или порядка канонического лога краснит эталон, даже когда ядро не менялось, — и тот же набор матчей сверяет будущую вторую реализацию (CLI-6) без написания параллельного сьюта.

#### Scenario: Сетевой слой изменил канонический лог

- **WHEN** правка сетевого слоя меняет подстановку отсутствующих кадров ввода
- **THEN** golden записанного матча краснеет, хотя ядро и его собственные эталоны не менялись, и изменение либо принимается с обновлением эталона явной командой, либо откатывается

#### Scenario: Вторая реализация реплеит записанные матчи

- **WHEN** появляется вторая реализация ядра
- **THEN** записанные матчи прогоняются её адаптером как обычные сценарии набора, и вывод совпадает с эталонами побитово
