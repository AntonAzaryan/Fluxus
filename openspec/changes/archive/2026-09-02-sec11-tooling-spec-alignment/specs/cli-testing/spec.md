# cli-testing — дельта

## MODIFIED Requirements

### Requirement: CLI-2 Вход CLI

Входом CLI SHALL быть один JSON-документ: сценарий (сцена по SER-7) + начальная расстановка + seed + `inputs[]`.

Начальная расстановка сценария SHALL задаваться документом начальной расстановки в формате `serialization` SER-8 — том же, что у конфига сцены и конфига матча. Собственного формата расстановки у сценария MUST NOT быть, и порядок применения — сущности-носители порождённых компонентов, затем расстановка сцены, затем расстановка сценария — задаёт то же SER-8. Расстановкой, а не плоской формой мира, начальное состояние задаётся потому, что документ прогона пишется и читается человеком и попадает в дифф вместе с эталоном.

Инпуты SHALL раскладываться по тикам по собственному полю `tick` (TICK-2).

Документ, содержащий поле `inputs`, SHALL содержать упорядоченный список игроков — тот, что задаёт слоты (TICK-5). Требование определяется наличием самого поля `inputs`, а не непустотой списка: пустой `inputs: []` без описанных фреймов — уже документ со списком инпутов по контракту. Без списка игроков адресация фреймов к сущностям не определена, а прогон невоспроизводим; порядок игроков SHALL быть частью документа, а не аргументом командной строки, чтобы его изменение попадало в дифф вместе с эталоном.

Документ SHALL допускать необязательное поле включения физики (`physics` PHYS-4) с параметрами её системы. Оно живёт в сценарии, а не в конфиге сцены: физика — зависимость сборки (DI-3), и SER-7 прямо запрещает её присутствие среди данных сцены. Прогон без этого поля SHALL идти без физики — прежние сценарии и их эталоны от изменения не зависят.

Документ SHALL допускать необязательное поле включения нативного передвижения (`locomotion` LOC-1) с параметрами его системы, по тому же основанию, что и `physics`: какой механизм превращает ввод в скорость — зависимость сборки (DI-3), а не данные сцены, и имена компонентов и биты кнопок система берёт параметрами (LOC-1), которых в конфиге сцены нет. Прогон без этого поля SHALL идти без нативного передвижения — прежние сценарии и их эталоны от изменения не зависят.

Документ SHALL допускать необязательное поле включения пересчёта видимости (`fog-of-war` FOW-4) с параметрами её системы, по тому же основанию, что и `physics`: системе видимости нужен `raycast` (`physics` PHYS-6), то есть зависимость сборки (DI-3), а не данные сцены. Прогон без этого поля SHALL идти без пересчёта видимости.

Документ SHALL допускать необязательное поле включения поиска пути (`pathfinding` NAV-1) с параметрами его сборки, по тому же основанию, что и `physics`: навигационные данные производны от ассета террейна и печутся сборкой (NAV-3), а бюджет раскрытий (NAV-5) и предел радиуса агента (`terrain` TERR-7) — числа геймдизайнера, которых в конфиге сцены нет (SER-7). Поле включает не систему, а API навигации, которое спрашивает политика движения (NAV-4). Прогон без этого поля SHALL идти без поиска пути — движение NPC деградирует до прямого seek (`npc-behavior` NPC-6), а прежние сценарии и их эталоны от изменения не зависят. Именно в этой роли поле нормировано и для конфига матча (`netcode-transport` NTR-14).

#### Scenario: Воспроизведение матча по записи

- **WHEN** на вход подаются seed и записанные инпуты
- **THEN** CLI воспроизводит матч без дополнительных данных

#### Scenario: Тот же сценарий из теста и из командной строки

- **WHEN** сценарий прогоняется golden-тестом и командой CLI
- **THEN** обе стороны зовут один и тот же прогон и дают побитово одинаковый вывод

#### Scenario: Инпуты без списка игроков

- **WHEN** сценарий содержит поле `inputs` (пустое или нет), но не содержит списка игроков
- **THEN** прогон падает до первого тика: слоты не определены

#### Scenario: Расстановка сцены и расстановка сценария вместе

- **WHEN** сцена сценария сама содержит расстановку, и сценарий содержит свою
- **THEN** в мире оказываются обе, применённые в порядке SER-8: сначала сущности сцены, затем сущности сценария

#### Scenario: Сценарий с физикой

- **WHEN** документ содержит поле включения физики, а сцена — террейн и коллайдеры
- **THEN** статика обрывов строится при загрузке (PHYS-10), система разрешения движения регистрируется, и эталон фиксирует блокировки вместе с событиями столкновений

#### Scenario: Сценарий с нативным передвижением

- **WHEN** документ содержит поле включения передвижения, а сцена — компоненты ввода и конфигурации героя
- **THEN** система передвижения регистрируется сборкой прогона, и эталон фиксирует скорость и состояние героя по ходу тиков

#### Scenario: Сценарий с пересчётом видимости

- **WHEN** документ содержит поле включения видимости, а сцена задаёт наблюдателей и коллайдеры с тегом `blocksVision`
- **THEN** система видимости регистрируется, и эталон фиксирует `visibleTo` вместе с прочим состоянием мира

#### Scenario: Сценарий с поиском пути

- **WHEN** документ содержит поле включения поиска пути, а сцена — террейн и NPC с маршрутом
- **THEN** навигационные данные пекутся при сборке из ассета террейна, движение NPC идёт по найденным путям, и эталон фиксирует пройденный путь вместе с прочим состоянием мира

#### Scenario: Поиск пути без террейна

- **WHEN** документ содержит поле включения поиска пути, а террейна у сцены нет
- **THEN** сборка отказывает с названной причиной: навигационные карты производны от карты уровней, и строить их не из чего
