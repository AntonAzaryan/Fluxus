## 1. Дельты спек

- [x] 1.1 SRV-3: два названных пути предъявления кода пейринга (локальная команда и автопейринг локального агента контейнера) с названной ценой и границей — проверка: `openspec validate --changes sec09-host-apps-spec-alignment --strict` зелёный, текст дельты содержит полный блок требования со всеми сценариями
- [x] 1.2 SRV-4: величина названа серверной половиной отклика, названа и граница (полное «нажал → увидел» в админ-канал не едет, NTR-4) — проверка: та же валидация, сценарий «Игрок с плохой сетью» согласован с новым текстом
- [x] 1.3 SRV-6: одно условие постмортема (аварийный выход = выход, которого агент не просил), судьба несостоявшегося запуска, сценарий штатной остановки ненулевым кодом — проверка: та же валидация
- [x] 1.4 SRV-7: чем держится «версии совпадают по построению» (страница собирается той же командой из того же дерева, готовая со стороны не принимается) и прогон с пропущенной страницей — проверка: та же валидация
- [x] 1.5 MGR-3: серверная половина отклика и открытые детали упавшего сервера (код выхода, материалы разбора, хвост лога; счётчики названы отсутствующими, админ-операции недоступны) — проверка: та же валидация

## 2. Сверка с кодом (изменений кода дельты не требуют)

- [x] 2.1 Автопейринг: `bin/agent.mjs` пишет адрес и код в файл с правами `0o600` и ротирует код, `state/tokens.ts` выдаёт его отдельной операцией — проверка: `npx vitest run test/pinFile.test.ts test/security.test.ts -w @fluxus/server-agent` зелёные
- [x] 2.2 Отклик: `SlotView.serverResponseMs` и «отклик сервера» в представлении менеджера называют половину — проверка: тесты `@fluxus/server-manager` зелёные (`mg-slot__response`)
- [x] 2.3 Вердикт выхода: постмортем пишется по вердикту `crashed`, штатная остановка глухого к SIGTERM стенда — `stopped` — проверка: `test/supervision.test.ts` зелёный
- [x] 2.4 Сборка дистрибутива: `--client-dist` отвергается названным отказом, `--skip-client` называет отсутствие страницы — проверка: `test/bundle.test.ts` в `@fluxus/server-agent`
- [x] 2.5 Детали упавшего сервера открываются: отказ `not-running` на подписку не закрывает разбор — проверка: тест «детали упавшего сервера открываются (MGR-3, SRV-6)» в `@fluxus/server-manager`

## 3. Ворота

- [x] 3.1 `npm run check` из корня зелёный; `openspec validate --specs --strict` без замечаний
