# Tasks: terrain-texturing

## 1. Формат: карта раскраски и tileset (D1, D2, D5; ASSET-15, ASSET-6)

- [x] 1.1 Валидатор документа карты раскраски: закрытый состав, ряды по сетке, алфавит `0`–`9`, адресные находки; проверка — юнит-тесты на каждую находку с адресом клетки/ряда
- [x] 1.2 Загрузчик вида `terrain-paint` в реестре (ASSET-3): `JSON.parse` + валидация, причина в `failed`; проверка — тест загрузки и тест отказа
- [x] 1.3 Раздел `terrain` манифеста: `tileset` (слоты пола и запись стенок) и `paintMap`; валидация состава и ссылок (`derivatives`); проверка — тесты валидации манифеста и перечня ссылок

## 2. Геометрия: сварка подклеток (D7, T-6)

- [x] 2.1 Пол клетки с кривизной — индексированная решётка `(N+1)²` вершин; квадов столько же, нормали и позиции побитово прежние; проверка — тесты числа вершин/индексов и совпадения выборок на общих рёбрах подклеток
- [x] 2.2 Веса слотов атрибутом вершины (`aPaint`), счётчик `terrainPaintVertices` в стоке стоимости; проверка — тест весов в узле четырёх разных слотов и на однородной области

## 3. Материал текстурирования (D2, D3, D8; REND-39)

- [x] 3.1 `MeshStandardMaterial` + `onBeforeCompile`: смесь до четырёх слотов по мировой проекции с периодом слота, ключ кэша программы по числу слотов; проверка — тесты текста шейдера и униформ
- [x] 3.2 Стенки и юбка — запись `wall` tileset'а с проекцией вдоль стенки; проверка — тест раскладки UV стенки и отсутствия смешивания слотов пола
- [x] 3.3 Загрузка текстур слотов (ASSET-2, ASSET-4), владелец `terrain` в учёте памяти, освобождение REND-31; проверка — тесты подписки, предупреждения о недоступном ассете и освобождения
- [x] 3.4 Несовпадение сетки карты и слот за пределом tileset'а — предупреждение и рендер без текстурирования; проверка — тесты обеих находок

## 4. Ручка качества и снятие покрытий (D6, D9; QUAL-1, QUAL-3)

- [x] 4.1 Ручка `terrain.textureSlots` — потолок с семантикой `min(авторское, потолок)`, 1 выключает смешивание; проверка — тесты клампа и отсутствия мутации документов
- [x] 4.2 Снятие `floorCover`/`wallCover` и `terrainCover.ts`; демо переезжает на tileset манифеста; проверка — гейт зелёный, второго способа объявить покрытие в коде нет

## 5. Конвейер Blender (D4; BLND-14)

- [x] 5.1 Канал `_PAINT` в чтении клеточных данных, слот клетки из единогласия вершин, адресные отказы; проверка — тесты на фикстурах `.gltf` (без Blender, BLND-7)
- [x] 5.2 Карта раскраски в целевом слое и её запись операцией (документ адресует манифест); отказ при неадресованной карте; проверка — тесты слоя, записи и отказа
- [x] 5.3 Детерминизм и атомарность: повторный импорт даёт байт-в-байт тот же документ, отказ не оставляет половины; `--dry-run` называет карту; проверка — тесты повторного импорта и отчёта
- [x] 5.4 Аддон: кисть раскраски по клеткам и предпросмотр материалом, читающим тот же атрибут (Python, вне гейта)

## 6. Контент и демо

- [x] 6.1 Генератор `npm run textures:ground` дорастает до слотов арены дуэли (пол ×3 и порода стенок), `CREDITS.md` обновлён
- [x] 6.2 `content/visuals/manifest.json`: раздел `terrain` с tileset'ом и ссылкой на карту; `content/visuals/duel.paint.json` — раскраска арены
- [x] 6.3 Демо строит террейн без параметров покрытия; проверка — тест демо-сборки и глазами `npm run demo`

## 7. Гейт и документация

- [x] 7.1 `npm run golden:cost` — регенерация эталонов там, где счётчики и ресурсы сдвинулись, с причиной в отчёте (PERF-4)
- [x] 7.2 `CLAUDE.md` — строки о `textures:ground`; `docs/architecture.md` — строка roadmap
- [x] 7.3 `openspec validate --specs --strict`, `npm run spec-graph -- check`, `npm run check`
