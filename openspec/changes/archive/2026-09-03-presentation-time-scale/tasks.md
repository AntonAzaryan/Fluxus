# Tasks: presentation-time-scale

## 1. Мировой множитель часов презентации (D1–D3, REND-25, REND-38)

- [x] 1.1 `ViewBuffer`: опция `timeScale`, сеттер `setTimeScale(k)` с отказом на отрицательном и не-числе, множитель под клампом кадрового шага; проверка — тест «шаг подсистем множится, `realDt` и альфа не трогаются»
- [x] 1.2 Композиция умножением с темпом обратного хода (REND-25); проверка — тест «скраб вдвое быстрее под множителем 0.5 идёт как живой мир»
- [x] 1.3 `k = 0` — тождество заморозки; проверка — тест «ноль даёт ровно тот же шаг, что `Paused`»
- [x] 1.4 Композиция с персональной шкалой сущности (REND-38) — умножением, ниже по тракту; проверка — тест подсистемы моделей «мировой множитель × личная шкала»

## 2. Наблюдаемость и потребители (D4, D5, RDBG-7)

- [x] 2.1 `TickView.presentationTimeScale` — поле доставленного состояния, обновляемое и на доставке, и в кадре
- [x] 2.2 `RenderHost`: опция сборки и сеттер, проброшенные в буфер; `RemoteHost`: множитель запоминается до handshake и переносится в буфер при его создании
- [x] 2.3 Источник `net.delivery` печатает множитель в ЧАСОВОЙ секции; проверка — тест «величина в `clock`, тела секции не касается»
- [x] 2.4 Демо: `-`/`=`/`0` вне режима `Running`; проверка — тест «в `Running` клавиши не меняют ничего»

## 3. Память воркер-половины доставки (D6, PERF-8)

- [x] 3.1 `FootprintState.extractStateBytes` и проба в `Extractor.extract`; байты ёмкости плоской формы, буферов обхода и зеркала кадра
- [x] 3.2 `FrameMirror.byteLength`; проверка — тест «рост сцены двигает величину, стоящая сцена — нет»
- [x] 3.3 `npm run golden:cost` — новое поле в `*.footprint.json`, прежние величины не двигаются

## 4. Закрепление швов прошлого change'а

- [x] 4.1 Тест ИЛИ-накопления: `snapAll`/`branchChanged` тика, съеденного конфляцией, уезжают с ближайшим конвертом (SHELL-4)
- [x] 4.2 Тест `forgetDelivered()`: оболочка, переиспользующая экстрактор между подписчиками, отдаёт свежему полный кадр (SHELL-3)

## 5. Гейт

- [x] 5.1 Полный гейт `npm run check`
