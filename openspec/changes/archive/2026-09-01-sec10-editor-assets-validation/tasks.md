# Tasks: sec10-editor-assets-validation

## 1. Заявка вида — одно место на репозиторий (REND-37)

- [x] 1.1 Функция «кто рисует вид, у которого нет модельной записи» в `@fluxus/assets` рядом с резолверами манифеста; перечень источников — тот же, что у REND-37 (эмиттерный вид ASSET-14, `particles.byKind`, `effects.byKind`), порядок опроса сохранён. Проверка — юнит-тест модуля ассетов на каждый источник и на вид без заявки
- [x] 1.2 Подсистема моделей `render-ts` переходит на общую функцию вместо своей `claimOf`; проверка — существующие тесты REND-37 (`manifestResupply`, `particles`) зелёные без правок

## 2. Правило пары ED-19: исключение заявки и важность

- [x] 2.1 `editor.visualForPrefab` не сообщает находку о prefab'е, чей вид заявлен другим источником манифеста (ED-19, REND-37); заявка спрашивается у владельца формата, своего перечня секций правило не заводит. Проверка — тест на префаб с записью `effects.byKind`, с записью `particles.byKind`, с эмиттерной записью раздела decoration-видов и на префаб без всего этого
- [x] 2.2 Важность находки «prefab без записи манифеста» — предупреждение (ED-19); строка причины и описание правила в обеих локалях переписаны под новую формулировку, отпечатки перевода подтверждены. Проверка — тест важности плюс гейт бандлов
- [x] 2.3 Сохранение: находка-предупреждение записи не отвергает, находка «запись манифеста без prefab'а» отвергает (ED-21). Проверка — тесты `projectSave` на обе половины пары
- [x] 2.4 Реальная сцена репозитория проходит правила без находок этого правила (ED-19): тест-страж на `content/scenes/duel.scene.json` + `content/visuals/manifest.json`

## 3. Существование ассета в правилах редактора (ED-14, ASSET-14)

- [x] 3.1 Синхронный индекс дерева контента: множество путей файлов, наполняемое обходом (`walkContentTree`), с состоянием «не перечислено». Проверка — юнит-тесты наполнения, обновления и ответа «не перечислено»
- [x] 3.2 Правило-вклад «ссылка манифеста не разрешается в файл дерева»: модельные ссылки записи, текстуры скинов и эмиттерные ссылки обеих секций; важность — предупреждение (ED-14). Проверка — тест на каждый род ссылки, на разрешающуюся ссылку и на молчание при неперечисленном дереве
- [x] 3.3 Регистрация правила и наполнение индекса в сборке редактора: открытие проекта и канал изменений дерева (ED-12). Проверка — тест сборки: правка дерева извне обновляет находки
- [x] 3.4 Строки причины и описания правила в обеих локалях, отпечатки подтверждены; проверка — гейт бандлов и тест строк интерфейса

## 4. Текст требований

- [x] 4.1 ED-9: ссылка на `client-shell` — SHELL-1..8, с упоминанием режима оболочки превью (SHELL-8)
- [x] 4.2 ED-19: исключение REND-37, сим-префаб вне таксономии, разная важность половин пары
- [x] 4.3 ED-21: что именно отвергает сохранение
- [x] 4.4 ASSET-14: снятие нормативного описания пробела правил редактора
- [x] 4.5 `npm run check` из корня зелёный
