# Tasks: sec01-math-api-no-matrices

Требование — `specs/determinism-core/spec.md` (**DI-2**). Гейт один и он локальный: `npm run check` из корня репозитория (CI нет).

design.md у изменения нет намеренно: технического решения здесь не принимается — из требования убирается обещание, которого реализация никогда не давала (proposal.md, «Why»).

## 1. Дельта требования

- [x] 1.1 `specs/determinism-core/spec.md`: MODIFIED DI-2 — состав Math API назван прямо (скаляры FP-1 и двумерные векторы), матрицы объявлены отсутствующими и не задельными, основание — DET-8. Блок MODIFIED несёт требование целиком вместе с действующим сценарием.
- [x] 1.2 Там же: новый сценарий «Реализации понадобилась матрица».
- [x] 1.3 Перенести дельту в `openspec/specs/determinism-core/spec.md`. Изменение при этом НЕ архивируется: оно остаётся в очереди как запись о принятом решении. Проверка — `npx openspec validate --specs --strict` и `npm run spec-graph -- check`.

## 2. Гейт

- [x] 2.1 `npm run check` из корня. Кода правка не касается: проверяется, что спека и `MathApi` сошлись, а не что что-то поехало.
