## 1. Ассет и его валидация (TERR-2, TERR-3, TERR-7)

- [x] 1.1 `src/terrain.ts`: `TerrainDef` — размеры, `tileSize` в Q16.16, две текстовые карты
- [x] 1.2 Разбор карт: алфавит уровней `0-9A-F`, алфавит флагов `.^_`, размеры рядов
- [x] 1.3 Отказ, если арена не помещается в i32 (FP-1)
- [x] 1.4 Одиночная клетка-рампа отвергается (TERR-7)

## 2. Запросы (TERR-4)

- [x] 2.1 `levelAt(position)` в fixed-point; вне сетки — ближайшая клетка
- [x] 2.2 `levelOf(entity)`: override (ARENA-6) приоритетнее производного значения
- [x] 2.3 `hasFloorAt(position)` читает бит из компонента, а не из ассета
- [x] 2.4 `TerrainApi` в `SystemContext` и `Simulation` — опционально, как физика (DI-3)

## 3. Cliff-геометрия (TERR-5)

- [x] 3.1 Правило перехода: равные уровни либо перепад в единицу с рампой
- [x] 3.2 Отрезки по общей стороне клеток, построчный порядок обхода (DET-6)

## 4. Карта пола как компонент (TERR-6)

- [x] 4.1 Схема `TerrainFloor` порождается из размеров сетки, поля-слова по 32 клетки
- [x] 4.2 Prefab носителя с начальным состоянием пола из флагов ассета
- [x] 4.3 Singleton спавнится загрузчиком до начальной расстановки (ID-2, DET-6)

## 5. Формат и сцена (SER-5, SER-7)

- [x] 5.1 `SceneDef.terrain`; `loadScene` отдаёт запрос вместе с миром и реестром
- [x] 5.2 `runScenario` передаёт террейн в сборку
- [x] 5.3 `terrain.schema.json` и перегенерированные `scene`/`scenario` (`npm run schemas`)

## 6. Проверка

- [x] 6.1 `test/terrain.test.ts`: валидация, `levelAt`, override, обрывы, раскладка пола
- [x] 6.2 Golden-сценарий `terrain`: раскладка пола по битам и порядок ID
- [x] 6.3 `npm test` и `npm run typecheck` зелёные; эталоны прежних сценариев не изменились
- [x] 6.4 `openspec validate terrain-height-field --strict`
- [x] 6.5 `docs/architecture.md` §5 — этап 11 отмечен выполненным
