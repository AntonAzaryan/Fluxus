# Tasks: npc-grid-optimization

## 1. Документ поведения: интервал пересчёта расхождения (D1, NPC-2, NPC-6)

- [x] 1.1 `document.ts`/`model.ts`: необязательное поле `separationIntervalTicks` (целое ≥ 1, нормативное умолчание 3 — константа с цитатой NPC-2/NPC-6); ноль — находка валидации с путём поля. Проверка — юнит-тесты парсера: молчание → 3, явная единица принимается, ноль → находка
- [x] 1.2 Регенерация JSON-схем (`npm run schemas`); проверка — дифф `engine/schemas/` содержит только новое поле

## 2. Компонент агента: держимый вектор (D2, SNAP-1)

- [x] 2.1 `components.ts`: поля `sepX`/`sepY` (`fixed`, умолчание 0) в `NPC_AGENT_SCHEMA`; `handles.ts`: их handle'ы. Проверка — существующие тесты снапшота/перемотки зелёные (состав полей входит в снапшот даром)

## 3. Окно пересчёта в движении (D1, D3, NPC-6)

- [x] 3.1 `movement.ts`: в окне `(tick + slot) % K === 0` — пересчёт `separation()` по сетке, применение свежего вектора на ЭТОМ тике из полей системы и запись обоих полей через Command Buffer; вне окна — применение вектора из компонента (чтение handle'ами). `behavior.separation <= 0 || separationWeight <= 0` — как раньше: ноль без запроса и без записей. Проверка — юнит-тесты: вектор держится между окнами (сосед сдвинулся — вектор прежний), пересчитывается в окне, окна разных агентов размазаны slot'ами
- [x] 3.2 Ноль аллокаций на агента и ноль структур платформы вне мира; сетка движения по-прежнему наполняется каждый тик внутри одного прогона (NPC-6). Проверка — чтение диффа + существующий паттерн тестов аллокаций

## 4. Спека и цитаты

- [x] 4.1 Дельты NPC-2/NPC-6 применяются к основной спеке при архивации; цитаты требований в новом коде (NPC-2, NPC-6, SNAP-1, CMD-5, QUERY-2); проверка — `npm run spec-graph -- check`

## 5. Эталоны и приёмка (acceptance criteria design'а)

- [x] 5.1 Golden-тесты зелёные БЕЗ регенерации `*.golden.json` (сцены матчей NPC не содержат — их неизменность и есть проверка)
- [x] 5.2 `npm run golden:cost` — принять дифф `npc-stress.cost.json` (падение `npcNeighbors`, рост `commandsApplied`), дифф в коммит
- [x] 5.3 Два прогона нагрузки побитово равны (`npcStress.test.ts`), демо-тесты босса зелёные, полный `npm run check` зелёный
