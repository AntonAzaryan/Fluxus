# Tasks: shadow-static-share-scenario

## 1. Имя сценария REND-30

- [x] 1.1 Дельта `specs/rendering/spec.md`: MODIFIED REND-30 с полным телом требования, в котором сценарий назван «Поле staticShare прежнего формата отвергается»; тело WHEN/THEN и все прочие сценарии — байт в байт прежние; проверка — дифф дельты против главной спеки состоит из одной строки заголовка
- [x] 1.2 Проверка ссылок: имя сценария нигде не цитируется кодом и соседними спеками (`npm run spec-graph -- code REND-30`, поиск старого имени по репозиторию)

## 2. Гейт

- [x] 2.1 `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные до архивации
- [x] 2.2 Архивация чейнджа: главная спека несёт новое имя сценария, те же проверки зелёные после архивации
