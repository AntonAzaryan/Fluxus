## 1. Перечень-владелец

- [x] 1.1 `openspec/specs/assets/spec.md`: `ASSET-1` — в перечень добавить сетевой трафик (`netcode` NET-8, NET-12) и канонический контент-пак матча (`netcode` NET-17); добавить сценарий на вход в матч при разных presentation-слоях
- [x] 1.2 Там же: правило ссылки переформулировать по признаку «требование называет последствия presentation-влияния», а не «требование о виде presentation-данных»; перечисление адресатов оставить примером и назвать в нём `PRES-4`, `REND-16`, `ED-19`; добавить сценарий на требование из другой capability

## 2. Ссылающиеся требования

- [x] 2.1 Там же: `ASSET-9` — последний абзац заменить ссылкой на `ASSET-1`
- [x] 2.2 Там же: `ASSET-10` — последний абзац заменить ссылкой на `ASSET-1`; проверить, что ссылка на `ASSET-6` тоже появилась (сегодня секция не сослана ни на то, ни на другое)
- [x] 2.3 `openspec/specs/rendering/spec.md`: `REND-16` — перечень заменить ссылкой, сохранив запрет обратного канала в мир (`REND-1`) с его основанием
- [x] 2.4 `openspec/specs/presentation-scene/spec.md`: `PRES-4` — перечень заменить ссылкой, сохранив механизм изоляции (`PRES-1`), равенство хешей входа (`netcode-transport` NTR-5) и проверяемость эталонами (`cli-testing` CLI-3, CLI-5)
- [x] 2.5 `openspec/specs/editor/spec.md`: `ED-19` — перечень заменить ссылкой, сохранив «decoration MUST NOT попадать в конфиг сцены» как собственную норму и сославшись на `presentation-scene` PRES-1
- [x] 2.6 Проверить, что сценарии пяти требований сохранены дословно: THEN, называющий наблюдаемый исход («hash `worldInit` не меняется, снапшоты и golden-прогоны прежние»), дублем нормы не является и снятию не подлежит

## 3. Проверка

- [x] 3.1 `openspec validate dedup-presentation-consequences --strict` — зелено
- [x] 3.2 `openspec validate --specs --strict` после применения дельт — зелено
- [x] 3.3 `npm run spec-graph -- check` — зелено; убедиться, что все ID, на которые ссылаются новые формулировки (`NET-8`, `NET-12`, `NET-17`, `PRES-1`, `PRES-4`, `REND-1`, `REND-16`, `ED-19`, `NTR-5`, `CLI-3`, `CLI-5`), существуют
- [x] 3.4 Прогнать по главным спекам поиск перечня в перечислительном виде («`worldInit`, снапшоты, golden-файлы», «снапшоты, FoW») — вне `ASSET-1` вхождений остаться не должно
- [x] 3.5 Убедиться, что требования-владельцы не тронуты: `NET-8`, `NET-12`, `NET-17`, `DET-1`, `SER-1`, `SNAP-1`, `FOW-5`, `CLI-5`, `CLI-6`, `REND-1`, `PRES-1`, `NTR-5`, `CLI-3`
- [x] 3.6 Убедиться, что дифф содержит только `openspec/`: кода, эталонов и схем работа не касается
