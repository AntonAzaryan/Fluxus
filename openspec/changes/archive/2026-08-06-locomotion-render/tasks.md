## 1. Манифест визуалов (ASSET-6)

- [x] 1.1 Добавить в `engine/assets-ts/src/manifest.ts` тип секции записи `verticalOffset { jumpArc?, fallSpeed?, fallDepth? }` и включить ключ в список известных полей записи
- [x] 1.2 Валидация секции рядом с `surfaceAlign`: неизвестные ключи, тип числа, неотрицательность; сообщения — в стиле соседних
- [x] 1.3 Тест валидации манифеста: корректная секция, отрицательное значение, неизвестный ключ, отсутствие секции

## 2. Плоская форма и канал (REND-1, SHELL-2, SHELL-3)

- [x] 2.1 `extractor.ts`: опции `locomotion { stateComponent?, configComponent? }`, колонки `motion: Uint8Array` и `motionPhase: Float32Array` в `ExtractedTick`, перевыделение в `ensureCapacity`
- [x] 2.2 `extractor.ts`: чтение поля `state` и вычисление фазы `(total − ticksLeft) / total` по полю длительности текущего манёвра; вне манёвра — `LOCOMOTION_NORMAL` и `NaN`; `total <= 0` даёт `NaN`
- [x] 2.3 `client-ts/codec.ts`: две колонки в раскладку (`motionPhase` — к секции f32, `motion` — к секции u8), `CODEC_VERSION` → 2, обновить комментарий раскладки
- [x] 2.4 Тесты: extractor заполняет состояние и фазу по ходу манёвра (уклон → окно → перекат, прыжок от старта до приземления), roundtrip кодека переносит обе колонки

## 3. Presentation-состояние (REND-2, REND-4)

- [x] 3.1 `types.ts`: `EntityView.motion` (состояние манёвра последнего тика) и `prevMotionPhase`/`currMotionPhase`; описать `NaN` как «манёвра нет»
- [x] 3.2 `viewBuffer.ts`: скольжение фазы вместе с позицией — на замороженном тике не двигать, при snap схлопывать prev = curr
- [x] 3.3 Тесты `viewBuffer`: фаза скользит по тикам, snap схлопывает, conflation не ломает

## 4. Вертикальное смещение (REND-12)

- [x] 4.1 Новый модуль `render-ts/src/model/verticalOffset.ts`: `jumpArc(phase, height)` (парабола `4p(1−p)h`, `NaN` → 0) и `advanceFall(offset, speed, depth, dt)`
- [x] 4.2 Юнит-тесты модуля: нули на концах и максимум в середине, отсутствие высоты, снижение упирается в глубину, `dt = 0`
- [x] 4.3 `subsystems/models.ts`: читать `verticalOffset` записи манифеста при создании инстанса, складывать вклад дуги (интерполяция альфой по `arc(prev)`/`arc(curr)`) и вклад снижения с базовой высотой
- [x] 4.4 `subsystems/models.ts`: опция `fallEvent` (дефолт `FellThroughFloor`), включение снижения по событию, сброс снижения по `snap`

## 5. Состояния анимации (REND-4)

- [x] 5.1 `subsystems/models.ts`: таблица «состояние локомоушена → имя состояния анимации», `fall` во время снижения, иначе `move`/`idle` по скорости; применять и при отложенном `attachModel`
- [x] 5.2 Тесты подсистемы: перекат играет клип `roll`, прыжок — `jump`, окно даблтапа — `move`/`idle`, отсутствие записи состояния в манифесте оставляет текущий клип, провал играет `fall`
- [x] 5.3 Тест дуги и снижения на уровне подсистемы: высота инстанса по ходу прыжка, снижение после события, сброс на телепорте

## 6. Контент и проверка

- [x] 6.1 `content/visuals/manifest.json`: секция `verticalOffset` у записи `Hero`. Состояния `dodge`/`roll`/`jump`/`fall` не заведены намеренно: у `SkeletonBarbarian.mdx` таких секвенций нет (Stand/Walk/Attack/Death/Spell), а придумывать им клипы — не задача рендера; без записи состояние оставляет текущий клип (REND-4), и модель с нужными секвенциями подхватится правкой одного манифеста
- [x] 6.2 Прогнать `npm run check` из корня; убедиться, что golden-эталоны не тронуты (симуляция не менялась)
- [x] 6.3 Обновить `docs/architecture.md`, если описание presentation-слоя расходится с новым поведением
