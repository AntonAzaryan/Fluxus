# Tasks: sculpt-paint-channel

## 1. Сэмплер скалпта несёт канал (D1, D2; BLND-13, BLND-14)

- [x] 1.1 Треугольники объединения читаются вместе с каналом `_PAINT`, и объект без объявленного атрибута отмечается как «канала не несёт»; проверка — тест, что источник с каналом и без него различимы по состоянию сэмплера, а не по значениям
- [x] 1.2 `topAt(x, y)` отдаёт высоту и состояние канала, `heightAt` становится его обёрткой (второй реализации верхнего пересечения нет); проверка — существующие тесты BLND-13 (уровни, рампы, кривизна) остаются зелёными без правки чисел
- [x] 1.3 Полный порядок кандидатов `(высота, ранг канала)`: без канала `−1`, слот — его значение, расходящаяся грань старше слотов; проверка — тест наложенных на одной высоте пластин с разными слотами и тест перестановки объектов местами

## 2. Слой раскраски из скалпта (D3, D4; BLND-14)

- [x] 2.1 Волна «от клеток с полом, в кольце максимум» выносится в общую функцию и переиспользуется уровнем дыры; проверка — тесты уровня дыры остаются зелёными и волна вызывается из обоих мест
- [x] 2.2 Слоты клеток из выборок центра: единогласие грани, целый индекс, алфавит карты — отказы с адресом клетки и именем объекта; проверка — по тесту на каждую находку
- [x] 2.3 Слот клетки без пола — волной (при недостижимости пола `0`); проверка — тест дыры в раскрашенном плато
- [x] 2.4 Наличие раскраски — «канал хотя бы у одного объекта»: нет ни у кого — слота в слое нет; есть — клетка на объекте без канала отказывает; проверка — два теста на обе ветки
- [x] 2.5 Манифест без адреса карты раскраски отказывает и на скалпт-пути; проверка — тест находки с ключом `terrain.paintMap`

## 3. Импорт целиком (D5; BLND-2, BLND-4, BLND-6)

- [x] 3.1 Документ карты открывается под слой только когда источник его даёт — по объявлению атрибута у sculpt-мешей; проверка — тест «скалпт без канала не создаёт и не пишет документ карты»
- [x] 3.2 Запись карты той же операцией и тем же сохранением, повторный импорт без диффа, отказ не оставляет половины; проверка — тесты записи, повторного импорта и атомарности
- [x] 3.3 Режим проверки называет карту раскраски скалпт-источника, не записывая ничего; проверка — тест отчёта `--dry-run`

## 4. Фикстуры и документация (BLND-7)

- [x] 4.1 Общий сборщик sculpt-источников переезжает в `test/support.ts` и умеет канал раскраски (единый слот на объект и по-вершинные значения грани); проверка — тесты BLND-13 идут от него без изменения ожиданий
- [x] 4.2 Закоммиченные фикстуры экспорта: раскрашенная арена с дырой, расхождение на грани пересечения, скалпт без канала — в перечне фикстур с причиной каждой; проверка — `fixtures.test.ts` сверяет дерево с перечнем, тесты читают слои из закоммиченных байтов
- [x] 4.3 `CONVENTIONS.md`: раздел sculpt-объектов описывает канал раскраски, модель «объект — слот» и адресность отказов; проверка — текст расходится со спекой нигде (чтение делом ревью)

## 5. Аддон (D6; BLND-8, вне гейта)

- [x] 5.1 Кисть раскраски работает на sculpt-объекте заливкой и пипеткой, оставаясь клеточной на grid-объекте; проверка — `python3 -m py_compile` по всем модулям аддона
- [x] 5.2 Материал предпросмотра раскраски назначается и sculpt-объектам; проверка — та же компиляция плюс упоминание в панели/README аддона

## 6. Гейт

- [x] 6.1 `npm test -w @fluxus/blender-ts` зелёный
- [x] 6.2 `npm run check:impact` зелёный на выборке пакета (план выборки — ПОЛНЫЙ гейт: правки аддона (`tools/blender-addon/**`) правилом карты тянут `full`)
- [x] 6.3 Полный `npm run check` из корня: `typecheck`, `lint`, `lint:dead`, `lint:dup`, `lint:arch`, `spec-graph check` зелёные; в `test` — 6525/6532, семь падений вида «Test timed out in 5000ms» в `@fluxus/integration` (cost/footprint/memory) и `@fluxus/demo` (demoBoss, demoDebugRun). Ни одного в `tools/blender-ts`, ни одного проваленного утверждения: машина держала до семи параллельных прогонов гейта (load average 20–32), и каждый из этих файлов зелен при прогоне без соседей
