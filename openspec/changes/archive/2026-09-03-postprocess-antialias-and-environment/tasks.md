# Tasks: postprocess-antialias-and-environment

## 1. Спека (REND-34, REND-29, QUAL-5)

- [x] 1.1 Дельта `rendering`: MODIFIED REND-34 — ручка сэмплов цели сцены, экранное сглаживание при нуле, экспозиция без оператора, проходы списком; сценарии «Кадр с цепочкой сглажен так же, как без неё», «Устройство без мультисэмплированной цели», «Экспозиция без оператора»
- [x] 1.2 Дельта `rendering`: MODIFIED REND-29 — подсекция `environment` (фон плоский либо градиент, интенсивность окружения, карта из тонов секции, фон принадлежит подсистеме, фазе цикла подсекция не принадлежит, REND-31); пять сценариев
- [x] 1.3 Дельта `render-quality`: ADDED QUAL-5 — ручки, объявляемые хостом; `frame.renderScale` потолком над плотностью пикселей
- [x] 1.4 `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные

## 2. Проходы списком и экранное сглаживание (L-12, §4.1.4)

- [x] 2.1 `postprocess/passes.ts`: программа FXAA (яркость приближением к воспринимаемой), материал прохода
- [x] 2.2 `postprocess/chain.ts`: `PostPass` (имя, «двигает ли кадр», `render(renderer, input, output)`), список, пересобираемый событием; кадр идёт по списку, промежуточная цель заводится только когда двигающих кадр проходов больше одного; освобождение по REND-31
- [x] 2.3 Проверка — тесты «проходы кадра — список, и его порядок читается по именам», «ноль сэмплов ставит в конец списка экранное сглаживание», «маска тумана остаётся финальным проходом кадра», «неактивная цепочка экранного прохода не добавляет», «промежуточная цель заводится только под экранный проход»

## 3. Ручка хоста (QUAL-5)

- [x] 3.1 `quality.ts`: `declareHost(declaration, apply)` тем же путём, что декларация подсистемы; `FRAME_RENDER_SCALE`, `frameScaleKnob()`, `resolveRenderScale(values, dpr, cap)`
- [x] 3.2 Проверка — тесты «ручка хоста в реестре и в отчёте», «имя занято подсистемой», «негодное значение отвергается адресно», «смена пресета доходит до хоста»
- [x] 3.3 Демо: объявление хоста в `quality.ts`, применение к рендереру в `main.ts`, `frame.renderScale` в `presets/performance.json` вместе с `postprocess.antialias: 0`
- [x] 3.4 Вьюпорт редактора: тот же путь объявления (умолчание ручки — «потолка нет», кадр не меняется)

## 4. Окружение как данные (§4.1.8)

- [x] 4.1 `assets-ts`: подсекция `environment` секции `lighting` — состав, взаимоисключение «плоский тон против градиента», отказ подсекции в фазе цикла
- [x] 4.2 `lighting/config.ts`: разбор подсекции в действующую конфигурацию
- [x] 4.3 `lighting/environment.ts`: фон и карта окружения из тонов (плоский тон — цветом, градиент — равнопромежуточной текстурой), поворот под мир Z-вверх, освобождение (REND-31)
- [x] 4.4 Подсистема: применение через `applyResolved`; шов цикла вынесен из подсистемы, чтобы модуль остался в границах размера
- [x] 4.5 Демо теряет строку фона, сцена дуэли получает подсекцию с тем же тоном; вьюпорт редактора получает фон из той же секции
- [x] 4.6 Проверка — тесты «плоский тон — цвет сцены, без единой текстуры», «градиент — равнопромежуточная текстура с поворотом мира Z-вверх», «окружение из тонов подсветки, когда фона нет», «интенсивность без тонов не действует», «снос отдаёт текстуру и снимает фон», «фаза цикла окружение не несёт»

## 5. Вьюпорт редактора и эталоны

- [x] 5.1 Вьюпорт редактора передаёт подсистеме освещения порт рендерера: `hybrid` там идёт сведением ярусов, а не как `full`
- [x] 5.2 `npm run golden:cost`: секция пресета «производительность» — на проход и на промежуточную цель больше (экранное сглаживание вместо мультисэмплинга); секция «ультра» не двигается
- [x] 5.3 `npm run check`
