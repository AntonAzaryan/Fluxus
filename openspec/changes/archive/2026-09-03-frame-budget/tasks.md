# Tasks: frame-budget

## 1. Сцена и контракт подсистемы (D1, D2, D5; REND-44)

- [x] 1.1 `FrameBudget` в нейтральном модуле контрактов: «есть ли время», «не ограничен», отметка отложения; переиспользуемый объект сцены без аллокаций на кадр (REND-26)
- [x] 1.2 `PresentationStage`: бюджет и часы параметрами сборки, бюджетная фаза перед `updateFrame`, сдвиг старта обхода; проверка — тесты порядка, сдвига и отсутствия фазы у сцены без вписавшихся
- [x] 1.3 Синхронные точки: `snapAll` доставки и снос сцены доделывают отложенное; проверка — тесты обеих
- [x] 1.4 Бюджет кадра продюсера четвёртым аргументом `frame`; документный источник — неограниченный; проверка — тест кадра документного источника

## 2. Счётчик отложений (D7; PERF-3)

- [x] 2.1 `frameBudgetDeferrals` в стоке стоимости на стадии `frame`, отметка — через объект бюджета; проверка — тест нуля без бюджета и роста под бюджетом

## 3. Террейн (D3, D4, D8)

- [x] 3.1 `flushDirty` под бюджетом: пересборка ближайших к камере чанков, остальные остаются помеченными; камера — необязательная опция сборки; проверка — тесты нарезки, порядка близости и сохранности пометок
- [x] 3.2 Первая сборка и смена арены синхронны; прямой драйв подсистемы (без сцены) работает как прежде; проверка — тесты обоих путей

## 4. Вода (D9)

- [x] 4.1 Перезаполнение глубинной текстуры полосами клеток под бюджетом, остаток — прямоугольник; проверка — тесты нарезки и равенства суммы текселей

## 5. Хосты и приложения (D6)

- [x] 5.1 `RenderHostConfig.frameBudgetMs` и `RemoteHostConfig.frameBudgetMs`; константа рекомендованного значения; проверка — тесты проброса
- [x] 5.2 Демо называет бюджет сборкой клиента; редактор (документный источник и вьюпорт) остаётся неограниченным; проверка — тест демо-сборки

## 6. Гейт и документация

- [x] 6.1 `docs/architecture.md` — строка roadmap
- [x] 6.2 `openspec validate --specs --strict`, `npm run spec-graph -- check`, `npm run check`
