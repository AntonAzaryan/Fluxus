# Tasks: water-floor-depth

## 1. Спека (REND-35)

- [x] 1.1 Дельта `rendering`: MODIFIED REND-35 — клетка тела без пола (TERR-6) имеет неположительную глубину; мутация пола входит в перечень изменений, видимых не позже следующего кадра; сценарий «Выбитая клетка под телом воды»
- [x] 1.2 `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные

## 2. Чтение карты пола подсистемой воды

- [x] 2.1 Своя копия карты пола: заводится из сетки при сборке и при смене арены; `syncTick` применяет дельту доставки (`floorBits` + `floorChangedCells`) и метит клетки тел; проверка — тест «доставка выбивает пол под телом: тексели клетки уходят в ноль, соседняя клетка воды не теряет»
- [x] 2.2 `applyGrid` сверяет пол сетки документа с собственной копией и метит разошедшиеся клетки (ED-9); проверка — тест «сетка документа возвращает пол превью»

## 3. Заливка глубины

- [x] 3.1 `fillWaterDepth` пишет ноль для текселей клеток ТЕЛА без пола; клетки за телом правилом пола не затронуты; проверка — тесты п. 2 плюс сохранение берега внутри тела
- [x] 3.2 Цитаты требований в коде (REND-35, TERR-6)

## 4. Гейт

- [x] 4.1 `npm run golden:cost` — дифф эталонов стоимости в коммите (PERF-4)
- [x] 4.2 `npm run check`: `lint:all` и `spec-graph check` зелёные, тесты — 5971 из 5994 (23 падения — таймаут 5 с у длинных тестов на общей с пятью агентами машине, ни одного расхождения проверок; каждый из них зелёный в отдельном прогоне). Golden симуляции не менялись (PRES-4) — правка чисто презентационная
