# Tasks: terrain-cliff-skirt

## 1. Генератор юбки (D1, D2, D3, REND-7, REND-9)

- [x] 1.1 `buildSkirtGeometry` в `engine/render-ts/src/subsystems/terrainSkirt.ts` (свой модуль рядом с `terrainGeometry.ts` — лимит длины файла): рёбра клеток с полом, за которыми пола нет (сосед без пола либо край сетки), дают полосу вниз на глубину; верх кромки — `min` высот двух клеток теми же `cornerHeight`/`terrainFormHeightInCell`, что у стенок; разбиение — по кривизне любой из клеток ребра; глубина ≤ 0 — пустая геометрия; счётчик `terrainSkirtQuads`; проверка — юнит-тесты: остров, край сетки, глубина 0
- [x] 1.2 Шов со стенкой cliff-отрезка: на ребре с перепадом уровней юбка начинается от нижней высоты пары — встык со стенкой, без перекрытия; проверка — тест верхней кромки юбки на cliff-ребре без пола за ним
- [x] 1.3 Владение ребром — клетка с полом; `bounds` фильтрует по ней, объединение чанков — разбиение; проверка — тест «ребро достаётся ровно одному чанку»

## 2. Подсистема (D3, D4, REND-7, REND-31)

- [x] 2.1 `TerrainSubsystem`: опции `skirtDepth` (умолчание 8, 0 — выкл) и `skirtColor`; третий меш чанка `terrain:skirt:x,y` со своим материалом в `rebuildChunk`; юбка не регистрируется теневым кастером; снос в `dispose`/`clearMeshes`; `skirtVertexCount`; проверка — тесты подсистемы: init строит юбку, dispose отдаёт материал
- [x] 2.2 Инвалидация пола радиусом 1 (юбка соседей): доставка `syncTick` и `applyGrid` метят чанки окрестности клетки; проверка — тест «выбитый пол у границы чанка пересобирает соседний чанк и его юбку не позже следующего кадра»

## 3. Стоимость (D5, PERF-3, QUAL-3, QUAL-4)

- [x] 3.1 Счётчик `terrainSkirtQuads` в `cost.ts`: поле, стадия `frame`, нулевая инициализация; проверка — тест стока стоимости
- [x] 3.2 Регенерация эталонов `engine/tests/golden/*.cost.json` через `npm run golden:cost`, дифф в коммите

## 4. Спека и ворота

- [x] 4.1 Дельта REND-7 (этот change), `openspec validate --strict`
- [x] 4.2 `npm run check` зелёный
