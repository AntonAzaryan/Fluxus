# Tasks: minimap-layers-and-spectator-camera

## 1. Слои подложки миникарты (HUD-6)

- [x] 1.1 `MinimapTerrainGrid.levels` и `MinimapTerrainSource.layers` (вода как клеточная маска, следы декораций как мировые круги); палитра уровней и цвета слоёв — параметры записи композиции.
- [x] 1.2 Отрисовка: уровень и вода — в `paintCell` (точечная перерисовка по дельтам сохраняется), следы декораций — проходом после правки подложки.
- [x] 1.3 Тесты `hud-ts`: палитра по уровням, прижатие к последнему цвету, вода, декорации, отсутствие слоёв = прежняя картинка, дельта перерисовывает клетку со всеми её слоями.

## 2. Наблюдение за сущностью (CAM-10)

- [x] 2.1 `camera/spectate.ts`: источник субъекта — кандидаты по объявленному стату, детерминированный порядок, цикличный перебор, устойчивость субъекта к смене состава и к `snapAll`.
- [x] 2.2 Экспорты; тесты — порядок, цикл, исчезнувший субъект, пустой список, перемотка.

## 3. Кинематографический путь (CAM-10, ASSET-17)

- [x] 3.1 `camera/pathTypes.ts`: машинное описание ключа и сглаживаний (CAM-9-образец).
- [x] 3.2 `camera/path.ts`: сборка пути из записи манифеста и чистая оценка по времени (сплайн точки наблюдения, кратчайшая дуга курса, сглаживание на параметре отрезка).
- [x] 3.3 `camera/rig.ts`: режим `path` — `playPath`/`stopPath`, выход по концу и по вводу панорамирования.
- [x] 3.4 `assets-ts`: секция `cameraPaths` манифеста и её валидация против описания.
- [x] 3.5 Тесты: оценка (чистота, границы, кратчайшая дуга, наследование каналов), переходы режима, валидация секции.

## 4. Демо

- [x] 4.1 Перебор субъекта наблюдения клавишами; маленькая подпись наблюдаемого.
- [x] 4.2 Слои миникарты: вода из секции документа, следы декораций из инстансов рендера, палитра уровней в записи композиции.
- [x] 4.3 Фото-режим: снятие HUD, заморозка эффектов, кадр и PNG; описание в `game/demo-ts/README.md`.
- [x] 4.4 Тесты демо: композиция резолвится, слои считаются, перебор субъекта.

## 5. Хвосты фазы 1

- [x] 5.1 `editor/ui-ts/src/areas/sceneCamera.ts` — своя запись осей edge-pan (REND-26).
- [x] 5.2 `DebugPainter.circle`/`disc` — на визуальную поверхность общим правилом дробления (`surfaceCells.ts`).

## 6. Ворота

- [x] 6.1 `npm run check:impact` по группам.
- [x] 6.2 `openspec validate --specs --strict`, `npm run spec-graph -- check`.
- [x] 6.3 Полный `npm run check`; строка roadmap 61 в `docs/architecture.md`.
