## 1. Установка камеры: поза, follow, зум

- [ ] 1.1 Создать `engine/render-ts/src/camera/` с типами `CameraPose`, `CameraConfig` (дефолты всех настроек — Decision 2) и `CameraRig.update(sample, dt, view) → CameraPose`; экспорт из `index.ts`
- [ ] 1.2 Перенести follow-логику из демо в rig: сглаживание к интерполированной позиции цели по x/y, высота — уровень клифа под точкой наблюдения из `TickView` со сглаживанием, не z сущности (CAM-2); снап при разрыве больше порога конфига (CAM-5)
- [ ] 1.3 Зум колесом: дистанция вдоль оси наклона, кламп min/max, сглаживание, сохранение при смене режимов (CAM-4)
- [ ] 1.4 Тесты rig'а без WebGL (`engine/render-ts/test/camera.test.ts`): поза как данные, follow-сглаживание, высота по уровню клифа — подброс цели не меняет позу по вертикали, снап по порогу, кламп зума (CAM-1, CAM-2, CAM-4, CAM-5)

## 2. Free-RTS и маршрутизация ввода

- [ ] 2.1 Конечный автомат режимов: follow ↔ free-RTS по Dota-семантике — пан открепляет, тап центрирует с коротким перелётом, удержание держит, залипающий follow (CAM-2)
- [ ] 2.2 Панорамирование: стрелки, edge-pan по границам канваса, drag средней кнопкой; кламп target к арене с запасом; высота target по уровню террейна из `TickView` со сглаживанием (CAM-3)
- [ ] 2.3 Таблица владения вводом по режиму в демо: стрелки и колесо — камере, убрать стрелки из `pushInput` (Decision 3); тест, что ввод камеры не попадает в `InputFrame` (CAM-1)
- [ ] 2.4 Тесты режимов: переходы, кламп к арене, высота по террейну, «панорамирование не шлёт ввод героя»

## 3. Fly-режим

- [ ] 3.1 Fly: pitch/yaw drag'ом правой кнопки, перемещение WASD, колесо — скорость; явный переключатель режима (CAM-2)
- [ ] 3.2 Перехват WASD камерой в fly: движение героя не отправляется в воркер, пока режим активен; тест на `InputFrame`

## 4. Слой эффектов

- [ ] 4.1 Стек эффектов: контракт `CameraEffect` (офсет позиция/roll/pitch/yaw/fov, признак завершения), композиция офсетов поверх логической позы, глобальный множитель с нулём (CAM-6)
- [ ] 4.2 Trauma-шейк: аккумулятор trauma, амплитуда ∝ trauma², perlin-noise смещения, ослабление по расстоянию от точки наблюдения (Decision 5)
- [ ] 4.3 Sway-эффект (длящийся): активация присутствием состояния на сущности-цели в снапшоте, плавное затухание при исчезновении
- [ ] 4.4 Диспетчер эффектов: события тика из `view` → импульсные, состояния → длящиеся, по таблицам манифеста; подавление импульсных при `isReplay` (CAM-6)
- [ ] 4.5 Секция эффектов камеры в манифесте визуалов: схема секции, загрузка, предупреждение и пропуск неизвестного типа эффекта (ASSET-7); пример секции в `content/visuals/`
- [ ] 4.6 Тесты эффектов: нулевой вклад после завершения, стакание trauma, множитель 0, `isReplay` не стреляет, неизвестный тип — предупреждение без ошибки

## 5. Интеграция в демо

- [ ] 5.1 Демо `client-ts`: применение `CameraPose` к THREE-камере одной функцией; `groundPoint` по финальной позе, плоскость луча по уровню под курсором (Decision 6)
- [ ] 5.2 HUD-подсказка управления камерой (режим, клавиши); подобрать клавиши центрирования/залипания/fly (Open Question 1)
- [ ] 5.3 Прогон `npm test` и `npm run typecheck` из корня; проверить, что golden-файлы не изменились (камера не касается симуляции)
