## 1. Ассет (engine/assets-ts)

- [x] 1.1 Переписать парсер карты кривизны (`src/curvature.ts`): узловая сетка `(w+1)×(h+1)`, числовые ряды целых множителей решётки 1/32 шага; адресные отказы — нецелое значение, вне безопасного диапазона, ряд неверной длины; старый строковый формат отвергается с внятной причиной
- [x] 1.2 Обновить загрузчик и типы handle (`src/loaders/curvature.ts`, `src/types.ts`): узловые смещения в единицах решётки; несовпадение сетки — предупреждение и игнор, как прежде
- [x] 1.3 Обновить тесты assets-ts: валидные/невалидные heightfield-фикстуры, сценарии ASSET-7 (амплитуда больше шага — валидна)

## 2. Поле рендера (engine/render-ts)

- [x] 2.1 Переписать `visualSurface.ts`: слагаемое кривизны — бикубическая (Catmull-Rom) выборка узловой сетки; удаление правила «среднее смежных клеток того же уровня»; окно выборки не пересекает cliff-границу (зажим каждого уровня своим полом), узел на кромке смещает оба уровня одним значением
- [x] 2.2 Обновить `surfaceSource.ts` и `subsystems/terrain.ts`: разбиение клетки по ненулевым узлам, кромка стенки той же выборкой, сцена без кривизны — прежняя геометрия
- [x] 2.3 Проверить потребителей единого поля (`overlays.ts`, picking, посадка инстансов REND-10) — сэмплинг через одну точку, без второй реализации
- [x] 2.4 Обновить тесты render-ts: сценарии REND-9 — холм выше шага без стенок, узел на кромке (перепад точен, стенка вертикальна), швы пол/стенка, непрерывность C1 внутри уровня

## 3. Редактор (editor/core-ts, editor/ui-ts)

- [x] 3.1 Обновить правила валидации (`validation/engineRules.ts`, `crossDocument.ts`): целочисленность решётки и узловая сетка вместо алфавита; локали находок (ru/en + fingerprints)
- [x] 3.2 Переделать кисть кривизны на узлы (ED-11): рисование без клампа амплитуды, превью сглаженным рельефом, live-подсветка несовпадения сеток
- [x] 3.3 Обновить тесты editor-core (`validationRules.test.ts`, i18n) и образцы ui (`gallery/sampleDocument.json`, `sceneStage`/`sceneDocuments`)

## 4. Импортёр (tools/blender-ts)

- [x] 4.1 Переписать сэмплирование кривизны (`src/maps.ts` и цепочка import): вершины curvature-объекта → узлы 1:1, квантование к 1/32, удаление клампа ±7/16 и предупреждения об амплитуде; ошибка представления — адресом узла (BLND-6)
- [x] 4.2 Обновить фикстуры `.gltf`/`.glb` и тесты импортёра: скульпт выше шага уровня переносится как есть, повторный импорт — пустой дифф (BLND-4)

## 5. Аддон (tools/blender-addon)

- [x] 5.1 Убрать кламп из кисти и превью кривизны (`brushes.py`, `preview.py`, `grids.py`), шаг кисти — решётка 1/32; live-проверка — вызовом импортёра, без своих правил (BLND-8)

## 6. Контент и финал

- [x] 6.1 Мигрировать `content/visuals/arena-curvature.json` одноразовым скриптом (скрипт в scratchpad, не коммитится): узел = округлённое среднее смежных клеток того же уровня × 2; глазами сверить сцену duel до/после
- [x] 6.2 Прогнать `npm run check` из корня; убедиться, что голдены и `worldInit` не тронуты (изменение — presentation-only)
