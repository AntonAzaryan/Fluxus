# Tasks: editor-authoring-areas

Порядок групп — порядок зависимостей. Группы 2 (WP1) и 3–6 (WP2–WP5) начинаются сразу и идут параллельно; группа 2 — точка сериализации для всего `editor/ui-ts/test`, поэтому её не откладывают. Группы 7 (WP6) и 8 (WP7) параллельны друг другу. Группа 9 (WP8) сводит всё вместе и единственная правит `assembly.ts` и файлы локалей.

## 1. Спека (дельта ED-6)

- [ ] 1.1 Дельта `openspec/changes/editor-authoring-areas/specs/editor/spec.md`, `MODIFIED` ED-6: материал редактора схем — перечень схем компонентов конфига сцены (`serialization` SER-7); опубликованные JSON Schema (SER-5) — описание формата, редактором не записываются
- [ ] 1.2 Там же, ED-6: порядок перечня нормативен (битовые id SER-7 → `worldInit` DET-1), перестановка MUST NOT быть побочным эффектом, новая схема и новое поле дописываются в конец
- [ ] 1.3 Там же, ED-6: схемы, синтезируемые загрузчиком (`terrain` TERR-6, `arena` ARENA-1, `time-system` TIME-2/TIME-7/TWEEN-1, `fog-of-war` FOW-1..3), — производные и напрямую не правятся; объявление их контентом ядро отвергает (`ecs-foundation` ECS-5); автор правит ассет или флаг сцены
- [ ] 1.4 Сверить, что дельта не занимает чужих номеров и не трогает соседних требований: правится только ED-6, ED-2/ED-4/ED-5/ED-7/ED-10/ED-16/ED-19/ED-23 упоминаются ссылками; спеки `fog-of-war`, `ecs-foundation`, `serialization` не правятся
- [ ] 1.5 Сверить дельту с действующим текстом `ecs-foundation` ECS-3 перед синхронизацией: набор типов поля там закрыт и уже расширен до трёх (`i32`, `fixed`, `entity`), поэтому ED-6 его не перечисляет и ссылается на соседа. Второй дельты на ED-6 в очереди нет (`spec-modality-and-refs` правит ED-10/ED-11/ED-14, `editor-viewport-ergonomics` — ED-15/ED-22/ED-31/ED-32) — перед синхронизацией проверку повторить
- [ ] 1.6 Координация с `editor-viewport-ergonomics`: он вводит ED-31 (знак, имя и подсказка у инструментов бара) и ED-32 (порядок разбора клавиатуры, раскладка области объявляется данными вклада). Обе нормы общие и на две новые области действуют без оговорок — раскладки клавиш областей объектов и систем SHALL объявляться вкладом и разбираться порядком ED-32, а не своим; навигаторы обеих областей — списки и деревья, то есть ровно сценарий ED-32 «Стрелки в дереве навигатора». По файлам эти два change'а сходятся на `editor/ui-ts/test/frameKeyboard.test.ts` (здесь его правит WP1, там он переписывается под ED-32)
- [ ] 1.7 `openspec validate editor-authoring-areas --type change --strict` и `openspec validate --specs --strict` — зелено

## 2. WP1: заглушка систем переезжает в тесты (точка сериализации)

- [x] 2.1 Перенести `editor/ui-ts/src/areas/material.json` → `editor/ui-ts/test/support/material.json` и `src/areas/material.ts` → `test/support/material.ts`, сохранив `materialStrings`
- [x] 2.2 Собрать в `editor/ui-ts/test/support/stubArea.ts` фикстурную область на этом материале (бывшая `systemsArea` со стубом): свой id (`area.stub`), своя горячая клавиша, три зоны непохожие на область сцены, правка карты флагов зарегистрированной операцией — по образцу `outsiderArea` из `frameExtension.test.ts`
- [x] 2.3 `editor/ui-ts/test/support/frame.ts`: заменить `systemsArea` на фикстурную область в умолчании `buildFrame`
- [x] 2.4 Перевести на фикстуру импорты в `frame.test.ts`, `frameExtension.test.ts`, `frameKeyboard.test.ts`, `inspector.test.ts`, `palette.test.ts`, `renderContext.test.ts`, `scenePreview.test.ts`, `sceneTool.test.ts`, `shellCommands.test.ts`, `strings.test.ts` — смысл проверок не меняется, меняется происхождение второй области
- [x] 2.5 Удалить `editor/ui-ts/src/areas/material.{ts,json}`; убедиться поиском, что в `src/` и `app/` ссылок на них не осталось. Сделано вместе с двумя следствиями, о которых WP6 и WP8 обязаны знать: `src/areas/systems.ts` ужат до объявления вида документа (`SYSTEM_DOCUMENT_KIND`, его читает `sceneProject.ts` — файл WP8 не тронут), а `app/assembly.ts` больше не регистрирует область систем (снята одна строка импорта и одна строка регистрации; вернёт её WP8 вместе с настоящей областью)
- [x] 2.6 `npm test -w @game-mvp/editor-ui` зелёный до появления новых областей: каркас проверяется на двух непохожих областях, ни одна из которых не требует открытого проекта

## 3. WP2: перечисление компонентов и prefab'ов мира (engine/core-ts)

- [x] 3.1 `engine/core-ts/src/ecs/world.ts`: read-only `componentNames(state)` — имена компонентов в порядке объявления (он нормативен, SER-7), и `prefabNames(state)` — имена зарегистрированных prefab'ов
- [x] 3.2 `engine/core-ts/src/index.ts`: добавить обе в пространство `world` рядом с `componentSchema`/`prefabOf`; мутирующих хелперов не добавлять
- [x] 3.3 Тест в `engine/core-ts/test/world.test.ts`: порядок имён совпадает с порядком объявления схем; синтезированные загрузчиком компоненты (пол, арена, FoW) в перечне присутствуют; `prefabNames` содержит `TERRAIN_PREFAB` и `ARENA_PREFAB` для сцены с ними
- [x] 3.4 Скил `determinism-review` по диффу `engine/core-ts`: чтение, вне тика, без аллокаций в горячем пути; `engine/tests/golden/` не тронут

## 4. WP3: модель блоков DSL (editor/core-ts/src/dsl)

- [x] 4.1 `editor/core-ts/src/dsl/slots.ts`: виды слотов конвенции SYS-3 — список действий (`do`/`then`/`else`), карта выражений (`values`/`data`/`bindings`), выражение (`entity`/`cond`/`bound`), запрос (`query`), переопределения (`overrides`), строковый литерал (`component`/`field`/`prefab`/`type`/`as`/`subStream`). Шапка модуля называет источник — SYS-3 — и объясняет, почему таблицы «действие → аргументы» здесь нет (ED-1)
- [x] 4.2 `editor/core-ts/src/dsl/catalog.ts`: каталог блоков из `actions.actionNames` и `expr.operators` ядра, с ключами описаний из `i18n/paths.ts` (`dslDescriptionPaths`); четыре структурных оператора (`var`, `getComponent`, `hasComponent`, `eventField`) помечены своей формой, остальные — n-арные
- [x] 4.3 `editor/core-ts/src/dsl/world.ts`: чтение поднятого мира для пикеров — компоненты, поля компонента, prefabs, компоненты prefab'а; строится на `loadScene` + `world.*` (WP2), собственного разбора документа не заводит
- [x] 4.4 `editor/core-ts/src/dsl/scope.ts`: имена, связанные объемлющими `as` и `bindings`, по пути до узла. В шапке — правило решения 5: аффорданс производный, вердикт за ядром; ошибка подсказки всплывает находкой `validateSystem`, а не молча
- [x] 4.5 `editor/core-ts/src/dsl/build.ts`: сборка узла действия и узла выражения из заполненных слотов — незаполненный слот в документ не пишется; ровно один ключ у узла (ACT-1, EXPR-1)
- [x] 4.6 Экспорт из `editor/core-ts/src/index.ts` (одна строка; файл делится с WP5)
- [x] 4.7 `editor/core-ts/test/dslBlocks.test.ts`: каталог покрывает `actionNames` и `operators` в обе стороны (по образцу `i18nBundles.test.ts`); у каждого имени есть ключ описания; собранный узел проходит `validateSystem` против сцены-фикстуры; незаполненный слот в JSON не появляется
- [x] 4.8 Там же: пикер компонентов предлагает ровно то, что принимает `componentSchema`; пикер полей — ровно то, что принимает `checkFields`; `var` вне области видимости отвергается ядром, а не редактором

> WP3 закрыт. Ресурсных ключей слой не добавляет: описания блоков — уже написанные `action.*` / `operator.*` (ключ выводится из пути, ED-28), новых операций и правил валидации в пакете не появилось, поэтому `editor.{ru,en}.json` этим пакетом не правится и `i18nBundles.test.ts` остаётся зелёным. Подписи слотов и видов слотов — строки интерфейса (`ui.*`), их список приносит WP8 тот пакет, который эти слоты рисует (WP6).

## 5. WP4: правило систем адресуется записью-носителем

- [x] 5.1 `editor/core-ts/src/validation/engineRules.ts`: `systemRule` получает адреса мест `DocumentSite { kind, path }` по образцу `placementPrefabRule`; пустой путь — «документ и есть система», непустой — список систем внутри носителя
- [x] 5.2 Там же: находка адресуется путём до записи системы (`['systems', index]`), а не документом целиком; `by: VALIDATE_SYSTEM` и `against: <документ сцены>` сохраняются
- [x] 5.3 Там же: мир для проверки берётся из уже кэшированного `loadedScene` (`run.derive`) — второго `loadScene` на ту же правку не появляется
- [x] 5.4 `editor/core-ts/test/validationRules.test.ts`: система с неизвестным компонентом внутри конфига сцены подсвечивается на своей записи; система отдельным документом (умолчание) продолжает подсвечиваться как прежде; сломанная сцена по-прежнему молчит правилом систем

## 6. WP5: базовая операция `document.renameKey`

- [x] 6.1 `editor/core-ts/src/operations/builtin.ts`: операция `document.renameKey` (document, path, from, to) — переименование ключа объекта **с сохранением его позиции**; в шапке — почему позиция важна (ED-21, `project/canonical.ts` не сортирует ключи)
- [x] 6.2 Там же: отказ `OperationError`, если ключа `from` нет или ключ `to` уже занят; операция реализуется поверх существующих примитивов контекста — поверхность сессии не расширяется
- [x] 6.3 Ключи описаний для WP8 (файлы локалей правит он): `operation.document.renameKey`, `operation.param.from`, `operation.param.to`. **Закрыто в WP7:** три ключа дописаны в `editor.{ru,en}.json`, отпечатки пересчитаны `UPDATE_FINGERPRINTS=1 npx vitest run`, `registryCatalog.test.ts` зелен. Задаче 9.4 остаётся только сверка
- [x] 6.4 `editor/core-ts/test/operations.test.ts`: позиция ключа сохранена (сравнение `Object.keys` до и после); `operationsConformance.test.ts` (применить → отменить → сравнить) проходит на новой операции автоматически — проверить, что она в перечне

## 7. WP6: рабочая область систем (ED-4, ED-5)

- [x] 7.1 `editor/ui-ts/src/areas/systemsDocuments.ts`: открытие конфига сцены с отслеживаемым списком `['systems']` (по образцу `PLACEMENT_LIST` в `sceneProject.ts`); дескрипторы записей — адреса операций и ключи выделения; идемпотентность повторного открытия
- [x] 7.2 `editor/ui-ts/src/areas/systemsOperations.ts`: операции над системами — завести систему (`systems.system.create`: `appendRecord` в конец, `name`+свободный `order`+пустое `do`), править поле записи (`document.list.setValue`), удалить (`document.list.remove`); операции перестановки нет и не заводится — проверено перебором ВСЕГО реестра в `systemsArea.test.ts` по образцу `documentOrder.test.ts`
- [x] 7.3 Там же: операции над узлами тела — добавить (`systems.node.append`), заменить (`document.list.setValue`), снять (`document.list.removeValue`); адрес узла — путь внутри записи, дескриптор — сама запись (ED-29). Доменных операций из трёх только одна: дописывание адресуется САМИМ списком, а не индексом в нём, — «в конец» полная инструкция, а индекс верен лишь при той длине, которую вызывающий успел прочитать (ED-29: операции исполнимы без интерфейса). Замена и снятие — базовые: доменная обёртка отличалась бы от них только именем в каталоге (ED-30)
- [x] 7.4 `editor/ui-ts/src/areas/systemsBlocks.ts`: отрисовка блока действия и блока выражения по модели WP3 — слот-список, слот-карта, слот-выражение, литерал с пикером; подсказка блока по ключу `action.*`/`operator.*` (ED-28)
- [x] 7.5 Там же: три блока триггера ED-4 на записи системы — источник (`query` или `forEachEvent`), условие (`if.cond`), действия (`do`); переключение источника не теряет заполненное тело
- [x] 7.6 `editor/ui-ts/src/areas/systems.ts` переписан: навигатор — список систем документа с `name`/`order`; поверхность правки — раскрытый триггер блоками; инспектор — поля записи системы по JSON-схеме `system.schema.json` (`schemaFiles`, тем же обходом, что `sceneSchema.ts`)
- [x] 7.7 Там же: `search` по имени системы (ED-24), `editableTypes` вида `system` (ED-30), поведение без открытого проекта — причина, а не пустой список (решение 9)
- [x] 7.8 `editor/ui-ts/test/systemsArea.test.ts`: «ED-4: реакция на событие собирается из блоков» — от пустой системы до записи, проходящей `validateSystem`, без единой строки JSON, набранной автором; «ED-29: каждая правка системы обратима»; «ED-23: правка системы отменяется из области сцены»
- [x] 7.9 `editor/ui-ts/test/systemsBlocks.test.ts`: «ED-5: формула с вложенными условиями строится блоками, а на диск уходит JSON-AST» — вложенный `if` в выражении; «ED-1: ни одного правила DSL в редакторе» — сканер по исходникам области на отсутствие таблицы имён аргументов; «ED-8: неизвестный компонент в блоке подсвечивается на своей записи»

> WP6 закрыт. Что он отдаёт сборке (WP8) и чего сам не делает:
>
> - `createSystemsArea({ host, config })` и `systemsArea` (`area.systems`, `F2`, знак `graph`) — регистрация областью вкладом, `app/assembly.ts` этим пакетом не тронут (ED-25);
> - `registerSystemOperations(registry)` — три доменные операции рядом с `registerPlacementOperations` и соседями;
> - `systemSites(kind)` (`systemsDocuments.ts`) — адреса систем для правила `core.system` (WP4): `[{ kind, path: ['systems'] }]`. Сегодня `sceneProject.ts` их не подаёт, и находка адресует документ целиком, а не запись;
> - **открытие**: конфиг сцены должен быть открыт со списком `['systems']`. Вторым открытием список не дописать, поэтому `openSceneProject` обязан открыть конфиг сразу со ВСЕМИ списками проекта — `OBJECT_LISTS` (WP7) плюс `['initial']` и `SYSTEM_LISTS` (`['systems']`). Без него область не показывает пустой перечень, а называет причину (`ui.area.systems.noList`), и это проверено тестом;
> - **строки**: ключи области систем (`ui.area.systems.*`), её трёх операций (`ui.operation.systems.*`) и их параметров (`path`, `node`, `source`, `eventType`, `systemName`) дописаны в `ui.{ru,en}.json` обеими локалями — задаче 9.3 остаётся сверка. Ресурсов `editor.{ru,en}.json` пакет не добавляет: описания блоков — уже написанные `action.*`/`operator.*` (ED-28), отпечатки не пересчитывались.

## 8. WP7: рабочая область объектов (ED-6, ED-7, ED-19)

- [x] 8.1 `editor/ui-ts/src/areas/objectsSchemas.ts`: перечень схем компонентов конфига сцены (`components`, SER-7) как отслеживаемый список; разделение на объявленные контентом (правятся) и синтезированные загрузчиком (только чтение, WP2 даёт их имена) — решение 1 и дельта ED-6
- [x] 8.2 Там же: операции схем — завести компонент (в конец), завести поле (в конец состава), сменить тип поля, задать значение по умолчанию, снять поле, снять компонент. Перечень типов берётся из опубликованной схемы формата (`schemaFiles`, SER-5), а не литералом в редакторе: набор закрыт и нормирован `ecs-foundation` ECS-3 (сегодня — `i32`, `fixed`, `entity`), и второй его копии не заводится. Операции перестановки нет; проверяется перебором реестра, как в `documentOrder.test.ts`
- [x] 8.3 `editor/ui-ts/src/areas/objectsPrefabs.ts`: prefab как запись `prefabs` — набор компонентов и начальные значения полей; добавить компонент prefab'у, снять компонент, править значение поля (типы и допустимые значения — из схемы компонента, ED-24), править `tags`
- [x] 8.4 `editor/ui-ts/src/areas/objectsOperations.ts`: `objects.prefab.create` — запись в `prefabs` конфига и запись `entities[name]` манифеста одним вызовом и одной записью истории (решение 7); модель приходит параметром (выбор — просмотрщик ассетов, ED-20)
- [x] 8.5 Там же: `objects.prefab.rename` — `prefabs[].name`, ключ манифеста через `document.renameKey` (WP5) и ссылки по адресам-параметрам: записи расстановки (`initial[].prefab`) и литералы `prefab` действий `spawnEntity` в системах (решение 7а); неоткрытые документы не трогаются
- [x] 8.6 Там же: `objects.prefab.delete` — обе половины пары одним вызовом (сценарий ED-19 «Удалён префаб, оставшийся в расстановке» требует, чтобы удаление существовало)
- [x] 8.7 `editor/ui-ts/src/areas/objects.ts`: область `area.objects`, горячая клавиша `F4`, знак рельса; навигатор — два раздела (компоненты, prefabs), поверхность правки — состав выбранного, инспектор — значения; `editableTypes` для обоих видов (ED-30); `search` по именам компонентов и prefab'ов (ED-24)
- [x] 8.8 `editor/ui-ts/test/objectsArea.test.ts`: «ED-7: новый prefab снаряда собирается визуально» (состав компонентов и начальные значения, документ проходит `loadScene`); «ED-6: поле, добавленное в схему, появляется в инспекторе расстановки со своим типом»; «ED-6: синтезированные компоненты показаны и не правятся» — операции, которая писала бы `Vision`, в наборе нет вовсе; «ED-6: порядок схем не переставляется ни одной операцией»
- [x] 8.9 `editor/ui-ts/test/objectsPair.test.ts`: «ED-19: создание юнита — одно действие над одной сущностью» (появились обе половины, находок рассинхронизации нет, undo снимает обе); «ED-19: переименование меняет оба согласованно» (пара, расстановка и `spawnEntity` в системе; позиция ключа в манифесте не сдвинулась — ED-21); «ED-18: пара — одна запись истории»
- [x] 8.10 `editor/ui-ts/test/objectsPair.test.ts`: «ED-19: один префаб, три размещения» — три записи расстановки на одну пару, и удаление префаба даёт находки на все три, а не на документ целиком

## 9. WP8: сборка, раскладка проекта, строки

- [ ] 9.1 `editor/ui-ts/src/areas/sceneProject.ts`: адреса мест проекта одним местом — список систем (`['systems']`) для правила WP4 и адреса ссылок на имя prefab'а для операции переименования WP7; виды документов дописаны в `SCENE_KINDS`. **От WP7:** `openSceneProject` обязан открыть конфиг сразу со ВСЕМИ списками проекта — `OBJECT_LISTS` (`['components']`, `['prefabs']`) плюс `['initial']` и `['systems']`: сессия открывает документ однажды, и вторым открытием списки не дописать. Умолчание адресов ссылок — `defaultPrefabReferences` в `objects.ts`, сборка вправе подать свои опцией `references`
- [ ] 9.2 `editor/ui-ts/app/assembly.ts`: регистрация области объектов и переписанной области систем, регистрация их наборов операций рядом с существующими (`registerPlacementOperations`, `registerTerrainOperations`, `registerVisualsOperations`); правила валидации — тем же `registerValidationRules`
- [ ] 9.3 `editor/ui-ts/src/i18n/locales/ui.{ru,en}.json`: подписи, заголовки и описания двух областей, их операций и слотов блоков — `ru` и `en` равноправны (ED-27). **Закрыто в WP7 и WP6:** ключи области объектов (`ui.area.objects.*`), её шести операций (`ui.operation.objects.*`), ключи области систем (`ui.area.systems.*`), её трёх операций (`ui.operation.systems.*`) и параметров обеих дописаны в обе локали; WP8 остаётся сверка
- [x] 9.4 `editor/core-ts/src/i18n/locales/editor.{ru,en}.json`: описание операции `document.renameKey`; отпечатки `editor.ru.fingerprints.json` пересчитаны (ED-28) — сделано в WP7 вместе с задачей 6.3
- [ ] 9.5 `editor/ui-ts/test/strings.test.ts` и `editor/core-ts/test/i18nBundles.test.ts` зелёные: ни одного строкового литерала интерфейса вне ресурсов, бандл и схемы сходятся в обе стороны

## 10. Сквозные проверки

- [ ] 10.1 `frameExtension.test.ts` не правится содержательно: две новые области зарегистрированы вкладом, каркас и соседние вклады не тронуты (ED-25) — проверить диффом, что `src/frame/` в изменениях нет
- [ ] 10.2 `frameDomain.test.ts` и `registryFrame.test.ts` зелёные: доменных имён нового материала («компонент», «prefab», «система», «триггер») в каркасе не появилось; новые файлы областей помечены `@contribution`
- [ ] 10.3 `openspec validate editor-authoring-areas --type change --strict`, `openspec validate --specs --strict`
- [ ] 10.4 `npm run check` из корня (typecheck + lint + test) — зелено; `npm run coverage` прогнан один раз и прочитан как список неисполняемого в новых модулях
- [ ] 10.5 `git diff --stat`: тронуты `openspec/changes/editor-authoring-areas/`, `editor/`, `engine/core-ts/{src/ecs/world.ts,src/index.ts,test}`; `content/`, `engine/tests/golden/`, `engine/schemas/`, `openspec/specs/` не тронуты
- [ ] 10.6 Дописать в `docs/reviews/2026-08-08-editor-coverage.md` строку о закрытии пунктов ED-4..ED-7 и клауз ED-19/ED-23 (ненормативный отчёт правится после кода, а не вместо него)
