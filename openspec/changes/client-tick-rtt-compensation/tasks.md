## 1. Контроллер lead в клиенте

- [ ] 1.1 Завести в `MatchClient` состояние `lead` (старт `inputDelay` из `Welcome.pacing`, границы `[inputDelay, inputWindow]`) и разметку `pushInput` через него вместо константы; проверка — существующие тесты клиента зелёные на loopback (lead не растёт без сигнала, разметка совпадает с прежней).
- [ ] 1.2 Детектор опоздания по разрыву `sentSeq − appliedSeq` (design D1): окно наблюдения, порог `pipelineDepth = lead + tickRate/snapshotRate`, нечувствительность к молчанию игрока и заморозке; проверка — юнит-тест детектора на синтетической последовательности отправок/эхо без таймеров.
- [ ] 1.3 Контроллер AIMD (design D2): подъём на период рассылки по сигналу до `inputWindow`, спуск на 1 тик после интервала стабильности до `inputDelay`; проверка — тест на симулированном канале с задержкой: predicted у слота прекращаются после подъёма, спуск после выправления медленнее подъёма.
- [ ] 1.4 Смена эпохи не сбрасывает `lead` (дельта NTR-7); проверка — тест: перемотка посреди адаптированного состояния, `lead` прежний, фреймы новой эпохи успевают.
- [ ] 1.5 Снять оба `ponytail` в `advance()`/`resyncTick()` с обоснованием design D5 (компенсация живёт в разметке); проверка — `grep ponytail engine/net-ts/src/client/matchClient.ts` пуст по этим двум местам.

## 2. Наблюдаемость

- [ ] 2.1 Поле `inputLead` в `ClientMetrics` (`engine/net-ts/src/metrics.ts`), публикуемое рядом с `inputToVisibleMs`; проверка — тест метрик: значение следует контроллеру, на loopback равно `inputDelay`.
- [ ] 2.2 Показ `inputLead` там, где демо уже показывает клиентские сетевые метрики (debug-панель/HUD demo-ts); проверка — ручной прогон `npm run demo`, величина видна и равна 2 на loopback.

## 3. Спека и контент

- [ ] 3.1 Прогнать `openspec validate client-tick-rtt-compensation --strict` и `npm run spec-graph -- check` — зелёные.
- [ ] 3.2 Откатить обходной путь: `content/matches/duel.match.json` → `inputDelay: 2`, `inputWindow: 30` остаётся; проверка — `npm run demo` управляем на loopback, тест конфига демо зелёный.

## 4. Интеграционная проверка

- [ ] 4.1 Интеграционный тест net-ts: матч на транспорте с искусственной задержкой больше `inputDelay` тиков — до изменения слот жил на predicted, после — `applied` растёт, `predicted` выходит на плато; golden-базлайны не трогаются (канонический лог меняется только составом фреймов конкретного прогона, записанные матчи перегенерации не требуют — проверить `npm run golden` дифом, диф пустой).
- [ ] 4.2 `npm run check` на node 22 (`.nvmrc`) — целиком зелёный; на node 24.19 известен пре-существующий краш CLI-тестов `journal.mjs` (libuv assert, вне этого изменения).
- [ ] 4.3 Ручная проверка через `npm run demo:tunnel` вдвоём: второй игрок управляем, `inputLead` у него поднялся и стабилен.
