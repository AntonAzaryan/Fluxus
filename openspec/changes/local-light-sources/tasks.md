# Tasks: local-light-sources

## 1. Формат и валидация (engine/assets-ts)

- [ ] 1.1 Тип блока `light` в записях манифеста (сущностной и decoration-вида): состав ASSET-16, единицы — доли оборота и мировые единицы (design D4), конверсия углов на границе разбора
- [ ] 1.2 Валидация: закрытый состав адресно, обязательность `type`/`color`/`intensity`/`distance` (> 0), spot-поля только у `spot`, `angle` в (0, 0.25], `penumbra` в [0, 1]; тексты находок на русском
- [ ] 1.3 Тесты формата: валидные point/spot, вырожденные значения, неизвестный ключ, spot-поле в point, поле теней, запись без блока разбирается как прежде

## 2. Порт носителей (engine/render-ts)

- [ ] 2.1 `LightCarrierSink` у подсистемы освещения (design D1): регистрация/снятие носителя, устойчивый ключ для тай-брейка (design D3)
- [ ] 2.2 Подсистема моделей: регистрация носителя для инстанса записи с блоком `light` — оба продюсера presentation-состояния, оба яруса; снятие с исчезновением инстанса; пересмотр на переподаче манифеста (REND-17, ED-15)
- [ ] 2.3 Подсистема декораций: то же для decoration-инстансов; пересмотр на правке набора декораций

## 3. Пул и отбор (engine/render-ts/src/subsystems/lighting.ts)

- [ ] 3.1 Пул источников размера потолка (design D2): создание на инициализации/applyQuality, неактивный слот `intensity = 0`, освобождение точкой сноса (REND-31, design D6)
- [ ] 3.2 Кадровый путь: позиции активных из провайдеров трансформов + `offset`, отбор по метрике D3 детерминированно, без аллокаций пропорционально числу носителей сверх переиспользуемых структур
- [ ] 3.3 Ручка качества `lighting.maxLocalLights` (QUAL-1/QUAL-3) и счётчики `localLightCarriers`/`localLightsActive` в сток стоимости (PERF-2/PERF-3, design D5)
- [ ] 3.4 Тесты подсистемы: жизненный цикл источника с инстансом, отбор при носителях сверх потолка воспроизводим, переподача манифеста зажигает/гасит свет, локальный источник не входит в реестр кастеров теней, снос отдаёт пул

## 4. Сквозная проверка слоёв

- [ ] 4.1 Интеграционный тест: сцена с блоками `light` и без — `worldInit` и снапшоты не отличаются ни байтом (ASSET-1, PRES-4)
- [ ] 4.2 Вьюпорт редактора получает локальный свет без правок своего кода (ED-22); правки — дефект шва, разобраться до пуша

## 5. Контент демо

- [ ] 5.1 `content/visuals/`: блок `light` виду `Torch` (point, тёплый тон, offset на чашу); потолки в пресетах `game/demo-ts/app/presets/*.json` по бенчу (design Open Questions)
- [ ] 5.2 Прогнать `npm run demo`: факелы светят, юниты подсвечиваются у огня, ночная фаза цикла читается лучше; `npm run bench:demo` без деградации порядка

## 6. Спека и гейт

- [ ] 6.1 `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные после синка дельт
- [ ] 6.2 `npm run check` целиком; дифф `*.cost.json` демо принять отдельно через `npm run golden:cost` с объяснением в коммите (design D5)
