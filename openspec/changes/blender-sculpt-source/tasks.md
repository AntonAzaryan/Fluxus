## 1. Спека и конвенции

- [ ] 1.1 Синхронизировать дельту в главную спеку `blender-pipeline` (BLND-13, правка BLND-3) — `/opsx:sync` либо при архивации; `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные
- [ ] 1.2 CONVENTIONS.md (`tools/blender-ts`): раздел «Sculpt-объекты» — свойство `sculpt`, объединение множества мешей верхним пересечением (пересечения легальны, булевы не нужны), custom property `cliffJump` с умолчанием 0.5 и правилом единственности, взаимоисключение с terrain/curvature

## 2. Импортёр: сэмплер и деривация (`tools/blender-ts`)

- [ ] 2.1 Модуль `sculpt.ts`: рейкаст сверху по треугольникам всех sculpt-объектов (Möller–Trumbore, верхнее пересечение объединения), выборка по решётке центров и узлов с ε-смещением на границах; юнит-тесты на плоскости, наклоне, ступени, дыре, навесе и пересекающихся кубах (инвариантность к порядку объектов)
- [ ] 2.2 Деривация уровней: квантование к ближайшему целому, ошибка с адресом клетки при уровне вне алфавита схемы (без клампа); дыра над центром → флаг `_`
- [ ] 2.3 Деривация рамп: по-граничное правило с порогом `cliffJump` (custom property sculpt-объекта, умолчание из конвенций), флаг `^` на нижней клетке пары; тесты: гладкий склон — цепочка рамп, обрыв — без рамп, порог из свойства уважается
- [ ] 2.4 Остаток кривизны: узловая высота террейн-формы из `@game-mvp/render` (`visualSurface.ts`; при необходимости открыть чистый модуль отдельным export'ом пакета — без затягивания three.js в рантайм CLI), `offset = h − base`, квантование 1/32, проверка диапазона ASSET-7
- [ ] 2.5 Интеграция в `importer.ts`: распознавание семантики `sculpt` (BLND-3), сбор всех sculpt-объектов в объединение, ошибки «sculpt вместе с terrain/curvature-объектом», «walkable на sculpt», «разные `cliffJump` на разных sculpt-объектах»; запись слоёв прежним путём операций (BLND-5), dry-run показывает дифф слоёв
- [ ] 2.6 Строки находок в `locales/` (ru/en) для всех новых ошибок; фикстуры `.gltf` со скалптом в `test/`, включая детерминизм повторного импорта (байт-в-байт, BLND-4)

## 3. Аддон (`tools/blender-addon`)

- [ ] 3.1 Семантика `Sculpt` в панели размещения (`ui.py`, `placement.py`): назначение/снятие сырого свойства `sculpt`, показ конфликта с terrain/curvature текстом панели (правда — находки импорта)
- [ ] 3.2 Оператор «посадить decoration на рельеф»: рейкаст вниз от объекта к поверхности под ним (evaluated depsgraph — работает и по заскалпченному мешу с модификаторами)
- [ ] 3.3 README аддона: воркфлоу скалпта (скалптишь меш → свойство `sculpt` → сохранение → живая проверка показывает дифф уровней/рамп/кривизны), пункт чек-листа ручного прогона

## 4. Сквозная проверка

- [ ] 4.1 [вручную] Прогон в Blender 4.5: заскалптить холм с обрывом на тестовой сцене, импортировать, сверить кадр движка со скалптом (BLND-11), откалибровать умолчание `cliffJump`, наблюдения — в `docs/reviews/`
- [ ] 4.2 `npm run check` зелёный из корня; голдены и схемы не тронуты (изменение не касается движка)
