# Tasks — physics-column-model

## 1. Спека

- [ ] 1.1 Синхронизировать delta в основную спеку `openspec/specs/physics/spec.md` (PHYS-1 MODIFIED, PHYS-14 ADDED) и прогнать `openspec validate --specs --strict` + `npm run spec-graph -- check`

## 2. Механизм в ядре

- [ ] 2.1 Помощник «эффективный уровень сущности» в `systems/physics.ts` — единственная точка чтения TERR-4/ARENA-6 для гейтов; переиспользуемый скретч-кэш уровней на тик (без аллокаций на тик), сцена без террейна → уровень 0
- [ ] 2.2 Поле высоты в компоненте коллайдера: чтение в физике, семантика «нет поля / 0 → полоса не ограничена», `h ≥ 1 → [L, L+h−1]`
- [ ] 2.3 Гейт полос в разрешении движения и событиях: блокировка (PHYS-8), `Collision` (PHYS-9), `Overlap` (PHYS-12) — вторым условием рядом с масками, полоса движущегося по разрешённому состоянию тика; `collisionGeometry.ts` не трогается
- [ ] 2.4 Гейт полос в `raycast` для динамических коллайдеров по существующей опции уровня (PHYS-13); статика обрывов без изменений
- [ ] 2.5 `npm run schemas` — поле высоты в генерированных схемах

## 3. Тесты

- [ ] 3.1 Тесты сценариев PHYS-14 в `engine/core-ts/test/physics.test.ts`: подножие обрыва, рампа (высота 2), летающий над зоной, поле без значения, луч с уровнем/без, полоса снаряда один раз за тик, сцена без террейна
- [ ] 3.2 Тест согласованности: гейт полос и гейт обрыва (PHYS-11) читают уровень через один помощник — уровни одной сущности в обоих путях совпадают
- [ ] 3.3 Обновить тесты, читающие PHYS-1 (2D-проекция «независимо от уровня»), под новые формулировки сценариев

## 4. Гейт качества

- [ ] 4.1 `npm run check` из корня зелёный; golden-эталоны и `*.cost.json` без диффа (гейт аддитивен) — если дифф есть, это дефект реализации, не повод перезаписи
