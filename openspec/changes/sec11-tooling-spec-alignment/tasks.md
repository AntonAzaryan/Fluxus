## 1. Дельты спек

- [x] 1.1 PERF-6: ось отделена от счётчика, оси объявлены по обе стороны конвейера, названы оси стороны симуляции, названа судьба счётчика без оси — проверка: `npx openspec validate sec11-tooling-spec-alignment --type change --strict` зелёный
- [x] 1.2 BLND-1: запрет рантайм-чтения адресован источнику сцены и парному экспорту, модели контента в glTF выведены из-под него сценарием (BLND-11) — проверка: та же валидация, сценарий «Рантайм тянется к .glb» согласован с новым текстом
- [x] 1.3 BLND-2: сопряжённость считается от `.blend` ИЛИ его экспорта, сцена с источником без экспорта получает названную находку — проверка: та же валидация
- [x] 1.4 CLI-2: необязательное поле включения поиска пути (NAV-1) и его роль, на которую ссылается NTR-14 — проверка: та же валидация
- [x] 1.5 DIAG-3: уровень «границы систем» несёт сводку объёма работы тика (PERF-3) — проверка: та же валидация

## 2. PERF-2: стадия «экстракция и канал доставки»

- [x] 2.1 `engine/render-ts/src/cost.ts`: стадия `extract` в `CostStage`, её счётчики (вызовы, просмотренные и скопированные сущности, пары статов, события, клетки зеркала пола, объём плоской формы канала) в `RenderCostCounters`, `createCostCounters` и `COST_COUNTER_STAGES` — проверка: тест «у каждого счётчика объявлена стадия конвейера» зелёный
- [x] 2.2 `engine/render-ts/src/extractor.ts`: инкременты в конце `extract`, сток читается один раз на вызов — проверка: новый тест `engine/render-ts/test/extractCost.test.ts` считает сущности, статы, события и объём канала на известной нагрузке
- [x] 2.3 Выключенный сток бесплатен: без стока экстракция не делает работы учёта — проверка: тест на инертность в `extractCost.test.ts` (PERF-3)
- [x] 2.4 `engine/render-ts/src/debug/sources.ts`: раскладка источника `cost.counters` знает три стадии (RDBG-8) — проверка: `npx vitest run test/debug.test.ts -w @fluxus/render` зелёный
- [x] 2.5 `engine/integration-ts/test/cost.test.ts`: секция `extract` в документе матча одна на документ, равенство её счётчиков между пресетами — проверка (QUAL-2); документ осей масштабирования секции не несёт
- [x] 2.6 `game/demo-ts/app/benchProbe.ts`: раздел «что этими числами НЕ измерено» называет экстракцию измеренной счётчиками, а не пробелом

## 3. PERF-6: второй размер осей стороны симуляции

- [x] 3.1 `engine/integration-ts/test/benchLoad.ts`: прореживание нагрузки NPC (каждый второй агент) и нагрузки поиска пути (один агент маршрута против четырёх) — величина оси и есть единственное отличие размеров
- [x] 3.2 `engine/integration-ts/test/cost.test.ts`: эталоны `npc-stress` и `nav-path` становятся двухразмерными документами оси той же формы, что `scaling.cost.json`; тесты «нагрузка не мёртвая» и «повторяется побитово» читают оба размера
- [x] 3.3 Отношение L/S проверяется тестом: счётчик соседей NPC и счётчик узлов поиска растут между размерами, и рост назван в тесте — проверка: `npx vitest run test/cost.test.ts -w @fluxus/integration`

## 4. BLND-2: пара считается от `.blend`

- [x] 4.1 `tools/blender-ts/src/pairing.ts`: расширение источника (`.blend`) и перечень «что рядом со сценой делает её сопряжённой» — проверка: типы и экспорт из `src/index.ts`
- [x] 4.2 `tools/blender-ts/src/rule.ts`: состояние «источник есть, экспорта нет», его код причины в `reasonCodes` и находка-предупреждение — проверка: `npx vitest run test/rule.test.ts -w @fluxus/blender`
- [x] 4.3 Обе локали вкладов (`locales/blender.{ru,en}.json`) — проверка: `test/bundles.test.ts` (ED-27, ED-28) зелёный
- [x] 4.4 `editor/ui-ts/app/assembly.ts`: появление `.blend` рядом со сценой обновляет кэш источников — проверка: `npx vitest run test/blenderSync.test.ts -w @fluxus/editor-ui`
- [x] 4.5 Сцена без `.blend` и без экспорта молчит как прежде, чтений содержимого нет — проверка: существующий тест «ни находки, ни попытки чтения» остаётся зелёным

## 5. Эталоны и ворота

- [x] 5.1 `npm run golden:cost` из корня: секция `extract` у матчей, двухразмерная форма у нагрузок симуляции; эталоны руками не правятся (PERF-4)
- [x] 5.2 Пакетные тесты: `@fluxus/render`, `@fluxus/integration`, `@fluxus/blender`, `@fluxus/editor-ui`, `@fluxus/core` зелёные
- [x] 5.3 `npm run typecheck`, `npm run lint`, `npm run lint:dead`, `npm run spec-graph -- check`, `npx openspec validate --specs --strict` и `--changes --strict` зелёные
- [x] 5.4 Дельты слиты в основные спеки (`openspec/specs/**`) — change остаётся незаархивированным
