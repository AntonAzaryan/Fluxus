## 1. Контракт границы (`src/bridge/`)

- [x] 1.1 Добавить в `types.ts` возможность `service` и три операции моста: поднять по идентификатору (в ответ — адрес), остановить, спросить состояние; операции — необязательные поля поверхности, как остальные возможности (DSK-5)
- [x] 1.2 Поднять `BRIDGE_VERSION`: поверхность меняется несовместимо
- [x] 1.3 Расширить схему профиля в `profile.ts` разделом сервисов (идентификатор, команда, аргументы, адрес) с разбором и отказом на кривом поле — русским текстом, называющим манифест и поле
- [x] 1.4 Убрать саму возможность расхождения портов подстановкой `{port}`/`{host}` из адреса (design D1) и проверить в разборе согласованность «сервисы объявлены ⇔ возможность `service` в whitelist» (по образцу проверки корня с записью при профиле без `write`)
- [x] 1.5 Тест `profile.test.ts`: сервис разобран; кривое поле — отказ с его именем; сервисы без возможности в whitelist и возможность без сервисов — отказ; повтор имени — отказ

## 2. Реализация на чистом Node (`src/host/`)

- [x] 2.1 Новый модуль сервисов: проба адреса, спавн объявленной команды, ожидание готовности соединением по адресу с таймаутом (design D2), запоминание владения (design D3)
- [x] 2.2 Повторный запуск работающего сервиса возвращает адрес и не спавнит второй процесс (DSK-7)
- [x] 2.3 Останов: SIGTERM, затем SIGKILL по истечении grace-периода; невладеемый сервис не снимается (design D3, D4)
- [x] 2.4 Снятие всех владеемых сервисов при закрытии сессии и при завершении процесса контейнера
- [x] 2.5 Отказ запуска (процесс умер, готовность не наступила) приходит вызывающему отказом с причиной, а не молчанием
- [x] 2.6 Собрать операции в мост по whitelist в `bridge.ts`: без возможности `service` полей в поверхности нет

## 3. Контрактный сьют границы (`test/`)

- [x] 3.1 Сервис-пустышка для сьюта: короткий Node-скрипт, слушающий объявленный порт; ни движка, ни контента (DSK-3)
- [x] 3.2 Случаи сьюта: сессия называет объявленные сервисы; запуск отдаёт адрес; повторный запуск не плодит процесс; останов гасит; закрытие сессии снимает владеемый сервис; незнакомый идентификатор — отказ; профиль без возможности не даёт ни операций, ни имён. Занятый адрес и подстановка проверяются в `test/service.test.ts` — это механика хоста, а не словарь границы
- [x] 3.3 Проверить, что случаи проходят в гейтовом прогоне поверх чистого Node (`hostBridge.contract.test.ts`) без установленного Electron (DSK-6)

## 4. Клей Electron (`src/electron/`)

- [x] 4.1 Провести три операции через IPC и preload; поверхность в странице собирается тем же способом, что и остальные возможности
- [x] 4.2 Снятие владеемых сервисов на закрытии окна (`closed` → `openApp().close()`, тем же путём, что и корни)
- [x] 4.3 Прогнать тот же сьют в контейнере (`xvfb-run -a npm run contract:electron`) — вне гейта: 31 случай зелёный в настоящем окне, включая семь сервисных. Счётчик процессов (`serviceProcesses`) харнесс контейнера не даёт, поэтому две проверки, опирающиеся на него, там пропускаются: механика владения — дело хоста и проверена в гейте (`test/service.test.ts`), а в контейнере проверяется то, чего гейт не видит, — маршрутизация через IPC и сборка поверхности в preload (DSK-6)

## 5. Профиль игры

- [x] 5.1 Объявить в `apps/game.app.json` сервис стенда демо-арены (`game/demo-ts/bin/demo-serve.mjs`, порт и адрес по design D1) и возможность `service` в whitelist
- [x] 5.2 Убедиться, что `editor.app.json` не изменён и редакторская сессия сервисов не видит (тест `apps.test.ts`)

## 6. Демо: публикация сессии и кнопка

- [x] 6.1 Добавить `@game-mvp/desktop-shell` в зависимости `game/demo-ts` (импортируется только слой `bridge`, направление «приложение → контракт», DSK-3)
- [x] 6.2 Публикация сессии в `app/desktopStand.ts`: поднять объявленный сервис, получить адрес, дальше — обычный сетевой режим страницы по нему (design D5; мост живёт в странице, а не в воркере, поэтому реализацией в `rendezvous.ts` это быть не может)
- [x] 6.3 Кнопка «поднять стенд» в `app/main.ts` по признаку «мост есть и сервис объявлен» (design D6); в вебе кнопки нет, `?server=` работает как прежде
- [x] 6.4 Показать человеку отказ запуска тем же путём, что и прочие сообщения демо (`showNotice`)
- [x] 6.5 Тесты демо на публикацию с мостом-заглушкой: кнопки нет без моста и без объявленного сервиса, публикация зовёт сервис по имени и отдаёт адрес, пустой адрес — отказ

## 7. Документация и гейт

- [x] 7.1 Обновить `desktop/shell-ts/README.md`: таблица профилей (у игры появилась возможность), раздел про сервисы и почему в мост не приходит команда
- [x] 7.2 Обновить `game/demo-ts/app/README.md`: раздел игры вдвоём получает десктопный путь рядом с `npm run demo:lan`
- [x] 7.3 `npm run check` зелёный без установленного Electron; `openspec validate --specs --strict` зелёный. Дельта синхронизирована в главную спеку (`/opsx:sync`): без неё `spec-graph check` красный — код цитирует DSK-7, а граф видит только главные спеки
