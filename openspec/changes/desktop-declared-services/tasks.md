## 1. Контракт границы (`src/bridge/`)

- [ ] 1.1 Добавить в `types.ts` возможность `service` и три операции моста: поднять по идентификатору (в ответ — адрес), остановить, спросить состояние; операции — необязательные поля поверхности, как остальные возможности (DSK-5)
- [ ] 1.2 Поднять `BRIDGE_VERSION`: поверхность меняется несовместимо
- [ ] 1.3 Расширить схему профиля в `profile.ts` разделом сервисов (идентификатор, команда, аргументы, адрес) с разбором и отказом на кривом поле — русским текстом, называющим манифест и поле
- [ ] 1.4 Проверить в разборе профиля согласованность порта адреса с портом в аргументах запуска (design D1) и согласованность «сервисы объявлены — возможность `service` в whitelist» (по образцу проверки корня с записью при профиле без `write`)
- [ ] 1.5 Тест `profile.test.ts`: сервис разобран; неизвестное поле — отказ; расхождение портов — отказ; сервисы без возможности в whitelist — отказ

## 2. Реализация на чистом Node (`src/host/`)

- [ ] 2.1 Новый модуль сервисов: проба адреса, спавн объявленной команды, ожидание готовности соединением по адресу с таймаутом (design D2), запоминание владения (design D3)
- [ ] 2.2 Повторный запуск работающего сервиса возвращает адрес и не спавнит второй процесс (DSK-7)
- [ ] 2.3 Останов: SIGTERM, затем SIGKILL по истечении grace-периода; невладеемый сервис не снимается (design D3, D4)
- [ ] 2.4 Снятие всех владеемых сервисов при закрытии сессии и при завершении процесса контейнера
- [ ] 2.5 Отказ запуска (процесс умер, готовность не наступила) приходит вызывающему отказом с причиной, а не молчанием
- [ ] 2.6 Собрать операции в мост по whitelist в `bridge.ts`: без возможности `service` полей в поверхности нет

## 3. Контрактный сьют границы (`test/`)

- [ ] 3.1 Сервис-пустышка для сьюта: короткий Node-скрипт, слушающий объявленный порт; ни движка, ни контента (DSK-3)
- [ ] 3.2 Случаи сьюта: запуск отдаёт адрес; повторный запуск не плодит процесс; останов освобождает адрес; закрытие сессии снимает владеемый сервис; занятый адрес возвращается без спавна и на закрытии не снимается; незнакомый идентификатор — отказ
- [ ] 3.3 Проверить, что случаи проходят в гейтовом прогоне поверх чистого Node (`hostBridge.contract.test.ts`) без установленного Electron (DSK-6)

## 4. Клей Electron (`src/electron/`)

- [ ] 4.1 Провести три операции через IPC и preload; поверхность в странице собирается тем же способом, что и остальные возможности
- [ ] 4.2 Снятие владеемых сервисов на закрытии окна и на `before-quit`
- [ ] 4.3 Прогнать тот же сьют в контейнере (`npm run contract:electron`) — вне гейта

## 5. Профиль игры

- [ ] 5.1 Объявить в `apps/game.app.json` сервис стенда демо-арены (`game/demo-ts/bin/demo-serve.mjs`, порт и адрес по design D1) и возможность `service` в whitelist
- [ ] 5.2 Убедиться, что `editor.app.json` не изменён и редакторская сессия сервисов не видит (тест `apps.test.ts`)

## 6. Демо: публикация сессии и кнопка

- [ ] 6.1 Добавить `@game-mvp/desktop-shell` в зависимости `game/demo-ts` (импортируется только слой `bridge`, направление «приложение → контракт», DSK-3)
- [ ] 6.2 Новая реализация рандеву в `app/rendezvous.ts`: поднять объявленный сервис, получить адрес, дальше — как прямой адрес (design D5)
- [ ] 6.3 Кнопка «поднять стенд» в `app/main.ts` по признаку «мост есть и сервис объявлен» (design D6); в вебе кнопки нет, `?server=` работает как прежде
- [ ] 6.4 Показать человеку отказ запуска тем же путём, что и прочие сообщения демо (`showNotice`)
- [ ] 6.5 Тесты демо на новую реализацию рандеву с мостом-заглушкой: адрес попадает в транспорт, отказ доезжает сообщением

## 7. Документация и гейт

- [ ] 7.1 Обновить `desktop/shell-ts/README.md`: таблица профилей (у игры появилась возможность), раздел про сервисы и почему в мост не приходит команда
- [ ] 7.2 Обновить `game/demo-ts/app/README.md`: раздел игры вдвоём получает десктопный путь рядом с `npm run demo:lan`
- [ ] 7.3 `npm run check` зелёный без установленного Electron; `openspec validate --strict` зелёный
