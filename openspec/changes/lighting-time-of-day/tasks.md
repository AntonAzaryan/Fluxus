# Tasks: lighting-time-of-day

## 1. Формат и валидация (engine/assets-ts)

- [x] 1.1 Расширить тип `PresentationLighting` подсекцией `cycle` (`transitionSeconds?`, `phases[]` — `name?`, `seconds`, `ambient?`, `directional?`) — состав закрыт (design D1)
- [x] 1.2 Валидация подсекции: неизвестный ключ адресно, ≥ 2 фаз, `seconds > 0`, `transitionSeconds ≥ 0` и короче слота самой короткой фазы (design У2), запрет теневых полей в фазе; тексты находок на русском (design D4)
- [x] 1.3 Тесты формата: валидные/вырожденные документы, адресность находок, сцена без `cycle` разбирается как прежде

## 2. Разрешение конфигурации (engine/render-ts/src/lighting)

- [x] 2.1 `config.ts`: разрешение фаз в плоские наборы значений света поверх статической части секции (правила дыр PRES-2); документированное умолчание `transitionSeconds`
- [x] 2.2 Тесты разрешения: дыры фазы закрываются статической частью, потолки пресета фаз не касаются (QUAL-1 — только теневые поля)

## 3. Исполнение цикла (engine/render-ts/src/subsystems/lighting.ts)

- [x] 3.1 Состояние цикла в подсистеме: предразрешённые фазы (`THREE.Color` и числа — без аллокаций и парсинга на кадре, REND-26), часы от `realDt`, сброс на первую фазу в `applyConfig`/`applyQuality`/`applyGrid` (REND-32, design D2)
- [x] 3.2 `updateFrame`: продвижение фаз, кроссфейд (lerp цветов/интенсивностей/направления, точечная перестановка позиции и цели — без пересчёта фрустума и bias'ов), зацикливание без скачка
- [x] 3.3 Тени перехода: `staticStale` каждый кадр перехода только при движущемся направлении; равные направления соседних фаз — кэш не трогается; установившаяся фаза событийна (design D3); счётчики стоимости честны (PERF-3)
- [x] 3.4 `DebugLightingState`: фаза (номер, авторское имя, авторские тона), прогресс фазы и перехода (RDBG-2, design D5); декларация качества называет покадровую работу цикла — постоянную часть и перестановку флагов кастеров на кадре перехода (QUAL-3, design У3)
- [x] 3.5 Тесты подсистемы: сцена без цикла байт-в-байт как прежде; переход интерполирует значения на живых источниках; перерисовки кэша — только по D3; правка секции перезапускает цикл (ED-15); подставные часы для воспроизводимости кадра (ED-22, design Risks)

## 4. Сквозная проверка слоёв

- [x] 4.1 Интеграционный тест изоляции presentation (`engine/integration-ts`): сцена с циклом и без — `worldInit` и снапшоты не отличаются ни байтом (PRES-4, сценарий REND-32)
- [x] 4.2 Проверить, что редактор (вьюпорт, валидация документа) получает цикл без правок своего кода; если правки нужны — они дефект шва, разобраться до пуша

## 5. Контент демо

- [x] 5.1 `content/scenes/duel.presentation.json`: `cycle` из четырёх фаз («утро», «день», «закат», «ночь») по 120 с, `transitionSeconds` ≈ 15; палитры по design D6, подгонка глазами через `npm run demo`
- [ ] 5.2 Прогнать `npm run demo` и убедиться: полный круг за 8 минут, кроссфейды плавные, возврат к утру без скачка, переключателя в демо нет и код демо не менялся
      <!-- глазами не проверено: браузера в окружении прогона нет. Механически
      это же пиннится тестом `game/demo-ts/test/demoLighting.test.ts`: круг —
      восемь минут, четыре названные фазы в авторском порядке, возврат к утру
      значение в значение, кода цикла в демо нет. -->

## 6. Спека и гейт

- [x] 6.1 `openspec validate --specs --strict` и `npm run spec-graph -- check` зелёные; ссылки REND-29 ↔ REND-32 ↔ PRES-2 целы
      <!-- Дельта синхронизирована в `openspec/specs/rendering/spec.md`
      (`/opsx:sync`): валидация 37/37, «Граф спек зелёный», находок класса
      «REND-32 не определён» больше нет. -->
- [x] 6.2 `npm run check` целиком; дифф `*.cost.json`, если всплыл, принимать отдельно через `npm run golden:cost` с объяснением в коммите (design Risks)
      <!-- Зелёный целиком после синка: lint:all, spec-graph check и все
      пакетные сюиты, включая `integration-ts/test/specGraph.test.ts`; диффа
      `*.cost.json` нет — стенд стоимости берёт свою секцию света без цикла. -->
