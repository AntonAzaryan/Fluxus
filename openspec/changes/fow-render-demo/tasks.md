## 1. Формат: секция fog presentation-документа (PRES-2, FOW-10)

- [x] 1.1 Определить состав секции `fog` (сила затемнения, цвет, ширина градиента края, коэффициент консервативности, разрешение маски, длительность fade) и документированные дефолты; расширить валидацию presentation-документа в `engine/assets-ts` (`validatePresentationScene` — единственная реализация) (закрытый состав, адресные отказы, небулевы/нечисловые значения) — тесты на принятие, отказ по неизвестному ключу, отсутствие секции
- [x] 1.2 Проверить round-trip канонического сохранения (ED-21) с секцией `fog` и что импорт Blender не трогает её байт-в-байт (BLND-2) — тест в `tools/blender-ts` на документ с fog-секцией

## 2. Маска видимости (FOW-7, FOW-9)

- [x] 2.1 Модуль маски в `engine/render-ts`: world-space grayscale канвас по прямоугольнику террейна, reveal-круг с радиальным градиентом настраиваемой ширины, консервативный коэффициент радиуса; юнит-тесты на геометрию (в круге светло, вне — темно, градиент монотонен)
- [x] 2.2 2D shadow-casting: обрезка reveal-полигона тенями cliff-отрезков (`TerrainGrid.cliffs`) в радиусе наблюдателя; тесты: точка за обрывом в радиусе — под туманом, в обход обрыва — открыта, расхождение — в сторону тумана
- [x] 2.3 Отбор наблюдателей из `TickView` по статам (радиус обзора + команда), фильтр по команде игрока — тест: круг видимого врага в маску не попадает

## 3. Подсистема fog в рендере (FOW-7, FOW-10)

- [x] 3.1 Подсистема `fog` за контрактом REND-8: владеет маской, конфигом и пост-проходом; сцена в render target, полноэкранный проход с реконструкцией мировых XY по глубине, сэмпл маски, затемнение силой/цветом из конфига; без активного тумана конвейер прежний (прямой рендер)
- [x] 3.2 Вход обновления конфига в рантайме без пересборки (FOW-10) — тест: смена силы затемнения применяется, подсистема не пересоздаётся
- [x] 3.3 Регистрация подсистемы в сборке игрового клиента (не в вьюпорте редактора); проверить сосуществование с picking и floorMirror

## 4. Fade «туман ≠ смерть» (FOW-8)

- [x] 4.1 В подсистеме моделей: исчезновение сущности из доставленного состояния без `EntityDied` → fade-out длительностью из конфига (инстанс доживает до конца анимации), появление → fade-in, смерть → существующий путь; тесты на все три ветки

## 5. Миникарта (HUD-6)

- [x] 5.1 Продюсер маски отдаёт стабильный объект «канвас + прямоугольник мира»; виджет миникарты в `engine/hud-ts` рисует его поверх подложки с силой затемнения из того же конфига; тест: зона вне обзора затемнена, при отключённом тумане слой отсутствует

## 6. Включение FoW в демо

- [x] 6.1 Контент: `fog: true` и `Vision` героя (radius `1572864` — полкарты) в `duel.scene.json`, `visibility: {}` в `duel.match.json`, секция `fog` в `duel.presentation.json`; статы радиуса обзора и команды в конфиге экстрактора демо
- [x] 6.2 Демо загружает парный presentation-документ: `decorations` → `DecorationSet` (декорации появляются в демо-кадре), `fog` → подсистема; работает в обоих режимах оболочки (SHELL-8: локальная симуляция и сетевой клиент)
- [ ] 6.3 Прогон демо (`npm run demo` и `npm run demo:serve`): туман виден, край плавный, за обрывом темно, враг в тумане отсутствует и появляется через fade-in, миникарта затемнена; скриншот-проверка глазами
  - headless-часть выполнена: dev-сервер и стенд поднимаются, петля «фильтрация → доставка → маска» закрыта тестами (`demoNet.test.ts`, `fog.test.ts`); сама скриншот-проверка глазами ждёт окружения с браузером

## 7. Гейт

- [x] 7.1 `npm run check` зелёный; убедиться, что golden-наборы не затронуты (фикстуры движка не читают `content/`, CONT-4) — иначе разобраться, а не перегенерировать вслепую
