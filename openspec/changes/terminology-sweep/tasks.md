## 1. Предусловия синхронизации

- [ ] 1.1 Убедиться, что `spec-terminology` синхронизирован: `openspec spec show snapshot-rewind` содержит SNAP-1 в редакции с `WorldState`/`SimulationState`
- [ ] 1.2 Убедиться, что `filter-ownership` синхронизирован: в `netcode` есть NET-18 и NET-12 в редакции с абзацем про сущность без `Visibility`
- [ ] 1.3 Убедиться, что `shell-modes` синхронизирован: в `client-shell` есть SHELL-8 и SHELL-1 в редакции «всё, что не рендер, не ввод и не UI»
- [ ] 1.4 Только после 1.1–1.3 синхронизировать этот change: дельты NET-12 и SHELL-1 построены на текстах соседей и в обратном порядке затрут их

## 2. Применение дельт

- [ ] 2.1 `fog-of-war` FOW-4: `WorldView` заменён read-only частью `SystemContext` (SYS-5), добавлен сценарий про попытку прямого чтения памяти мира
- [ ] 2.2 `rendering` REND-1: `dispatch()` заменён ссылкой на окно OBS-3, добавлен сценарий про удержание `TickResult` до кадра
- [ ] 2.3 `netcode` NET-12: `SimulationState` вместо `WorldState`, абзац о двух значениях слова «снапшот» со ссылкой на NET-18, сценарий про попытку продолжить симуляцию из персонального снапшота
- [ ] 2.4 `client-shell` SHELL-1 и SHELL-2: `SimulationState` вместо `WorldState` во всех вхождениях, включая сценарии
- [ ] 2.5 `snapshot-rewind` REW-2: перечисление заменено именем объёма с изъятием машины состояний мира, добавлены сценарии про режим при повторном `seekTo` и про спавн после перемотки
- [ ] 2.6 `determinism-core` DET-1: в п. 3 названо имя объёма при сохранённом перечислении, абзац изъятий связан с SNAP-1

## 3. Проверка полноты подметания

- [ ] 3.1 `grep -rn "WorldView\|dispatch()" openspec/specs/` даёт пусто
- [ ] 3.2 Каждое вхождение `WorldState` в `openspec/specs/` перепроверено на объём: широкое прочтение — дефект, узкое — норма
- [ ] 3.3 `openspec validate --specs --strict` проходит
- [ ] 3.4 Ни одного нового номера требования не занято: дельты состоят только из `## MODIFIED Requirements`

## 4. Хвосты соседних change'ов

- [x] 4.1 `editor-render-source`: его дельта REND-1 несёт `dispatch()` и при повторной синхронизации откатит 2.2 — заархивировать либо подтянуть текст (работа владельца change'а)
  - Закрыто архивацией 2026-08-08: `editor-render-source`, а следом `editor-terrain-source` заархивированы, их дельты в очереди больше не лежат. Обратный хвост, который эта архивация завела, устранён здесь же: дельта REND-1 настоящего change'а перестроена на слитый текст главной спеки — оба пункта закрытого списка про REND-14, «доставка сетки террейна» в перечне точек приёма и сценарий «Правленый документ доезжает до подсистемы без модуля ассетов» сохранены, поверх них лежат ровно правки задачи 2.2.
  - Второй раз то же самое 2026-08-09: архивация `presentation-scene-layer` подняла в главную спеку третью правку REND-1 — пункт про набор decoration-инстансов (REND-18) в закрытом списке несимуляционных входов и фразу о том, что величины парного документа (`presentation-scene` PRES-3) конверсии не требуют. Дельта перестроена ещё раз, теперь по этому тексту; поверх него по-прежнему лежат ровно правки задачи 2.2 — окно валидности через OBS-3 вместо `dispatch()` и сценарий «Рендер удержал TickResult до кадра».
- [ ] 4.2 `entity-id-state`: его дельта DET-1 совпадает с доправочной редакцией и при повторной синхронизации откатит 2.6 — то же лечение
- [ ] 4.3 Проверить, что правок кода не потребовалось: `engine/core-ts/src/types.ts` уже различает `WorldState` и `SimulationState`, поведение ни одного из семи требований не изменилось
