# Tasks: sec04-targeting-shape-gate

Требование — `specs/ability-system/spec.md` (**ABIL-5**), разбор решений — design.md (D1–D7). Гейт один и он
локальный: `npm run check` из корня репозитория (CI нет).

Изменение заводилось по факту сделанной работы: валидация спек против кода
(`docs/reviews/spec-validation-2026-09-01/04-gameplay.md`) нашла, что фигура шага не участвует ни в одной
проверке симуляции, а конус демо-сцены описан в документе дважды.

## 1. Дельта требования (D1–D6)

- [x] 1.1 `specs/ability-system/spec.md`: MODIFIED ABIL-5 — абзац о фигуре переписан (якорь и ориентация,
      что сужает направленная фигура, чего не сужает ненаправленная, граница с `range`, вырожденные случаи);
      абзац о проверке на подтверждении получил оговорку «фигуру эта проверка не повторяет»; два новых
      сценария. Блок MODIFIED несёт требование ЦЕЛИКОМ со всеми действующими сценариями.
- [x] 1.2 Перенести дельту в `openspec/specs/ability-system/spec.md`. Изменение НЕ архивируется: остаётся в
      очереди как запись о принятом решении. Проверка — `npx openspec validate --specs --strict` и
      `npm run spec-graph -- check`.

## 2. Ядро: гейт фигуры (D2, D4, D6, D7)

- [x] 2.1 `engine/core-ts/src/systems/abilities/runtime.ts`: `StepShapeGate` — связывание фигуры с тиком
      накопления (размеры выражениями определения, ось от начала шага к точке прицела) и предикат
      принадлежности точки; долгоживущий объект, аллокаций на кандидата нет (ABIL-10).
- [x] 2.2 Там же: `CandidatePicker.nearest` отсеивает кандидатов гейтом до предиката контента; позиция
      кандидата читается один раз.
- [x] 2.3 `engine/core-ts/src/systems/abilities/targeting.ts`: гейт — поле системы, связывается перед выбором
      сущности шагом `unit`.
- [x] 2.4 `engine/core-ts/test/abilitySystems.test.ts`: блок «Фигура шага сужает выбор сущности (ABIL-5)» —
      цель внутри конуса, вне раствора, за спиной, дальше радиуса фигуры и фигура без полуугла.

## 3. Контент (игровые данные)

- [x] 3.1 `content/scenes/duel.scene.json`: у способности `capture` из `filter` убран рукописный конус
      (скалярное произведение против `cos(5461)`); предикат остался о компонентах, раствор и радиус живут в
      `shape`.

## 4. Гейт

- [x] 4.1 Тесты `engine/core-ts`, `engine/integration-ts`, `game/demo-ts`; `npm run typecheck`,
      `npm run lint`, `npm run lint:dead`, `npm run spec-graph -- check`,
      `npx openspec validate --specs --strict` и `--changes --strict`.
