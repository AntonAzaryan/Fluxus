# Tasks: character-movement

## 1. Terrain: уровни сторон в отрезках обрывов

- [x] 1.1 Расширить производную геометрию обрывов в `engine/core-ts/src/systems/terrain.ts`: каждый отрезок несёт уровни обеих сторон (`levelNeg`/`levelPos` по D6); тесты terrain на значения уровней в отрезках (TERR-5)

## 2. Physics: маски, TimeScale, cliff-гейт, Overlap

- [x] 2.1 `StaticCollider` получает `layer` и (для обрывов) уровни сторон; `staticsFromTerrain` заполняет их; слой статики — параметр сборки (PHYS-2, D5, D6)
- [x] 2.2 Блокировка по маскам в `PhysicsSystem`: препятствия фильтруются по `(obstacle.layer & mover.blockMask) ≠ 0` вместо тега `blocksMovement`; юнит-тесты: маска 0 — насквозь, сущность-с-сущностью, условная блокировка мутацией маски (PHYS-2, PHYS-8)
- [x] 2.3 TimeScale в интеграции: шаг оси = `fixed.mul(velocity, timeScale(entity))`, редукция `TimeScaleModifiers` с дефолтом 1.0; юнит-тест «TimeScale 0.5 — половина дистанции, Velocity не мутирован» (PHYS-8, D4)
- [x] 2.4 Направленный cliff-гейт: при `cliffRise > 0` подъём `rise ≤ cliffRise` проходит, спуск свободен; при 0 — блок в обе стороны; юнит-тесты на все четыре сценария PHYS-11
- [x] 2.5 Sensor-пересечения: `hitMask` по фактически исполненному свипу, событие `Overlap` (mover, other/STATIC_COLLIDER), ≤1 на пару за тик, порядок статика→динамика по индексу; юнит-тесты: насквозь с событием, быстрый снаряд через маленькую цель, событие каждый тик контакта (PHYS-12)

## 3. Locomotion: новая нативная система

- [x] 3.1 `engine/core-ts/src/systems/locomotion.ts`: конструктор с опциями (имена компонентов, биты кнопок, order), запрос по конфиг-компоненту, разгон/торможение к желаемой скорости без осцилляции (LOC-1, LOC-2); юнит-тесты: старт, остановка, разворот
- [x] 3.2 Машина состояний Dodge/Window/Roll/Airborne в компонентах (D1): фронты кнопок по `buttons`/`prevButtons`, игнор уклона без направления, даблтап-окно, направление на момент нажатия (LOC-3, LOC-4); юнит-тесты: одиночный уклон, даблтап-перекат, истечение окна, уклон стоя, прыжок из Roll игнорируется
- [x] 3.3 Прыжок: вход в Airborne ставит override уровня (ARENA-6) и `cliffRise = jumpHeight`, приземление снимает оба; юнит-тесты: перелёт дыры, приземление в дыру (`FellThroughFloor` на тике посадки), запрыгивание на уступ, уступ выше допуска (LOC-5)
- [x] 3.4 Тайминги по глобальным тикам — без опт-ина в TimeScale; тест «перекат в куполе 0.5: те же тики, половина дистанции» (LOC-6); регистрация в `systems/registry.ts`, экспорт из `src/index.ts`

## 4. Контент и схемы

- [x] 4.1 `content/scenes/duel.scene.json`: компоненты `Locomotion`/`LocomotionState`, маски в `Collider`, удаление JSON-системы `Movement`, `Fireball` с коллайдером `blockMask=0`/`hitMask` через физику, `FireballFlight` — только декремент `Lifetime` (D7); прогон сцены через `bin/sim.mjs`
- [x] 4.2 `npm run schemas` — перегенерация JSON-схем

## 5. Интеграция, эталоны, гейт

- [ ] 5.1 Интеграционные тесты в `engine/integration-ts`: конвейер Input → Locomotion → Physics на живой сцене (движение с разгоном, перекат, прыжок через дыру, снаряд насквозь с `Overlap`)
- [x] 5.2 `npm run golden` — перезапись матчей и эталонов, дифф эталонов в коммит (CLI-10)
- [ ] 5.3 `npm run check` зелёный; ревью изменений ядра по чек-листу determinism-review
