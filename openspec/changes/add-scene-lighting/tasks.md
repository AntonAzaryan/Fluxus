## 1. Конфиг и формат данных

- [x] 1.1 `engine/render-ts/src/lighting/config.ts`: типы секции `lighting`, `resolveLightingConfig(section?)`, `DEFAULT_LIGHTING_CONFIG` — умолчания один в один со снимаемым хардкодом (ambient 0.65, направленный 1.7, режим `none`); юнит-тесты умолчаний и разбора (REND-27)
- [x] 1.2 Валидация секции в `editor/core-ts` (закрытый состав, адресный отказ на неизвестный ключ и нетиповое значение, режим только `none`/`hybrid`/`full`) + канонический порядок секции при сохранении (ED-21); тесты валидации на обоих локалях (REND-27, PRES-2)

## 2. Подсистема освещения

- [x] 2.1 `LightingSubsystem` в `engine/render-ts`: источники ambient + направленный из конфига, регистрация в `PresentationStage`, `applyConfig` для живой правки (ED-15); тест «сцена без секции — те же источники-умолчания» (REND-27)
- [x] 2.2 Режимы теней: `none`/`full` на одном источнике; `hybrid` — пара S/D с разделением кастеров по фазе кадра (флаг `castShadow`; слои теневой проход three не читает — см. design D2), кэш S с `autoUpdate=false` и швом `invalidate` (события: загрузка, правка декораций, смена света, правка сетки террейна) (REND-28, design D2)
- [x] 2.3 Классификация кастеров данными: ярус инстанса из происхождения (syncTick/syncDecorations) и анимации записи манифеста; ярус кастера в ключе батча; `receiveShadow` у террейна и моделей; тесты классификации и однородности батча (REND-28, design D3)
- [x] 2.4 `customDepthMaterial` батчевого яруса на общем VAT-chunk'е (design D4) — тень следует клипу, не позе покоя (REND-28)

## 3. Качество и стоимость

- [x] 3.1 Декларация ручек `lighting.shadowMode` (потолок по рангу `none` < `hybrid` < `full`) и `lighting.shadowMapSize` (числовой потолок); `applyQuality` в подсистеме; тесты `min(авторское, потолок)` (QUAL-1, QUAL-3, REND-28)
- [x] 3.2 Счётчики стока стоимости: кастеры по ярусам за кадр, перерисовки кэша S; `npm run golden:cost` и дифф бейзлайнов в коммит (PERF-2, PERF-3, design D7)

## 4. Потребители

- [x] 4.1 Демо: снять хардкод света из `main.ts`, включить `renderer.shadowMap` (PCFSoft), отдать секцию документа подсистеме тем же швом, что `fog`; значения ручек в `presets/{performance,balanced,ultra}.json` (design D5, D8)
- [x] 4.2 Вьюпорт редактора: снять хардкод из `sceneStage.ts`, свет — подсистемой; живое применение правки секции; проверка тождества кадра клиента и вьюпорта на сцене без секции (ED-22)

## 5. Гейт и спеки

- [x] 5.1 Интеграционные сценарии: hybrid на украшенной сцене (статика в кэше, перерисовка событием при правке декорации), потолок пресета поверх авторского `full` (REND-28)
- [x] 5.2 `npm run check` целиком (typecheck, eslint, knip, jscpd, depcruise, spec-graph, тесты); `npm run bench:demo` глазами на тени как смок (PERF-7, вне гейта)
- [ ] 5.3 Синк дельт в основные спеки и архив изменения (`/opsx:sync`, `/opsx:archive`)
