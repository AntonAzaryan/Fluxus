# Tasks: blender-level-design-integration

## 1. Документы и норма

- [ ] 1.1 Импортировать отчёт-обоснование в `docs/reviews/2026-08-09-blender-level-editor.md` (текст PDF «Blender as a Level Editor for Fluxus», ненормативно, с пометкой источника)
- [ ] 1.2 Написать `tools/blender-ts/CONVENTIONS.md`: фиксированная LTS-версия Blender, «+Y Up», 1 юнит = 1 м, соответствие осей `(x, y) ← (x, −z)`, `yaw` вокруг Y долями оборота, применённые трансформы, схема custom properties (`prefab`, `visual`, `<Компонент>.<поле>`, `skin`)
- [ ] 1.3 Сверить дельты спек с главными спеками (`openspec validate --strict`, `npm run spec-graph -- check` после архивации — ссылки BLND-* ↔ PRES-*/CONT-*/ED-*)

## 2. Пакет импортёра

- [ ] 2.1 Завести workspace-пакет `tools/blender-ts` (`@game-mvp/blender-ts`): tsconfig по образцу editor-core, зависимости `@game-mvp/editor-core` и `@game-mvp/core`, скрипты `typecheck`/`lint`/`test`, включение в корневые `check`/`test`
- [ ] 2.2 Разбор glTF: `.gltf` (JSON) и `.glb` (контейнер → JSON-чанк), только `nodes`/`scenes` — имя, трансформ (матрица либо TRS), `extras`; бинарные буферы игнорируются (design, решение 2)
- [ ] 2.3 Нормализация: декомпозиция матрицы, конверсия осей по CONVENTIONS, извлечение `yaw`, коэрция типов `extras`; тест-фикстура пиннит знаки соответствия осей (design, решение 5)
- [ ] 2.4 Генерация целевого слоя: узлы с `prefab` → записи `initial` (позиция/курс переопределением компонентов из настройки проекта, как у ED-16; прочие `<Компонент>.<поле>` → `overrides`), узлы с `visual` → записи `decorations` (позиция, `yaw`, `scale`, `skin`); порядок — лексикографически по имени объекта (BLND-4); квантование Q16.16 / шаг PRES-3 (BLND-3, BLND-4)
- [ ] 2.5 Ошибки: объект с `prefab` и `visual` сразу, неизвестное имя prefab'а, компонент/поле вне схемы, значение вне Q16.16 — отказ с именем объекта Blender до записи (BLND-6); `visual` без записи манифеста — предупреждение, не отказ

## 3. Операция импорта в editor-core

- [ ] 3.1 Зарегистрировать операцию `blender.importSpatialLayer` вкладом (ED-25, ED-29): вход — целевой слой, действие — переписать `initial` конфига сцены и `decorations` парного документа, не трогая прочих полей байт-в-байт (BLND-2); операция обратима (ED-18) и видна в каталоге (ED-30)
- [ ] 3.2 Атомарность: находки важности «ошибка» на результате — запись не выполняется целиком (BLND-6); тест «ошибка на середине — на диске ни байта»
- [ ] 3.3 CLI-вход: `npm run import -- <путь к .glb|.gltf>` из корня — открывает проект хостом в памяти/файловым, исполняет ту же операцию, сохраняет каналом ED-21; тест «CLI и операция дают байт-в-байт одно» (BLND-5)
- [ ] 3.4 Тесты детерминизма: повторный импорт — пустой дифф; перемещение объекта — только его переопределения; переименование — порядок и ID меняются законно (BLND-4)

## 4. Правило валидации сопряжённой сцены

- [ ] 4.1 Читаемость источника через хост среды: `.glb` рядом со сценой доступен по правилу имени (BLND-2); в средах без чтения бинарей — честный отказ правила, а не пустой результат (образец — ED-12/ED-20)
- [ ] 4.2 Правило-вклад `blender.spatialLayerSync`: dry-run импорт источника в память, сравнение с документами; расхождение — предупреждение с адресом «слой производный, выполните импорт» (BLND-2, design решение 6); кэш по байтам источника
- [ ] 4.3 Тесты: правка `initial` сопряжённой сцены подсвечена; сцена без источника — ни находки, ни чтения; расхождение после правки `.blend` (новый `.glb` в фикстуре) подсвечено

## 5. Фикстуры и сквозная проверка

- [ ] 5.1 Закоммитить фикстуры: рукописные `.gltf` (минимальная сцена: empty+`prefab`, mesh+`visual`, вспомогательный объект, ошибочные случаи) и `.glb`, собранный скриптом из того же JSON; фикстуры живут в `tools/blender-ts/test/fixtures/`, не в `content/` и не в `engine/` (BLND-7, CONT-4)
- [ ] 5.2 Сквозной тест: импорт фикстуры → документы валидны загрузчиком сцены и модулем ассетов, `worldInitHash` совпадает при повторном прогоне, прогон сцены с decorations и без парного документа — побитово те же эталоны (PRES-4)
- [ ] 5.3 Проверить гейт в среде без Blender: `npm run check` зелёный, Blender не зовётся нигде (BLND-7)
- [ ] 5.4 [вручную, автор с Blender] Собрать `content/scenes/duel.blend`, воспроизводящий текущую расстановку `duel`; экспорт, импорт, сверка: дифф `initial`/`decorations` пуст либо объяснён; закоммитить `.blend`+`.glb` (design, решение 8)

## 6. Ревизия смежного

- [ ] 6.1 Править `openspec/changes/terrain-texturing/proposal.md`: ассет+рендер остаются, клауза «кисть заливки в editor» заменяется кандидатом «раскраска в Blender → конвейер» с решением в его design (design, решение 9)
- [ ] 6.2 Обновить `docs/architecture.md`: строка этапа 12 — «направление level-дизайна заморожено, превью и data-driven-области живут»; новый этап конвейера Blender со ссылкой на `blender-pipeline`
- [ ] 6.3 Обновить `CLAUDE.md`: пакет `tools/blender-ts`, команда `npm run import`, число capability/требований, абзац о Blender как главном инструменте level-дизайна
- [ ] 6.4 Финальная сверка: `npm run check`, `openspec validate --strict`, дифф ветки глазами
