# Tasks: bench-stand-subsystems

## 1. Счётчики подсистем (PERF-3)

- [x] 1.1 Модели: выборы LOD, пересборки инстансов, записи поз/атрибутов, отсечённые инстансы (`subsystems/models.ts`); стадийные теги, ноль работы без стока; тесты пропорциональности по числу инстансов и LOD-множителю
- [x] 1.2 Частицы: синхронизации эмиттеров на доставке, шаги оболочек на кадре (`subsystems/particles.ts`) — без чтения внутренностей three.quarks (D3); тест пропорциональности по числу эффектов и плотности
- [x] 1.3 Террейн: перемаркированные чанки, тесселированные ячейки (`subsystems/terrain.ts`); тест пропорциональности по площади и потолку тесселяции
- [x] 1.4 `cost.ts`/`COST_COUNTER_STAGES` расширены; тест тождества наборов ключей зелёный

## 2. Стенд и эталоны (PERF-6, QUAL-4)

- [x] 2.1 Стенд `benchLoad.ts` регистрирует models/particles/terrain с контент-фикстурами (D2); счётчики не мертвы на записанных матчах
- [x] 2.2 Оси масштабирования: ось инстансов двигает счётчики моделей; ось эффектов/плотности — частиц; потолок тесселяции — террейна
- [x] 2.3 Пресеты стенда различаются на новых счётчиках моделей и террейна (ярус, LOD-множитель, тесселяция) — QUAL-4 перестаёт быть fog-only; плотность частиц счётчиков не двигает по построению (D3: эмиссия — стохастика внутри three.quarks, её цена — зона wall-time и браузерного бенча), и это заасерчено явно
- [x] 2.4 `npm run golden:cost`: перегенерация, идемпотентность, двойной прогон побитово; дифф эталона в коммите

## 3. Гейт

- [ ] 3.1 Ревью отдельным субагентом, правки
- [ ] 3.2 `npm run check` зелёный; поведенческие голдены не тронуты; архив change
