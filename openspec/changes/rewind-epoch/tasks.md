## 1. Протокол

- [ ] 1.1 `engine/net-ts/src/protocol/messages.ts`: поле эпохи у `Snapshot` (рядом с `tick`) и у сообщения `Input` (одно на сообщение, не на `InputFrame` — контракт ядра TICK-2 не трогается)
- [ ] 1.2 Там же: валидация полей эпохи на разборе кадра тем же способом, что и `tick` (целое, неотрицательное); неизвестное или отсутствующее поле — разрыв соединения по NTR-4, а не умолчание
- [ ] 1.3 `engine/net-ts/src/protocol/codec.ts`: порядок ключей плоской формы (SER-6) с новыми полями; проверить, что JSON- и MessagePack-режимы дают одну раскладку
- [ ] 1.4 Конфиг матча: параметр `inputWindow` рядом с `tickRate`/`snapshotRate`/`inputDelay`, проверка `inputWindow >= inputDelay` на загрузке конфига; передача в `Welcome` в составе параметров темпа

## 2. Сервер: эпоха и рассылка

- [ ] 2.1 `engine/net-ts/src/server/matchServer.ts`: пара `(эпоха, тик)` последнего разосланного состояния; увеличение эпохи ровно там, где формируется рассылка, по правилу «тик меньше предыдущего» (NTR-16) — одно место, а не наблюдение за переходами WSM
- [ ] 2.2 Рассылка авторитетного состояния по факту восстановления во время `Rewinding` (мимо расписания `snapshotRate`), с персональной фильтрацией по видимости целевого тика (NTR-9, REW-11)
- [ ] 2.3 Штамповка эпохи в `Snapshot` каждому клиенту

## 3. Сервер: приём ввода

- [ ] 3.1 Проверка эпохи входящего `Input`: расхождение — отброс и счётчик вышедших за окно (NTR-11), нового счётчика не заводить
- [ ] 3.2 Сброс накопленного неприменённого ввода всех слотов при смене эпохи
- [ ] 3.3 Верхняя граница окна: `tick > currentTick + inputWindow` — отброс и тот же счётчик; проверить, что буфер неприменённого ввода на слот ограничен конфигом
- [ ] 3.4 Нижняя граница: тик, уже исполненный в текущей эпохе, — отброс и счётчик опозданий (обоснование переформулировано, правило прежнее)
- [ ] 3.5 Дедуп «первый выигрывает» по паре (слот, тик) в пределах эпохи; отброшенный повтор молча и без счётчиков
- [ ] 3.6 Predicted-фрейм после смены эпохи повторяет фрейм, применённый на тике восстановления, а не последний полученный сервером

## 4. Сервер: канонический лог

- [ ] 4.1 Лог матча — последовательность сегментов `{ эпоха, фреймы }`; сегмент открывается первым исполненным тиком новой эпохи, эпоха без исполненных тиков сегмента не порождает
- [ ] 4.2 Матч без перемотки даёт ровно один сегмент; проверить, что существующая запись и её потребители не изменились
- [ ] 4.3 Прогон записи с сегментами: перед первым фреймом сегмента состояние восстанавливается на предшествующий тик; путь восстановления (снапшот+реплей или прогон от `worldInit`) — выбор прогонщика

## 5. Клиент

- [ ] 5.1 `engine/net-ts/src/client/matchClient.ts`: отбрасывание снапшота по лексикографическому сравнению пары вместо сравнения по тику
- [ ] 5.2 Смена эпохи: сброс буфера интерполяции (`interpolation.ts`), применение состояния snap'ом, признак разрыва в доставке оболочке (SHELL-7)
- [ ] 5.3 Пересинхронизация оценки серверного тика по первому снапшоту новой эпохи (без постепенного подтягивания)
- [ ] 5.4 `inputRing.ts`: хранение эпохи рядом с фреймом; фреймы стёртой эпохи не переотправляются, но остаются для диагностики
- [ ] 5.5 Штамповка эпохи последнего применённого снапшота в отправляемое сообщение `Input`

## 6. Тесты

- [ ] 6.1 Тест правила эпохи на последовательности состояний, включая скраб вперёд внутри эпохи (500 → 480 → 490 → 470) и повторную рассылку того же состояния
- [ ] 6.2 Тест клиента: снапшот перемотки применяется, устаревший той же эпохи отбрасывается, равная пара отбрасывается
- [ ] 6.3 Тесты приёма ввода: далёкий будущий тик, исполненный тик текущей эпохи, чужая эпоха, дубль фрейма — раздельно по счётчикам NTR-11
- [ ] 6.4 Тест predicted-фрейма после перемотки: повторяется фрейм тика восстановления
- [ ] 6.5 `engine/integration-ts`: loopback-матч с перемоткой — запись, сегментированный лог, прогон записи, побитовое совпадение состояний (парность NTR-8)
- [ ] 6.6 Тест обратной совместимости: матч без перемотки даёт один сегмент и прежние состояния; существующие записанные матчи не двигаются

## 7. Проверка

- [ ] 7.1 `npm run typecheck` и `npm test` из корня — зелено
- [ ] 7.2 Убедиться, что `engine/core-ts` не менялся и golden-эталоны не двинулись: эпоха в мир не попадает
- [ ] 7.3 `openspec validate --specs --strict` после `/opsx:archive`
