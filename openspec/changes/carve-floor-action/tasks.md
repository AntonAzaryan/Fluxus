## 1. Механизм в ядре

- [ ] 1.1 `engine/core-ts/src/types.ts`: добавить в `TerrainApi` идентификатор сущности-носителя карты пола; заполнить его в `createTerrainApi` (`systems/terrain.ts`)
- [ ] 1.2 `engine/core-ts/src/systems/terrain.ts`: функция снятия пола по TERR-8 — bounding box области, пересечение с сеткой, построчный обход, круг по центрам клеток через тот же `distSqLe`, что фильтр `withinRadius`; без радиуса — `cellAt`
- [ ] 1.3 Там же: чтение слова через `commands.peekField(...) ?? ctx.get(...)` (CMD-5), одна команда на слово и только при изменившемся значении, порядок команд по возрастанию индекса слова
- [ ] 1.4 Сцена без террейна — ошибка с внятным текстом (TERR-8)

## 2. DSL

- [ ] 2.1 `engine/core-ts/src/dsl/actions.ts`: действие `carveFloor { at, radius }` — `at` вектор, `radius` необязателен; разбор аргументов и вызов механизма из терреин-модуля
- [ ] 2.2 `engine/core-ts/src/dsl/expr.ts`: оператор `hasFloorAt [position]`; расширить `ExprWorld` полем `terrain` (sandbox EXPR-3 не ослабевает — `TerrainApi` только читает)
- [ ] 2.3 Проверить, что валидация дерева на регистрации (SYS-3) принимает новое действие и оператор без правок таблиц-дублей
- [ ] 2.4 `npm run schemas` из `engine/core-ts`, дифф `engine/schemas/*.json` — в коммит

## 3. Тесты ядра

- [ ] 3.1 Тесты действия: круг по центрам клеток (включительная граница), отсутствующий радиус = одна клетка, повторное снятие не порождает команды, два снятия в одном тике дают объединение
- [ ] 3.2 Тесты границ: круг, не пересекающийся с сеткой, — no-op; точка вне сетки без радиуса — ближайшая клетка; сцена без террейна — ошибка у действия и у оператора
- [ ] 3.3 Тест оператора `hasFloorAt`: чтение до и после flush в одном тике, тотальность за краем сетки
- [ ] 3.4 Тест отката: снапшот до снятия, перемотка, пол на месте (TERR-8, SNAP-1)

## 4. Эталон

- [ ] 4.1 Завести golden-пару `engine/tests/golden/carve-floor.scenario.json` / `.golden.json`: маленькая сетка, снятие с радиусом на несколько клеток плюс перекрывающееся повторное снятие
- [ ] 4.2 `npm run golden` из корня; убедиться, что прочие эталоны не двинулись от этой работы

## 5. Контент и демо

- [ ] 5.1 `content/scenes/duel.scene.json`: JSON-система `FireballImpact` (order 50) — обход снарядов с истёкшим `Lifetime`, `carveFloor` по позиции, `emitEvent FireballExploded { x, y }`, `destroyEntity`
- [ ] 5.2 `engine/client-ts/demo/sim.ts`: удалить нативную `FireballImpact`, локальную адресацию слова пола (`floorWordField`, `floorWordTotal`) и её регистрацию; переписать шапку модуля — нативными остаются `InputSystem` и `PhysicsSystem`
- [ ] 5.3 Проверить, что демо всё ещё импортирует из ядра только нужное (`FLOOR_COMPONENT`/`cellAt` больше не нужны)

## 6. Проверка

- [ ] 6.1 `npm run typecheck` и `npm test` из корня — зелено
- [ ] 6.2 `npm run golden` из корня после всех правок контента; дифф разобран
- [ ] 6.3 Прогнать демо вручную: снаряд оставляет дыру в полу, герой в неё проваливается (`FellThroughFloor`, ARENA-5), перемотка возвращает пол
