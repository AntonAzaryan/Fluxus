## 1. Документ бот-аннотаций

- [x] 1.1 Форма и валидация документа `<база>.bots.json` в `game/demo-ts` (D1): `schema` v1, ключи — `id` определений сцены, поля `target`/`hands`/`steps[].aim`/`range`/`requiresMoving`/`rise`/`cooldownTicks`; находки по-русски по образцу `profile.ts`; цитаты BOT-12 у кода
- [x] 1.2 `content/scenes/duel.bots.json`: аннотации боевых способностей дуэли (ульта отката отсутствует — D5)
- [x] 1.3 Тесты валидации: валидный документ, неизвестная `schema`, неизвестное поле цели/рук, шаги, отсутствие `cooldownTicks` при кулдауне-выражении

## 2. Модуль вывода механики

- [x] 2.1 Один модуль «определение сцены + аннотация → ожидаемая запись профиля без чисел сложности» (D2): бит триггера, биты подтверждения/отмены (из данных сцены/биндингов, не константами), вид подтверждения из фаз, шаги, `holdTicks`, нижняя граница кулдауна; цитаты BOT-13
- [x] 2.2 Тесты правил вывода на определениях дуэли и синтетических определениях (commit против release, кулдаун-литерал против выражения, способность без прицеливания)

## 3. Сверка

- [x] 3.1 Тест `game/demo-ts/test/botContract.test.ts` (D3): все профили с дуэльной сценой сверены по механике, семантике и кулдаун-инварианту `profile >= scene`; находки перечисляются разом, русским текстом с именем способности и обоими значениями
- [x] 3.2 Негативные проверки: подделанный бит триггера и заниженный кулдаун в фикстуре дают находки (сценарии BOT-13)

## 4. Генерация

- [x] 4.1 CLI `game/demo-ts/bin/bots-sync.mjs` + скрипт `npm run bots:sync` из корня (D4): обновление механики/семантики, скелеты новых записей, merge по полям не трогает числа сложности
- [x] 4.2 Тесты CLI на временном дереве: идемпотентность (второй запуск — ноль изменений), «сложность не тронута», скелет новой способности

## 5. Гейт и увязка

- [x] 5.1 Комментарий у `cooldownTicks` в `engine/bot-ts/src/profile.ts` обновлён: конвенция «чуть выше» стала проверяемым инвариантом сверки в игровом приложении (BOT-13)
- [ ] 5.2 `npm run check` из корня зелёный; сверка существующих профилей с дуэлью зелёная (или найденный рассинхрон починен профилем — Migration Plan)
  - Сверка отгруженных профилей (`easy`, `normal`) с дуэлью зелёная СРАЗУ, как и ожидал Migration Plan: рассинхрона не нашлось, контент не правился.
  - Гейт зелёный весь, кроме `spec-graph check` и `specGraph.test.ts`: они краснеют находками `dangling-code` на BOT-12/BOT-13 — ID существуют только в делте этого изменения, а инструмент видит `openspec/specs/`. Проверено локально: с делтой, влитой в `openspec/specs/bot-player/spec.md`, обе проверки зелёные (41 находка исчезает); файл спеки восстановлен, `openspec/specs/` не тронут. Пункт закрывается синхронизацией делты при архивации.
