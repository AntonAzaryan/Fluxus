## 1. Чтение документов страницей (`game/demo-ts/app`, design D1–D5)

- [ ] 1.1 `match.ts`: убрать статические импорты сцены и документа матча; ввести `DEMO_MATCH_ID`, `DemoDocuments` и `loadDemoDocuments(source: AssetSource, matchId?)` с разрешением `contentPack` относительно ID документа матча (CONT-5, D2); константы модуля (`DEMO_PLAYERS`, `DEMO_REWIND`, `DEMO_NAVIGATION`, `DEMO_SCRUB_EVERY`, `DEMO_TICK_RATE`, `DEMO_SNAPSHOT_RATE`, `DEMO_SCENE_REF`, `DEMO_MATCH`) и синхронные `demoScene`/`demoContentPack`/`demoMatchConfig` заменить функциями от `DemoDocuments` (D3). Проверка: `npm run typecheck -w @fluxus/demo` зелёный после переводa потребителей (1.2–1.6).
- [ ] 1.2 Новый тест загрузчика (`game/demo-ts/test/demoDocuments.test.ts`) над fs-источником `AssetSource` по `content/`: (а) `MatchConfig` загрузчика равен `matchConfigOf(readMatchFile(...), contentPack(...))` запускалки — версия и сцена совпадают; (б) ссылка `../scenes/duel.scene.json` разрешается в ID `scenes/duel.scene.json`, ссылка за корень дерева — названный отказ; (в) отсутствующий документ — отказ с ID документа; (г) правка сцены в источнике меняет хеш у загрузчика и у запускалки одинаково (CONT-5, сценарий «Правка сцены в раздаваемом дереве»). Проверка: тест зелёный.
- [ ] 1.3 `sim.ts`: `createDemoSimulation(def, { navigation })` — навигация параметром вместо `DEMO_NAVIGATION`; шапку про «браузер импортирует его через vite» переписать. Проверка: `demoScene.test.ts`, `demoHud.test.ts` передают `navigation` документа явно и зелёные.
- [ ] 1.4 `wiring.ts`: `DemoServerInit` и `DemoClientInit` несут `documents: DemoDocuments`; новый `DemoSoloInit` с guard'ом `isDemoSoloInit` (D4). Проверка: guard'ы покрыты unit-тестом (принимают свой конверт, отвергают чужой и объект без `documents`).
- [ ] 1.5 `main.ts`: документы читаются один раз через `assetSource` параллельно с манифестом, до спавна воркеров; флаг тумана и каталог способностей превью — из прочитанной сцены; конверты всех трёх воркер-сторон получают `documents`; отказ чтения — `showNotice` с ID документа и причиной, воркеры не спавнятся. Проверка: `npm run typecheck`; ручной прогон `npm run demo` — матч с ботом, `?solo`, `?server=` против `npm run demo:serve` поднимаются; правка `content/scenes/duel.scene.json` без пересборки видна после перезагрузки страницы.
- [ ] 1.6 `serverWorker.ts`, `clientWorker.ts`, `netClient.ts`, `observerClient.ts`, `worker.ts`: конфиг матча, ростер, `rewind`, `scrubEvery`, `snapshotRate` — из документов конверта; `worker.ts` собирает симуляцию только по `DemoSoloInit`. Проверка: `demoNet.test.ts`, `demoObserver.test.ts`, `matchPause.test.ts`, `detachPause.test.ts`, `demoStandRewind.test.ts`, `demoAbilities.test.ts`, `demoHud.test.ts` переведены на fs-фикстуру `demoDocuments()` (в `test/fixtures.ts`) и зелёные.
- [ ] 1.7 Guard-тест источника (`game/demo-ts/test/contentImports.test.ts`, по образцу `engine/integration-ts/test/contentBoundary.test.ts`): ни один модуль `game/demo-ts/app/**` не импортирует `content/scenes/*` и `content/matches/*` статически или динамически; документы ботов в `bots.ts` — названное исключение с причиной (Non-Goals design). Проверка: тест зелёный, а с временно возвращённым `import sceneJson from '../../../content/scenes/duel.scene.json'` — красный с именем модуля (CONT-5, сценарий «Документ контент-пака запекли в сборку»).

## 2. Дистрибутив хоста и агент (`game/server-agent-ts`, design D6–D7)

- [ ] 2.1 `bin/bundle.mjs`: шапка и текст отказа `--client-dist` — новое основание (происхождение кода страницы не покрыто версией рукопожатия, SRV-7); `START.md` дистрибутива — правка дерева доезжает и до сервера, и до страницы, пересборка нужна для новой сборки кода. Проверка: `test/bundle.test.ts` (обновлённые ожидания текста отказа) зелёный; `grep -rn "вкомпилир" game/` пуст.
- [ ] 2.2 `bin/agent.mjs`, `versions()`: `buildId` и хеш контент-пака — всегда из документа матча дерева контента (`readMatchFile` + `contentPack`); из `distribution.json` берётся только имя дистрибутива (D7). Проверка: в `test/bundle.test.ts` — после правки `content/scenes/duel.scene.json` внутри собранного дистрибутива `Welcome` агента называет новый `contentPackHash`, равный `contentPack(readMatchFile(...)).hash` по правленому дереву (SRV-7, сценарий «Правка контента внутри дистрибутива»).
- [ ] 2.3 Комментарии-повествования о «документах в бандле» переписать без потери причины заслонения живого дерева: `src/httpServer.ts` (шапка), `game/demo-ts/app/vite.desktop.config.ts`, `test/bundle.test.ts` (комментарий сценария «страница со стороны»). Проверка: `grep -rn "несёт документы\|вкомпилир\|статическим импортом сцены" game/ desktop/` пуст.

## 3. Гейт и спека

- [ ] 3.1 `npm run check:impact -- --explain`, затем полный `npm run check` — зелёные; `knip` не находит осиротевших экспортов `match.ts` (удалены, а не оставлены), `jscpd` — дублей между загрузчиком демо и `matchFile.mjs`.
- [ ] 3.2 `openspec validate sec09-client-content-from-tree --strict` и `npm run spec-graph -- check` зелёные; в коде процитированы SRV-7 и CONT-5 в местах, которые их реализуют (загрузчик, guard-тест, `versions()` агента).
