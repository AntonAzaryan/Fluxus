## 1. Запекание навигационных данных

- [x] 1.1 Вынести предикат проходимости перехода из `buildCliffs` (`systems/terrain.ts`) в общую функцию и переиспользовать в обоих местах; проверка — существующие тесты террейна зелёные, дубликата предиката нет (`npm run lint:dup`)
- [x] 1.2 Модуль `systems/nav/bake.ts`: passability-маска клеток (noFloor → непроходима) и целочисленная clearance-карта brushfire'ом из `TerrainGrid` (NAV-9); проверка — юнит-тест на ручной карте: зазоры у стены/в коридоре/на открытом поле совпадают с посчитанными вручную
- [x] 1.3 Валидация TERR-7 в сборке: `tileSize < 2 × maxAgentRadius` → находка с обоими значениями; проверка — тест на отвергнутую и принятую сборку

## 2. Поиск и сглаживание

- [x] 2.1 `systems/nav/astar.ts`: A* по 4-связности на переиспользуемых typed arrays с поколенческой очисткой, целые стоимости, манхэттен, tie-break по NAV-8, бюджет раскрытий → `budgetExhausted` (NAV-5); проверка — юнит-тесты: found/unreachable/бюджет, два равных пути дают путь, предписанный NAV-8
- [x] 2.2 Тотальность краёв (NAV-5): снап точек вне сетки к ближайшей клетке правилом TERR-4, старт == цель → `found` + пустой список, старт в непроходимой клетке не бросает; проверка — юнит-тесты каждого сценария NAV-5
- [x] 2.3 Целочисленный LOS по сетке и жадное string pulling (NAV-10): точки — центры клеток в Q16.16, последняя — точная цель, `from` не входит (NAV-1); проверка — юнит-тесты: прямой коридор → одна точка, Г-обход → угол + цель, спорный угол непроходим с обеих сторон
- [x] 2.4 `agentRadius` через clearance с `ceil` в клетки (NAV-9); проверка — тест «проход уже агента»: с радиусом `unreachable`, без радиуса `found` (сценарий NAV-1)
- [x] 2.5 `buildNavigation(grid, { budget, maxAgentRadius }) → NavigationApi` и включение в сборку сцены рядом с `TerrainApi` (`sim/scene.ts`, `sim/build.ts`); проверка — тест `tick.test.ts`: собранный API доходит до `SystemContext`, сцена без навигации тикает штатно (NAV-6)

## 3. Детерминизм

- [x] 3.1 Тест чистоты (NAV-2): повторный запрос на другом тике, после других запросов и на свежесобранном API байт-в-байт совпадает; перемотка не меняет ответов (NAV-3); проверка — тесты зелёные
- [x] 3.2 Прогнать `determinism-review` чеклист по `systems/nav/` (float, Math.*, порядок итераций, аллокации в горячем пути); проверка — находок нет, `npm run lint` зелёный

## 4. NPC movement поверх findPath

- [x] 4.1 Поля держимой точки пути в компоненте агента NPC + `npm run schemas`; проверка — сгенерированные схемы в диффе, снапшот/перемотка возвращают поля (тест по образцу держимого вектора)
- [x] 4.2 Перезапрос пути в окне решений и seek к держимой точке в `followRoute`/`seekTarget` при `ctx.navigation`; не-`found` → сброс и прямой seek; ветка без навигации нетронута (NPC-6 дельта); проверка — юнит-тесты: обход обрыва через рампу, деградация при `unreachable`, сцена без навигации байт-в-байт со старым поведением
- [x] 4.3 Существующие golden-эталоны без навигации не изменились; проверка — `npx vitest run` golden-тестов без перегенерации

## 5. Перф-контур

- [x] 5.1 Счётчик `navNodes` в `TICK_COST` (раскрытия A* + пробы клеток LOS) по образцу `npcNeighbors`: инкремент в контексте диагностики, при отключённом стоке — ноль работы и аллокаций (PERF-3); проверка — юнит-тест: с включённым стоком счётчик равен раскрытиям + пробам детерминированно, повторный прогон побитово совпадает
- [x] 5.2 Навигационный записанный матч в cost-baselines: `npm run golden:cost` с новым сценарием, секция `tick` содержит `navNodes` (PERF-4); проверка — дифф `*.cost.json` в коммите, `npm run check` краснеет от искусственного удорожания поиска (локальная проба, откатить)
- [x] 5.3 Wall-time сторож `findPath` в `bench.test.ts` ядра: синтетическая сетка (лабиринт + открытое поле), оси — размер сетки и число запросов (PERF-6), печать `[bench]` всегда, ассерт только на порядок величины (PERF-5); проверка — тест зелёный, числа печатаются

## 6. Голдены и ворота

- [x] 6.1 Новый golden-сценарий с собранной навигацией (путь через рампу, вокруг обрыва, узкий проход) в `engine/tests/golden/`; проверка — `golden.test.ts` зелёный, пара scenario/golden в коммите
- [x] 6.2 Цитаты NAV-7..10 в коде и тестах на местах реализации; проверка — `npm run spec-graph -- check` без висячих ID
- [x] 6.3 Полные ворота `npm run check` зелёные из корня

## 7. Нити пути в отладочном рендере

- [x] 7.1 Отладочный источник `demo.navPaths` в `game/demo-ts` по контракту `render-debug` (RDBG-1..3): проба — по каждой доставленной сущности с полями держимой точки пути ломаная «позиция → держимая точка» плюс полный путь, пересчитанный в пробе через `findPath` над клиентской сборкой навигации из того же террейн-ассета (NAV-2 — пересчёт побитово равен серверному); рисование — ломаными словаря примитивов; проверка — юнит-тест пробы: сущность с держимой точкой даёт ломаную, без неё — ничего, дамп кадра несёт ту же пробу (RDBG-2)
- [x] 7.2 Выключенный источник бесплатен и не меняет кадр (RDBG-4) и не трогает счётчики стоимости (RDBG-8: пересчёт пути в пробе НЕ инкрементит navNodes — сборка навигации отладочного слоя без стока диагностики); проверка — тест: с выключенным источником проба не считается, TICK_COST/каунтеры кадра идентичны прогону без источника
- [x] 7.3 Регистрация источника в демо-приложении рядом с существующими debug-источниками, включение — существующим механизмом настроек отладки демо; проверка — существующие тесты debug-слоя зелёные, `npm run check` зелёный
