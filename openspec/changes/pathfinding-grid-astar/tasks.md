## 1. Запекание навигационных данных

- [ ] 1.1 Вынести предикат проходимости перехода из `buildCliffs` (`systems/terrain.ts`) в общую функцию и переиспользовать в обоих местах; проверка — существующие тесты террейна зелёные, дубликата предиката нет (`npm run lint:dup`)
- [ ] 1.2 Модуль `systems/nav/bake.ts`: passability-маска клеток (noFloor → непроходима) и целочисленная clearance-карта brushfire'ом из `TerrainGrid` (NAV-9); проверка — юнит-тест на ручной карте: зазоры у стены/в коридоре/на открытом поле совпадают с посчитанными вручную
- [ ] 1.3 Валидация TERR-7 в сборке: `tileSize < 2 × maxAgentRadius` → находка с обоими значениями; проверка — тест на отвергнутую и принятую сборку

## 2. Поиск и сглаживание

- [ ] 2.1 `systems/nav/astar.ts`: A* по 4-связности на переиспользуемых typed arrays с поколенческой очисткой, целые стоимости, манхэттен, tie-break по NAV-8, бюджет раскрытий → `budgetExhausted` (NAV-5); проверка — юнит-тесты: found/unreachable/бюджет, два равных пути дают путь, предписанный NAV-8
- [ ] 2.2 Тотальность краёв (NAV-5): снап точек вне сетки к ближайшей клетке правилом TERR-4, старт == цель → `found` + пустой список, старт в непроходимой клетке не бросает; проверка — юнит-тесты каждого сценария NAV-5
- [ ] 2.3 Целочисленный LOS по сетке и жадное string pulling (NAV-10): точки — центры клеток в Q16.16, последняя — точная цель, `from` не входит (NAV-1); проверка — юнит-тесты: прямой коридор → одна точка, Г-обход → угол + цель, спорный угол непроходим с обеих сторон
- [ ] 2.4 `agentRadius` через clearance с `ceil` в клетки (NAV-9); проверка — тест «проход уже агента»: с радиусом `unreachable`, без радиуса `found` (сценарий NAV-1)
- [ ] 2.5 `buildNavigation(grid, { budget, maxAgentRadius }) → NavigationApi` и включение в сборку сцены рядом с `TerrainApi` (`sim/scene.ts`, `sim/build.ts`); проверка — тест `tick.test.ts`: собранный API доходит до `SystemContext`, сцена без навигации тикает штатно (NAV-6)

## 3. Детерминизм

- [ ] 3.1 Тест чистоты (NAV-2): повторный запрос на другом тике, после других запросов и на свежесобранном API байт-в-байт совпадает; перемотка не меняет ответов (NAV-3); проверка — тесты зелёные
- [ ] 3.2 Прогнать `determinism-review` чеклист по `systems/nav/` (float, Math.*, порядок итераций, аллокации в горячем пути); проверка — находок нет, `npm run lint` зелёный

## 4. NPC movement поверх findPath

- [ ] 4.1 Поля держимой точки пути в компоненте агента NPC + `npm run schemas`; проверка — сгенерированные схемы в диффе, снапшот/перемотка возвращают поля (тест по образцу держимого вектора)
- [ ] 4.2 Перезапрос пути в окне решений и seek к держимой точке в `followRoute`/`seekTarget` при `ctx.navigation`; не-`found` → сброс и прямой seek; ветка без навигации нетронута (NPC-6 дельта); проверка — юнит-тесты: обход обрыва через рампу, деградация при `unreachable`, сцена без навигации байт-в-байт со старым поведением
- [ ] 4.3 Существующие golden-эталоны без навигации не изменились; проверка — `npx vitest run` golden-тестов без перегенерации

## 5. Голдены и ворота

- [ ] 5.1 Новый golden-сценарий с собранной навигацией (путь через рампу, вокруг обрыва, узкий проход) в `engine/tests/golden/`; проверка — `golden.test.ts` зелёный, пара scenario/golden в коммите
- [ ] 5.2 Цитаты NAV-7..10 в коде и тестах на местах реализации; проверка — `npm run spec-graph -- check` без висячих ID
- [ ] 5.3 Полные ворота `npm run check` зелёные из корня
