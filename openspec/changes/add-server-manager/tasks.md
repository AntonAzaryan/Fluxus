# tasks: add-server-manager

## 1. Сетевой слой (engine/net-ts)

- [ ] 1.1 RTT несущего канала: ping/pong в `webSocketTransportServer`, необязательная наблюдаемая «RTT либо явное отсутствие» в интерфейсе транспорта (NTR-11, D7); тест: RTT появляется у ws-соединения, порт-каналы ботов сообщают отсутствие явно
- [ ] 1.2 Метрики хоста: длительность тика (кольцо + p99) и broadcast lag в `MatchHost`, размер снапшотов по соединениям (NTR-11, D9); тест: величины растут на лупбэк-матче и не входят в канонический лог
- [ ] 1.3 Запертый слот: `bar(slot)`/`unbar(slot)` в `MatchServer`, именованный исход в `RejectReason`, разрыв живого владельца существующим путём «исход + разрыв» (NTR-19, D8); тесты: отказ владельцу отличим от «слот занят», заместитель не тронут, после `unbar` — штатный реконнект
- [ ] 1.4 Тест парности записи: матч с запиранием/снятием реплеится побитово (NTR-8, сценарий NTR-19); прогон в `engine/integration-ts`

## 2. Control-адаптер стенда (game/demo-ts)

- [ ] 2.1 Периодический отчёт стенда по stdout JSON-линиями: фаза, слоты со статусами (`connecting/active/disconnected/removed/left/rejected`), метрики матча и процесса (event-loop delay через `perf_hooks`, RSS) (D2, D9); тест: отчёт читается из дочернего процесса и не смешивается с логом без маркировки
- [ ] 2.2 Приём команд по stdin: `disconnect-player`, `bar-slot`, `unbar-slot`, `stop` → вызовы API сервера (SRV-5); тест: команда меняет состояние слота, исход возвращается ответной линией
- [ ] 2.3 Отчёт при выключенной подписке не собирается (D9, риск p99); тест: без флага адаптера стенд ведёт себя как сегодня — существующие demo-тесты зелёные

## 3. Агент хоста (game/server-agent-ts — новый пакет)

- [ ] 3.1 Каркас пакета в workspace (в гейте `npm run check`), каталог состояния, генерация ключа и self-signed сертификата при первом старте (D4, D5); тест: повторный старт переиспользует сертификат
- [ ] 3.2 Супервизия: запуск процесса стенда с параметрами, реестр (`starting/listening/crashed/stopped` + фаза матча отдельным полем, счётчик рестартов), изоляция крашей (SRV-1); тесты: три сервера независимы, краш одного помечается кодом выхода
- [ ] 3.3 Управляющий протокол поверх wss: рукопожатие с версией, операции реестра и подписки, push-события, названные отказы (SRV-2, D3); тесты: несовпадение версий — названный отказ, событие смены статуса доходит подписчику без опроса
- [ ] 3.4 Пейринг и токены: код по локальной команде, обмен на токен, перечень и отзыв, TOFU-отпечаток на клиентской стороне протокола (SRV-3); тесты: операция без токена — отказ, отзыв рвёт активное подключение, смена отпечатка — громкий отказ клиентской библиотеки
- [ ] 3.5 Транзит админ-операций и наблюдаемости: команды SRV-5 до адаптера стенда, слоты/RTT/отклик/метрики/хвост лога в деталях подписки (SRV-4); тест: сквозной — запирание через протокол агента видно статусом `removed` в подписке
- [ ] 3.6 Краш-постмортем: сохранение кода выхода, хвоста лога и артефактов матча при ненулевом выходе, ссылка в реестре, сорвавшееся сохранение названо (SRV-6); тест: убитый процесс оставляет материалы разбора
- [ ] 3.7 Книга процессов и сверка на старте: пережившие менеджер серверы находятся, записи без процесса — `stopped` (D5); тест: перезапуск агента при живом сервере сохраняет управление им
- [ ] 3.8 Раздача клиента: статика бандла и дерева контента по HTTP, ссылка входа для сервера реестра, перечень документов матча операцией протокола (SRV-8, D10, D11); тест: страница и контент отдаются, записи нет

## 4. Дистрибутив хоста

- [ ] 4.1 Скрипт host bundle в пакете агента: агент + стенд + `vite build` клиента + контент-пак одной командой, версии соединяются при сборке (SRV-7, D10); проверка: собранный каталог самодостаточен — агент из него поднимает сервер и раздаёт клиента без репозитория
- [ ] 4.2 Версии в протоколе: агент сообщает версию протокола и версии сборки/контента дистрибутива (SRV-7); тест: значения видны в рукопожатии

## 5. Десктоп-контейнер (desktop/shell-ts)

- [ ] 5.1 Отвязываемый сервис: свойство объявления, спавн в своей группе процессов, пере-обнаружение адреса через границу сессий, явная остановка (DSK-7, D6); контрактный сьют: обе ветви (умирает/переживает) на чистом Node, POSIX и Windows-опции spawn
- [ ] 5.2 Профиль `apps/server-manager.app.json`: оконная интеграция + объявленный отвязываемый сервис агента, без дерева контента (DSK-5, MGR-5); контрактный тест: чтение контента из профиля менеджера — отказ
- [ ] 5.3 Автопейринг локального агента: адрес и материал пейринга приезжают через объявленный сервис (MGR-5, D6); тест: страница получает адрес без зашитых констант

## 6. Приложение Server Manager (game/server-manager-ts — новый пакет)

- [ ] 6.1 Каркас веб-приложения в workspace (в гейте), клиентская библиотека управляющего протокола (общая с тестами агента), лавовая тема как стиль (D11); проверка: dev-сборка поднимается
- [ ] 6.2 Хосты: локальный автоматически, удалённый адресом + пейринг-кодом, книга хостов в хранилище страницы с названным риском (MGR-1, D11); тест: добавленный хост неотличим от локального в операциях
- [ ] 6.3 Список серверов и запуск: агрегированный список, форма параметров запуска (документ матча — из перечня агента), названные отказы, ссылка входа игрока (MGR-2); тест: занятый порт показывает причину агента
- [ ] 6.4 Окно деталей сервера: слоты со статусами, RTT и отклик, метрики, хвост лога, действия «дисконект / убрать / вернуть», `removed` остаётся в перечне (MGR-3); сквозной тест на живом агенте с ботами
- [ ] 6.5 Тумблер kill-on-exit (умолчание — включён): stop-all + остановка агента при закрытии, при выключенном — повторный запуск находит серверы (MGR-4); тест: обе ветви против живого агента

## 7. Интеграция и гейт

- [ ] 7.1 Сквозной сценарий: агент → запуск сервера → игрок-бот подключён → дисконект с возвратом → запирание → снятие → остановка; проверяется статусами подписки и записью матча (парность NTR-8)
- [ ] 7.2 `npm run check` зелёный: новые пакеты в workspace, депкруз-барьеры не нарушены (агент и менеджер — слой игры, контент им законен), спек-граф без висячих ID
- [ ] 7.3 Ручная проверка на двух машинах: удалённый хост из host bundle, пейринг, тестер по ссылке входа из браузера; результат — заметка в `docs/reviews/` с найденными шероховатостями
