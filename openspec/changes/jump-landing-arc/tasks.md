## 1. Математика вертикального смещения

- [x] 1.1 В `engine/render-ts/src/model/verticalOffset.ts` добавить вывод концов манёвра из фазы и позиции двух тиков (`takeoff`/`landing` по формулам design.md) и высоту основания `h0 + (h1 − h0)·p`; чистые функции, без THREE и без состояния инстанса
- [x] 1.2 Обработать вырожденные входы: нефинитная или неположительная разность фаз, нулевой шаг позиции (прыжок на месте), фаза вне `[0, 1]` — во всех случаях концы схлопываются в текущую позицию
- [x] 1.3 Юнит-тесты в `engine/render-ts/test/verticalOffset.test.ts`: точность концов на первом и последнем тике манёвра, монотонность основания по фазе, вырожденные входы, неизменность дуги `4p(1−p)h`

## 2. Применение в подсистеме моделей

- [x] 2.1 В `engine/render-ts/src/subsystems/models.ts` включить новую ветку базы только для `motion === LOCOMOTION_AIRBORNE` с конечной фазой; сущности с override уровня вне прыжка (снаряды, провал) остаются на прежней ветке
- [x] 2.2 Брать `h0`/`h1` с визуальной поверхности (REND-9) в выведенных точках; при отсутствии источника поверхности сохранить прежнее поведение по высоте уровня
- [x] 2.3 Убедиться, что горячий путь кадра не аллоцирует: точки концов пишутся в переиспользуемые scratch-объекты рядом с существующими

## 3. Тесты рендера

- [x] 3.1 В `engine/render-ts/test/locomotion.test.ts` (или соседнем файле подсистемы) — сценарий прыжка на клетку уровнем выше: высота инстанса монотонно приходит к высоте верхнего уровня, на границе обрыва шаг между кадрами ограничен, на тике приземления скачка нет
- [x] 3.2 Сценарий прыжка вниз и сценарий записи без параметра дуги (движение по прямой между опорными высотами)
- [x] 3.3 Сценарий разрыва непрерывности внутри прыжка: опорные высоты берутся под сущностью, со следующего доставленного тика концы манёвра снова выводятся

## 4. Контент: падение

- [x] 4.1 `content/visuals/manifest.json` — увеличить `fallSpeed` и `fallDepth` записи героя (падение быстрее и глубже)
- [x] 4.2 `content/scenes/duel.scene.json` — согласовать `FallConfig.speed`/`deathDepth` героя с визуальными числами, чтобы смерть наступала на дне видимого падения
- [x] 4.3 `content/scenes/duel.scene.json` — убрать обнуление `Velocity` в JSON-системе `FallStart`: импульс движения сохраняется при провале

## 5. Проверка

- [x] 5.1 `npm run check` из корня (typecheck + lint + test)
- [ ] 5.2 Прогнать демо-сцену глазами: прыжок вверх/вниз по клифу и провал в дыру на скорости
- [x] 5.3 `openspec validate jump-landing-arc --strict`
