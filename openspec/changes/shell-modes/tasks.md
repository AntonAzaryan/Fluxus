## 1. Режим в протоколе оболочки

- [ ] 1.1 `engine/client-ts/src/protocol.ts`: handshake (SHELL-5) несёт режим оболочки; поле обязательное, умолчания у него нет
- [ ] 1.2 Там же: форма сообщений обратного канала одна на оба режима (SHELL-6) — ввод и запрос перехода машины состояний отправляются главным потоком одинаково
- [ ] 1.3 `engine/client-ts/src/remoteHost.ts`: режим доступен потребителю из `onReady`; проверить, что ничего сверх этого на main-стороне менять не пришлось

## 2. Локальный режим

- [ ] 2.1 `engine/client-ts/src/workerShell.ts`: объявить объект воркер-стороной локального режима — шапка модуля, режим в handshake
- [ ] 2.2 Убедиться, что поведение локального режима не изменилось: ввод латчится и собирается в `InputFrame` на границе тика, команды управления зовут core-API переходов (SHELL-6, WSM-5)
- [ ] 2.3 `engine/client-ts/demo/worker.ts`: демо объявляет локальный режим явно, а не получает его умолчанием

## 3. Сетевой режим

- [ ] 3.1 Завести вторую воркер-сторону — сетевую: держит клиент `engine/net-ts`, применяет персональные снапшоты, извлекает presentation-состояние тем же `Extractor`
- [ ] 3.2 У сетевой воркер-стороны MUST NOT быть `Simulation` и тикера: запрет структурный, а не проверкой в рантайме (SHELL-8)
- [ ] 3.3 Ввод главного потока уходит сообщением `Input` серверу и локально не применяется (`netcode` NET-7, NTR-10)
- [ ] 3.4 Запрос перехода машины состояний уходит вводом на сервер; `RewindController` в сетевой сборке не используется (`netcode` NET-11, REW-6)
- [ ] 3.5 `HistoryProvider` мира в сетевой сборке не поднимается (`netcode` NET-10); кольцо своих `InputFrame` глубиной по NET-9 — поднимается
- [ ] 3.6 Локальный `worldInit` поднимается ради сверки хешей (NTR-5, NTR-10) и не тикается

## 4. Общий код и неизменность main-стороны

- [ ] 4.1 Вынести общий код доставки (`sender`, `codec`, конверты) так, чтобы обе воркер-стороны использовали один путь; раскладка буфера не ветвится по режиму (SHELL-3)
- [ ] 4.2 Разделение conflatable-состояния и reliable-событий (SHELL-4) одинаково в обоих режимах: в сетевом события берутся из потока `Events` (NTR-15), а не из шины снапшота
- [ ] 4.3 Признак разрыва непрерывности (SHELL-7) в сетевом режиме ставится по смене эпохи (NTR-16)

## 5. Тесты

- [ ] 5.1 Тест: в сетевом режиме `tick()` не вызван ни разу за сессию (SHELL-8) — проверяемо, а не по ревью
- [ ] 5.2 Тест: одна и та же подсистема рендера подключается к локальной и к сетевой сборке без правок и получает тот же контракт доставки
- [ ] 5.3 Тест: запрос перемотки в сетевом режиме не двигает машину состояний воркера и уходит вводом; картинка перематывается только с приходом авторитетных состояний
- [ ] 5.4 Тест: ввод в сетевом режиме локально не применяется — состояние до прихода снапшота не меняется
- [ ] 5.5 Тест: режим приезжает главному потоку в handshake до первой доставки состояния

## 6. Проверка

- [ ] 6.1 `npm run typecheck` и `npm test` из корня — зелено
- [ ] 6.2 Прогнать демо вручную: локальный режим работает как прежде, перемотка из UI на месте
- [ ] 6.3 Прогнать сетевую сборку против локального сервера (NTR-12): ввод доезжает, снапшоты рисуются, перемотка приходит от сервера
