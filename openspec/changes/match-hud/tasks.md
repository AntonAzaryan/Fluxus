## 1. Каркас пакета

- [x] 1.1 Завести `engine/hud-ts` (`@game-mvp/hud`) в workspace: package.json (зависимость только `@game-mvp/client`), tsconfig, vitest; `npm run check` зелёный с пустым `src/index.ts`
- [x] 1.2 Лёгкая узловая DOM-абстракция пакета (создание/обновление узлов, классы, текст) по образцу `editor/ui-ts/src/dom/node.ts`, тестируемая без браузера

## 2. Модель композиции и реестры

- [x] 2.1 Тип `HudComposition` — JSON-сериализуемое значение (виджет-вид, зона, params, bindings и actions по именам); тест сериализуемости round-trip (HUD-4)
- [x] 2.2 Реестры видов виджетов, именованных селекторов и именованных действий; резолв композиции с внятной ошибкой на неизвестное имя
- [x] 2.3 Интерфейс `HudWidget` (mount/update/dispose): виджет получает отрезолвленные значения селекторов и фасад действий; тест «смена композиции не трогает код виджета» (HUD-4, сценарий «другой арене — другой HUD»)

## 3. Оверлей-хост и подписка на доставку

- [x] 3.1 Оверлей-хост поверх canvas: словарь зон, `pointer-events: none` на корне и `auto` на интерактиве; тест прохождения указателя вне интерактивных элементов (HUD-3)
- [x] 3.2 Подписка на доставку `remoteHost`: обновление по каденсу доставки, reliable-события списком с номерами тиков ровно один раз, снап по признаку разрыва; тесты сценариев HUD-5 («пять пропущенных тиков», «перемотка»)
- [x] 3.3 Фасад действий с двумя адресатами: мировое действие → отправитель обратного канала (тот же семантический ввод, что у клавиатурного биндинга), presentation-действие → контракт камеры без сообщений воркеру; тесты «кнопка неотличима от клавиши» и «камера — локально» (HUD-2)

## 4. Иконки и миникарта

- [ ] 4.1 Резолв изображений по asset ID через asset-слой; таблица «идентификатор из симуляции → asset ID» как данные контента; тест: в композиции нет URL, симуляция изображений не знает (HUD-4)
- [ ] 4.2 Тип таблицы маркеров (форма/имя отрисовщика, командная окраска, размер, приоритет, политика неизвестного типа) как часть JSON-сериализуемой композиции; реестр именованных отрисовщиков маркеров (HUD-6)
- [ ] 4.3 Виджет миникарты на canvas 2D: фон из террейна handshake с перерисовкой по дельтам пола, маркеры по доставленному состоянию, клик → presentation-действие камеры; тесты сценариев HUD-6 (новый тип — записью, тип без записи — по политике, скрытое не рисуется)
- [ ] 4.4 Виджет живого портрета: изолированный стенд модели со вторым WebGL-контекстом по образцу `assetModule.ts` редактора, модель по kind из манифеста, реакции от доставленных событий, общий asset-сервис с рендером; тесты сценариев HUD-7 (HUD-7)

## 5. Первые виджеты и демо

- [ ] 5.1 Виджет статуса матча (тик, режим, пауза) и виджет панели действий героя (каст/уклон/прыжок) на биндингах-именах, с иконками по asset ID
- [ ] 5.2 Перевести демо `engine/client-ts/demo` на пакет: композиция демо как TS-значение `HudComposition` (включая миникарту), ad-hoc HUD из `main.ts` удалить
- [ ] 5.3 Ручной прогон демо: клик мимо панели уходит в picking/камеру, клик миникарты двигает камеру, кнопка паузы показывает паузу только по доставленному состоянию (HUD-2, HUD-3, HUD-6)

## 6. Финализация

- [ ] 6.1 `openspec validate --specs --strict` и `npm run check` зелёные; сверка формулировок ссылок на SHELL-1/SHELL-6 с состоянием change `shell-modes`
