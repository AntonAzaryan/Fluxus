## 1. Дуги вертикального смещения (REND-12)

- [x] 1.1 `assets-ts`/манифест: параметры `maneuverArc` и `flightArc` в `verticalOffset` записи; парсер и типы
- [x] 1.2 `render-ts`: выбор высоты дуги по виду состояния (Airborne → jumpArc, Dodge/Roll → maneuverArc, иначе 0) — фикс бага «уклон с прыжковой дугой»; полётная дуга по `EntityView.flightPhase`
- [x] 1.3 Кодек/экстрактор: опциональный `flightPhase` через границу; демо-экстрактор заполняет его для снаряда из `Lifetime`
- [x] 1.4 Тесты: уклон получает низкую дугу, а не прыжковую; манёвр без параметра — без дуги; снаряд с фазой — дуга, без фазы — нет; перемотка/разрыв — по REND-12
- [x] 1.5 Демо-манифест: `Hero.maneuverArc` (существенно ниже 0.9), `Fireball.flightArc`

## 2. Подсистема эффектов (REND-23)

- [x] 2.1 `render-ts`: `EffectsSubsystem` за контрактом REND-8 — разделяемая сфера, per-instance альфа/цвет/масштаб, пул инстансов без аллокаций на кадр
- [x] 2.2 Записи манифеста `effects.byKind` (оболочка при живой сущности) и `effects.byEvent` (вспышка длительностью по часам кадра); парсер в `assets-ts`
- [x] 2.3 Разрывы непрерывности: вспышки сбрасываются, оболочки восстанавливаются из доставленного; picking эффекты не видит
- [x] 2.4 Тесты: оболочка появляется/исчезает с сущностью; вспышка играет один раз и умирает; новая запись манифеста не требует кода; rewind сбрасывает вспышку
- [x] 2.5 Демо-манифест: оранжевый шарик фаербола (замена magenta-заглушки — запись эффекта или модельная запись со сферой), записи щита/взрыва/превью зоны — под события/kind следующей фазы

## 3. Статы в доставке (HUD-8)

- [ ] 3.1 Конфигурация экстрактора воркера `stats: [{name, component, field}]`; чтение в плоскую форму разреженно
- [ ] 3.2 Кодек: имена статов в handshake (SHELL-5), значения по индексам в кадре; буферная дисциплина SHELL-3 сохранена (тест на отсутствие аллокаций/рост буфера)
- [ ] 3.3 `hud-ts`: `HudDeliveredState.statNames` + `entities[].stats`; фильтрация по доставке (стат скрытой сущности не существует)
- [ ] 3.4 Тесты: конфиг → стат доезжает до HUD; отсутствующий стат → «нет данных»; переименование компонента = правка конфига

## 4. Виджеты и композиция

- [ ] 4.1 Флип миникарты: отрисовка и обратное клик-преобразование (HUD-6); тест «маркер движется вверх, клик попадает в ту же точку»
- [ ] 4.2 Виджеты: `hp-bar` (под портретом), `cooldowns` (два ряда снизу по центру, затемнение + секунды из тиков × tickMs, пустое состояние без статов), `deaths` (top-center по слотам), `runtime` (fps главного потока, тик, число сущностей)
- [ ] 4.3 Композиция демо: миникарта влево к рантайм-панели, портрет −15% (параметр композиции), панель способностей снизу по центру; `#help` удалён (управление — в README демо)
- [ ] 4.4 Тесты виджетов: кд-оверлей (доля и секунды), hp-бар (границы 0/полный/нет данных), deaths, runtime; композиция демо валидна

## 5. Ворота

- [ ] 5.1 `openspec validate demo-render-hud --strict`, `npm run spec-graph -- check` зелёные
- [ ] 5.2 `npm run check` из корня зелёный; golden-эталоны и `engine/schemas/` без диффа; `npm run demo:build -w @game-mvp/client` зелёный
- [ ] 5.3 Headless-прогон демо-сборки: статы и flightPhase доезжают до `HudDeliveredState`/`TickView` в интеграционном тесте
