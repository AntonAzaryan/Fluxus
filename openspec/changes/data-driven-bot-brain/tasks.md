## 1. Документ поведения: форма и валидация

- [ ] 1.1 Типы и константы документа поведения в `engine/bot-ts` (`behavior.ts`): `schema` v1, закрытые словари исполнителей (`pressure|kite|retreat|dodge`), входов (`enemyDistance`, `enemyKnown`, `threatDistance`, `threatClosing`, `edgeProximity`, `abilityCooldownFraction`) и кривых (`linear`, `quadratic`, `logistic`, `constant`) — цитаты BOT-8/BOT-9 в комментариях
- [ ] 1.2 Валидация документа по образцу `profile.ts` (структура находок, локали ru/en): обязательная `schema`, неизвестные исполнитель/вход/кривая — находка с именем, пустые considerations — находка
- [ ] 1.3 Тесты валидации: валидный документ, каждая категория находок, неизвестная `schema` (BOT-8 «Документ прошлой версии»)

## 2. Evaluator-мозг

- [ ] 2.1 Перенести переиспользуемые слои классики (`perception.ts`, `abilities.ts`, `micro.ts`, `random.ts`, `planFor` и steering-хелперы из `utility.ts`) в `brains/layers/` без изменения поведения; классический мозг временно импортирует их оттуда, `npm test -w @game-mvp/bot` зелёный
- [ ] 2.2 Скоринг по документу (`brains/evaluated/scoring.ts`): вычисление входов из `PerceivedWorld` с нормировкой по профилю, кривые с клампом в [0, 1], полезность действия произведением; unit-тесты на каждую кривую и каждый вход
- [ ] 2.3 Мозг `brains/evaluated/evaluatedBrain.ts` за `BotBrainFactory`: каденс передумывания и гистерезис из `profile.decision`, умолчание `retreat` при нулевых очках (механизм, BOT-9), та же обработка разрыва непрерывности и `hold()`, что у классики; документ приезжает опцией конструирования (D3)
- [ ] 2.4 Трасса решений (BOT-10): опция `onDecision` с разбором пересчёта (тик, оценки considerations, полезности, победитель); тест — включённая трасса не меняет последовательность намерений
- [ ] 2.5 Тест детерминизма (BOT-11): два прогона на одном записанном потоке `ClientStep` с одним seed → идентичные намерения

## 3. Контент: референсные документы и профиль schema 3

- [ ] 3.1 Референсный документ `content/bots/behaviors/classic.json`: четыре формулы `scoreBehaviors` переписаны considerations один в один (агрессивность — вес пары pressure/kite)
- [ ] 3.2 Профиль schema 3: удалить `utility` и `aggression`, добавить `behavior` (путь документа поведения); валидация и `BOT_PROFILE_SCHEMA` обновлены, комментарий версии дополнен
- [ ] 3.3 Мигрировать существующие профили `content/bots/*` на schema 3 и завести им документы поведения; обновить тесты профиля (`profile.test.ts`)

## 4. Ворота паритета

- [ ] 4.1 Паритетный тест: классический мозг и evaluator с `classic.json` на одном потоке наблюдений и одном seed → одинаковые последовательности выбранных маршрутов и намерений (контракт — решения, не биты float)
- [ ] 4.2 Смоук на стенде: `npm run demo:debug` с evaluator-ботами доигрывает матч, `runs/latest/run.json` без аномалий

## 5. Удаление классики и переключение сборок

- [ ] 5.1 Переключить сборки на evaluator: `fill.ts`, `game/demo-ts` (стенд, `botWorker`, `demo-serve.mjs`), интеграционные тесты; scripted не трогается
- [ ] 5.2 Удалить `brains/classic/classicBrain.ts` и `utility.ts` вместе с паритетным тестом (ворота пройдены); `brains/registry.ts` и экспорт пакета обновлены — реализаций BOT-2 ровно две: evaluated и scripted
- [ ] 5.3 Вычистить упоминания классики из комментариев/доков пакета; цитаты BOT-8..11 стоят у кода, который их реализует (`spec-graph check` без дэнглингов)

## 6. Гейт

- [ ] 6.1 `npm run check` из корня зелёный (typecheck, eslint, knip, jscpd, depcruise, spec-graph, тесты); голдены не тронуты — мозг в эталоны не входит (BOT-5)
