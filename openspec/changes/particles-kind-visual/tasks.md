# Tasks: particles-kind-visual

Требование — `specs/rendering/spec.md`, **REND-37**. Разбор решений — design.md (D1–D6). Гейт один и он локальный: `npm run check` из корня репозитория (CI нет).

## 1. Правка подсистемы моделей (D6, REND-37)

- [ ] 1.1 `engine/render-ts/src/subsystems/models.ts`, метод `attachVisual` (~строка 2583): в ветку `visual === undefined`, третьим вопросом после `resolveEffectByKind` и `resolveVisualEmitter`, добавить `resolveParticlesByKind(this.manifest, kind)` — при попадании `record.emitter = true; return`, без заглушки и без предупреждения. Резолвер уже экспортирован из `@fluxus/assets` (`engine/assets-ts/src/manifest.ts:165`, `src/index.ts:133`), нового кода в `assets-ts` не требуется. Проверка — тесты п. 2.
- [ ] 1.2 Там же: переписать комментарий ветки (`models.ts:2589-2606`) и комментарий поля `InstanceRecord.emitter` (`models.ts:666-672`) под новый смысл флага — «вид рисует подсистема частиц» на обе дороги (эмиттерный decoration-вид ASSET-14 и запись `particles.byKind` REND-24), с ссылкой на REND-37. **Нормативных формулировок в комментариях быть не должно** — довод и ссылка, а не пересказ требования (CLAUDE.md, «Normative statements live only in specs»). Проверка — `npm run lint` зелёный, в комментарии нет SHALL/MUST.
- [ ] 1.3 `boundsOf` (`models.ts:3625-3633`) и константу `EMITTER_BOUNDS` (`models.ts:412-433`) **не трогать**: второго объёма под ту же ситуацию не заводится (D2). Сверить, что комментарий у константы говорит про обе дороги, а не только про размещённый факел. Проверка — тест 2.4 попадает существующим объёмом.
- [ ] 1.4 Проверить, что комментарий `resolveEffectByKind`/`resolveVisual` в `engine/assets-ts/src/manifest.ts:74-78, 126-130` не остался неполным (он объясняет, почему модельная подсистема не должна принимать чужой вид за отсутствие записи, — теперь поводов три, а не два). Правка только комментария; кода `assets-ts` чейндж не меняет. Проверка — `npm run lint`.

## 2. Тесты рендера (REND-37, сценарии требования)

Все фикстуры — инлайн в тестах пакета; `content/` в доказательство не входит (D5, п. 2).

- [ ] 2.1 `engine/render-ts/test/particles.test.ts`, describe про decoration-эмиттеры либо новый describe «Вид, нарисованный только частицами (REND-37)»: сущность типа с записью `particles.byKind` и **без** записи в `entities` — `instanceFor(id)!.placeholder === false`, `warnings` пуст на двух подряд сущностях того же типа. Это сценарий требования «Пятно огня, нарисованное только частицами»; образец рига — существующий тест `эмиттерный вид не получает magenta-заглушки моделей (ASSET-4, ASSET-14)` (`particles.test.ts:402`).
- [ ] 2.2 Там же: сценарий «Опечатка в ключе записи эмиттера» — запись заведена под `Smok`, приходит сущность `Smoke`: у `Smoke` заглушка и ровно одно предупреждение, частиц нет ни у одного ключа (`ParticlesSubsystem.activeCount === 0`).
- [ ] 2.3 Там же: сценарий «Частицы состояния заглушки не отменяют» — тип без единой записи `byKind`, доставленное состояние несёт состояние с записью `particles.byState`: заглушка и её предупреждение на месте и при приходе состояния, и при его уходе (заявку даёт только ключ по визуальному типу, D4).
- [ ] 2.4 Там же, в describe «Частицы и picking (REND-15, REND-24)» (`particles.test.ts:638`): сценарий «Клик по сущности, нарисованной частицами» — у сущности вида, заявленного `particles.byKind`, `models.proxyOf(id, proxy)` возвращает `true`, объём ненулевой и **совпадает** с объёмом размещённого эмиттерного decoration-вида (существующий тест `particles.test.ts:654`), а `ParticlesSubsystem` источником прокси по-прежнему не является.
- [ ] 2.5 `engine/render-ts/test/models.test.ts`, рядом с `сущность без записи в манифесте — заглушка и предупреждение один раз (ASSET-6)` (`models.test.ts:104`): сценарий «Забытая запись манифеста» остаётся зелёным без правок — тип, о котором манифест не говорит ничего, получает заглушку и одно предупреждение. Если тест пришлось править — это находка: граница ASSET-6 сдвинулась, и правку надо откатывать, а не подгонять тест.
- [ ] 2.6 `engine/render-ts/test/models.test.ts` либо `particles.test.ts`: сценарий «Снаряд с моделью и хвостом из частиц» — тип несёт и запись в `entities`, и `particles.byKind` с сокетом: инстанс строится моделью, до готовности ассета показана заглушка (ASSET-4), объём-прокси — габариты модели, а не фиксированный объём эмиттера.
- [ ] 2.7 `engine/render-ts/test/manifestResupply.test.ts`, рядом с `появление записи у типа, рисовавшегося заглушкой, даёт ему модель (ASSET-6)` (`manifestResupply.test.ts:292`): сценарий «Переподача манифеста заводит запись эмиттера» — переподача, добавившая `particles.byKind` виду с заглушкой, снимает заглушку; обратная переподача возвращает заглушку и предупреждение; в обе стороны инстанс из ассета не пересобирается (REND-17).
- [ ] 2.8 `engine/render-ts/test/effects.test.ts`: сценарий «Купол эффекта остаётся невыделяемым» — у типа с записью `effects.byKind` заглушки нет (существующий тест `effects.test.ts:303`) и объёма-прокси нет: `models.proxyOf(id, proxy)` даёт `false`. Тест фиксирует осознанное решение D3, а не недосмотр.

## 3. Демо: снять ложное объяснение (D5)

- [ ] 3.1 `game/demo-ts/test/demoBoss.test.ts:2445-2456`: переписать комментарий и текст assert'а `expect(shell, 'без оболочки по виду полоса рисуется заглушками (ASSET-6)')`. После правки запись `effects.byKind.BossFire` держится не ради подсистемы моделей, а как свечение по земле, радиус которого сверен с зоной урона; про заглушку в объяснении не должно остаться ни слова. Сам assert и сама запись остаются. Проверка — `npm test -w @fluxus/demo` зелёный, в файле нет утверждения, что без записи полоса рисуется заглушками.
- [ ] 3.2 `content/visuals/manifest.json` **не править**: снятие `effects.byKind.BossFire` — художественное решение владельца демо, а не часть этого чейнджа (D5). Проверка — `git status` показывает `content/` нетронутым.

## 4. Гейт и приёмка

- [ ] 4.1 `npm run check` из корня зелёный (`lint:all` + `spec-graph check` + `test`). Отдельно убедиться, что `spec-graph check` не ругается на REND-37: требование цитирует ASSET-4, ASSET-6, ASSET-14, REND-15, REND-17, REND-18, REND-20, REND-23, REND-24, ED-17, RDBG-1 — все существуют.
- [ ] 4.2 Эталоны стоимости `*.cost.json` **не должны** двигаться: у `BossFire` заглушка уже подавлена записью `effects.byKind`, и кадр записанных матчей не меняется. Если эталон покраснел — не принимать `npm run golden:cost` не глядя: разобраться, что именно изменилось, и записать разбор в коммит (PERF-4).
- [ ] 4.3 Ручная приёмка живьём: временно убрать `effects.byKind.BossFire` из `content/visuals/manifest.json`, прогнать `npm run demo`, довести босса до полосы огня — сиреневых ящиков нет, предупреждения про `BossFire` в консоли нет, пятна попадаемы курсором. **Правку контента после проверки откатить** (п. 3.2): она инструмент приёмки, а не часть чейнджа. Снимки «до» для сверки — `runs/fire-shots/before-purple-boxes-3.png`, `after-single-patch.png`.
- [ ] 4.4 Свериться с параллельным чейнджем `rendering` про скорость анимации (REND-25, колонки view-буфера, тот же `models.ts`): если он уже в дереве — перебазироваться на него, делта REND-37 при этом не меняется (proposal.md, «Notes»).
