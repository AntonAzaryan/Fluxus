# tasks: dead-state-from-delivery

## 1. Рендер: фиксация смерти следует доставленному состоянию (REND-4)

- [x] 1.1 `AnimationBackend.playFinal(index)` — последний кадр клипа без кроссфейда и без уведомления о конце one-shot'а; реализации в `MixerAnimationBackend` и `VatAnimationBackend` (D3, REND-20); тесты обоих ярусов
- [x] 1.2 `AnimationController`: `enterDeath()` (фиксация без переигрывания one-shot'а) и `releaseDeath()` наружу; `isDead` уже есть; тесты: повторный вход — no-op, выход возвращает клип текущего состояния
- [x] 1.3 `ModelsOptions.stateComponents` + `ModelsOptions.deadState`, читатель бита общим `createStateReader`; предупреждение об имени вне словаря — один раз
- [x] 1.4 `syncPool`: созданный этой доставкой инстанс с поднятым состоянием смерти → `enterDeath()`; любой инстанс с опущенным состоянием и фиксацией → `releaseDeath()` (D2); тесты: гибель на глазах играет клип смерти событием, а не состоянием
- [x] 1.5 Тесты подсистемы: труп вернулся из тумана трупом; сцена с двумя видами возрождения поднимает обоих; сборка без `deadState` ведёт себя как прежде

## 2. Демо: доставка состояния и опции

- [x] 2.1 `Dead` в конец `STATE_COMPONENTS` (`sim.ts`) — биты существующих состояний не сдвигаются; экспорт имени
- [x] 2.2 `main.ts`: `stateComponents`/`deadState` в опции подсистемы моделей
- [x] 2.3 `extractor.ts`: событие доворота босса в `aimEvents`

## 3. Контент: труп, коллайд и отталкивание

- [x] 3.1 `projectile.onHit` фаербола: труп — не цель (страж `Dead` рядом с `Health`/`Player`)
- [x] 3.2 Слой босса 8, `blockMask` героя 9, `blockMask` босса 3, `hitMask` снарядов 10, `BossRespawn` возвращает слой 8 (D4)
- [x] 3.3 Ability `bossRepel`, prefab `SlotBossRepel`, выдача слота в `GrantBossSlots`, система `BossProximity` (D5)
- [x] 3.4 Манифест: клип, эффект и частицы события отталкивания

## 4. Тесты и гейт

- [x] 4.1 `demoBoss.test.ts`: герой не проходит сквозь босса и не оказывается за ним; маски адресны (герой героя не держит); отталкивание — 250 урона, дистанция стрейфа и направление на жертву; кулдаун держит паузу; мёртвый босс не отталкивает
- [x] 4.2 `demoBoss.test.ts`: труп босса не задерживает ни героя, ни снаряд; словарь журнала знает оба новых события (DIAG-10)
- [x] 4.3 Эталоны проверены и НЕ тронуты: golden-пары и `*.cost.json` сняты с фикстур движка (CONT-4), а не с дерева контента — правка сцены их не двигает
- [x] 4.4 `npm run check` зелёный
