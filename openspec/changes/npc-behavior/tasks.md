# Tasks: npc-behavior

## 1. Общая модель скоринга (D2, NPC-3, BOT-9)

- [ ] 1.1 Перенести композицию considerations/кривых из `engine/bot-ts/src/brains/evaluated/scoring.ts` в модуль ядра `engine/core-ts` (рядом с `dsl/`); проверка — `npm run typecheck` и ноль новых зависимостей у ядра (`npm run lint:arch`)
- [ ] 1.2 Перевести evaluated-мозг бота на модуль ядра, оставив словари входов/исполнителей в `bot-ts`; проверка — существующие тесты `engine/bot-ts` зелёные без правки ожиданий (паритет BOT-8)
- [ ] 1.3 Обновить цитаты требований в коде (NPC-3, BOT-9) и прогнать `npm run spec-graph -- check`

## 2. Компоненты и документ поведения NPC (D3, NPC-2)

- [ ] 2.1 Завести SoA-компоненты NPC: состояние HFSM, tier/каденс, threat-таблица top-K плоскими массивами, прогресс маршрута; проверка — юнит-тесты компонентов и отсутствие аллокаций в тике на агента (существующий паттерн тестов аллокаций core-ts)
- [ ] 2.2 Формат документа поведения NPC (`schema`-версия, состояния/переходы, действия со скорингом, правила threat) + парсер с находками валидации; проверка — юнит-тесты парсера: валидный документ, неизвестный исполнитель/вход/кривая → находка с именем
- [ ] 2.3 Регенерация JSON-схем при необходимости (`npm run schemas`) и проверка `npm run check` без контент-дерева (правило CONT-4 для ядра)

## 3. Система поведения NPC (D1, D4, D5, D9, NPC-1, NPC-4, NPC-5)

- [ ] 3.1 Нативная система за интерфейсом `System`: HFSM-исполнение, детерминированный round-robin каденс с decision-budget, RNG-поток `npc-ai`; проверка — юнит-тесты: два прогона побитово равны, решения пересматриваются в предсказуемых окнах
- [ ] 3.2 Событийное накопление threat по шине (урон/лечение/таунт) с вытеснением минимума при переполнении; проверка — тесты сценариев NPC-5 (переключение по порогу из документа, переполнение ёмкости)
- [ ] 3.3 Выбор цели через broad-phase соседей + threat; форс-пересмотр по смерти цели/таунту в пределах бюджета тика; проверка — тест: смерть цели ведёт к перевыбору на том же/следующем тике без нарушения каденса остальных

## 4. Движение (D6, NPC-6)

- [ ] 4.1 Waypoint-маршруты как сущности/компоненты `initial` демо-сцены и следование им: seek к текущей точке, переход по достижении; проверка — сим-тест: волна проходит маршрут в сцене без Navigation API
- [ ] 4.2 Separation по соседям сетки (без O(N²)); проверка — тест: скученные крипы расходятся, стоимость шага не растёт квадратично (счётчики стоимости)

## 5. Director волн (D8, NPC-8)

- [ ] 5.1 Система-режиссёр: таблица волн из контента, спавн префабами через Command Buffer, cap активных NPC из данных сцены; проверка — тесты сценариев NPC-8 (смена состава без кода, отложенный спавн при переполнении)

## 6. Босс (D7, NPC-7)

- [ ] 6.1 Фазы HFSM по порогу здоровья/таймеру/событию + ротация со скорингом, кастующая публикацией события под триггер `event` способности сцены; проверка — сим-тест: переход фазы меняет ротацию, каст идёт штатной машиной ABIL-4, прерывание — по ABIL-6
- [ ] 6.2 Демо-контент: босс с двумя фазами и телеграфированным кастом на демо-сцене; проверка — `npm run demo:debug` с ботами, журнал показывает фазы и касты босса

## 7. Гейт масштаба (D10, NPC-9)

- [ ] 7.1 Синтетический стресс-сценарий «сотни массовых крипов» в `engine/tests/golden/` + cost-эталон (`npm run golden:cost`) и bench-сентинел (PERF-5); проверка — эталон в гейте, дифф в коммите
- [ ] 7.2 Прогнать полный `npm run check`; существующие golden/cost-эталоны без изменений (текущие сцены NPC не содержат)
