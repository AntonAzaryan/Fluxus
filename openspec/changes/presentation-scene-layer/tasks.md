## 1. Манифест визуалов: раздел decoration-видов (ASSET-9)

- [ ] 1.1 `engine/assets-ts/src/manifest.ts`: раздел decoration-видов рядом с `entities` — запись того же состава (`EntityVisual`), собственный ключ
- [ ] 1.2 Там же: валидация единого пространства ключей — имя, занятое в обоих разделах, отвергается с этим именем в причине; `checkUnknownKeys` пополняется новым разделом
- [ ] 1.3 Разрешение визуального ключа в запись — одно место на оба раздела, чтобы потребители не выбирали раздел сами
- [ ] 1.4 Тесты `engine/assets-ts/test/manifest.test.ts`: пересечение имён отвергается; вид без prefab'а валиден; запись decoration-вида с неприменимыми частями (таблицы клипов, кости, дуга прыжка) валидна и смысла не получает

## 2. Парный документ: формат и загрузка (PRES-1, PRES-2, PRES-3)

- [ ] 2.1 `engine/assets-ts/src/loaders/`: загрузчик парного presentation-документа, регистрация в реестре (ASSET-3), типизированный handle (ASSET-2); отсутствие файла — не ошибка загрузчика, а отсутствие слоя у потребителя
- [ ] 2.2 Валидатор документа: закрытый состав документа и записи, обязательные `visual`/`x`/`y`, необязательные `yaw`/`scale`/`skin`, конечность чисел, `scale > 0`; сообщение адресует запись индексом, а не документ целиком
- [ ] 2.3 Правило имени пары (базовое имя сцены + `.presentation.json`) — одна функция на все пакеты; путь считается от корня дерева контента (ASSET-2, CONT-2)
- [ ] 2.4 Тесты: разбор валидного документа; каждая форма нарушения — своё сообщение и свой адрес; отсутствующий и пустой `decorations` неразличимы

## 3. Рендер: набор decoration-инстансов (REND-18, REND-1)

- [ ] 3.1 `engine/render-ts/src/`: приём декларативного набора decoration-инстансов — сведение по ключам (создать/обновить/убрать), собственная нумерация, набор живёт рядом с `PresentationStage`, а не внутри продюсера
- [ ] 3.2 `subsystems/models.ts`: построение и обновление decoration-инстансов тем же путём, что документные, — запись манифеста, скин (REND-6), перёд (REND-13), наклон по нормали (REND-10), посадка на визуальную поверхность (REND-9)
- [ ] 3.3 Явные отсутствия: интерполяции нет (snap), событийных клипов и доворота костей нет, вертикального смещения (REND-12) нет, фильтра видимости нет; клип — состояние покоя записи
- [ ] 3.4 Смена продюсера (`publish`/`detach`, REND-11) набор decoration MUST NOT гасить — проверить на входе в превью и выходе из него
- [ ] 3.5 `picking.ts`: decoration-инстансы участвуют в разрешении курсора наравне с инстансами presentation-состояния; при совпадающей дистанции — после них
- [ ] 3.6 `subsystems/overlays.ts`: подсветка выделения работает по decoration-инстансу тем же наложением, что по сим-объекту
- [ ] 3.7 Тесты `engine/render-ts/test/`: сведение набора по ключам без пересоздания при правке полей; сосуществование с потоком тиков; попадание picking'а в декорацию на кривизне; пустой набор даёт прежний кадр

## 4. Редактор: документ, операции, валидация

- [ ] 4.1 `editor/core-ts/src/project/pairing.ts`: снять раздел «Шов третьего документа» — формат теперь есть; `presentation` перестаёт быть документом без правил
- [ ] 4.2 Открытие проекта (`sceneProject.ts`, `sceneDiscovery.ts`): поиск парного документа по правилу имени, отслеживаемый список `decorations` (дескрипторы ED-29), отсутствие файла — сцена без декораций
- [ ] 4.3 Операции авторинга (`editor/ui-ts/src/areas/scenePlacement.ts` либо соседний вклад): поставить, подвинуть, повернуть, отмасштабировать, удалить decoration; дописывание в конец, перестановки записей нет; квантование по PRES-3 в самой операции
- [ ] 4.4 Удаление выделенного одной операцией по мультивыделению из сим-объекта и decoration (сценарий ED-16)
- [ ] 4.5 `sceneDocuments.ts`: набор decoration-инстансов как производная документа — рядом с `placementsOf`, тем же способом (ключи из дескрипторов сессии)
- [ ] 4.6 `sceneInteraction.ts`: выделение и picking по decoration наравне с сим-объектами (ED-17); снять из шапки раздел «Чего здесь нет: decorations»
- [ ] 4.7 `editor/core-ts/src/validation/crossDocument.ts`: правило «`visual` не разрешается в запись манифеста» — междокументное, раскладку получает параметром (`DocumentSite`), молчит при незагруженном манифесте
- [ ] 4.8 Операция перевода decoration ↔ prop (PRES-5): перенос между документами с приведением позиции и курса; вид не переписывается
- [ ] 4.9 Сохранение: парный документ — член группы записи (уже так), пустой слой файла не создаёт; проверить round-trip ED-21 на документе, написанном руками

## 5. Тесты инварианта и контента

- [ ] 5.1 `engine/integration-ts`: прогон сцены с парным документом и той же сцены без него дают побитово совпадающие эталоны (PRES-4, CLI-6, CLI-8)
- [ ] 5.2 Там же: два клиента с разными парными документами входят в один матч — хеши входа совпадают (NTR-5), рассинхронизации нет
- [ ] 5.3 `contentBoundary.test.ts`: новый вид контентного документа учтён в данных проверки границы (CONT-1)
- [ ] 5.4 `content/scenes/duel.presentation.json`: демонстрационный слой для сцены дуэли — контент, а не фикстура; golden-эталоны движка при этом не меняются (CONT-4)
- [ ] 5.5 `editor/ui-ts` тесты: постановка, перемещение, удаление и undo/redo декорации; единообразие выделения; квантование в операции

## 6. Гейт

- [ ] 6.1 `npm run check` из корня (typecheck + lint + test)
- [ ] 6.2 `openspec validate presentation-scene-layer --type change --strict` и `openspec validate --specs --strict`
- [ ] 6.3 Схемы `engine/schemas/` не перегенерируются: парный документ и манифест — не документы ядра (SER-5 их не порождает); убедиться, что `npm run schemas` даёт пустой дифф
