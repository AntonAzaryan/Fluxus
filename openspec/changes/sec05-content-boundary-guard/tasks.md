# Tasks: sec05-content-boundary-guard

Требования — `specs/game-content/spec.md` (**CONT-1**, **CONT-2**, **CONT-4**), разбор решений — design.md (D1–D7). Гейт локальный: `npm run check` из корня (CI нет).

Изменение заводится по факту сделанной работы: валидация спек против кода (`docs/reviews/spec-validation-2026-09-01/05-state-serialization.md`) назвала две дыры в барьере CONT-1 и одну устаревшую классификацию CONT-2.

## 1. Дельты требований (D1, D2, D5, D7)

- [x] 1.1 `specs/game-content/spec.md`: MODIFIED CONT-1 — полнота словаря проверки объявлена нормой, опознание вида по месту узаконено; добавлен сценарий «Вид контента, которого словарь проверки не знает». Блок MODIFIED несёт требование ЦЕЛИКОМ со всеми действующими сценариями.
- [x] 1.2 Там же: MODIFIED CONT-2 — третий класс документов (не читаемых ни одним из двух каналов), владельцы классов названы (CONT-6, `bot-player` BOT-12, BOT-6, BOT-8); два новых сценария — бот-аннотации рядом со сценой и профили/поведения ботов.
- [x] 1.3 Там же: MODIFIED CONT-4 — эталонные фикстуры парсеров и потребителей ассетов названы третьим классом фикстур движка; перечень классов освобождения закрыт требованием; запрет на поддерживаемую копию документа дерева контента; два новых сценария.

## 2. Код: словарь проверки границы (D1–D4)

- [x] 2.1 `engine/tests/guard/contentLocation.ts`: в словарь по суффиксу добавлены `.effect.json` (`assets` ASSET-14) и `.svg` (`match-hud` HUD-4, ASSET-2).
- [x] 2.2 Там же: словарь по директории получает `visuals` (presentation-документы дерева визуалов), а запись `bots` называет оба вида документов ботов (BOT-6, BOT-8); словарь по имени получает `arena-curvature.json` (карта кривизны `assets` ASSET-7) — на одном лишь месте её копия вне `visuals/` границу проезжала бы молча (D4).
- [x] 2.3 Там же: имя контентной директории ищется на ЛЮБОМ уровне пути от корня скана, а не у непосредственного родителя — иначе `bots/behaviors/*.json` остаются за границей.
- [x] 2.4 Там же: освобождения называют класс из CONT-4; добавлено освобождение `render-ts/test/fixtures` (фикстуры потребителя эмиттерных ассетов) — прежде они не краснели лишь потому, что словарь не знал их вида.

## 3. Тесты и комментарии

- [x] 3.1 `engine/integration-ts/test/contentBoundary.test.ts`: в набор сканера добавлены документ поведения бота (`bots/behaviors/`), карта кривизны ВНЕ дерева визуалов (по имени), документ дерева визуалов с произвольным именем (по месту), документ эффекта и иконка; ожидаемый список нарушений расширен; отдельный тест «виды, которых словарь прежде не знал, ловятся наравне с прочими» проверяет оба признака.
- [x] 3.2 `engine/assets-ts/test/mdx.test.ts`: роль эталонной фикстуры названа комментарием (CONT-4) — она образец формата, а не копия игровой модели, и синхронизации с деревом контента не подлежит.

## 4. Синхронизация и гейт

- [x] 4.1 Перенести дельту в `openspec/specs/game-content/spec.md`. Изменение при этом НЕ архивируется: оно остаётся в очереди как запись о принятом решении.
- [x] 4.2 Целевые проверки: `npx vitest run` в `engine/integration-ts` и `engine/assets-ts`; `npm run typecheck`, `npm run lint`, `npm run lint:dead`, `npm run spec-graph -- check`, `npx openspec validate --specs --strict`, `npx openspec validate --changes --strict`.
