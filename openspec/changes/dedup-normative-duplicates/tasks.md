## 1. Presentation-ассеты: один перечень последствий

- [ ] 1.1 `openspec/specs/assets/spec.md`: `ASSET-1` — собрать полный перечень (объединение разъехавшихся редакций) со ссылкой на владельца каждого пункта: `DET-1`, `SER-1`/`SNAP-1`, `FOW-5`, `CLI-5`/`CLI-6`; добавить сценарии про видимость и про новое требование о presentation-данных
- [ ] 1.2 Там же: `ASSET-6` — перечень заменить ссылкой на `ASSET-1`, сохранив норму о направлении ссылок «только из манифеста в sim-идентификаторы» и добавив сценарий на неё
- [ ] 1.3 Там же: `ASSET-7` и `ASSET-8` — перечни заменить ссылкой на `ASSET-1`
- [ ] 1.4 `openspec/specs/rendering/spec.md`: `REND-9` — перечень заменить ссылкой на `assets` ASSET-1, сохранив запрет менять cliff-геометрию (`terrain` TERR-5) и проходимость с его основанием (обрывы потребляет `physics` PHYS-6)
- [ ] 1.5 Проверить, что ни в одной из четырёх правок не осталось слов «`worldInit`, снапшоты, golden-файлы» в перечислительном виде

## 2. Глубина перемотки и формат потока server → client

- [ ] 2.1 `openspec/specs/netcode/spec.md`: `NET-10` — вместо «snapshot ring 420 тиков (7 с)» ссылка на `snapshot-rewind` REW-1 (глубина) и SNAP-6 (геометрия), с явным указанием, почему числа здесь быть не должно (SNAP-4 против прочтения «420 слотов»); клиентский ring инпутов — ссылкой на `NET-9`
- [ ] 2.2 Там же: `NET-8` — снять частоту (`netcode-transport` NTR-7), формат кадра (NTR-13) и пересказ per-client фильтрации (`NET-12`); оставить политику «полные снапшоты в MVP, delta потом и per-client» и оговорить, что «полный» значит «не дельта»
- [ ] 2.3 Убедиться, что `REW-1`, `SNAP-4`, `SNAP-6`, `NTR-7`, `NTR-13`, `NET-9`, `NET-12` не тронуты

## 3. Command Buffer и порядок команд

- [ ] 3.1 `openspec/specs/ecs-foundation/spec.md`: `CMD-4` — правило сформулировать по виду операции (создание/уничтожение сущности, добавление/удаление компонента) без перечня имён действий; сослаться на `data-driven-systems` ACT-1 (набор) и ACT-2 (прохождение через буфер); добавить сценарий на новое действие в ACT-1
- [ ] 3.2 `openspec/specs/data-driven-systems/spec.md`: `ACT-3` — снять первую фразу, дословно повторяющую `CMD-3`, заменив её ссылкой, объясняющей разделение ролей: `CMD-3` владеет порядком применения, `ACT-3` — порядком создания
- [ ] 3.3 Убедиться, что `ACT-1`, `ACT-2` и `CMD-3` не тронуты

## 4. Порядок ключей и владелец формата документа

- [ ] 4.1 `openspec/specs/serialization/spec.md`: `SER-6` — сузить сортировку до карт с ключами-именами данных; ввести правило о документах фиксированной формы (порядок задаёт требование-владелец, примеры — `CLI-3`, `SER-8`, `SER-7`); оговорить, что списки не сортируются и что канонический поток `DET-1` сужением не затронут; добавить два сценария
- [ ] 4.2 `openspec/specs/cli-testing/spec.md`: `CLI-3` — объявить документ выхода документом фиксированной формы, порядок полей нормировать здесь, `worldInitHash` между `seed` и `ticks` — частный случай; зафиксировать, что снапшот предшествует событиям, а карты имён данных внутри записи сортируются по `SER-6`; добавить сценарий на реализацию, отсортировавшую верхний уровень
- [ ] 4.3 Сверить обе правки с фактическими эталонами `engine/tests/golden/*.golden.json`: верхний уровень и плоская форма мира — в фиксированном порядке, карты компонентов и полей — отсортированы; убедиться, что ни один байт эталонов правкой не затрагивается
- [ ] 4.4 Убедиться, что `DET-1` и `SER-8` не тронуты

## 5. Порядок системы видимости и формат расстановки сценария

- [ ] 5.1 `openspec/specs/fog-of-war/spec.md`: `FOW-6` — первый абзац заменить ссылкой на `determinism-core` DET-9; сохранить эмит только при изменении битов с его основанием (дельта `NET-8`, `TickResult.changes` OBS-6); во втором сценарии показать порядок как следствие якоря DET-9
- [ ] 5.2 `openspec/specs/cli-testing/spec.md`: `CLI-2` — описание формата расстановки заменить ссылкой на `serialization` SER-8 (формат и порядок применения), сохранив основание «документ прогона пишется и читается человеком»; добавить сценарий на сложение расстановок сцены и сценария
- [ ] 5.3 Убедиться, что `DET-9` и `SER-8` не тронуты, а сценарно-специфичное в `CLI-2` (seed, `inputs[]`, список игроков, поля физики и видимости) сохранено дословно

## 6. Проверка

- [ ] 6.1 `openspec validate dedup-normative-duplicates --strict` — зелено
- [ ] 6.2 `openspec validate --specs --strict` после применения дельт — зелено
- [ ] 6.3 Пройти по каждой снятой формулировке и убедиться, что её норма читается у владельца, а на месте снятия стоит ссылка со спекой и ID — ни одна норма не потеряна и ни одна не осталась в двух местах
- [ ] 6.4 Убедиться, что дифф содержит только `openspec/`: кода, эталонов и схем работа не касается, поэтому их прогон её не проверяет и красным сигналом для неё быть не может
