# tasks: service-cert-pinning

## 1. Хост-слой контейнера (desktop/shell-ts, чистый Node — в гейте)

- [x] 1.1 Пин-файл в `detached.ts`: `pinFile` в `DetachedFiles` (`<service>.pin`), чтение с валидацией «ровно 64 hex нижнего регистра после trim, иначе пусто», уборка вместе с адресным и pid-файлом (DSK-8, D1); тесты: валидное читается, мусор и отсутствие — «закрепления нет», `forgetDetached` убирает файл
- [x] 1.2 Подстановка `{pinFile}` в `serviceArgs` (`service.ts`) по образцу `{addressFile}` (D2); тест: аргументы объявления получают путь пин-файла
- [x] 1.3 `certificatePins()` у хост-сервисов: валидные закрепления объявленных сервисов профиля, включая переживший сессию сервис (DSK-8, D3); тесты: закрепление появляется после записи файла сервисом, невалидный файл не попадает в множество, survivor-случай читает тот же файл
- [x] 1.4 Функция «PEM → SHA-256 DER hex» в `src/host/` (D4); тест на committed-фикстуре сертификата: значение совпадает с посчитанным `fingerprintOf` агента для той же фикстуры

## 2. Клей Electron (вне гейта, DSK-6)

- [x] 2.1 Обработчик `app.on('certificate-error')` в `openSession` (`main.ts`): отпечаток предъявленного PEM входит в `certificatePins()` открытой сессии → принять, иначе отказ; снятие обработчика в `closed` (DSK-8, D6); `typecheck:electron` зелёный
- [x] 2.2 Случай контрактного сьюта Electron (`contract:electron`): фиктивный wss-сервис с self-signed сертификатом и пин-файлом — страница открывает канал; тот же сервис без пин-файла — канал отвергнут (сценарии DSK-8)

## 3. Агент (game/server-agent-ts — в гейте)

- [x] 3.1 Флаг `--pin-file <файл>` в `bin/agent.mjs`: запись отпечатка сертификата (`0o600`) сразу после `startAgent`, до выдачи адреса (D5); тест: файл содержит ровно `certificate.fingerprint`, перезапуск с тем же каталогом состояния пишет то же значение

## 4. Профиль менеджера и сквозная проверка

- [x] 4.1 `apps/server-manager.app.json`: `--pin-file {pinFile}` в аргументах сервиса агента (MGR-5)
- [ ] 4.2 Ручная сквозная проверка: `npm run start:manager -w @fluxus/desktop-shell` — локальный агент появляется в списке хостов без ручного ввода, запуск сервера и ссылка входа работают; отказ удалённого хоста по-прежнему называет границу (`PAGE_LIMIT`)
- [x] 4.3 `npm run check` зелёный; `spec-graph check` не находит висячих ссылок (DSK-8 процитирован в коде и тестах)
