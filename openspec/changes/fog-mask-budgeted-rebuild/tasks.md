## 1. Маска: двойной буфер и чанкуемые проходы (mask.ts)

- [ ] 1.1 Двойной буфер `VisibilityMask`: задний растр для порционных операций, `commit()` — своп ссылок; синхронный путь на переднем растре сохранён (snapAll, тесты)
- [ ] 1.2 Порционные варианты проходов: `clear` по диапазону строк, тексельный цикл `reveal` по диапазону `ty` при построенном depth-буфере наблюдателя, `smooth` раздельно H/V по строкам; состояние курсора между порциями
- [ ] 1.3 Юнит-тесты mask: порционная сборка побитово равна синхронной на тех же входах; commit атомарен

## 2. Фазовая машина и коалесинг (subsystems/fog.ts)

- [ ] 2.1 Тройка сигнатур `signature`/`building`/`pending`; `syncTick` больше не строит растр — только скан, кандидат и конфляция в `pending`; `snapAll` — синхронный снап (текущая семантика)
- [ ] 2.2 Исполнение порций в `updateFrame` до шага рассеивания под тексельный бюджет (по умолчанию — от площади маски, полная перестройка ≤3 кадров; `Infinity` — commit в первый кадр); запрет рестарта в полёте, после commit — старт от `pending`
- [ ] 2.3 Обработка граней: смена разрешения/конфига/качества и dispose сбрасывают полётное состояние; `built` — только после первого commit (прямой проход кадра до него, FOW-7)
- [ ] 2.4 Тесты: коалесинг (две доставки в полёте → одна перестройка от последней), нет полурастра ни в одном кадре, snapAll-аборт, смена разрешения в полёте, продвижение при доставке каждый кадр

## 3. Грязное окно рассеивания (fog.ts + fog/layer.ts)

- [ ] 3.1 Блочная сетка 16×16: набор грязных блоков на commit поблочным сравнением старого/нового переднего растра, объединение с неустоявшимися блоками прошлого окна
- [ ] 3.2 Проход рассеивания только по флагнутым блокам; блок, достигший цели, снимает флаг; `settling = (набор непуст)`; счётчик `fogDissolveTexels` — посещённые тексели (обновить doc-комментарий в cost.ts)
- [ ] 3.3 Блит миникарты по грязным блокам; `putImageData` с грязным прямоугольником — расширить `FogLayerContext`, учесть Y-флип
- [ ] 3.4 Тесты: устоявшаяся сцена платит ноль (steadyFrame сохраняется), окно сжимается по мере схождения, прерванное рассеивание доигрывается

## 4. Стадии счётчиков и cost-эталоны

- [ ] 4.1 Растровые счётчики тумана (`fogRevealCalls`…`fogMinimapTexels`, `fogMaskUploadBytes`) → стадия `frame`; `fogEntitiesScanned` остаётся `syncTick`; поправить cost.test.ts и render-ts/test/fog.test.ts (прокачка `updateFrame`, `rebuilds` = закоммиченные)
- [ ] 4.2 `npm run golden:cost` — регенерация всех `*.cost.json`, дифф в коммит

## 5. Тени (subsystems/lighting.ts)

- [ ] 5.1 Чередование фаз: кадр статической перерисовки допустим, только если предыдущая фаза не `static` или динамических корней нет; тесты lighting.test.ts / sceneLighting.test.ts на чередование под непрерывной инвалидацией
- [ ] 5.2 Точечные флаги нового корня: `setCaster` нового динамического корня применяет `applyFlagsTo` вместо взвода `flagsStale`; тест — реестр не обходится целиком при появлении кастера
- [ ] 5.3 Проверить дифф perf-секций эталонов: `lightingDynamicCasters` уходит от нуля в match-парах (это и есть доказательство фикса)

## 6. Ворота и валидация

- [ ] 6.1 `npm run check` целиком зелёный (typecheck, eslint, knip, jscpd, depcruise, spec-graph, тесты)
- [ ] 6.2 `npm run bench:demo -- --drive cliff-jump` до/после: p99 интервала кадра и стадия `present` — числа в коммит-сообщение
