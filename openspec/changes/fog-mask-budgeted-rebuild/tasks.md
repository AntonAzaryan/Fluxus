## 1. Маска: двойной буфер и чанкуемые проходы (mask.ts)

- [x] 1.1 Двойной буфер `VisibilityMask`: задний растр для порционных операций, `commit()` — своп ссылок; синхронный путь на переднем растре сохранён (snapAll, тесты)
- [x] 1.2 Порционные варианты проходов: `clear` по диапазону строк, тексельный цикл `reveal` по диапазону `ty` при построенном depth-буфере наблюдателя, `smooth` раздельно H/V по строкам; состояние курсора между порциями
- [x] 1.3 Юнит-тесты mask: порционная сборка побитово равна синхронной на тех же входах; commit атомарен

## 2. Фазовая машина и коалесинг (subsystems/fog.ts)

- [x] 2.1 Тройка сигнатур `signature`/`building`/`pending`; `syncTick` больше не строит растр — только скан, кандидат и конфляция в `pending`; `snapAll` — синхронный снап (текущая семантика)
- [x] 2.2 Исполнение порций в `updateFrame` до шага рассеивания под тексельный бюджет (по умолчанию — от площади маски, полная перестройка ≤3 кадров; `Infinity` — commit в первый кадр); запрет рестарта в полёте, после commit — старт от `pending`
- [x] 2.3 Обработка граней: смена разрешения/конфига/качества и dispose сбрасывают полётное состояние; `built` — только после первого commit (прямой проход кадра до него, FOW-7)
- [x] 2.4 Тесты: коалесинг (две доставки в полёте → одна перестройка от последней), нет полурастра ни в одном кадре, snapAll-аборт, смена разрешения в полёте, продвижение при доставке каждый кадр

## 3. Грязное окно рассеивания (fog.ts + fog/layer.ts)

- [x] 3.1 Блочная сетка 16×16: набор грязных блоков на commit поблочным сравнением старого/нового переднего растра, объединение с неустоявшимися блоками прошлого окна
- [x] 3.2 Проход рассеивания только по флагнутым блокам; блок, достигший цели, снимает флаг; `settling = (набор непуст)`; счётчик `fogDissolveTexels` — посещённые тексели (обновить doc-комментарий в cost.ts)
- [x] 3.3 Блит миникарты по грязным блокам; `putImageData` с грязным прямоугольником — расширить `FogLayerContext`, учесть Y-флип
- [x] 3.4 Тесты: устоявшаяся сцена платит ноль (steadyFrame сохраняется), окно сжимается по мере схождения, прерванное рассеивание доигрывается

## 4. Стадии счётчиков и cost-эталоны

- [x] 4.1 Растровые счётчики тумана (`fogRevealCalls`…`fogMinimapTexels`, `fogMaskUploadBytes`) → стадия `frame`; `fogEntitiesScanned` остаётся `syncTick`; поправить cost.test.ts и render-ts/test/fog.test.ts (прокачка `updateFrame`, `rebuilds` = закоммиченные)
- [x] 4.2 `npm run golden:cost` — регенерация всех `*.cost.json`, дифф в коммит

## 5. Тени (subsystems/lighting.ts)

- [x] 5.1 Чередование фаз: кадр статической перерисовки допустим, только если предыдущая фаза не `static` или динамических корней нет; тесты lighting.test.ts (чередование, счётчики, вырожденный случай без динамики) и sceneLighting.test.ts (кросс-слойный сценарий REND-30 «непрерывная мутация пола не выключает тени юнитов»)
- [x] 5.2 Точечные флаги нового корня: `setCaster` нового динамического корня применяет `applyFlagsTo` вместо взвода `flagsStale`; тест — реестр не обходится целиком при появлении кастера
- [x] 5.3 Проверить дифф perf-секций эталонов: `lightingDynamicCasters` уходит от нуля в match-парах (это и есть доказательство фикса)

## 6. Ворота и валидация

- [x] 6.1 `npm run check` целиком зелёный (typecheck, eslint, knip, jscpd, depcruise, spec-graph, тесты)
- [x] 6.2 `npm run bench:demo -- --drive cliff-jump` до/после — снято в headless (софтверный GL, стадия «рисование» и период кадра непоказательны): перестроек маски за окно 177 (8,9/с) → 48 (2,4/с) — коалесинг работает; стадия «кадр подсистем» p50/p99 1,4/12,9 → 2,4/9,4 мс, хотя после правки она несёт в себе всю перестройку, раньше жившую между кадрами; стадия ввода p99 3,7 → 1,3 мс (сходится). Прогон с настоящим GPU — за машиной с экраном, диагностика PERF-7
