# Tasks: memory-budget

## 1. Величины ядра (PERF-8, design D2)

- [ ] 1.1 Код записи `TICK_FOOTPRINT` в `DIAGNOSTIC_CODES` (`core-ts/src/types.ts`) и запись `tickFootprint` в `debug.ts` в конце штатного Running-тика с тем же контрактом числа записей, что у `TICK_COST`; проверить: `apiSurface.test.ts` зелёный (экспортов не прибавилось), `tickCost.test.ts`-подобный тест считает ровно по одной записи на Running-тик и ни одной на оборванный
- [ ] 1.2 `worldBytes` — сумма `byteLength` колонок плоской таблицы, масок и индекса, посчитанная один раз в `createWorld` и хранимая во внутреннем состоянии; `entitiesAlive`, `entitiesFree`, `tagEntries` — из индекса и тегов на момент записи; проверить: юнит-тест — спавн и гибель двигают `alive`/`free` на единицу, тег двигает `tagEntries`, `worldBytes` равен ручной сумме на мире из двух компонентов
- [ ] 1.3 Пики за тик `commandsPeak`, `eventsPeak` — буфер команд и журнал событий пишут максимум длины в поля контекста диагностики перед сбросом; `navHeapCapacity` — ёмкость кучи поиска пути после перевыделения; проверить: тест — тик с N командами даёт `commandsPeak >= N`, запрос пути на большой сетке поднимает `navHeapCapacity`, и оно не падает
- [ ] 1.4 Инертность и детерминизм: без стока учёт не исполняется и не аллоцирует (тест по образцу «Выключенный учёт бесплатен» `tickCost.test.ts`); два прогона одного сценария дают побитово одинаковый поток `TICK_FOOTPRINT`; прогнать чек-лист `determinism-review`; `traceParity.test.ts` и `traceCli.test.ts` зелёные с новой записью

## 2. Учёт рендера (PERF-8, PERF-9, design D3, D4)

- [ ] 2.1 `render-ts/src/footprint.ts`: сток с `own(kind, owner, resource)` (created/disposed по виду × владельцу, слушатель `dispose`) и `peak(field, value)`; три формы владения стоком как у `cost.ts`; экспорт из `index.ts`; проверить: юнит-тест — `own` без стока возвращает ресурс и ничего не считает, со стоком живое число растёт на создании и падает на `dispose()`, повторный `dispose()` не уводит в минус
- [ ] 2.2 Инструментировать все места создания геометрий, материалов, текстур и целей отрисовки в `render-ts/src` обёрткой `own` (владелец — имя подсистемы или разделяемого модуля); проверить: `npm run check` до и после даёт те же `*.cost.json` и поведенческие эталоны, `lifetime.test.ts` зелёный
- [ ] 2.3 Величины состояния через `peak`: `ViewBuffer` (записи, память курсов), модели (инстансы, записи и число батчей), частицы (пул), террейн (чанки), маска тумана (байты растра); проверить: тест — доставка с K сущностями даёт `viewRecords >= K`, пересборка кэша батчей меняет `modelsBatches`
- [ ] 2.4 Правило сканера в `engine/tests/guard/scanner.ts`: `new <класс-ресурс>(` в `render-ts/src` только аргументом `own(`, список классов и исключений с причинами — данные правила; `render-ts/test/guard.test.ts` по образцу `core-ts`; проверить: тест сканера — голое создание краснит с файлом и строкой, обёрнутое и исключённое — нет; правило зелёное на текущем `src`
- [ ] 2.5 Инвариант освобождения в `render-ts/test/lifetime.test.ts`: стенд пакета собирается, отыгрывает доставки и сносится десять раз — после каждого сноса `live === 0` по каждому владельцу и виду, величины состояния после десятого цикла равны величинам после первого; проверить: тест зелёный, а с закомментированным `dispose` одной текстуры тумана — красный с именем подсистемы и вида

## 3. Гейт эталонов (PERF-8, design D5)

- [ ] 3.1 `integration-ts/test/footprint.test.ts` на общей нагрузке `benchLoad.ts`: записанные матчи по обоим пресетам (секции `tick` и `history` один раз с проверкой равенства между пресетами, секции рендера — на пресет), оси `scaling`, `npc-stress`, `nav-path`, `extract` с полем памяти при каждом размере; сборка стенда с обоими стоками; проверить: тест пишет и сверяет `engine/tests/golden/*.footprint.json`, `UPDATE_COST=1` перезаписывает
- [ ] 3.2 Расширить `golden:cost` в `engine/integration-ts/package.json` вторым файлом теста; записать эталоны `*.footprint.json` и включить в коммит; проверить: `npm run golden:cost` перезаписывает оба набора, повторный прогон без изменений — пустой дифф
- [ ] 3.3 Машинная независимость: два прогона в одном процессе дают побитово тот же документ; секция `history` равна `size × worldBytes` провайдера; проверить: тест зелёный, документ не содержит ни одного поля с байтами кучи

## 4. Инварианты устоявшегося состояния (PERF-9, design D6)

- [ ] 4.1 Оборот ядра: синтетический сценарий в `integration-ts/test/fixtures.ts` (JSON-система: каждый тик спавн сущности с тегом и гибель самой старой); `TICK_FOOTPRINT` в точках A и B после прогрева равны полем в поле; проверить: тест зелёный, а с искусственно сломанным возвратом слота (локально) — красный по `entitiesFree`
- [ ] 4.2 Оборот доставки: `syntheticTick` с вращающимися идентификаторами через `ViewBuffer` в `client-ts/test` и через полный стенд в `integration-ts`; величины состояния в точках A и B равны; проверить: тест зелёный, с отключённым снятием исчезнувших записей — красный по `viewRecords`
- [ ] 4.3 Цикл полного тракта в `integration-ts` (с синтетическими ассетами `benchContent.ts`): десять циклов «собрать → доставки → снести», после каждого `live === 0`; проверить: тест зелёный на обоих пресетах

## 5. Сторож кучи (PERF-10, design D7)

- [ ] 5.1 `engine/tests/bench/memory.ts`: `forceGc()` через `v8.setFlagsFromString` + `vm.runInNewContext('gc')`, `sampleMemory()` — минимум из нескольких проб `heapUsed`/`arrayBuffers` после полной сборки, `growthOf()`; проверить: юнит-тест — после `forceGc` большой отпущенный массив не числится в `heapUsed`, помощник работает в worker thread и в fork
- [ ] 5.2 `integration-ts/test/memory.test.ts`: оборот сессий (K сборок и прогонов записанного матча, рост между K/2 и K), устоявшийся мир массы NPC (точки A/B), цикл стенда (N сборок и сносов); печать `[bench]` уровня и роста при каждом прогоне; ассерт только на относительный рост с запасом, обоснованным в шапке теста; проверить: тест зелёный на контейнере гейта и авторской машине, а с искусственно подвешенным слушателем на доставку (локально) — красный
- [ ] 5.3 Сторож оборота `ViewBuffer` в `client-ts/test/bench.test.ts` по тем же конвенциям; проверить: печать уровня и роста, зелёный

## 6. Браузерный бенч (PERF-7, design D8)

- [ ] 6.1 Источник `render.memory` в `render-ts/src/debug/sources.ts` (читает `renderer.info.memory`, отказ с причиной без набора), экспорт и регистрация в `game/demo-ts/app/main.ts` рядом с источником программ; проверить: тест источника по образцу `programsDebugSource` — число живого набора и причина при отсутствии
- [ ] 6.2 `bin/bench-demo.mjs`: `HeapProfiler.collectGarbage` + `Runtime.getHeapUsage` и чтение `render.memory` в тех же точках, что пробы отладки (до `reset()` и после `report()`); строки отчёта «было → стало (Δ)» под строкой программ с оговоркой метода; отказ CDP — «не измерено — причина»; проверить: `npm run bench:demo -- --drive cliff-jump --headless` печатает кучу и геометрии/текстуры на границах, вердикт и код возврата не зависят от них

## 7. Отладочный прогон (CLI-11, design D9)

- [ ] 7.1 `bin/demo-serve.mjs` в режиме `debugRun`: проба `process.memoryUsage()` в хуке после тика каждые `--memory-every <ticks>` (умолчание `tickRate × 10`), по окончании — первая, последняя проба и наклон на 1000 тиков (МНК) в stdout; `--memory-out <path>` пишет ряд JSONL; проверить: `npm run demo:debug -- --max-ticks 3000` печатает три строки памяти, число проб равно `⌊ticks / every⌋`
- [ ] 7.2 `run.json`, трейс, запись и журнал не меняются: `game/demo-ts/test/demoDebug.test.ts` дополнить проверкой, что сводка не содержит полей памяти, а ряд по флагу лежит отдельным файлом; проверить: тест зелёный, два прогона одной записи сравниваются `diff`'ом

## 8. Документация и гейт

- [ ] 8.1 CLAUDE.md, раздел «Commands»: эталоны `*.footprint.json` под `npm run golden:cost`, сторожа кучи как PERF-10, строки памяти `bench:demo`, флаги `--memory-every`/`--memory-out` у `demo:debug`; таблица capability в `docs/architecture.md` (`performance-budget` PERF-1..10) и строка roadmap; проверить: `npm run spec-graph -- check` без висячих ID (PERF-8..10 цитируются кодом и тестами)
- [ ] 8.2 Smoke гейта локально без коммита: убрать `dispose` одной текстуры — PERF-9 красный с именем подсистемы; подвесить слушатель на доставку — PERF-10 красный, PERF-8 не двигается; поднять ёмкость мира вдвое — `*.footprint.json` красный точным диффом `worldBytes`; проверить: все три сценария наблюдены, правки откачены
- [ ] 8.3 `npm run check` зелёный: typecheck, eslint, knip, jscpd, depcruise (стенд не читает `content/`, CONT-4), spec-graph, тесты; поведенческие эталоны, записи матчей и `*.cost.json` не изменились
