## 1. Сверка реализации с нормой

- [x] 1.1 `engine/core-ts/src/ecs/entityIndex.ts`: проверить, что `allocate`/`free` соответствуют ID-6 обоими концами — освобождение кладёт слот в конец списка, аллокация берёт с конца, при пустом списке берётся новый слот по счётчику
- [x] 1.2 Там же: проверить, что счётчик инкрементируется только при взятии нового слота и не уменьшается при освобождении (ID-2)
- [x] 1.3 `engine/core-ts/src/ecs/world.ts`: проверить, что все три части состояния схемы идентификаторов (счётчик, поколения, список свободных слотов) входят в плоскую форму, в клонирование и в восстановление, и что восстановление не выводит список из признака занятости слотов (ID-4)
- [x] 1.4 `engine/core-ts/src/sim/worldInit.ts`: убедиться, что представление берёт плоскую форму мира целиком, без отбора полей, и что второго пути сериализации мира для хеша не заводится (DET-1 п. 3)
- [x] 1.5 Расхождений быть не должно; любое найденное — дефект реализации (CORE-3), чинится здесь же, а не переписыванием нормы

## 2. Комментарии в коде

- [x] 2.1 `entityIndex.ts`: в шапке и у `allocate`/`free` сослаться на ID-6 как на источник правила LIFO — сейчас порядок описан как свойство реализации («стек освобождённых индексов, LIFO — переиспользование детерминировано»)
- [x] 2.2 `world.ts`, комментарий к `PlainWorld`: сослаться на ID-4 (состав) и на DET-1 п. 3 (плоская форма входит в хеш целиком) вместо самостоятельного объяснения, почему список свободных слотов нельзя восстановить из перечня живых

## 3. Тесты: политика выделения слота (ID-6)

- [x] 3.1 `engine/core-ts/test/entityIndex.test.ts`: спавн после смерти занимает последний освобождённый слот, а не наименьший свободный и не новый
- [x] 3.2 Там же: две смерти, затем спавн — занимается слот, освобождённый вторым
- [x] 3.3 Там же: спавн при пустом списке берёт очередной новый слот; исчерпание `capacity` при пустом списке по-прежнему бросает (ID-1) — существующий тест не переписывать, а дополнить случаем «непустой список при `nextIndex == capacity`»: аллокация законна
- [x] 3.4 `engine/core-ts/test/tick.test.ts` (или `ecs.test.ts`): порядок освобождений задаётся порядком применения команд, а не порядком их создания — два `destroy`, поставленные в одном теле системы в одном порядке, дают предсказуемое содержимое стека, и следующий спавн берёт ожидаемый слот (CMD-3)

## 4. Тесты: счётчик (ID-2)

- [x] 4.1 `entityIndex.test.ts`: три спавна при пустом списке дают последовательные индексы, счётчик вырастает на три
- [x] 4.2 Там же: три спавна при непустом списке берут индексы из списка, счётчик не меняется
- [x] 4.3 Там же: удаление не уменьшает счётчик
- [x] 4.4 Проверить, что существующие тесты не полагаются на «спавн всегда двигает счётчик» — старая формулировка ID-2 могла успеть стать тестом

## 5. Тесты: снапшот и откат (ID-4, SNAP-1)

- [x] 5.1 `engine/core-ts/test/rewind.test.ts`: откат через удаление — сущность умирает на T+3, `seekTo(T)`, реплей; слот освобождается там же, где в первом прогоне, и спавн после T+3 занимает тот же слот
- [x] 5.2 Там же: спавн сразу после восстановления из снапшота даёт тот же `{index, generation}`, что и прогон без восстановления
- [x] 5.3 `engine/core-ts/test/serialization.test.ts`: круг «плоская форма → мир → плоская форма» сохраняет все три части состояния схемы идентификаторов, включая непустой список свободных слотов
- [x] 5.4 `engine/core-ts/test/filter.test.ts`: персональный снапшот расходится с каноническим по поколениям и списку свободных слотов, канонический мир при этом не тронут (NET-12) — если тест уже есть, дополнить проверкой канонического мира

## 6. Тесты: хеш `worldInit` (DET-1)

- [x] 6.1 `engine/core-ts/test/worldInit.test.ts`: список свободных слотов присутствует в каноническом представлении пустым списком сразу после стартовой расстановки, а счётчик равен числу расставленных сущностей
- [x] 6.2 Там же: хеши существующих сцен не изменились — зафиксировать значением, а не только сравнением двух прогонов

## 7. Эталон (опционально, см. Open Questions в design.md)

- [x] 7.1 Решить, заводить ли golden-пару на «удаление → перемотка → спавн»; если да — `engine/tests/golden/entity-slot-reuse.{scenario,golden}.json` как фикстуру движка (CONT-4)
- [x] 7.2 При заведении — `npm run golden` из корня; убедиться, что прочие эталоны не двинулись (поведение не менялось)

## 8. Проверка

- [ ] 8.1 `npm run typecheck` и `npm test` из корня — зелено
- [ ] 8.2 `npm run golden` из корня: диффов быть не должно — change нормирует существующее поведение
- [ ] 8.3 `openspec validate --specs --strict` после apply
