# Tasks — ramp-chain-surface

## 1. Правило углов в рендере

- [ ] 1.1 Обобщить `cornerLevels` (`engine/render-ts/src/visualSurface.ts`): притяжение вверх — как прежде, вниз — только к соседу без рампы (design, решение 1); обновить комментарий модуля про смыкание пар
- [ ] 1.2 Юнит-тесты правила: непрерывность узловой базы на цепочке рамп (общее ребро пары рамп — уровень верхней клетки); байт-паритет одиночной рампы, пар «рампа+плато» и «рампа+низина»; двузначность cliff-узлов; детерминизм при конфликте пар в одном углу
- [ ] 1.3 Тест геометрии пола на цепочке (`subsystems/terrain.ts` через существующий генератор): вершины соседних клеток-рамп на общем ребре совпадают — вертикального разрыва без cliff-стенки нет; обновить пиновки существующих тестов, задетые цепочками, с диффом в коммите
- [ ] 1.4 Проверить документный источник (`update` прямоугольником): правка уровня или флага рампы клетки пересчитывает углы соседей — существующее расширение на клетку покрывает новую зависимость от флага; тест на инвалидацию кистью

## 2. Паритет импорта скалпта

- [ ] 2.1 Тест инвариантности остатка: узловая база остатка (максимум по примыкающим клеткам) на цепочке рамп совпадает до и после обобщения (design, решение 2 и риск «невидимая связка»); числа в тесте «цепочка рамп» (`tools/blender-ts/test/sculpt.test.ts`) не меняются
- [ ] 2.2 Сквозная проверка восстановления: рендерная поверхность из импортированных слоёв совпадает со скалптом в узлах цепочки с точностью квантования 1/32 (сценарий REND-9 «Импорт скалпта восстанавливает длинный склон»)
- [ ] 2.3 Переписать комментарий «известный предел» у теста «цепочка рамп» на утверждение точного восстановления

## 3. Документация и комментарии

- [ ] 3.1 `tools/blender-ts/CONVENTIONS.md`: в «Кривизна — остаток» заменить абзац «Известный предел» утверждением непрерывности цепочек (ссылка на REND-9 вместо ссылки на этот change)
- [ ] 3.2 Пройти по упоминаниям change `ramp-chain-surface` и предела в комментариях кода (`visualSurface.ts`, `sculpt.ts`, тесты) и снять устаревшие оговорки

## 4. Ворота

- [ ] 4.1 `npm run check` зелёный с корня; голдены симуляции не тронуты (правка — рендер, `TickResult` не меняется)
- [ ] 4.2 Cost-базлайны без изменений (число квадов прежнее); при сдвиге счётчиков — разобраться в причине, а не принимать `golden:cost` молча
