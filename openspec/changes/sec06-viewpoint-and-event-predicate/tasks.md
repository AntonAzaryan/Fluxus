# Tasks: sec06-viewpoint-and-event-predicate

Требования — `specs/netcode/spec.md` (**NET-13**) и `specs/netcode-transport/spec.md` (**NTR-9**), разбор решений — design.md (D1–D8). Гейт локальный: `npm run check` из корня (CI нет).

Изменение заводится по факту сделанной работы: валидация спек против кода (`docs/reviews/spec-validation-2026-09-01/06-netcode.md`) назвала обе находки, и обе чинятся кодом — спека требует правильного (NET-12, NET-15, NET-13), а расходится с ней реализация (CORE-3).

## 1. Предикат видимости события — имя, а не функция (D5–D8)

- [x] 1.1 `engine/core-ts/src/sim/filter.ts`: тип `EventVisibilityName` (закрытый набор `any-referenced` / `all-referenced`), второй предикат набора `allReferencedVisible`, резолв `eventVisibilityByName` с названным отказом на незнакомом имени. Нормированный предикат остаётся значением параметра, а не спрятанным умолчанием.
- [x] 1.2 `engine/core-ts/src/index.ts` и `test/api-surface.golden.json`: экспорт резолва и типа имени (расширение поверхности принято явно, `UPDATE_API=1`).
- [x] 1.3 `engine/net-ts/src/server/matchServer.ts`: поле `eventVisibility` конфига — имя; резолв один раз на сборке, до подъёма мира; `filterFor` зовёт разрешённый предикат.
- [x] 1.4 `engine/net-ts/bin/matchFile.mjs`, `matchFile.d.mts`, `game/demo-ts/app/match.ts`: `eventVisibility` переезжает из полей сборки запуска в поля документа матча.
- [x] 1.5 Тесты: `engine/core-ts/test/filter.test.ts` — расхождение двух имён ровно на разной видимости сторон, общее событие при обоих именах, отказ на незнакомом имени; `engine/net-ts/test/events.test.ts` — имя из конфига действует на кадр и на поток одним вызовом, незнакомое имя роняет сборку; `engine/net-ts/test/matchFile.test.ts` — поле доезжает документом.

## 2. Точка зрения слота — команда его сущности (D1–D4)

- [x] 2.1 `engine/net-ts/src/match/world.ts`: `slotTeams(world, slots)` — команды слотов по миру матча, с отказом на слоте, названном двумя командами.
- [x] 2.2 `engine/net-ts/src/server/matchServer.ts`: `resolveViewpoints` на сборке — объявленная команда обязана совпасть с командой сущности слота, иначе отказ; фолбэк на номер слота только для мира без команд; диапазон `[0, 31]` проверяется и у выведенного значения. `viewpointOf` читает посчитанное.
- [x] 2.3 Тесты `engine/net-ts/test/filtering.test.ts`: кооперативная сцена (регрессия — слот 1 в команде 0 видит себя и союзника), совпавшая объявленная команда, отказ на расхождении, отказ на слоте с двумя командами.
- [x] 2.4 Фикстура `engine/integration-ts/test/eventDelivery.test.ts`: расстановка называет команду каждого героя — прежде оба стояли в команде A, и клиент B смотрел глазами команды, которой в мире нет.

## 3. Дельты требований

- [x] 3.1 `specs/netcode/spec.md`: MODIFIED NET-13 — блок «Набор предикатов» (закрытый набор имён, расширение — правка спеки, незнакомое имя — отказ), поле конфига называет предикат ИМЕНЕМ, закрытие вопроса любым из имён — правка документа матча. Сценарии «Закрытие открытого вопроса о видимости сторон» (переписан) и «Имя предиката с опечаткой» (новый).
- [x] 3.2 `specs/netcode-transport/spec.md`: MODIFIED NTR-9 — блок «Откуда берётся точка зрения слота» (команда сущности, отказ на расхождении, фолбэк для мира без команд, диапазон и сентинель), предикат событий назван полем ДОКУМЕНТА матча. Три новых сценария.
- [x] 3.3 Перенести дельты в `openspec/specs/netcode/spec.md` и `openspec/specs/netcode-transport/spec.md`; правка вне требований из `## Notes` — абзац открытого вопроса «Кто получает `viewpoint = ALL`». Изменение НЕ архивируется: остаётся в очереди записью о принятом решении.

## 4. Тексты, которые обязаны идти в шаге со спекой

- [x] 4.1 `docs/architecture.md`, открытый вопрос 7 («Кто получает `viewpoint = ALL`»): абзац о ненормированной проверке диапазона снят — диапазон точки зрения слота и отказ на сентинеле нормирует теперь NTR-9.
- [x] 4.2 Там же, открытый вопрос 8 («Политика видимости событий и снарядов»): сказано, что открыт геймплейный выбор, а не механизм, — предикат называется именем из документа матча.

## 5. Гейт

- [x] 5.1 Пакетные тесты: `engine/core-ts`, `engine/net-ts`, `engine/integration-ts`, `engine/client-ts`, `game/demo-ts`.
- [x] 5.2 `npm run typecheck`, `npm run lint`, `npm run lint:dead`, `npm run spec-graph -- check`, `npx openspec validate --specs --strict` и `--changes --strict`.
