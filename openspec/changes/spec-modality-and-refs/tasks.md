## 1. Ссылки на CLI-6

- [ ] 1.1 `openspec/specs/rng/spec.md`, RNG-6: «сверяется между реализациями (CLI-6)» → «(`cli-testing` CLI-6)»
- [ ] 1.2 Там же, RNG-8: «(`determinism-core` CLI-6)» → «(`cli-testing` CLI-6)»
- [ ] 1.3 Убедиться, что прочие упоминания `CLI-6` в `rng` не появились и не изменились: правка ровно в двух строках

## 2. Модальность в редакторе

- [ ] 2.1 `openspec/specs/editor/spec.md`, ED-10: «— ручная правка текстовых карт не должна быть обязательной» → «; ручная правка текстовых карт MUST NOT быть обязательной»
- [ ] 2.2 Там же, ED-11: то же для «ручная правка текстовых рядов»
- [ ] 2.3 Там же, ED-14: «Ручная правка манифеста не должна быть обязательной» → «Ручная правка манифеста MUST NOT быть обязательной»
- [ ] 2.4 Проверить, что ED-20 не тронут: он образец формы, а не предмет правки

## 3. Типы полей по ECS-3

- [ ] 3.1 `openspec/specs/arena/spec.md`, ARENA-3: `ArenaState { inside: i32, onFloor: i32 }`; кодирование флага — ссылкой на `ecs-foundation` ECS-3, без пересказа правила
- [ ] 3.2 Там же: дефолт обоих полей объявить числом `1` со словесной расшифровкой «внутри арены, на полу»; следующий абзац про спавн за краем сохранить
- [ ] 3.3 Там же: добавить сценарий «В поле inside лежит не единица» — единственный новый сценарий этого изменения
- [ ] 3.4 `openspec/specs/fog-of-war/spec.md`, FOW-3: `Stealth { active: i32 }`; абзац-обоснование ниже не трогать
- [ ] 3.5 `openspec/specs/time-system/spec.md`, TWEEN-1: «поля компонента ECS SHALL быть скалярами `i32`/`fixed`» → «значение поля компонента ECS SHALL быть скаляром типа из закрытого набора, который задан там же»; сценарий «Путь к полю в компоненте» не трогать

## 4. Проверка

- [ ] 4.1 `openspec validate --specs --strict` — зелено (был зелёным и до правки: см. `design.md`, Context)
- [ ] 4.2 Дифф главных спек: ровно восемь требований, ни одного нового ID, ни одной новой capability
- [ ] 4.3 Убедиться, что `engine/`, `content/` и `docs/` не затронуты; `npm test` и `npm run golden` запускать незачем — правка текстовая
- [ ] 4.4 Сверить `engine/core-ts` на соответствие исправленному тексту: `ArenaState.inside`/`onFloor` и `Stealth.active` объявлены целочисленными, дефолт `inside`/`onFloor` — `1`. Расхождение здесь означало бы дефект реализации (CORE-3) и отдельный change, а не откат этой правки
