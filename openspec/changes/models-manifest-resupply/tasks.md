## 1. Норма

- [x] 1.1 Дельта `specs/rendering/spec.md`, `ADDED` REND-17: переподача манифеста после инициализации как вход подсистемы моделей; декларативность (манифест целиком, императивной правки поля записи нет); запрет пере-инициализации подсистемы и пересборки сцены
- [x] 1.2 Там же, REND-17: граница «пересобирается/переживает» — пересборка при смене того, что строится из разделяемых данных ассета (REND-3); обновление на месте наклона (REND-10), переда (REND-13), вертикального смещения (REND-12), таблиц анимаций (REND-4), контроля костей (REND-5), скинов (REND-6) и масштаба записи
- [x] 1.3 Там же, REND-17: назначенное поимённо (скин, клип и масштаб набора REND-11; скин смены REND-6) переживает переподачу, `defaultSkin` достаётся невыбранным; переподача без правок наблюдаемых последствий не имеет; отклик не позже следующего кадра (ED-15); манифест из памяти как перечисленный вход REND-1; неизменность игрового пути
- [x] 1.4 Сверить, что дельта не занимает и не правит чужих номеров: занят только REND-17 (новое), REND-1/REND-3/REND-8/REND-11/REND-14 и ASSET-1/ASSET-4..6, ED-1/ED-8/ED-14/ED-15/ED-18/ED-20 — только ссылками; REND-14 за `editor-terrain-source`, REND-15 и REND-16 за `viewport-services`
- [x] 1.5 `openspec validate models-manifest-resupply --type change --strict` и `openspec validate --specs --strict` — зелены

## 2. Подмена данных записи у долгоживущих объектов

- [x] 2.1 `engine/render-ts/src/model/animation.ts`: подмена таблиц анимаций у построенного контроллера; переигрывание только при смене записи, которая играла бы сейчас (назначенный клип либо запись текущего состояния); дедупликация предупреждений по записи не сбрасывается (REND-4)
- [x] 2.2 `engine/render-ts/src/model/boneControl.ts`: подмена параметров ролей; сглаженный угол роли переживает подмену, если кость роли не изменилась, иначе роль начинается заново вместе со своим однократным предупреждением (REND-5)
- [x] 2.3 `engine/render-ts/src/model/build.ts`: перестановка нормализующего масштаба у построенного инстанса с пересчётом габаритов тем же множителем — второго ответа на «какого размера нарисованный инстанс» не появляется (REND-15)

## 3. Переподача в подсистеме моделей

- [x] 3.1 `engine/render-ts/src/subsystems/models.ts`: вход `applyManifest` — манифест целиком, по образцу `TerrainSubsystem.applyGrid`; поля записи в `InstanceRecord` перестают быть однократными
- [x] 3.2 Там же: пересборка инстанса при смене модели либо набора рисуемых частей — тем же путём, что создание (заглушка на время загрузки, разделяемые данные из кэша, REND-3, ASSET-4); прежний инстанс убирается без утечки подписок ожидания ассета
- [x] 3.3 Там же: обновление на месте — наклон через `resolveSurfaceAlign` (с глобальным дефолтом манифеста), перёд, вертикальное смещение, таблицы анимаций, контроль костей, масштаб записи
- [x] 3.4 Там же: скин — бит «выбран поимённо» против «взят из записи»; повторное применение только при изменении подмен выбранного скина (REND-6)
- [x] 3.5 Там же: сущность без записи и появление записи у неё — заглушка и предупреждение один раз остаются в силе (ASSET-6), второго инстанса не появляется
- [x] 3.6 `engine/render-ts/src/index.ts`: публичный API пакета — переподача видна потребителю (редактору), состав экспортов в остальном прежний

## 4. Тесты (headless, без WebGL-контекста)

- [x] 4.1 `engine/render-ts/test/manifestResupply.test.ts`: правка параметра наклона обновляет инстанс, не пересобирая его — тот же объект модели, тот же скелет, материалы скина и фаза анимации на месте (REND-17, REND-10)
- [x] 4.2 Там же: смена модели записи пересобирает инстанс; разделяемые буферы прежней модели остаются в кэше и достаются другому инстансу (REND-3)
- [x] 4.3 Там же: скин, назначенный набором инстансов, переживает переподачу, меняющую `defaultSkin`; скин, не выбиравшийся, переезжает на новый `defaultSkin` (REND-11, REND-6)
- [x] 4.4 Там же: правка клипа текущего состояния меняет клип, правка клипа чужого состояния — нет; переподача без правок не трогает ни клип, ни скин, ни модель
- [x] 4.5 Там же: правка подмен выбранного скина доезжает до материалов инстанса и не трогает инстанс той же модели в другом скине (REND-6)
- [x] 4.6 Там же: игровой путь — сборка, которая переподачи не зовёт, ведёт себя как прежде; существующие тесты пакета не правятся

## 5. Приёмка

- [x] 5.1 `npm run typecheck -w @game-mvp/render -w @game-mvp/client`, `npm test -w @game-mvp/render`, `npm test -w @game-mvp/client`, `npm test -w @game-mvp/integration`, `npx eslint engine/render-ts --max-warnings 0` — зелены
- [x] 5.2 Проверить, что `engine/client-ts`, `content/`, `engine/tests/golden/` и пакеты симуляции не тронуты: работа целиком в presentation-слое (ASSET-1)
