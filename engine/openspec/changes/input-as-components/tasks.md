## 1. Оператор bitTest (EXPR-2)

- [ ] 1.1 `bitTest [mask, index]` в таблице операторов `src/expr.ts`, без приведения к Q16.16
- [ ] 1.2 Валидация арности и типов; тесты на границы (бит 0, бит 31, отрицательный индекс)

## 2. InputSystem (TICK-4, TICK-5)

- [ ] 2.1 `src/inputSystem.ts`: конструктор с именами компонентов ввода и слота, списком игроков, `order`
- [ ] 2.2 Раскладка фреймов тика в компоненты через Command Buffer
- [ ] 2.3 `prevButtons` — копия маски прошлого тика, пишется перед новой
- [ ] 2.4 Нет фрейма для слота на тике → `move`/`buttons` обнуляются
- [ ] 2.5 Фрейм слота без живой сущности → ошибка с номером слота
- [ ] 2.6 Экспорт из `src/index.ts`

## 3. Сценарий CLI (CLI-2)

- [ ] 3.1 Поле `players` в `ScenarioDef`; регистрация `InputSystem`, когда оно задано
- [ ] 3.2 `inputs[]` без `players` — ошибка до первого тика
- [ ] 3.3 `scenario.schema.json` перегенерирована (`npm run schemas`)

## 4. Тесты и эталоны

- [ ] 4.1 JSON-система движения по вводу: golden-сценарий с `players` и `inputs[]`
- [ ] 4.2 Golden краснеет при изменении фрейма — правка ввода меняет эталон
- [ ] 4.3 Фронт кнопки: JSON-система с `bitTest` реагирует на нажатие, но не на удержание
- [ ] 4.4 Пропуск фрейма обнуляет намерение движения
- [ ] 4.5 Эталоны существующих сценариев не изменились

## 5. Проверка

- [ ] 5.1 `npm test` и `npm run typecheck` в `core-ts` зелёные
- [ ] 5.2 `openspec validate input-as-components --strict`
- [ ] 5.3 `docs/architecture.md` — отметка о закрытии разрыва в покрытии ввода (этап 10)
