## 1. Коллайдеры и статика (PHYS-2, PHYS-7)

- [x] 1.1 `src/physics.ts`: формы (круг, AABB) полями компонента, блокировка тегами
- [x] 1.2 Статика обрывов из cliff-геометрии террейна — прямоугольники нулевой толщины
- [x] 1.3 Имена компонентов коллайдера и скорости — параметры, не конвенции ядра

## 2. Broad-phase / narrow-phase (PHYS-5)

- [x] 2.1 Равномерная сетка по статике; дедупликация по клеткам, порядок по индексу
- [x] 2.2 Динамика обходится линейно по живым позициям, без индекса
- [x] 2.3 Касание не считается пересечением

## 3. Разрешение движения (PHYS-8, PHYS-9)

- [x] 3.1 Нативная система за общим контрактом; `Position` — только через Command Buffer
- [x] 3.2 Разрешение по осям, гашение заблокированной оси, скольжение как следствие
- [x] 3.3 Свёрнутый тест — снаряд не проскакивает сквозь границу нулевой толщины
- [x] 3.4 Уже разрешённые позиции учитываются в пределах тика
- [x] 3.5 Событие `Collision` с участником и нормалью

## 4. Raycast (PHYS-6)

- [x] 4.1 Ближайшее попадание; коробка — метод слэбов, круг — через проекцию
- [x] 4.2 `mask` фильтрует по тегу, `ignore` исключает источник
- [x] 4.3 Попадание в статику — без идентификатора сущности; нулевой луч — без попадания
- [x] 4.4 Деление с насыщением вместо `fixed.div` на околонулевом знаменателе

## 5. Сборка и формат (DI-3, CLI-2)

- [x] 5.1 Поле `physics` в сценарии; статика строится из террейна, система регистрируется
- [x] 5.2 `Simulation.physics` получает реализацию; схема сценария перегенерирована

## 6. Проверка

- [x] 6.1 `test/physics.test.ts`: статика, broad-phase, блокировки, событие, raycast
- [x] 6.2 Golden-сценарий `physics`: обрыв, блокировка сущностью, отсутствие туннелирования
- [x] 6.3 `npm test` и `npm run typecheck` зелёные; прежние эталоны не изменились
- [x] 6.4 `openspec validate physics-collisions-raycast --strict`
- [x] 6.5 `docs/architecture.md` §5 — этап 13 отмечен выполненным
