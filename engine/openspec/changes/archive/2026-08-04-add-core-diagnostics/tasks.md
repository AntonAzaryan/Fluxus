## 1. Контракт диагностики

- [x] 1.1 Объявить в `src/types.ts` форму записи и контракт sink'а по DIAG-2: тик, сквозной номер, имя системы, вид записи, уровень, стабильный код, данные, исход. Отметки реального времени в форме нет
- [x] 1.2 Объявить перечисление уровней детализации по DIAG-3 (выключено / границы систем / полный поток); выключенный — значение по умолчанию
- [x] 1.3 Завести перечень стабильных кодов записей: нарушенные инварианты (переполнение, деление на ноль, `sqrt` от отрицательного, переполнение поколения, выход за границы маски, превышение capacity) и исходы команд
- [x] 1.4 Переписать `src/debug.ts`: `assert`/`assertInvariant` сохраняют сигнатуры, но пишут в контекст диагностики; `setAssertSink` и модульная переменная sink'а удаляются (design.md, решение 2)

## 2. Контекст диагностики и проводка в тик

- [x] 2.1 Реализовать контекст диагностики: текущий тик, текущая система, счётчик записей, уровень, ссылка на sink; создаётся на тик при подключённом sink'е независимо от уровня — уровень гасит трейс, но не диагностику инвариантов (DIAG-3, design.md решение 3)
- [x] 2.2 Добавить опциональное поле sink'а на `Simulation` рядом с `physics`/`navigation` по DI-5; в `SimulationState` не класть — иначе попадёт в снапшот
- [x] 2.3 В `advance()` создавать контекст в начале тика, проставлять имя текущей системы перед `system.run(ctx)`, снимать после
- [x] 2.4 Проверить, что без sink'а контекст не создаётся и точки съёма отсекаются одним сравнением

## 3. Трейс границ систем

- [x] 3.1 Писать записи начала и окончания системы со счётчиками по DIAG-3: заказано команд, применено, опубликовано событий
- [x] 3.2 Добавить счётчик отобранных запросом сущностей — ответ на пустой отбор, ради которого уровень и заведён

## 4. Трейс потока Command Buffer

- [x] 4.1 Съём записи о заказанной команде в `ecs/commands.ts` с атрибуцией по текущей системе (DIAG-5)
- [x] 4.2 Проставлять исход `applied` и `dropped:dead-target` из существующих веток `flush()`, не меняя поведение CMD-7: отбрасывание остаётся штатным и молчаливым для симуляции
- [x] 4.3 Определять перекрытие (CMD-3) картой последнего писателя по ключу «сущность + компонент + поле», строимой только при полном уровне (design.md, решение 4)
- [x] 4.4 Съём записи о публикации события в `ecs/events.ts` — вторая точка съёма ради общего порядка команд и событий (design.md, решение 5)

## 5. Вывод CLI

- [x] 5.1 Принять уровень трейса параметром запуска `bin/sim.mjs`, по умолчанию выключен (CLI-7)
- [x] 5.2 Выводить трейс в поток, отдельный от документа CLI-3, построчным JSONL; stdout не трогать
- [x] 5.3 Прокинуть уровень и sink через `runScenario`/`runScenarioBytes`, не меняя форму возвращаемого документа

## 6. Тесты

- [x] 6.1 Тест инертности (DIAG-4, DI-5): один сценарий прогоняется с sink'ом полного уровня и без него, потиковые снапшоты сверяются побитово
- [x] 6.2 Тест детерминированности трейса (DIAG-6): два прогона одного сценария дают побитово равный трейс
- [x] 6.3 Тест исходов команд (DIAG-5): сценарий с командой к убитой в этом же тике сущности и сценарий с системой, дважды пишущей одно поле (перекрытие возможно только внутри одного flush, CMD-2); проверяются исходы `dropped:dead-target` и `overwritten`
- [x] 6.4 Тест пустого отбора (DIAG-3): система с запросом, не отбирающим ничего, оставляет запись с нулевым счётчиком
- [x] 6.5 Тест недостижимости изнутри (DIAG-7): записи отсутствуют в `TickResult` и в снапшоте
- [x] 6.6 Тест обрыва (CLI-7): прогон, прерванный жёсткой границей, оставляет выведенные строки трейса валидными
- [x] 6.7 Прогнать `npm test` и `npm run typecheck`; убедиться, что golden-эталоны не изменились

## 7. Сведение документации

- [x] 7.1 Дописать `docs/architecture.md`: диагностика в карте спецификаций и в схеме слоёв как выход, параллельный `TickObserver`; нормативных формулировок не дублировать
- [x] 7.2 Отметить в roadmap появление capability `diagnostics` — как это сделано для `arena`, появившейся после составления roadmap
