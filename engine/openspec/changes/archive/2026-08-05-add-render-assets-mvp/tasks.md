# Tasks: add-render-assets-mvp

## 1. Применение дельт в основные спеки

- [x] 1.1 Создать `openspec/specs/assets/spec.md` с секцией `## Purpose` и требованиями ASSET-1..ASSET-6 из дельта-спеки (включая Open Questions)
- [x] 1.2 Создать `openspec/specs/rendering/spec.md` с секцией `## Purpose` и требованиями REND-1..REND-8 из дельта-спеки (включая Open Questions)
- [x] 1.3 Существующие спеки не трогать: `tick-loop`, `fog-of-war`, `terrain`, `serialization` упоминаются только ссылками из новых capability

## 2. Синхронизация архитектурного обзора

- [x] 2.1 `docs/architecture.md` §2 «Карта спецификаций»: добавить строки `assets` (ASSET-1..6) и `rendering` (REND-1..8)
- [x] 2.2 `docs/architecture.md` §3 «Механизм против политики»: добавить строки — проигрывание/кроссфейд клипов против маппинга «состояние → клип» в манифесте; override костей против лимитов и скоростей в манифесте; подмена текстур против состава скинов
- [x] 2.3 `docs/architecture.md` §5 «Roadmap»: добавить этап «Render + assets MVP» (пакеты `assets-ts`, `render-ts`, демо); отметить, что fog-mask из этапа 18 и частицы остаются последующими этапами

## 3. Пакет engine/assets-ts

- [x] 3.1 Каркас пакета (без зависимостей на Three.js/DOM): типизированные handle, кэш по ID, состояния loading/ready/failed с подпиской и синхронным опросом (ASSET-2, ASSET-4)
- [x] 3.2 Реестр загрузчиков по расширению/типу; ошибка «нет загрузчика» → failed (ASSET-3)
- [x] 3.3 Загрузчик MDX поверх `war3-model`: нормализованное представление модели — геометрия/веса как TypedArray, скелет с именами костей, секвенции с ключами, geoset-альфа; перенос знаний из `draft/ts-render/src/mdxModel.ts` (парсинговая половина)
- [x] 3.4 Загрузчик текстур (PNG/ImageBitmap с headless-фолбэком на raw-данные) и загрузчик JSON-манифеста визуалов со схемой-валидацией (ASSET-6)
- [x] 3.5 Тесты в Node без браузера: кэш и идентичность, реестр, состояния и failed-причины, парсинг эталонной MDX-модели, валидация манифеста (ASSET-5)

## 4. Пакет engine/render-ts

- [x] 4.1 Каркас: `RenderHost` как `TickObserver` — копирование в presentation-состояние двух последних тиков, конверсия Q16.16→float на границе, интерполяция/snap (REND-1, REND-2)
- [x] 4.2 Контракт подсистемы (`init`/`syncTick`/`updateFrame`) и упорядоченный реестр подсистем (REND-8)
- [x] 4.3 Подсистема террейна: ступени из карт уровней/пола и cliff-отрезков ядра, рампы, дыры; обновление по мутации пола (REND-7)
- [x] 4.4 Подсистема моделей: построение THREE-инстансов из нормализованных данных ассета (скелет/меши — THREE-половина бывшего `mdxModel.ts`), пул инстансов по снапшоту (REND-3)
- [x] 4.5 Анимационный контроллер: состояния idle/move из компонент движения, one-shot по событиям, кроссфейд, маппинг из манифеста (REND-4)
- [x] 4.6 Bone-контроль: пост-анимационный override корпуса/головы по данным симуляции, лимиты и сглаживание из манифеста, предупреждение при отсутствующей кости (REND-5)
- [x] 4.7 Скины: применение набора подмен текстур к материалам инстанса, смена без перезагрузки модели (REND-6)

## 5. Демо и контент

- [x] 5.1 Демо-приложение (vite): сцена с двухуровневым террейном и рампой, игрок под управлением, каст фаербола; перенос обвязки из `draft/ts-render/src/demo.ts` (`engine/render-ts/demo/`, `npm run dev`; сцена собрана из golden-сценариев ядра input-drive/terrain/physics)
- [x] 5.2 Контент: эталонная MDX-модель юнита + предконвертированные PNG-текстуры двух скинов, модель снаряда (или заглушка), манифест визуалов (снаряд — заглушка намеренно: частицы отложены; манифест заполнен по интроспекции модели — клипы Stand/Walk/Spell Slam/Death, кости Bone_Chest/Bone_Head, слоты 0/1)
- [x] 5.3 Ручная проверка сценариев спеки на демо: интерполяция при высоком FPS, смена клипов при беге/касте, доворот торса к цели, смена скина, выбитый пол (проверено в браузере 2026-08-05: движение/Walk с доворотом по курсу, подъём по рампе на уровень 1, каст Spell Slam с разворотом к цели и заглушкой снаряда, живая смена скина steel→ember, смерть по K с фиксацией кадра; консоль без ошибок. Выбитый пол — headless-смоук `smoke-sim.mjs` (hasFloorAt=false после взрыва) + юнит-тесты дельты чанков; статические дыры видны в кадре)

## 6. Когерентность

- [x] 6.1 `openspec validate --specs --strict` — 18 capability проходят (CLI `@fission-ai/openspec` 1.7.0 установлен в переносимый Node 22; change тоже валиден)
- [x] 6.2 Проверить, что ссылки из новых спек резолвятся: OBS-1..3 (`tick-loop`), SER-7 (`serialization`), TERR-2/5/6 (`terrain`), FOW-7..9 (`fog-of-war`) — все ID находятся ровно один раз
- [x] 6.3 Убедиться, что ядро не изменилось: `engine/core-ts` без диффа исходников (только package-lock: замена недоступного artifactory-registry на npmjs), 385/385 тестов включая golden проходят
- [x] 6.4 Убедиться, что `assets-ts` не импортирует Three.js/DOM (проверка зависимостей пакета) — импортов three/DOM нет, tsconfig lib = ES2022 без dom
