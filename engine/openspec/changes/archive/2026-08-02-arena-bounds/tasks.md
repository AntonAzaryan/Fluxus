## 1. Применение дельт в основные спеки

- [x] 1.1 Создать `openspec/specs/arena/spec.md` с секцией `## Purpose` и требованиями ARENA-1..ARENA-6 из дельта-спеки
- [x] 1.2 В `openspec/specs/terrain/spec.md` заменить TERR-4 на версию из дельта-спеки (третий сценарий про override); TERR-1..3, TERR-5..7 не трогать
- [x] 1.3 Из `## Open Questions` спеки `terrain` убрать вопрос №1 (уровень сущности, не стоящей на земле) — он закрыт ARENA-6; остальные вопросы и их нумерацию сохранить

## 2. Синхронизация архитектурного обзора

- [x] 2.1 `docs/architecture.md` §2 «Карта спецификаций»: добавить строку `arena` с диапазоном ARENA-1..6
- [x] 2.2 `docs/architecture.md` §3 «Механизм против политики»: добавить строки — граница и событие выхода против того, что за выход бывает; изменение радиуса против расписания сужения
- [x] 2.3 `docs/architecture.md` §6 «Открытые вопросы»: убрать пункт 11 (уровень сущности не на земле) — закрыт ARENA-6
- [x] 2.4 `docs/architecture.md` §5 Roadmap: арена реализуется вместе с террейном (этап 11), политика — после evaluator; отдельного этапа не заводить

## 3. Проверка когерентности

- [x] 3.1 `openspec validate --specs --strict` — 16 capability проходят
- [x] 3.2 Проверить, что перекрёстные ссылки из ARENA-* резолвятся: `determinism-core` DET-1/DET-8, `ecs-foundation` CMD-4/QUERY-1, `snapshot-rewind` SNAP-1/WSM-5, `terrain` TERR-4/TERR-6, `fog-of-war` FOW-5
- [x] 3.3 Проверить обратную ссылку: TERR-4 → ARENA-6
- [x] 3.4 Убедиться, что в `arena` не появилось ни одного балансного числа: ни глубины сужения, ни урона, ни таймингов

## 4. Вынесенное в открытые вопросы

- [x] 4.1 Политика арены (реакция на `LeftArena` и `FellThroughFloor`, расписание сужения) и вопрос формы границы вынесены в `## Open Questions` спеки `arena` — их можно будет завести отдельным change после этапа 8
