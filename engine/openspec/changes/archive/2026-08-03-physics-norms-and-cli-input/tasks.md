## 1. Спека `physics`

- [x] 1.1 PHYS-5: заменить ссылку `(PHYS-7)` на `(PHYS-10)` в описании broad-phase.
- [x] 1.2 PHYS-6: заменить ссылку `(PHYS-7)` на `(PHYS-10)`; добавить нормирование `ratio()` (деление с насыщением, `floor`, насыщение в `±(limit + 1.0)`) и предел ~181 единицы для дистанций в квадратичных вычислениях broad-phase/raycast; добавить сценарий на насыщение у почти параллельного луча.
- [x] 1.3 PHYS-7 (сохраняет номер): уточнить тай-брейк — строгий `<`, и явное правило «статика раньше динамики при равной дистанции»; добавить сценарий «Статика и динамика на одинаковой дистанции».
- [x] 1.4 REMOVED старого PHYS-7 «Статические коллайдеры выводятся, а не хранятся» с Reason/Migration на PHYS-10.
- [x] 1.5 ADDED PHYS-10 «Статические коллайдеры выводятся, а не хранятся» — текст перенесён дословно.
- [x] 1.6 PHYS-8: нормировать порядок осей X→Y, 64-битный промежуток вычисления `separation()`, правило «ход разрешения выводится из направления пересечения»; добавить сценарий «Угловое пересечение по обеим осям».

## 2. Спека `cli-testing`

- [x] 2.1 CLI-2: заменить ссылку `(PHYS-7)` на `(PHYS-10)`.
- [x] 2.2 CLI-2: добавить абзац и сценарий про необязательное поле `visibility` (по аналогии с `physics`, основание — DI-3/PHYS-6).
- [x] 2.3 CLI-2: уточнить правило `inputs[]` — требуется наличие поля `inputs`, а не непустой массив; обновить сценарий «Инпуты без списка игроков».

## 3. Код

- [x] 3.1 `engine/core-ts/src/sim/scenario.ts`: заменить условие на `def.inputs !== undefined && def.players === undefined` (убрать зависимость от `.length > 0`), сохранив текст ошибки.
- [x] 3.2 `engine/core-ts/test/inputSystem.test.ts`: добавить тест на сценарий `inputs: []` без `players`.
- [x] 3.3 `engine/core-ts/src/systems/physics.ts`: заменить `PHYS-1..9` на `PHYS-1..10` во всех комментариях, где встречается диапазон.

## 4. Проверка

- [x] 4.1 `host npx vitest run` — зелёный.
- [x] 4.2 `host npx tsc --noEmit` — чистый.
- [x] 4.3 `git diff --stat engine/tests/golden/` — пусто (эталоны не менялись).
- [x] 4.4 `host openspec validate physics-norms-and-cli-input --strict` — проходит.
