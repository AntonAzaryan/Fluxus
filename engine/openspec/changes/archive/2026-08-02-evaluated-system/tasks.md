## 1. Аксессоры мира

- [x] 1.1 `src/ecs/world.ts`: `componentSchema(state, name)` — схема компонента или `undefined`
- [x] 1.2 `src/ecs/world.ts`: `prefabOf(state, name)` — нужен и для проверки состава компонентов в `overrides`

## 2. EvaluatedSystem

- [x] 2.1 `src/evaluatedSystem.ts`: тип `SystemDef` (`name`, `order`, `query?`, `as?`, `do`)
- [x] 2.2 Конструктор: `query` разворачивается в одно действие `forEach` (SYS-1)
- [x] 2.3 `run(ctx)` — исполнение тела через Action Executor (SYS-4, SYS-6)

## 3. Валидация на регистрации (SYS-3)

- [x] 3.1 Обход дерева действий по конвенции имён аргументов, с накоплением пути до узла
- [x] 3.2 Обход выражений: закрытая таблица операторов, `getComponent`/`hasComponent` — проверка компонента и поля
- [x] 3.3 Область видимости переменных: `as` и `bindings` вводят имена в тело
- [x] 3.4 Проверка prefab'ов и `overrides` в `spawnEntity`
- [x] 3.5 Сообщение об ошибке содержит путь до узла

## 4. SystemRegistry (SYS-7)

- [x] 4.1 `registerFromJson(json, world)` — валидация и регистрация
- [x] 4.2 `override(system)` — требует существующего `name` и совпадения `order`
- [x] 4.3 Экспорт из `src/index.ts`

## 5. Тесты

- [x] 5.1 JSON-система с `query` и без него исполняется в `tick()` наравне с нативной
- [x] 5.2 Парность: один сценарий JSON-системой и нативной даёт одинаковое состояние мира (SYS-8)
- [x] 5.3 `override` подменяет реализацию, не меняя порядок; отвергает другой `order` и незнакомое имя
- [x] 5.4 Валидация ловит: неизвестный компонент, неизвестное поле, неизвестный prefab, несвязанную переменную, неизвестное действие, неизвестный оператор
- [x] 5.5 Ошибка валидации называет путь до узла
- [x] 5.6 Переменная, введённая `as`/`bindings`, валидна внутри тела и невалидна снаружи

## 6. Проверка

- [x] 6.1 `npm test` и `npm run typecheck` в `core-ts` зелёные
- [x] 6.2 `openspec validate evaluated-system --strict`
- [x] 6.3 `docs/architecture.md` §5 — этап 8 отмечен выполненным
