## 1. Контракт в типах ядра

- [x] 1.1 Добавить в `core-ts/src/types.ts` типы `NavigationApi`, `PathRequestOptions`, `PathResult` по NAV-1 — рядом с блоком `PhysicsApi`, с комментариями-ссылками на требования (`// NAV-…`), в том же стиле, что у `RaycastOptions`/`RaycastHit`
- [x] 1.2 Добавить в `SystemContext` опциональное поле `navigation?: NavigationApi` с комментарием об опциональности (DI-4) — состав контракта совпадает с блоком кода в SYS-5
- [x] 1.3 Экспортировать новые типы из `core-ts/src/index.ts` рядом с типами физики; реализацию Navigation API не экспортировать — её нет
  - Правки не потребовалось: `index.ts` начинается с `export * from './types.js'`, и типы уходят в публичную поверхность сами. Экспортировать нечего сверх типов — реализации нет.

## 2. Проброс зависимости

- [x] 2.1 Добавить `navigation?: NavigationApi` в состав симуляции в `core-ts/src/sim/tick.ts` и прокинуть его в `SystemContext` тем же условным спредом, что и `physics` (поле отсутствует, если зависимости нет)
- [x] 2.2 Проверить, что ни один путь загрузки сцены (`sim/scene.ts`, `sim/scenario.ts`) навигацию не создаёт и не требует: она зависимость сборки, а не данные сцены (SER-7), и в конфиге сцены её быть не должно

## 3. Проверки шва

- [x] 3.1 Тест: тик сцены без Navigation API отрабатывает штатно, и поля `navigation` в контексте системы нет (DI-4, SYS-5)
- [x] 3.2 Тест: система, получившая контекст с подставленным заглушечным `NavigationApi`, видит его в `ctx.navigation` — шов проходим сквозь тик, а форма результата соответствует NAV-1
- [x] 3.3 `npm run typecheck` и `npm test` — зелёные; golden-эталоны совпадают побитово без регенерации (шов не меняет поведение)
- [x] 3.4 Убедиться, что `npm run schemas` не даёт диффа: в схемах сцены навигация не появляется

## 4. Спека и документация

- [x] 4.1 `openspec validate --changes add-pathfinding-di --strict` — зелёный
- [x] 4.2 Обновить `docs/architecture.md`: строка `pathfinding` в карте спецификаций (NAV-1..6), диапазон `DI-1..4` у `determinism-core`, третья DI-стрелка на схеме слоёв рядом с Math и Physics
  - Заодно: строка в таблице «механизм против политики» (§3) и этап 21 в roadmap.
- [x] 4.3 Проверить, что открытые вопросы `terrain` (связность сетки, ширина рампы) остались открытыми и по-прежнему адресованы change'у с реализацией, а не этому
