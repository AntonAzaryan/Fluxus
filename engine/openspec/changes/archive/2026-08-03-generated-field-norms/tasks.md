## 1. Спека (SER-6, TERR-6)

- [x] 1.1 `serialization` SER-6: общее правило именования порождённых раскладок «список → фиксированные слоты» (zero-padding до минимально достаточной длины), без привязки к конкретному потребителю
- [x] 1.2 `terrain` TERR-6: имя поля-слова карты пола (`wN` с zero-padding) нормировано как частный случай SER-6, формулировка сверена с `src/systems/terrain.ts:198` (`wordName`)
- [x] 1.3 Проверено, что код не меняется: `wordName` уже реализует нормируемый формат

## 2. Проверка

- [x] 2.1 `openspec validate generated-field-norms --strict`
