## RENAMED Requirements

- FROM: `### Requirement: ARENA-6 Override уровня для сущностей не на земле`
- TO: `### Requirement: ARENA-6 Компонент LevelOverride для сущностей не на земле`

## MODIFIED Requirements

### Requirement: ARENA-5 Провал в клетку без пола — событие, не эффект

Переход сущности в клетку без пола (TERR-6) SHALL порождать событие `FellThroughFloor` с идентификатором сущности. Как и в ARENA-3, ядро MUST NOT применять никаких эффектов: смерть, урон, падение с последующим респавном, зависание — политика.

Участие в проверке пола SHALL определяться тем же компонентом `ArenaState` (ARENA-3): поле `onFloor` хранит признак «на полу» на прошлом тике с дефолтом «на полу». Сущность без `ArenaState` в проверке пола не участвует — те же основания, что и для проверки границы в ARENA-3.

#### Scenario: Пол под сущностью выбит

- **WHEN** бит пола под стоящей сущностью снимается мутацией компонента
- **THEN** эмитится `FellThroughFloor`, и дальнейшее определяет JSON-система

#### Scenario: Снаряд пролетает над дырой

- **WHEN** над клеткой без пола проходит снаряд, у которого задан `LevelOverride` (ARENA-6)
- **THEN** событие не эмитится: снаряд не стоит на земле

#### Scenario: Сущность без ArenaState над дырой

- **WHEN** сущность без компонента `ArenaState` проходит над клеткой без пола
- **THEN** `FellThroughFloor` для неё не считается и не эмитируется — она не участвует в проверке пола

### Requirement: ARENA-6 Компонент LevelOverride для сущностей не на земле

Уровень сущности SHALL определяться производным значением от позиции (TERR-4), кроме случая, когда на сущности присутствует опциональный компонент `LevelOverride` — тогда SHALL использоваться его значение.

Это закрывает случаи, в которых уровень из позиции не выводится: снаряд в полёте, сущность в падении сквозь клетку без пола, сущность на мосту-патче. Кто и когда ставит `LevelOverride` — политика; механизм лишь задаёт приоритет явного значения над производным.

`LevelOverride` SHALL быть обычным компонентом: он входит в снапшот и откатывается вместе с миром (SNAP-1), как и любые другие данные сущности.

#### Scenario: Снаряд летит над обрывом

- **WHEN** у снаряда стоит `LevelOverride`, а под ним рельеф меняет уровень
- **THEN** сущность считается на уровне из `LevelOverride`, и фильтр видимости по уровню (FOW-5) использует его

#### Scenario: Обычная сущность без LevelOverride

- **WHEN** на сущности нет компонента `LevelOverride`
- **THEN** её уровень выводится из позиции по TERR-4, поведение не меняется
