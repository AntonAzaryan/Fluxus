## 1. Спеки

- [x] 1.1 Новая capability `netcode-transport` — NTR-1..13 в `specs/netcode-transport/spec.md` change'а
- [x] 1.2 `specs/tick-loop/spec.md` — MODIFIED TICK-2: таймаут как дедлайн тика по расписанию, запрет досрочного тика при полном комплекте фреймов
- [ ] 1.3 `openspec validate netcode-transport-mvp --strict` проходит
- [ ] 1.4 При sync: `openspec/specs/netcode-transport/spec.md` получает `## Purpose` — «транспортный слой поверх модели `netcode`: чем является сервер матча, каким набором сообщений стороны разговаривают, в каком темпе идёт матч и как это проверяется локально; модель — что передаём, что фильтруем, какая версия — остаётся в `netcode` и здесь не дублируется»
- [ ] 1.5 При sync: `openspec/specs/netcode/spec.md` `## Purpose` — абзац «Прототип отзывчивости» переписать под принятое решение (стартовый режим — server-auth без предсказания, не lockstep-relay); требования `netcode` не трогать
- [ ] 1.6 При sync: `openspec/specs/netcode/spec.md` `## Open Questions` — «Модель отзывчивости» закрыть решением со ссылкой на NTR-10; «Кто получает `viewpoint = ALL`» сузить до продакшн-авторизации со ссылкой на NTR-9 (для локального прогона решено)
- [ ] 1.7 `openspec validate --specs --strict` — 17/17 после sync

## 2. Модуль

- [ ] 2.1 `engine/net-ts/package.json`: `@game-mvp/net`, ESM, зависимость `@game-mvp/core` через `file:../core-ts`, dev-зависимости vitest/typescript/@types/node тех же версий, что в `core-ts`
- [ ] 2.2 `engine/net-ts/tsconfig.json` по образцу `core-ts`; `npm run typecheck` и `npm test` — те же имена скриптов
- [ ] 2.3 `@msgpack/msgpack` в зависимости; lock-файл сгенерировать против публичного реестра
- [ ] 2.4 Проверить обратную границу (NTR-1): `engine/core-ts/package.json` не получает ни одной новой зависимости, `src/index.ts` не получает ни одного нового экспорта. Если понадобилось — остановиться и завести change в ядре

## 3. Протокол

- [ ] 3.1 `src/protocol/messages.ts`: закрытый набор из NTR-4, типы обоих направлений, исходы `Reject` и `End` как закрытые перечисления
- [ ] 3.2 `src/protocol/codec.ts`: кодирование через `Serializer` ядра, MessagePack по умолчанию, JSON — дебаг-режим (NTR-13); плоская форма строится с порядком ключей по SER-6
- [ ] 3.3 Разбор входящего: неизвестный тип, чужое направление и недопустимое для состояния соединения сообщение дают названный исход разрыва, а не игнор (NTR-4)
- [ ] 3.4 Санитизация `InputFrame` на границе: слот отправителя, `buttons` в `0..65535`, тик в допустимом окне (NTR-7)

## 4. Транспорт

- [ ] 4.1 `src/transport/transport.ts`: интерфейс — отправка, приём, закрытие, уведомление о закрытии (NTR-2)
- [ ] 4.2 `src/transport/loopback.ts`: внутрипроцессная пара, доставка через микротаск (синхронная доставка запрещена, NTR-2), опциональная искусственная задержка
- [ ] 4.3 `src/transport/websocket.ts`: серверная и клиентская стороны; границы сообщений даёт сам WebSocket
- [ ] 4.4 Проверить, что ни сервер, ни клиент не импортируют конкретную реализацию транспорта

## 5. Сервер матча

- [ ] 5.1 `src/server/matchConfig.ts`: слоты (упорядоченный список игроков), `seed`, описание матча (идентификатор сцены, стартовая расстановка), `tickRate`/`snapshotRate`/`inputDelay`, порог молчания слота, предикат видимости событий, разрешение наблюдателя
- [ ] 5.2 `src/server/matchServer.ts`: чистый цикл без сокетов, таймеров и часов (NTR-3); вход — сообщение от соединения и продвижение тика, выход — адресованные сообщения
- [ ] 5.3 Хендшейк: сверка пары версии через `contentPackHash` до выдачи чего-либо о матче, `Reject` называет разошедшуюся половину, `Welcome` несёт слот, описание матча и `worldInitHash` (NTR-5)
- [ ] 5.4 Ожидание полного состава, `Start`, запрет изменения состава после старта, `Reject` на повторный вход (NTR-6)
- [ ] 5.5 Расписание тиков и буфер задержки ввода: predicted-фрейм при отсутствии, отброс опоздавшего, оба со счётчиками (NTR-7)
- [ ] 5.6 Персональные снапшоты вызовом `filterSnapshot` ядра с `viewpoint` команды; `VIEWPOINT_ALL` только соединению-наблюдателю (NTR-9)
- [ ] 5.7 Канонический `inputs[]` и выгрузка матча в форме `ScenarioDef` (NTR-8)
- [ ] 5.8 Разрыв соединения: predicted-фреймы, порог молчания, `End` с названным исходом (NTR-6)
- [ ] 5.9 `src/server/driver.ts`: планировщик тиков и связка с транспортом — единственное место с таймерами и часами

## 6. Клиент

- [ ] 6.1 `src/client/matchClient.ts`: чистый цикл без I/O — приём сообщений, отправка ввода, применение снапшотов; `tick()` не зовётся (NTR-10)
- [ ] 6.2 Хендшейк на стороне клиента: подъём `worldInit` по своему контент-паку, сверка хеша, разрыв с исходом «разошлись данные матча», отличным от «версия устарела» (NTR-5)
- [ ] 6.3 `src/client/inputRing.ts`: кольцо своих `InputFrame` глубиной ~120 тиков (NET-9, NTR-10)
- [ ] 6.4 Применение снапшотов: отброс снапшота с тиком не больше применённого (NTR-10)
- [ ] 6.5 `src/client/interpolation.ts`: буфер интерполяции, отставание стартом ~100 мс (NET-3); сущность, пропавшая из снапшота, гасится, а не телепортируется (NET-3, NET-14)
- [ ] 6.6 `src/metrics.ts`: раздельные счётчики RTT, применённых/предсказанных/отброшенных фреймов, размера снапшота, отставания буфера (NTR-11)

## 7. Запуск

- [ ] 7.1 `bin/serve.mjs`: поднять сервер матча из конфига-файла, флаг разрешения наблюдателя, флаг JSON-режима сериализатора
- [ ] 7.2 `bin/play.mjs`: headless-клиент, ввод из записанного сценария ввода либо с клавиатуры, вывод — принятые снапшоты и счётчики (NTR-12)
- [ ] 7.3 `README` модуля: как поднять матч на два терминала, какие числа смотреть при замере отзывчивости

## 8. Тесты

- [ ] 8.1 `test/protocol.test.ts` — круг кодирования обоими сериализаторами; неизвестный тип, чужое направление, сообщение вне состояния дают разрыв с названным исходом
- [ ] 8.2 `test/handshake.test.ts` — расхождение идентификатора сборки; расхождение хеша контент-пака; отказ называет половину; о составе матча отклонённый не узнаёт
- [ ] 8.3 `test/handshake.test.ts` — версии совпали, ассет арены различается: расхождение `worldInitHash`, исход «разошлись данные матча», ни одного исполненного тика
- [ ] 8.4 `test/handshake.test.ts` — неизвестный клиенту идентификатор сцены даёт тот же исход, и сцена ему не досылается
- [ ] 8.5 `test/pacing.test.ts` — фрейм не пришёл: predicted-фрейм в каноническом логе, счётчик вырос
- [ ] 8.6 `test/pacing.test.ts` — фрейм опоздал: отброшен, мир не переигран, счётчик вырос
- [ ] 8.7 `test/pacing.test.ts` — комплект фреймов раньше срока: тик не исполняется досрочно (TICK-2)
- [ ] 8.8 `test/pacing.test.ts` — битая маска `buttons` отвергается на границе, матч продолжается
- [ ] 8.9 `test/filtering.test.ts` — два игрока разных команд: тик один, снапшота два, невидимого противника в снапшоте нет; игровому слоту `VIEWPOINT_ALL` не выдаётся ни при каких аргументах
- [ ] 8.10 `test/match.test.ts` — матч двух игроков на loopback: ввод игрока 1 доезжает до состояния, которое видит игрок 2
- [ ] 8.11 `test/match.test.ts` — **парность (NTR-8)**: записанный матч прогоняется через `runScenario` ядра и даёт побитово те же состояния и тот же `worldInitHash`
- [ ] 8.12 `test/match.test.ts` — снапшот с устаревшим тиком отбрасывается клиентом
- [ ] 8.13 `test/match.test.ts` — разрыв соединения: матч продолжается на predicted-фреймах, по порогу молчания приходит `End`; повторный вход получает `Reject`
- [ ] 8.14 `test/boundary.test.ts` — `engine/core-ts` не зависит от `net-ts` и не получил новых экспортов (NTR-1)

## 9. Документация

- [ ] 9.1 `docs/architecture.md` §1: слой netcode в схеме — сервер матча и транспорт вне ядра
- [ ] 9.2 `docs/architecture.md` §2: строка `netcode-transport` в карте спецификаций, диапазон NTR-1..13
- [ ] 9.3 `docs/architecture.md` §5: этап 17 закрывается, добавляется этап «Netcode transport MVP: сервер матча, транспорт, два headless-клиента»
- [ ] 9.4 `docs/architecture.md` §6 п.13 снять — механизм сверки версии реализован хендшейком (NTR-5)
- [ ] 9.5 `docs/architecture.md` §6 п.2 переформулировать: модель отзывчивости выбрана (server-auth без предсказания), открытым остаётся замер и решение о включении предсказания
- [ ] 9.6 `docs/architecture.md` §6 п.7 переформулировать: для локального прогона решено (NTR-9), открытой остаётся продакшн-авторизация полного потока
- [ ] 9.7 `docs/architecture.md` §3: строки «механизм против политики» из design.md change'а
- [ ] 9.8 `README.md` корня: раздел про `engine/net-ts` и команды локального матча

## 10. Проверка

- [ ] 10.1 `cd engine/net-ts && npx vitest run` — зелено
- [ ] 10.2 `cd engine/net-ts && npx tsc --noEmit` — чисто
- [ ] 10.3 `cd engine/core-ts && npx vitest run` — зелено, эталоны не тронуты
- [ ] 10.4 `git status` — `engine/tests/golden/` без изменений, `engine/core-ts/src/` без изменений
- [ ] 10.5 Ручной прогон: три процесса, два игрока, матч идёт; счётчики RTT и предсказанных фреймов осмысленны
