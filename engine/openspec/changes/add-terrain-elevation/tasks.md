## 1. Применение дельт в основные спеки

- [x] 1.1 Создать `openspec/specs/terrain/spec.md` с секцией `## Purpose` и требованиями TERR-1..TERR-7 из дельта-спеки
- [x] 1.2 В `openspec/specs/physics/spec.md` заменить PHYS-1 на версию из дельта-спеки (три сценария вместо одного); PHYS-2..PHYS-7 не трогать
- [x] 1.3 В `openspec/specs/fog-of-war/spec.md` заменить FOW-5 на версию из дельта-спеки (четыре сценария вместо одного); FOW-1..4, FOW-6..9 не трогать
- [x] 1.4 В `openspec/specs/determinism-core/spec.md` заменить DET-1 на версию с `worldInit` и дописать DET-8 после DET-7, до блока CORE-*
- [x] 1.5 В `openspec/specs/editor/spec.md` дописать ED-10 после ED-9

## 2. Синхронизация архитектурного обзора

- [x] 2.1 `docs/architecture.md` §2 «Карта спецификаций»: добавить строку `terrain` с диапазоном TERR-1..7; обновить диапазоны у `determinism-core` (DET-1..8) и `editor` (ED-1..10)
- [x] 2.2 `docs/architecture.md` §3 «Механизм против политики»: добавить строки из таблицы `design.md` — `levelAt` против баланса высоты, бит пола против того, что его выбивает, клетка без пола против судьбы попавшего в неё
- [x] 2.3 `docs/architecture.md` §5 «Roadmap»: вставить этап террейна перед текущим этапом 13 (Physics) — cliff-геометрия является входными данными для коллизий, а FOW-5 начинает зависеть от `levelAt`; перенумеровать последующие этапы
- [x] 2.4 `docs/architecture.md` §1 «Слои»: отразить террейн как иммутабельный входной ассет ядра рядом с DI-зависимостями
- [x] 2.5 `docs/architecture.md` §6 «Открытые вопросы»: перенесены только сквозные — уровень сущности не на земле добавлен пунктом 11, обрезка reveal-круга дописана к существующему пункту 9. Связность сетки и символы флагового слоя остались в `## Open Questions` спеки `terrain`: копировать их в обзор значило бы завести второй источник правды

## 3. Проверка когерентности

- [x] 3.1 `openspec validate --specs --strict` — 15 capability проходят
- [x] 3.2 Проверить, что перекрёстные ссылки из TERR-* резолвятся: `determinism-core` DET-1/DET-7, `physics` PHYS-1, `snapshot-rewind` SNAP-1, `fog-of-war` FOW-5/FOW-9
- [x] 3.3 Проверить обратные ссылки: PHYS-1 → TERR-1/TERR-4, FOW-5 → TERR-4, ED-10 → TERR-2/TERR-3/TERR-5/TERR-7, DET-8 → PHYS-1/PHYS-2/TERR-5
- [x] 3.4 Убедиться, что понятие «глобальный ресурс» нигде не введено: карта пола описана как компонент singleton-сущности, `ecs-foundation` и `snapshot-rewind` не изменены. Найдено и исправлено: формулировка DET-1 вводила «ресурсы симуляции» — заменена ссылкой на перечень SNAP-1
- [x] 3.5 Убедиться, что `ecs-foundation` QUERY-* не затронут: `withinRadius` остаётся 2D-запросом, фильтрация по уровню выполняется вызывающей стороной

## 4. Последующая работа (вне этого change)

- [ ] 4.1 Завести change `arena-bounds`: форма арены, края, «выталкивание за край», сужение, политика попадания в клетку без пола
- [ ] 4.2 Завести change на перестановку roadmap под приоритет «сначала редактор, потом геймплей» — сейчас редактор стоит предпоследним этапом
