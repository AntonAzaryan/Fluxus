# Статус исполнения плана сведения спек и кода — 2026-08-03

> Отчёт о ходе работ по `spec-alignment-plan-2026-08-03.md`. Находки — в
> `spec-compliance-2026-08-03.md`. Этот файл — точка передачи в новый контекст:
> что сделано, что осталось, чем именно продолжать.

## Сводка

| Волна | Change'и | Состояние |
|---|---|---|
| 1 | 5 (`arithmetic-and-rng-norms`, `physics-norms-and-cli-input`, `spec-catchup-arena-fow`, `generated-field-norms`, `rewind-norms`) | **готово**, синхронизировано, заархивировано, коммит `5780c05` |
| 2 | 2 (`scene-schema-contract`, `modifier-slots-and-actions`) | **готово**, синхронизировано, заархивировано, коммит `79408cf` |
| 3 | 2 (`events-in-systems`, `side-channel-and-command-norms`) | **готово**, синхронизировано, заархивировано |
| 4 | 1 (`worldinit-hash`) | не начат |

9 из 10 change'ей доведены до готовности и синхронизированы.
`openspec validate --specs --strict` — 16/16 после sync волны 3.

Состояние дерева на момент отчёта: 385 тестов зелёные (25 файлов),
`tsc --noEmit` чист, `git diff --stat engine/tests/golden/` пуст (единственное
изменение в golden — новые непроиндексированные пары `collision-bounce.*` и
`modifier-stack.*`).

## Что сделано в волнах 1–2

Волна 1 (`5780c05`):
- `assert` раскалывается на мягкий (диагностика через подключаемый sink, не
  бросает, значение не меняет) и `assertInvariant` (бросает в обоих сборках).
  FP-2 переформулирован под truncate toward zero, новое FP-6 (`sqrt` от
  отрицательного = 0), RNG-8 нормирует `nextFixed`/`nextBelow` дословно.
- Дубль PHYS-7 разведён: тай-брейк hit'ов сохранил номер, «статика выводится»
  переехала на PHYS-10. PHYS-8 нормирует 64-битный `separation` и вывод
  разрешённости хода из направления пересечения. CLI-2 — буква про `inputs[]`
  и поле `visibility`.
- `ArenaState` описан в спеке `arena` (участие в проверке границы и пола —
  решение контента). FOW-5 нормирует безусловный бит своей команды и то, что
  cliff-отрезок перекрывает LoS раньше правила высоты. FOW-1 — потолок радиуса
  со ссылкой на PHYS-6.
- SER-6/TERR-6 — zero-padding имён порождённых полей-слотов.
- REW-4 разводит внутренний реплей в `seekTo` и обычное исполнение систем;
  SNAP-6 — формула глубины `interval × (count − 1)`.

Волна 2 (`79408cf`):
- SER-5 получил критерий «схема принимает каждый golden-сценарий», SER-7 —
  полный состав полей конфига сцены. Схемы `scene`/`scenario` больше не
  отвергают `arena`/`timeScale`/`tweens`/`fog`/`visibility`. Новый тест
  `schemaConformance.test.ts` — ~45 строк ручного обхода (`$ref`,
  `additionalProperties`, `required`) без ajv, потому что SER-5 запрещает ядру
  зависеть от валидатора JSON Schema.
- `modifiers.ts` лишился модульного синглтона и `pending`-оверлея вне мира.
  Коллизия слотов внутри тика решается точечным чтением буфера команд через
  новый `CommandBuffer.peekField`; CMD-5 расширен нормой на это чтение
  (точечно можно, перечислять команды нельзя).
- TWEEN-1..7 и TIME-7..9 приведены к реализации, число слотов стало полем
  сцены `modifierSlots` (дефолт 4), кламп TimeScale записан сырым Q16.16
  (`3276`, а не `0.05`). ACT-1 больше не исключает `addTween`/`addModifier`;
  добавлены действия `addModifier`/`removeModifier`, TIME-8 стал выразим из
  JSON. SER-7 нормирует порядок порождённых компонентов
  floor → arena → timeScale → tween → fow и регистрацию `ArenaSystem`
  в загрузчике.

## Волна 3: что вошло

Оба change'а синхронизированы в главные спеки и заархивированы в
`openspec/changes/archive/2026-08-03-*`.

### `side-channel-and-command-norms` — готов
- Задачи 15/15 `[x]`, `openspec validate ... --strict` проходит, на своих
  правках в изоляции 367 тестов зелёные, golden не тронуты.
- Дельты: `tick-loop` MOD TICK-2 (единственный владелец ширины `buttons` = u16,
  с оговоркой, что `bitTest` 0..31 — общий оператор над `i32`-маской, а не
  второе определение той же ширины), MOD TICK-3 (три легальных исключения:
  расстановка `worldInit`, восстановление в `seekTo`/WSM-5, exempt-поля REW-9);
  `ecs-foundation` ADD CMD-7 (команда к не-живой сущности отбрасывается на
  flush), MOD QUERY-1 (краевые случаи пустых `all`/`any`/`not` и неизвестного
  компонента); `netcode` MOD NET-12 (вырезание через `destroy` меняет
  `generations`/`freeList` в копии — нормировано как есть, плюс требование
  строить baseline для NET-8 от персонального снапшота того же клиента).
- Код: только `src/index.ts` — `export * as world` заменён явным read-only
  объектом. Ушли `spawn`, `destroy`, `setField`, `addComponent`,
  `removeComponent`, `addTag`, `createWorld`, `fromPlain`, `copyWorldInto`,
  `clearDirty`, `componentMasks`. Добавлен единственный мутатор
  `worldInitSpawn`. Потребителей в репозитории не сломало: `bin/sim.mjs` тянет
  `src/sim/scenario.ts` напрямую, тесты — `src/ecs/world.js`.

### `events-in-systems` — готов
- Задачи 29/29 `[x]`, `openspec validate ... --strict` проходит.
- Дельта `data-driven-systems`: MOD SYS-5 (полный листинг контракта, включая
  `modifiers?`, про который SYS-5 не знал после волны 2), ACT-1
  (carry-forward от волны 2 + узел итерации), ACT-3, EXPR-2, EVT-2, EVT-4.
- Замкнут контур событий: действие `forEachEvent { type, as, do }` (обход шины
  тика, обязательный фильтр по литеральному `type`, длина шины снимается один
  раз до обхода — событие, эмитнутое телом, в текущем проходе не участвует) и
  оператор `eventField [event, field]` (поле данных события, масштаб не
  преобразуется, разрешение имени через `Object.hasOwn` по EXPR-6).
- Код: `types.ts` — одна строка (`events: EventEmitter & ReadonlyEventLog`);
  `expr.ts` — `ExprWorld` получает `events: ReadonlyEventLog` (именно
  read-only-часть: у выражения физически нет `emit`, sandbox EXPR-3 остаётся
  доказуемым типом) плюс оператор; `actions.ts` — узел итерации и замена
  `Object.entries` на отсортированные ключи; `evaluatedSystem.ts` — ветка
  валидации `eventField` (3 строки, иначе строковый литерал второго аргумента
  отвергается обходом валидации); `schemas.ts` — границы u16 у
  `inputFrame.buttons`. `src/sim/tick.ts` не тронут.
- Новый golden `collision-bounce`: тик 6 — `Collision` с `nx = -65536`,
  JSON-система с `order: 200` умножает `Velocity.x` на нормаль, тики 7–8 —
  сущность едет обратно. PHYS-9 стал выразим из данных.

## Что осталось сделать

1. **Волна 4 — `worldinit-hash`**, один change в одиночестве:
   `determinism-core` MOD DET-1 (что входит в хеш), `cli-testing` MOD CLI-3
   (поле вывода); новый `src/sim/worldInit.ts` на существующем `fnv1a32`
   (`src/math/rng.ts`) и сортировке ключей плоской формы (SER-6);
   `sim/scene.ts`/`sim/scenario.ts` — расчёт и вывод; `index.ts` — экспорт;
   **все правки `docs/architecture.md`** (строка 49 `PHYS-1..10`, §6 п.12
   переформулировать, п.13 снять); `npm run golden` — **все 9 эталонов
   перегенерируются**, потому что хеш выводится полем CLI. Поэтому change идёт
   последним и один. Тесты: стабильность хеша при переформатировании ассета,
   различие при другой сетке.
2. **Итоговая сверка:** перечитать `spec-compliance-2026-08-03.md` и убедиться,
   что каждый пункт закрыт либо правкой, либо явно зафиксирован как открытый
   вопрос. Осознанно открытыми остаются два: §4.5 (`validateRampWidth`
   проверяет необходимое условие, а не ширину рампы — уже живой Open Question
   в спеке `terrain`) и направленный обзор с высоты (внесён в Open Questions
   спеки `fog-of-war` волной 1).

## Процедура sync — как это делалось

**`sync` и `archive` субагентам не поручаются.** Скилл `openspec-sync-specs`
запрещает выбирать change автоматически, а sync — это агентный merge дельты в
главную спеку, который нельзя делать вслепую. Один из субагентов волны 1
(`spec-catchup-arena-fow`) самовольно вписал MODIFIED-требования прямо в
главные спеки; это было откачено, из его правок оставлена только легитимная
прямая правка `## Open Questions` (дельтой Open Questions не выражаются).

Рабочая процедура на change:
1. `host openspec status --change <name> --json` — взять список дельт из
   `artifactPaths.specs.existingOutputPaths`.
2. Прочитать дельту и соответствующее требование в главной спеке. Проверить,
   что заголовок совпадает дословно и что дельта ведёт текст **вперёд** от
   текущего состояния (carry-forward), а не откатывает работу предыдущего
   change'а. Для этого полезен `diff` блока требования из главной спеки против
   блока из дельты — видно ровно то, что меняется.
3. Заменить блок требования целиком (в этом репозитории `MODIFIED` содержит
   полный блок, не патч).
4. Для многотребовательных дельт быстрее скрипт замены блоков по заголовку.
   **Ловушка:** такой скрипт легко съедает пустую строку перед следующим
   `### Requirement:` — после прогона проверять и восстанавливать.
5. `host openspec validate --specs --strict` — должно быть 16/16.
6. Сверить, что число требований в каждой затронутой спеке не изменилось
   (`grep -c "^### Requirement:"` до и после) — это ловит потерю блока.
7. `mv openspec/changes/<name> openspec/changes/archive/2026-08-03-<name>`
   после проверки, что все задачи `[x]`.

## Отклонения от плана, принятые по ходу

1. **Change 6 вышел за список файлов.** Добавлены `CommandBuffer.peekField`
   (`src/ecs/commands.ts`, `src/types.ts`) и проброс `modifiers?` через
   `SystemContext` (`src/sim/tick.ts`). Причина: `pending`-оверлей нельзя было
   снять, не дав распределителю слотов способа увидеть собственную команду до
   flush, а состояние вне мира запрещено TICK-4. Проверено, что обратный проход
   по буферу даёт то же значение, что окажется в мире после flush, и что между
   тиками состояния не остаётся.
2. **Change 6 добавил пятую capability в дельту** — `ecs-foundation` MOD CMD-5,
   которой не было в плане. Без нормативного текста Rust-порт не получил бы
   этого чтения и распределение слотов разошлось бы.
3. **Порядок порождённых компонентов (пункт S4) переехал из change 5 в
   change 6.** Агент change 5 сознательно оставил в SER-7 оговорку «порядок не
   определяется», передав вопрос дальше; change 6 эту оговорку заменил нормой.
   Итог тот же, но если сверять план построчно — пункт закрыт не там, где
   написано.
4. **Change 2 столкнулся с внутренним противоречием промпта** по `inputs: []`:
   правило CLI-2 «требование определяется наличием поля, а не непустотой»
   несовместимо с «пустой массив без `players` не должен падать». Выбрано
   падение (соответствует букве правила и тексту схемы); сценарий CLI-2
   переформулирован явно — «пустое или нет».
5. **Change 8 сузил экспорт шире списка** — дополнительно убран
   `componentMasks`: он отдаёт живой `Uint32Array` мира, то есть запись в состав
   компонентов в обход команд. Тот же side-channel, что и остальные мутаторы.
6. **Change 7 правил `evaluatedSystem.ts`** (файла не было в списке): без ветки
   валидации `eventField` обход считает второй аргумент выражением и отвергает
   строковый литерал — новый оператор не прошёл бы `registerFromJson`. Три
   строки. Также расширен `ExprWorld` полем `events: ReadonlyEventLog` — иначе
   оператору не до чего дотянуться.
7. **Тест ACT-3 сделан на полях `"9"`/`"10"`, а не `"0"`/`"1"`.** На `"0"`/`"1"`
   лексикографический и численный порядок совпадают, дефект не проявляется.
   Проверено, что на старом `Object.entries` тест краснеет.

## Открытые хвосты

1. **Низкоуровневые namespace остались открыты.** `index.ts` по-прежнему
   экспортирует `entityIndex` и `componentMask` целиком; при наличии живого
   `WorldState` через них формально можно дотянуться до мутаций. Дыру, которую
   TICK-3 описывает дословно (`world.spawn` и прочие), change 8 закрыл; полная
   герметизация потребовала бы branded-типов или отказа от экспорта этих
   namespace — отдельный разговор, в серию не входит.

## Инварианты, которые нельзя нарушать

- `openspec` CLI и `npx vitest`/`tsc` живут **только на хосте**: `host <команда>`
  из нужной директории (`engine` для openspec, `engine/core-ts` для npm).
- Golden-эталоны до волны 4 меняться не должны. `arena-time.golden.json` и
  `visibility.golden.json` — приёмка change 6, проверялись побайтово.
  Единственный change, который их перегенерирует, — `worldinit-hash`.
- Changes, правящие DSL или состав схем, обязаны прогнать `host npm run schemas`
  из `core-ts`, иначе `schemas.test.ts` красный.
- Одна волна — один коммит.
