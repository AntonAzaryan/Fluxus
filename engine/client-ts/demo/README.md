# Демо-арена

Демо воркер-сборки клиента (`client-shell` SHELL-1..8): симуляция идёт в
воркере, главный поток рисует доставленное состояние и шлёт ввод.

```sh
npm run dev -w @game-mvp/client       # dev-сервер vite
npm run demo:build -w @game-mvp/client
```

Режим страницы выбирается один раз, при старте (SHELL-8):

| адрес | что поднимается |
| --- | --- |
| `/` | матч против бота на сетевом стеке: сервер вкладки плюс тонкий клиент |
| `/?server=ws://…` | тот же тонкий клиент против выделенного стенда |
| `/?solo` | одиночная симуляция в воркере — с паузой и отладочной перемоткой |

## Арена

Контент арены — `content/scenes/duel.scene.json`: сетка 48×48 по клетке,
играбельный пол — диск радиуса 22 вокруг центра `(24, 24)`, клетки вне его
без пола (`_`), и шаг за кромку — провал в пустоту. По квадрантам стоят четыре
плато уровней 2 (СЗ), 3 (СВ), 4 (ЮЗ) и 5 (ЮВ); к каждому ведёт рампа шириной
четыре клетки со стороны центра — подъёмы оспариваются из середины. В коридор
рампы входят только с земляной клетки в его же колонке: сбоку к земле примыкает
лишь нижняя полоса, все остальные отделены от неё обрывами в два уровня и выше,
и подойти к плато «наискосок» не выйдет. Герои матча спавнятся симметрично:
`p1` в `(8,5; 24,5)`, `p2` в `(39,5; 24,5)`.

## Управление

| клавиши | действие |
| --- | --- |
| `WASD` / стрелки | движение героя |
| `Shift` | уклон; дважды подряд — перекат (`locomotion` LOC-3, LOC-4) |
| `Space` | прыжок |
| `ЛКМ` | фаербол в точку клика; удержание копит заряд, отпускание стреляет |
| `F` | купол замедления: чужие снаряды внутри летят вчетверо медленнее |
| `R` | захват снаряда: удержание показывает зону, отпускание ловит |
| `K` | смерть героя (отладка) |
| `T` | сменить скин steel/ember (`rendering` REND-6) |
| `C` | центрировать камеру на герое; удержание — держать в центре |
| `Y` | режим слежения (follow) |
| `V` | режим облёта: `WASD` двигают камеру, `QE` — вверх/вниз, `ПКМ` — осмотр |
| стрелки, край экрана, `СКМ`, колесо | панорама и зум камеры (`camera` CAM-1..3) |

Тач и геймпад работают по тем же биндингам — раскладка устройств лежит в
`bindings.json` (`input-devices` INP-4), а смысл битов кнопок — в `sim.ts`
(`ACTION_BITS`), то есть в контенте демо, а не в движке.

## Способности

Все живут в JSON-системах сцены; числа — поля компонента `AbilityConfig`
prefab'а `Hero`, то есть балансная политика, а не код (mechanism vs policy).
Купол и захват работают ПРОТИВ соперника, поэтому в `?solo` (герой один) они
инертны: купол ставится и висит, но замедлять ему нечего — чужих снарядов в
мире нет, — а захват списывает кулдаун и не находит цели. Смотреть их надо в
матче с ботом (адрес `/`) или по сети.

**Фаербол** (`ЛКМ`, перезарядка 1,5 с). Снаряд — круглый коллайдер в половину
героического (радиус 0,15 против 0,3), летит быстро (0,625 клетки за тик) и
низко над полом: дуга полёта в манифесте (`verticalOffset.flightArc`) прижата
к земле. Заряженный `HeavyFireball` бьёт по своей картинке: его коллайдер —
0,3, то есть ровно вдвое больше и ровно тот радиус, которым он нарисован.
Попал в ЧУЖОГО героя — снимает ему свой урон (`Projectile.damage`, базовый
100) и исчезает; попал в обрыв плато или стену — взрывается там же. Оба случая
приходят из физики событиями `Overlap` и `Collision` (PHYS-12, PHYS-8), которые
читают системы `FireballHit` (попадание) и `FireballWall` (препятствие) —
раздельно, чтобы снаряд, сбитый в первой, до второй не дожил; свой снаряд
владельца не задевает (сравниваются `Owner.slot` и `Player.slot`). Долетевший
до конца жизни снаряд просто взрывается — пол он больше НЕ выбивает.

Урон одного тика СКЛАДЫВАЕТСЯ: два снаряда, попавших в героя одним тиком,
снимают 200, а передержка вместе с попаданием — 350. Система `DamageApply`
обходит не события, а ЦЕЛИ: на цель приходится одно чтение здоровья, сумма всех
адресованных ей `Damage` этого тика и одна запись. Иначе команды буфера
(последняя-побеждает на поле, CMD-3) оставили бы от N попаданий одно.

**Две честные границы попадания.** Первая — сенсор физики строится как ОБЪЕДИНЕНИЕ
границ снаряда до и после хода тика (`physics.ts`, помечено `ponytail`), то есть
как прямоугольник, описанный вокруг всего шага. По осям это точно: прямоугольник
шага и есть заметённый объём. По диагонали — нет: на скорости 0,625 клетки за
тик выстрел под 45° засчитывает попадание примерно в 1,9 раза дальше от своей
геометрии, чем следует, и герой рядом с траекторией ловит снаряд, который в него
не летел. Контент этого не чинит и чинить не может — числа скорости и коллайдера
тут ни при чём; чинится это заметающей (swept) проверкой в ядре, и она отложена
до профилировки на реальной сцене, как и прочие `ponytail`-компромиссы.

Вторая — снаряд останавливают ОБРЫВЫ, и в обе стороны сразу. Блокирующая
статика террейна (PHYS-8, TERR-5) не знает направления: `cliffRise: 0` в
коллайдере снаряда означает «любой перепад уровня — стена», и подъём, и спуск.
Выстрел С плато вниз детонирует о собственную кромку ровно так же, как выстрел
в плато снизу. Рампы же не останавливают ничего — они проходимы по построению
(перепад по единице), и снаряд идёт по ним насквозь. Одностороннего обрыва
механизм ядра сегодня не даёт, и демо живёт с этим как есть.

**Заряд каста.** Нажатие `ЛКМ` не стреляет, а начинает заряд: перед кастером
появляется оранжевый шар, который следует за ним и за прицелом и за 1 с роста
вырастает вдвое. Окно роста — 60 тиков, но тик НАЖАТИЯ в него не входит, и
полный заряд набирается 61-м тиком с кнопкой вниз (самый быстрый тап,
отпущенный на следующем тике, обязан дать обычный шар). Урон растёт как КВАДРАТ
размера — двойной шар бьёт на 400. Отпускание стреляет тем, что накопилось:
быстрый тап — обычный шар на 100. Во время заряда ходьба свободна, а манёвры
(уклон, перекат, прыжок) заперты тем же селективным локом `ActionLock`
(`locomotion` LOC-7), что и пойманный снаряд; захват на это время недоступен, и
его превью гаснет. Купол замедления (`F`) при этом остаётся доступен НАМЕРЕННО:
лок запирает манёвры, а взаимную блокировку прописаны только захват и каст —
поставить купол под собственный заряд игрок вправе.
Передержка — 300 мс (18 тиков) сверх максимума, то есть 78 тиков заряда, —
взрывает заряд В САМОМ кастере: 250 урона всем в радиусе 3 клетки, включая
его самого (свои промахи должны стоить), кулдаун списывается, шар исчезает.
Кулдаун в 90 тиков ставится и на выстрел, и на передержку.

Величина заряда приезжает в главный поток доставленным статом `charge`
(`Charging.ticks`, HUD-8), и шар растёт по НЕЙ: радиуса на сущность у оболочки
`effects.byKind` нет (REND-23), поэтому растущий шар рисует главный поток —
как и сектор захвата, и по той же причине. Шар рисуется КАЖДОЙ сущности с этим
статом, а не одному своему герою: заряд соперника обязан быть виден, и курс его
шара приезжает событием `ChargeAim` — сцена шлёт его каждый тик заряда, и по
нему же доворачивается торс кастера (REND-5). У УЛЕТЕВШЕГО снаряда размер
меняется ступенькой: заряд от полутора размеров и выше рождает prefab
`HeavyFireball` (тот же тег `Fireball` для систем, свой визуальный тип для
манифеста), и плавного роста в полёте нет — это известная граница механизма, а
не недосмотр. С того же порога шар заряда перекрашивается в цвет тяжёлого
снаряда: цвет на выстреле не должен меняться сюрпризом, а «тяжёлый уже сейчас» —
единственная подсказка, которую игрок успевает прочитать.

**Купол замедления** (`F`, перезарядка 5 с, живёт 3 с). Ставится на месте
героя радиусом 10 радиусов его коллайдера (3 клетки). Каждый тик системы
`DomeSlow` снимают свой источник со всех снарядов и ставят его заново тем, кто
внутри и принадлежит НЕ владельцу купола (`time` TIME-8): множитель 0,25
сводится `TimeScaleSystem`, а шаг снаряду умножает физика (TIME-3) — сама
скорость в компоненте не трогается, поэтому выход из купола восстанавливает
полёт точно. Свои снаряды купол не замедляет — на этом и держится связка с
захватом. Радиус купола в манифесте (`effects.byKind.SlowDome.radius`, 3) —
РУЧНОЕ зеркало произведения `Collider.radius × AbilityConfig.domeRadiusMul` из
сцены: оболочка эффекта числами симуляции не питается, и ретюнить их надо
вместе, иначе нарисованный купол перестанет совпадать с тем, который замедляет.

**Захват снаряда** (`R`, перезарядка 2 с). Пока клавиша зажата, главный поток
рисует сектор зоны под курсором — это чистое presentation (кадр идёт чаще
тика), и симуляция о превью не знает; превью гаснет, когда захват невозможен —
кулдаун не вышел, снаряд уже в руках или идёт заряд каста. Фигура сектора
совпадает с проверкой `CaptureRelease` по построению: круг радиуса
`captureRadius`, срезанный клином `captureHalfAngle` вокруг `aimDir`, — и пока
клавиша зажата, прицел уезжает в тик каждым опросом, а не только при движении
мыши (иначе идущий герой разъезжался бы с нарисованной зоной). На ОТПУСКАНИИ
(falling edge, INP-2) система берёт БЛИЖАЙШИЙ чужой снаряд, попавший и в радиус
зоны, и в её раствор (`nearestTo`, ACT-5). Пойманный снаряд замирает у
держателя, каст на это время заблокирован, а манёвры — локом `ActionLock`;
ходьба остаётся. Бросок — `ЛКМ` тем же прицелом: снаряд уходит по `aimDir`, и
ЕГО ВЛАДЕЛЕЦ СТАНОВИТСЯ БРОСИВШИМ, поэтому свой купол его больше не замедляет,
а купол прежнего хозяина — да; накопленный урон снаряд при этом сохраняет.
Не выброшенный за 2 с (окно броска — 120 тиков после тика захвата) снаряд
взрывается в руках и снимает держателю 250 hp общим путём урона сцены
(`Damage` → `DamageApply` → `HealthDeath`).

Каст ловит фронт кнопки, а захват и выстрел заряда — её ОТСУТСТВИЕ на фоне
прошлого тика, и поэтому по сети они ведут себя по-разному. Пропавший пакет
ввода сервер замещает повторением последнего кадра (`tick-loop` TICK-2): для
фронта это безобидно (нажатие просто приедет позже), а для отпускания означает,
что кнопка «остаётся зажатой» лишний тик — потеря пакета может ЗАДЕРЖАТЬ захват
или добавить снаряду каплю заряда, но не выдумать их и не потерять. У самой
границы передержки цена этого лишнего тика не капля: отпускание на 78-м тике,
пакет которого пропал, превращается в разрыв заряда в собственном кастере —
250 урона себе вместо выстрела. Своевременный выстрел — часть риска способности,
и по сети этот риск на один тик шире, чем локально.

**Рикошет от щита** механикой ещё не написан: щита в сцене нет (имя `Shielded`
в `STATE_COMPONENTS` заведено заранее, но бит не выставляется никогда). Всё,
что нужно от ядра, уже есть: событие `Collision` несёт нормаль поверхности в
точке контакта (PHYS-9), и отражение скорости через неё — политика в JSON.
Рикошет приедет вместе со щитом, а не отдельно.

## Что видно на экране

HUD собран из виджетов пакета `@game-mvp/hud` по значению композиции
(`hud.ts`, `match-hud` HUD-4): рантайм-панель и статус матча слева сверху,
счётчики смертей сверху по центру, миникарта слева, портрет героя с полосой
здоровья слева снизу, способности с оверлеем перезарядки снизу по центру.

Полоса здоровья, счётчики смертей и оверлеи перезарядки читают ДОСТАВЛЕННЫЕ
статы (HUD-8). Здоровье и перезарядки каста, купола и захвата в сцене есть и
едут значениями; счётчика смертей в ней по-прежнему нет, и его виджет показывает
пустое состояние — прочерк вместо выдуманного нуля; это ожидаемое поведение, а
не поломка. Кнопки `dodge` и `jump` держат нулевой предел перезарядки — они
всегда готовы, и оверлей на них не появляется.

Попадание фаербола по герою рождает событие `HeroHit`, на которое подвешен
короткий красный выброс частиц (`particles.byEvent`, REND-24; сам эмиттер —
`content/visuals/effects/blood-burst.effect.json`): брызги летят во все стороны
и падают — эмиттер сферический, и на частицы действует сила вниз. Взрыв
передержанного заряда рисуется растущей красной сферой
(`effects.byEvent.ChargeExploded`) и трясёт камеру сильнее обычного взрыва; его
конечный радиус — те же 3 клетки, которыми бьёт `overchargeRadius`. Вспышка
попадания снаряда (`FireballExploded`), наоборот, мелкая: урон у него
КОНТАКТНЫЙ, площади нет, и раздутый огненный шар обещал бы игроку взрыв, от
которого можно отойти.
