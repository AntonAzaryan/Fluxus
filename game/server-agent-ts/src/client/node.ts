/**
 * Канал управляющего протокола в Node — подпуть `@fluxus/server-agent/client
 * /node`.
 *
 * Отдельно от браузерного барьера (`./client`), потому что он тянет `ws` и
 * `node:tls`: страница менеджера собирается без них, а тесты агента и менеджер
 * под десктоп-контейнером берут именно эту фабрику — в ней TOFU-пиннинг
 * проверяется по-настоящему, на уровне TLS (SRV-3).
 */
export { nodeSocket } from './nodeSocket.js';
