/**
 * Строковые ресурсы вкладов конвейера (ED-27, ED-28): описание операции импорта
 * и её параметров, описание правила синхронизации и причина каждой его находки.
 *
 * Лежат здесь по тому же основанию, по которому причины правил редактора лежат
 * в его пакете: ключ причины выводится из пары «правило + код» (`reasonKey`), а
 * правило и операция живут в этом пакете. Строка, заведённая у потребителя,
 * была бы вторым набором формулировок и разошлась бы с правилом при первом же
 * переименовании (ED-28). Собирающий редактор или инструмент сводит бандлы —
 * ровно так же, как это делает `uiResources` пакета интерфейса.
 *
 * Пространство `blender.*` (описания операции) лежит ВНЕ видов, по которым
 * считается отчёт ED-28 о полях схем, — как и `ui.*` у интерфейса; пространство
 * `validation.*` в отчёт входит, и обе его стороны сверяет тест пакета.
 */
import type { LocaleBundles } from '@fluxus/editor-core';
import en from './locales/blender.en.json';
import ru from './locales/blender.ru.json';

export const BLENDER_BUNDLES: LocaleBundles = { en, ru };
