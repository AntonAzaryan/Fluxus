/**
 * Десктоп-контейнер веб-приложений репозитория (capability `desktop-shell`).
 *
 * Три слоя, и граница между ними — не стиль, а требование:
 *
 * - `bridge/` — нормативный контракт границы (DSK-2, DSK-5). Ноль импортов;
 *   его берут и контейнер, и приложения.
 * - `host/` — реализация на чистом Node: корни, атомарная запись, наблюдение,
 *   раздача байтов, сборка моста по профилю. Проверяется контрактным сьютом в
 *   гейте (DSK-6).
 * - `electron/` — тонкий клей первой реализации: main-процесс, preload,
 *   protocol handler. В этот индекс он не входит и в гейтовый tsconfig тоже:
 *   `npm run check` зелёный в окружении без установленного контейнера (DSK-6).
 *
 * Пакетов движка и редактора здесь нет ни одного — и это проверяется, а не
 * декларируется: `engine/integration-ts/test/desktopBoundary.test.ts` (DSK-3).
 */
export * from './bridge/index.js';
export * from './host/index.js';
