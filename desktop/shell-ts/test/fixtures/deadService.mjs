#!/usr/bin/env node
/**
 * Сервис, который не поднимается: выходит сразу и адрес слушать не начинает
 * (DSK-7, случай «сервис не поднялся»).
 *
 * Отдельным файлом, а не несуществующим путём: несуществующий путь даёт трассу
 * рантайма в общий вывод гейта, и зелёный прогон выглядит как сломанный.
 */
process.exit(3);
